
//: Declare String Begin

/*: "cmd" :*/
fileprivate let showTempApp:[UInt8] = [0x64,0x6d,0x63]

/*: "cancelCall" :*/
fileprivate let userStartMain:[Character] = ["c","a","n","c","e","l","C","a","l"]
fileprivate let userWindowNoti:[Character] = ["l"]

/*: "data" :*/
fileprivate let userEqualConst:[UInt8] = [0xed,0xea,0xfd,0xea]

fileprivate func leadingAction(to num: UInt8) -> UInt8 {
    let value = Int(num) + 119
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "logId" :*/
fileprivate let appBottomBeanNoti:String = "size mode returnlogId"

/*: "#322870" :*/
fileprivate let notiGiftK:String = "#male"
fileprivate let kModelInfoNoti:[Character] = ["2","2","8","7","0"]

/*: "Tap to start video chatting" :*/
fileprivate let kManagerMain:String = "center to to bigTap to"
fileprivate let kColorNoti:String = " stamoment for"
fileprivate let constValueShow:String = "IDEO"
fileprivate let dataEventMain:String = "areang"

/*: "icon_close_nor" :*/
fileprivate let constErrorBeAppMain:String = "data starticon_clo"
fileprivate let showToMain:[Character] = ["s","e","_","n","o","r"]

/*: "icon_views_number" :*/
fileprivate let appViewUser:String = "isuiten"
fileprivate let constPartyData:String = "wnumber"
fileprivate let constBlockMain:String = "_numbervar server color admin"

/*: "%@ chatting" :*/
fileprivate let constStartShow:String = "layer touch than self%@ cha"
fileprivate let constQuoteWillNoti:[Character] = ["t","t","i","n","g"]

/*: "icon_lounge" :*/
fileprivate let dataButtonNoti:String = "print label corner type tableicon_l"

/*: "icon_rank_coin" :*/
fileprivate let dataOriginOverConst:[Character] = ["i"]
fileprivate let showEqualNoti:[Character] = ["c","o","n","_","r","a","n","k","_","c","o","i","n"]

/*: "vip_btn_top" :*/
fileprivate let constSendToNewApp:String = "lab let female color phonevip_btn"
fileprivate let constAppMain:String = "_topsize observer self"

/*: "btn_nearby_settings_nor" :*/
fileprivate let constSucceedShow:String = "add viewbtn_"
fileprivate let appStyleShow:String = "voice extension manager_sett"
fileprivate let showSystemModeMain:String = "NOR"

/*: "%@ Coins/min" :*/
fileprivate let mainItemK:[Character] = ["%"]
fileprivate let appViewShow:String = "@ Coiinfo content detail error text"

/*: "get json error" :*/
fileprivate let constShareUser:String = "let user data self toolget js"
fileprivate let userManagerClearNoti:String = "equal application string appon e"

/*: "matchId" :*/
fileprivate let appEqualMain:[Character] = ["m","a","t","c","h","I","d"]

/*: "errno" :*/
fileprivate let userMagnitudeData:[UInt8] = [0xe,0x19,0x19,0x5,0x4]

private func genderBroadcast(array num: UInt8) -> UInt8 {
    return num ^ 107
}

/*: "VIPFee" :*/
fileprivate let showViewNoti:[Character] = ["V","I","P","F","e","e"]

/*: "Matching now" :*/
fileprivate let mainInfoConst:String = "Matchfatal view case point button"

/*: "userTouch" :*/
fileprivate let dataSharedK:[Character] = ["u","s","e","r","T","o","u"]
fileprivate let appSectionConst:[Character] = ["c","h"]

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let showEqualUser:[Character] = ["m","f","/","f","r","e","e","C","a","l","l","/","g","e","t","V"]
fileprivate let showNameDateInData:String = "info"
fileprivate let showInputK:String = "deoInfoself return size"

/*: "fromType" :*/
fileprivate let mainComponentConst:[UInt8] = [0x65,0x71,0x6c,0x6e,0x57,0x7a,0x73,0x66]

/*: "You've been barred from receiving calls" :*/
fileprivate let dataInfoApp:[UInt8] = [0x73,0x6c,0x6c,0x61,0x63,0x20,0x67,0x6e,0x69,0x76,0x69,0x65,0x63,0x65,0x72,0x20,0x6d,0x6f,0x72,0x66,0x20,0x64,0x65,0x72,0x72,0x61,0x62,0x20,0x6e,0x65,0x65,0x62,0x20,0x65,0x76,0x27,0x75,0x6f,0x59]

/*: "source" :*/
fileprivate let showImageMain:[UInt8] = [0x3c,0x20,0x3a,0x3d,0x2c,0x2a]

private func makeContainerRaw(index num: UInt8) -> UInt8 {
    return num ^ 79
}

/*: "type" :*/
fileprivate let notiKeyApp:[UInt8] = [0x41,0x4c,0x45,0x50]

private func errorCur(table num: UInt8) -> UInt8 {
    return num ^ 53
}

/*: "uid" :*/
fileprivate let appPostUser:[UInt8] = [0xe5,0xf9,0xf4]

private func playerInfo(content num: UInt8) -> UInt8 {
    return num ^ 144
}

/*: "fromFreeCall" :*/
fileprivate let kPicApp:[Character] = ["f","r","o","m","F","r","e","e","C","a","l"]
fileprivate let kInviteData:[Character] = ["l"]

/*: "requestCall" :*/
fileprivate let constGiftK:String = "requestCaextra voice user let cell"
fileprivate let dataStartRefreshK:[Character] = ["l","l"]

/*: "onVideoMatch" :*/
fileprivate let userTitleK:[Character] = ["o","n","V","i","d","e","o","M","a","t","c","h"]

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let kModelAddMain:[UInt8] = [0xdb,0xc1,0xf5,0xf4,0xef,0xed,0xe1,0xf4,0xe9,0xe3,0xa0,0xf6,0xe9,0xe4,0xe5,0xef,0xdd,0xa0,0xd2,0xe5,0xe3,0xe5,0xe9,0xf6,0xe5,0xa0,0xf6,0xe9,0xe4,0xe5,0xef,0xa0,0xe3,0xe1,0xec,0xec,0xa0,0xe9,0xee,0xe3,0xef,0xed,0xe9,0xee,0xe7]

private func rangeQuantityimate(content num: UInt8) -> UInt8 {
    return num ^ 128
}

/*: "refuseCall" :*/
fileprivate let dataListSourceImageK:[Character] = ["r","e","f","u","s","e","C","a","l","l"]

/*: "onCancelCall" :*/
fileprivate let kHeadConst:String = "onCamake back"

/*: "retryAfter" :*/
fileprivate let mainVoiceConst:String = "retrhidden"

/*: "onRequestCall" :*/
fileprivate let constCountShow:String = "onRequtext mode app case time"
fileprivate let appInputModelShow:String = "tab"
fileprivate let constToNoti:[Character] = ["s","t","C","a","l","l"]

/*: "failType" :*/
fileprivate let showOfMain:[Character] = ["f","a","i","l","T","y","p"]
fileprivate let dataSendNoti:String = "E"

/*: "callWaitingTime" :*/
fileprivate let mainToConst:[Character] = ["c","a","l","l","W","a","i","t","i","n","g","T","i","m","e"]

/*: "toUid" :*/
fileprivate let userSaveApp:String = "toUidview data user type"

/*: "Free 1 min" :*/
fileprivate let notiEqualK:String = "view selfFree "

/*: "%@/min" :*/
fileprivate let showUpUser:[Character] = ["%","@","/","m","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ValueRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class ValueRecognizerDelegate: EmptyRecognizerDelegate {
	var pointNumber: Double = -51.8
	var blockText: String = "gift"
	var tableArray: [AnyHashable] = []
	var photoTotal: Double = 76.5
	var beneathText: String = "push"
	var fileOfArray: [AnyHashable] = []

    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.appDelay()
            }
        }
        //: self.resetCamera()
        self.delay()
    
		if var shouldResultValue = videoManager.trtcRemotUid { 
			if var airValue = videoManager.trtcRemotUid { 
				if var atValue = videoManager.infoModel?.fee { 
			            if (vipPriceImg.forFirstBaselineLayout.center.x == 83.66) && (vipPriceImg.inputViewController != nil) {
			                //: SWIFT_CUSTOM_DANGER_File_Call
			                let terraceLet = AppView(frame: vipPriceImg.frame.intersection(CGRect(x: CGFloat(44.36), y: CGFloat(57), width: CGFloat(91), height: CGFloat(335.65))))
			            terraceLet.subTalkAtTotal = atValue
			            terraceLet.totalContent = airValue
			            
			            terraceLet.labelInterval = { [self] liveGreetNumber in
			            self.photoTotal = liveGreetNumber
			            
			                self.photoTotal -= 1
			                if Int(self.photoTotal) > -47 {
			                    self.photoTotal = self.photoTotal + 1
			                }
			            return self.photoTotal
			            }
			            terraceLet.tabTitle = { [self] plusTitle in
			            self.beneathText = plusTitle
			            
			            var terraceLet = shouldResultValue
			            terraceLet = String(repeating: "^" as Character, count: terraceLet.count)
			            if terraceLet.contains(self.beneathText) {
			                self.beneathText = terraceLet
			            }
			            
			            if let task = self.beneathText.firstIndex(of: "V") {
			                self.beneathText.remove(at: task)
			            }
			            return self.beneathText
			            }
			            terraceLet.topArray = { [self] dominantArray in
			            self.fileOfArray = dominantArray
			            
			            guard var value = self.fileOfArray as? [String] else {
			                return nil
			            }
			            return value
			            }
			                vipPriceImg.addSubview(terraceLet)
			            }
			
				}
			}
		}
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        actionHandle()
        //: setupSubViewsConstraint()
        stopImage()
        //: bindInteraction()
        outEachTitle()
        //: videoManager.startPreview(isMatch: true)
        videoManager.reMagnitude(isMatch: true)
        //: setPlayer(isRandon: false)
        periodicTable(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            statuteName()
        }
    
		if var onValue = videoManager.trtcRemotUid { 
			if var atTapWireValue = videoManager.infoModel?.pairNickName { 
		            if (chattingNumBtn.constraintsAffectingLayout(for: .vertical).count == 81) && (chattingNumBtn.inputViewController != nil) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let cellImage = AppView()
		            cellImage.subTalkAtTotal = self.chattingNum
		            cellImage.totalContent = atTapWireValue
		            
		            cellImage.labelInterval = { [self] liveGreetNumber in
		            self.pointNumber = liveGreetNumber
		            
		            self.pointNumber -= 1
		            return self.pointNumber
		            }
		            cellImage.tabTitle = { [self] plusTitle in
		            self.blockText = plusTitle
		            
		            var cellImage = onValue
		            cellImage = String.localizedName(of: .unicode)
		            if cellImage.contains(self.blockText) {
		                self.blockText = cellImage
		            }
		            
		            if self.blockText[self.blockText.startIndex].isUppercase {
		                self.blockText = ""
		            }
		            return self.blockText
		            }
		            cellImage.topArray = { [self] dominantArray in
		            self.tableArray = dominantArray
		            
		            guard var value = self.tableArray as? [String] else {
		                return nil
		            }
		            return value
		            }
		                chattingNumBtn.addSubview(cellImage)
		            }
		
			}
		}
	}

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            CurrentSocketManager.shared.towardAdd(info: [String(bytes: showTempApp.reversed(), encoding: .utf8)!: (String(userStartMain) + String(userWindowNoti)), String(bytes: userEqualConst.map{leadingAction(to: $0)}, encoding: .utf8)!: [(String(appBottomBeanNoti.suffix(5))): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        equalAdd()
        //: if TalkingSocketManager.shared.isShared == false {
        if CurrentSocketManager.shared.isShared == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            CurrentSocketManager.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            CurrentSocketManager.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            CurrentSocketManager.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            CurrentSocketManager.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: ObjectThen = {
        //: let m = TalkingVideoChatManager()
        let m = ObjectThen()
        //: let info = TalkingVideoChatModel()
        let info = EnableChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .read()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(statuteName), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: userDeviceKey + 1, height: appNameMessage + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (notiGiftK.replacingOccurrences(of: "male", with: "3") + String(kModelInfoNoti)))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(kManagerMain.suffix(6)) + String(kColorNoti.prefix(4)) + "rt v" + constValueShow.lowercased() + " chat" + dataEventMain.replacingOccurrences(of: "area", with: "ti")).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.imageName(name: (String(constErrorBeAppMain.suffix(8)) + String(showToMain))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(shoot), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: EmptyButton = {
        //: let btn = TalkingButton()
        let btn = EmptyButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.imageName(name: (appViewUser.replacingOccurrences(of: "suite", with: "co") + "_vie" + constPartyData.replacingOccurrences(of: "number", with: "s") + String(constBlockMain.prefix(7)))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.imageName(name: (appViewUser.replacingOccurrences(of: "suite", with: "co") + "_vie" + constPartyData.replacingOccurrences(of: "number", with: "s") + String(constBlockMain.prefix(7)))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.viewPath(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(constStartShow.suffix(6)) + String(constQuoteWillNoti)).targetPost(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.imageName(name: (String(dataButtonNoti.suffix(6)) + "ounge"))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.imageName(name: (String(dataOriginOverConst) + String(showEqualNoti)))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.viewPath(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.imageName(name: (String(constSendToNewApp.suffix(7)) + String(constAppMain.prefix(4))))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: EmptyButton = {
        //: let btn = TalkingButton()
        let btn = EmptyButton()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(stretch), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.imageName(name: (String(constSucceedShow.suffix(4)) + "nearby" + String(appStyleShow.suffix(5)) + "ings_" + showSystemModeMain.lowercased())), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.imageName(name: (String(dataOriginOverConst) + String(showEqualNoti)))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .endUserForColoration()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(mainItemK) + String(appViewShow.prefix(5)) + "ns/min").targetPost(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.viewPath(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: ListReactiveCompatible = //: return ListReactiveCompatible(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension ValueRecognizerDelegate {
    //: private func setPlayer(isRandon: Bool) {
    private func periodicTable(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = ShareImageThen.default.comeMagnitudeo(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(constShareUser.suffix(6)) + String(userManagerClearNoti.suffix(4)) + "rror"))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func bindArray(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(appEqualMain)): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: showTempApp.reversed(), encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: userMagnitudeData.map{genderBroadcast(array: $0)}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        EqualThen.situation(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.upButton()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(appEqualMain))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func greatPyrenees() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        EqualThen.lookAcrossOf { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(showViewNoti))].intValue
            //: self.refreshVideoCallPrice()
            self.appDelay()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension ValueRecognizerDelegate {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func stretch() {
        //: ViewPushManager.share.func__pushToSubscribeAlert()
        ViewPushManager.share.equalFinish()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func shoot() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        phosphoresce()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        constTimeStatusValue.fileStr(eventID: showKeyId)

        //: if PositionLabelReactiveCompatible.share.appConfigMode.displayMatchV4PopUp, PositionLabelReactiveCompatible.share.loginUserMode.freeCallTimes > 0,
        if PositionLabelReactiveCompatible.share.appConfigMode.displayMatchV4PopUp, PositionLabelReactiveCompatible.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.misplace()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        underTitle()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func statuteName() {
        //: guard TalkingSocketManager.shared.isShared == false else {
        guard CurrentSocketManager.shared.isShared == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.exceptAdd(showMsg: mainStatusId)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        phaseOfTheMoon()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(mainInfoConst.prefix(5)) + "ing now").localized
        //: setPlayer(isRandon: true)
        periodicTable(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if PositionLabelReactiveCompatible.share.loginUserMode.randomVideoType == 2 {
        if PositionLabelReactiveCompatible.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            constTimeStatusValue.fileStr(eventID: kAgeKey)
            //: req_getVideoInfo()
            pathSmall()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            constTimeStatusValue.fileStr(eventID: showVideoMineClickMsg)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            CurrentSocketManager.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            CurrentSocketManager.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            CurrentSocketManager.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            CurrentSocketManager.shared.errorDelegate = self
            //: restart_randomVideo()
            ergodicWith()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func ergodicWith(cmd: String = (String(dataSharedK) + String(appSectionConst)), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        equalAdd()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        bindArray(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func upButton() {
        // 停止定时器
        //: destroyWaitingTimer()
        equalAdd()
        //: showCloseBtn()
        phosphoresce()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(kManagerMain.suffix(6)) + String(kColorNoti.prefix(4)) + "rt v" + constValueShow.lowercased() + " chat" + dataEventMain.replacingOccurrences(of: "area", with: "ti")).localized
        //: setPlayer(isRandon: false)
        periodicTable(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isShared == false {
        if CurrentSocketManager.shared.isShared == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            CurrentSocketManager.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            CurrentSocketManager.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            CurrentSocketManager.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            CurrentSocketManager.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        appDelay()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func equalAdd() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func pathSmall() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(showEqualUser) + showNameDateInData.replacingOccurrences(of: "info", with: "i") + String(showInputK.prefix(7)))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: mainComponentConst.map{$0^3}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.upButton()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: PositionLabelReactiveCompatible.share.loginUserMode.randomVideoType = 1
                    PositionLabelReactiveCompatible.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.statuteName()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.exceptAdd(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = EqualTransformable.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = PlayerManager()
                //: player.setMute(bEnable: true)
                player.indueMuteSet(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.streetwise(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                ViewTimeReactiveCompatible.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                TimingThen.atomicNumber85 { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.field()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func field() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        constTimeStatusValue.fileStr(eventID: constButtonStr)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard ViewTimeReactiveCompatible.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.exceptAdd(showMsg: String(bytes: dataInfoApp.reversed(), encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            ViewTimeReactiveCompatible.shared.nameureTo(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.upButton()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = LiveViewDelegate()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.underTitle()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.upButton()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func phaseOfTheMoon() {
        //: if PositionLabelReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if PositionLabelReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(PositionLabelReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(PositionLabelReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func phosphoresce() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func delay() {
        //: guard TalkingSocketManager.shared.isShared == false else {
        guard CurrentSocketManager.shared.isShared == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.reMagnitude()
        }
    }
}

// MARK: - HalfMastMatchDelegate

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension ValueRecognizerDelegate: HalfMastMatchDelegate {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func nextSelect(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = ImageMeasurable.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(appEqualMain)): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: showImageMain.map{makeContainerRaw(index: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: notiKeyApp.map{errorCur(table: $0)}, encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: appPostUser.map{playerInfo(content: $0)}, encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = CountRelatableTarget.randow
        //: if PositionLabelReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && PositionLabelReactiveCompatible.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if PositionLabelReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && PositionLabelReactiveCompatible.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (String(kPicApp) + String(kInviteData)))
            //: TalkingSocketManager.shared.isFreeCall = true
            CurrentSocketManager.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: showTempApp.reversed(), encoding: .utf8)!: (String(constGiftK.prefix(9)) + String(dataStartRefreshK)), String(bytes: userEqualConst.map{leadingAction(to: $0)}, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        CurrentSocketManager.shared.towardAdd(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            CurrentSocketManager.shared.towardAdd(info: [String(bytes: showTempApp.reversed(), encoding: .utf8)!: (String(userStartMain) + String(userWindowNoti)), String(bytes: userEqualConst.map{leadingAction(to: $0)}, encoding: .utf8)!: [(String(appBottomBeanNoti.suffix(5))): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.ergodicWith(cmd: (String(userTitleK)))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func hypocorismData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(appBottomBeanNoti.suffix(5)))].intValue
    }
}

// MARK: - MinCurrentObjectProtocol【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension ValueRecognizerDelegate: MinCurrentObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func telecastingEvent(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func upServer(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func solidus(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func observerGift(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            CancelDelegateHelper.popTumble(body: String(bytes: kModelAddMain.map{rangeQuantityimate(content: $0)}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = ImageMeasurable.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            doTap(toUid: String(uid))
        }
        //: stop_randomVideo()
        upButton()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.replacementResource()
        //: PositionLabelReactiveCompatible.share.start1v1TalkCall(info: data, situation: .Calling)
        PositionLabelReactiveCompatible.share.voiceSend(info: data, situation: .Calling)
    }
}

// MARK: - TardinessObjectProtocol【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension ValueRecognizerDelegate: TardinessObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func withPlay(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        ergodicWith(cmd: (String(dataListSourceImageK)))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func beatBackData(data _: [String: Any]) {}
}

// MARK: - LabelErrorDelegate

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension ValueRecognizerDelegate: LabelErrorDelegate {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func ping(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(kHeadConst.prefix(4)) + "ncelCall") { // 取消通话异常
            //: stop_randomVideo()
            upButton()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(userTitleK)), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            upButton()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(appEqualMain))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.exceptAdd(showMsg: msg)
            //: uploadFailTime(type: 2)
            colorFront(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(userTitleK)), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(mainVoiceConst.replacingOccurrences(of: "hidden", with: "y") + "After")].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(appEqualMain))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.ergodicWith(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(constCountShow.prefix(6)) + appInputModelShow.replacingOccurrences(of: "tab", with: "e") + String(constToNoti)), errorNo == ObjectRelatableTarget.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            upButton()
            //: guard PositionLabelReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard PositionLabelReactiveCompatible.share.appStatus == ValueHashableRepresentation.normal.rawValue else { return }
            //: ViewPushManager.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            ViewPushManager.share.blockRequestEnd(clickEvent: appClickFormat, sufficient: false)
            //: uploadFailTime(type: 1)
            colorFront(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        ergodicWith(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension ValueRecognizerDelegate {
    //: func uploadFailTime(type: Int) {
    func colorFront(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(showOfMain) + dataSendNoti.lowercased())] = type
        //: parm["callWaitingTime"] = time
        parm[(String(mainToConst))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        constTimeStatusValue.split(eventID: userDeviceOfMessage, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func doTap(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(mainToConst))] = time
        //: parm["toUid"] = toUid
        parm[(String(userSaveApp.prefix(5)))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        constTimeStatusValue.split(eventID: mainViewKey, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension ValueRecognizerDelegate {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func appDelay() {
        //: guard PositionLabelReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard PositionLabelReactiveCompatible.share.appStatus == ValueHashableRepresentation.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !PositionLabelReactiveCompatible.share.loginUserMode.loungePlus
        vipIcon.isHidden = !PositionLabelReactiveCompatible.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = CountRelatableTarget.randow
        //: if PositionLabelReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && PositionLabelReactiveCompatible.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if PositionLabelReactiveCompatible.share.loginUserMode.freeCallTimes > 0, PositionLabelReactiveCompatible.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if PositionLabelReactiveCompatible.share.appUserConfigMode.callBtnStyle == 1 {
            if PositionLabelReactiveCompatible.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(notiEqualK.suffix(5)) + "1 min").localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = PositionLabelReactiveCompatible.share.loginUserMode.loungePlus ? vipFee:fee
            let price = PositionLabelReactiveCompatible.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(showUpUser)).targetPost(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.liveBackground(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(showUpUser)).targetPost(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.liveBackground(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = PositionLabelReactiveCompatible.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = PositionLabelReactiveCompatible.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = PositionLabelReactiveCompatible.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = PositionLabelReactiveCompatible.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !PositionLabelReactiveCompatible.share.loginUserMode.loungePlus
            priceStackView.isHidden = !PositionLabelReactiveCompatible.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func actionHandle() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func stopImage() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(showDomainUrl + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func outEachTitle() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(appDelay),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: notiFormatKey,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(greatPyrenees),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: userNetLineSafeMsg,
                                               //: object: nil)
                                               object: nil)
    }
}
