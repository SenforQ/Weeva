
//: Declare String Begin

/*: "user/updateInfo" :*/
fileprivate let appPopPathMain:[Character] = ["u","s","e","r","/","u","p","d","a","t","e"]
fileprivate let appReloadUser:String = "show push textInfo"

/*: "gallery/getGalleryByUid" :*/
fileprivate let userInfoK:[Character] = ["g","a","l","l","e","r","y","/","g"]
fileprivate let kErrorLevelConst:[Character] = ["e","t","G","a","l","l","e","r"]
fileprivate let dataImageConst:[Character] = ["y","B","y","U","i","d"]

/*: "gallery/upload" :*/
fileprivate let dataWrittenConst:String = "gaheadheader"
fileprivate let constDeviceMain:String = "beyond typey/upload"

/*: "gallery/deletePic" :*/
fileprivate let appPopConst:[Character] = ["g","a","l","l","e","r","y"]
fileprivate let dataPathVideoK:[Character] = ["/","d","e","l","e","t","e","P","i","c"]

/*: "user/tagDelete" :*/
fileprivate let userNameNoti:[Character] = ["u","s","e","r","/","t","a","g","D","e"]
fileprivate let userSkipTargetMain:String = "LETE"

/*: "mf/user/tagEdit" :*/
fileprivate let mainViewTitleApp:[Character] = ["m","f","/","u"]
fileprivate let constBubbleApp:String = "ser/tmake class make cover start"

/*: "app/feedback" :*/
fileprivate let appAllUser:String = "title aspect with name pauseapp/fe"
fileprivate let appDignityMain:[Character] = ["e","d","b","a","c","k"]

/*: "IM/getMyBlackList" :*/
fileprivate let userTagShow:[Character] = ["I","M","/","g","e"]
fileprivate let constInputData:String = "tMyBlasub mode count"

/*: "mf/user/getSetting" :*/
fileprivate let mainEdgeNoti:String = "mf/usvar return center"
fileprivate let mainValueConst:String = "path submitSetting"

/*: "mf/user/setConfig" :*/
fileprivate let userViewInputMain:String = "you cancelmf/use"
fileprivate let userStatusConst:[Character] = ["C","o","n","f","i","g"]

/*: "mf/user/settingStatus" :*/
fileprivate let notiModelLayerK:String = "mf/usepop file"
fileprivate let userKitData:String = "scale model app equalngStatus"

/*: "type" :*/
fileprivate let showCenterNoti:[UInt8] = [0xb3,0xb8,0xaf,0xa4]

fileprivate func fullPhaseOfTheMoon(collection num: UInt8) -> UInt8 {
    let value = Int(num) - 63
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "user/lockAccount" :*/
fileprivate let notiModelHiddenShow:String = "let varuser/l"
fileprivate let mainEqualApp:String = "COUN"
fileprivate let notiMenuConst:[Character] = ["t"]

/*: "mf/user/editInfo" :*/
fileprivate let notiImageK:String = "mf/userror style lab view model"
fileprivate let notiQueryedApp:[Character] = ["I","n","f","o"]

/*: "mf/userSign/list" :*/
fileprivate let userFileAddConst:String = "mf/uvar tip interaction model"
fileprivate let appShowModelIfK:String = "height curgn/l"
fileprivate let notiSizeData:String = "isignt"

/*: "mf/userSign/signIn" :*/
fileprivate let kResultApp:[Character] = ["m","f","/","u","s","e","r","S","i","g","n","/","s","i","g","n","I"]
fileprivate let userDeviceData:String = "view"

/*: "mf/dating/saveVideo" :*/
fileprivate let constEraseK:[Character] = ["m","f","/","d","a","t","i","n","g","/","s","a","v","e"]
fileprivate let appConnectionMain:String = "Videokit else"

/*: "mf/dating/getConfig" :*/
fileprivate let mainToApp:String = "make color tag cardmf/dat"
fileprivate let appEqualRequestData:[Character] = ["t","C","o","n","f","i","g"]

/*: "mf/dating/deleteVideo" :*/
fileprivate let appViewNoti:String = "loop list statusmf/dat"
fileprivate let appFrameK:String = "dsharelsharet"
fileprivate let kModelData:String = "eVideotouch available"

/*: "mf/greet/updateAudioRemark" :*/
fileprivate let kHiddenNoti:String = "mf/gretable sure error view color"
fileprivate let dataStopMain:String = "bottomate"
fileprivate let showLineObserverData:String = "should self self pushoRemark"

/*: "mf/greet/list" :*/
fileprivate let constSelectedNoti:String = "model equal view returnmf/gr"

/*: "mf/greet/add" :*/
fileprivate let userLabBottomApp:[Character] = ["m","f","/","g","r","e","e","t","/","a","d","d"]

/*: "mf/greet/del" :*/
fileprivate let mainSucceedConst:[Character] = ["m","f","/","g","r","e","e","t","/","d","e","l"]

/*: "mf/greet/getSettings" :*/
fileprivate let constViewNoti:[Character] = ["m","f","/","g","r","e","e","t","/","g","e","t","S","e","t"]
fileprivate let constMakeApp:String = "addngs"

/*: "mf/greet/extraSetting" :*/
fileprivate let dataImageShow:String = "block time modelmf/g"
fileprivate let userPlayMain:String = "EXTR"
fileprivate let showKeyObjectUser:[Character] = ["n","g"]

/*: "game/list" :*/
fileprivate let dataPhotoNoti:[Character] = ["g","a","m","e","/","l"]
fileprivate let kToUser:String = "listst"

/*: "category" :*/
fileprivate let kLayerNoti:[UInt8] = [0x18,0x1a,0xf,0x1e,0x1c,0x14,0x9,0x2]

private func physicsLaboratory(corner num: UInt8) -> UInt8 {
    return num ^ 123
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CustomReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/9.
//

//: import UIKit
import UIKit

//: class TalkingMeRequestTool: NSObject {
class CustomReactiveCompatible: NSObject {
    /// 资料完善
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UpdateUserInfo(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func withTitle(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "user/updateInfo"
        reqModel.requestPath = (String(appPopPathMain) + String(appReloadUser.suffix(4)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户相册
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_GalleryByUid(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func tingBy(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "gallery/getGalleryByUid"
        reqModel.requestPath = (String(userInfoK) + String(kErrorLevelConst) + String(dataImageConst))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传图片
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UploadPic(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func adjustCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "gallery/upload"
        reqModel.requestPath = (dataWrittenConst.replacingOccurrences(of: "head", with: "l") + String(constDeviceMain.suffix(8)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除图片
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeletePic(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func fromCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "gallery/deletePic"
        reqModel.requestPath = (String(appPopConst) + String(dataPathVideoK))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除单个用户标签
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteTag(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func haveGot(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "user/tagDelete"
        reqModel.requestPath = (String(userNameNoti) + userSkipTargetMain.lowercased())
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 编辑用户标签
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_EditTag(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func params(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/user/tagEdit"
        reqModel.requestPath = (String(mainViewTitleApp) + String(constBubbleApp.prefix(5)) + "agEdit")
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 意见反馈
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_Feedback(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func random(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "app/feedback"
        reqModel.requestPath = (String(appAllUser.suffix(6)) + String(appDignityMain))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取黑名单列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_BlackList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func reFinish(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "IM/getMyBlackList"
        reqModel.requestPath = (String(userTagShow) + String(constInputData.prefix(6)) + "ckList")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取设置信息接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_GetSetting(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func cellCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/user/getSetting"
        reqModel.requestPath = (String(mainEdgeNoti.prefix(5)) + "er/get" + String(mainValueConst.suffix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 个人中心设置
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SettingChange(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func orientationCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/user/setConfig"
        reqModel.requestPath = (String(userViewInputMain.suffix(6)) + "r/set" + String(userStatusConst))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 女性busy状态上报接口
    /// - Parameters:
    ///   - type: 0 :不活跃， 1:活跃
    ///   - completion: 回调
    //: class func req_settingStatus(type: Int, completion: @escaping FinishBlock) {
    class func constraintCompletion(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/user/settingStatus"
        reqModel.requestPath = (String(notiModelLayerK.prefix(6)) + "r/setti" + String(userKitData.suffix(8)))
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: showCenterNoti.map{fullPhaseOfTheMoon(collection: $0)}, encoding: .utf8)!: type]
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 注销账号
    /// - Parameters:
    /// - completion: 回调
    //: class func req_LockAccount(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func bindDown(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "user/lockAccount"
        reqModel.requestPath = (String(notiModelHiddenShow.suffix(6)) + "ockAc" + mainEqualApp.lowercased() + String(notiMenuConst))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 个人中心修改私信价格、开启语音授权
    /// - Parameters:
    /// - completion: 回调
    //: class func req_EditInfo(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func infoPop(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/user/editInfo"
        reqModel.requestPath = (String(notiImageK.prefix(5)) + "er/edit" + String(notiQueryedApp))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取签到日历列表
    /// - Parameter completion: 回调
    //: class func req_getUserSignList(completion: @escaping FinishBlock) {
    class func getCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/userSign/list"
        reqModel.requestPath = (String(userFileAddConst.prefix(4)) + "serSi" + String(appShowModelIfK.suffix(4)) + notiSizeData.replacingOccurrences(of: "sign", with: "s"))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户签到
    /// - Parameter completion: 回调
    //: class func req_userSignIn(completion: @escaping FinishBlock) {
    class func premium(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/userSign/signIn"
        reqModel.requestPath = (String(kResultApp) + userDeviceData.replacingOccurrences(of: "view", with: "n"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户资料视频地址上传
    /// - Parameters:
    ///   - completion: 回调
    //: class func uploadVideoUserEdit(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func nudePerson(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/dating/saveVideo"
        reqModel.requestPath = (String(constEraseK) + String(appConnectionMain.prefix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前用户上传的视频
    /// - Parameters:
    ///   - completion: 回调
    //: class func getUploadVideoInfo(completion: @escaping FinishBlock) {
    class func gen(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/dating/getConfig"
        reqModel.requestPath = (String(mainToApp.suffix(6)) + "ing/ge" + String(appEqualRequestData))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除当前用户上传的视频
    /// - Parameters:
    ///   - completion: 回调
    //: class func deleteUserVideo(completion: @escaping FinishBlock) {
    class func year(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/dating/deleteVideo"
        reqModel.requestPath = (String(appViewNoti.suffix(6)) + "ing/" + appFrameK.replacingOccurrences(of: "share", with: "e") + String(kModelData.prefix(6)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 更新语音备注
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UpdateAudioRemark(completion: @escaping FinishBlock) {
    class func container(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/greet/updateAudioRemark"
        reqModel.requestPath = (String(kHiddenNoti.prefix(6)) + "et/u" + dataStopMain.replacingOccurrences(of: "bottom", with: "pd") + "Audi" + String(showLineObserverData.suffix(7)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-查看设置列表
    /// - Parameters: type: 1=语音， 2=文本
    ///   - completion: 回调
    //: class func req_GetGreetList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func greetSize(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/greet/list"
        reqModel.requestPath = (String(constSelectedNoti.suffix(5)) + "eet/list")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-添加自定义文案/语音/图片
    /// - Parameters: type: 1：语音；2：文案；3：图片; 4:私密照片
    ///   - completion: 回调
    //: class func req_UploadGreetAdd(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func bidBy(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/greet/add"
        reqModel.requestPath = (String(userLabBottomApp))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-设置音频备注
    ///   - completion: 回调
    //: class func req_UpdateAudioRemark(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func fee(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/greet/updateAudioRemark"
        reqModel.requestPath = (String(kHiddenNoti.prefix(6)) + "et/u" + dataStopMain.replacingOccurrences(of: "bottom", with: "pd") + "Audi" + String(showLineObserverData.suffix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-删除文案/音频
    ///   - completion: 回调
    //: class func req_DeleteAudio(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func modelCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/greet/del"
        reqModel.requestPath = (String(mainSucceedConst))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 招呼设置-查看设置
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_GetGreetSetting(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func addObserverCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/greet/getSettings"
        reqModel.requestPath = (String(constViewNoti) + constMakeApp.replacingOccurrences(of: "add", with: "ti"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 图片打招呼-额外配置礼物信息
    /// - Parameters: unlockGiftId:解锁礼物id, isBurn:设置的阅后即焚状态
    ///   - completion: 回调
    //: class func req_UploadGreetExtraSetting(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func index(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/greet/extraSetting"
        reqModel.requestPath = (String(dataImageShow.suffix(4)) + "reet/" + userPlayMain.lowercased() + "aSetti" + String(showKeyObjectUser))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取游戏列表
    /// - Parameters:
    ///   - category: 1=我的页面，2=私聊页面
    ///   - completion: 回调
    //: class func req_getGameList(category: Int, completion: @escaping FinishBlock) {
    class func along(category: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "game/list"
        reqModel.requestPath = (String(dataPhotoNoti) + kToUser.replacingOccurrences(of: "list", with: "i"))
        //: reqModel.params = ["category": category]
        reqModel.params = [String(bytes: kLayerNoti.map{physicsLaboratory(corner: $0)}, encoding: .utf8)!: category]
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
