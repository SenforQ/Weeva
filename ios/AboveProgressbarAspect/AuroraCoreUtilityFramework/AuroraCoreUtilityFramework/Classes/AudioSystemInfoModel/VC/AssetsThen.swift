
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let appMakeK:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Greeting Message Settings" :*/
fileprivate let userManagerKeyNoti:String = "item device read make appGreet"
fileprivate let showTableMain:String = "essadd"
fileprivate let showArrayApp:String = "tininfos"

/*: "FrameCellThen" :*/
fileprivate let dataValueUser:String = "Talkitable make edit filter start"
fileprivate let notiPathConst:String = "custom mode equal centerngGre"
fileprivate let kAddShow:String = "etTacamera data"
fileprivate let showMediumNoti:[Character] = ["b","l","e","C","e","l","l"]

/*: "Voice greeting" :*/
fileprivate let showDismissRangeMain:[Character] = ["V","o","i","c","e"," ","g","r","e","e","t","i"]
fileprivate let kBackgroundLiveAreaConst:String = "info"

/*: "icon_me_greet_vioce" :*/
fileprivate let constUserApp:String = "size"
fileprivate let showEditApp:String = "data view return namecon_"
fileprivate let appViewUser:String = "play contentreet_v"

/*: "Text greeting" :*/
fileprivate let kNeedData:[Character] = ["T","e","x","t"," ","g","r","e","e","t","i","n"]
fileprivate let appTempPathSharedNoti:[Character] = ["g"]

/*: "icon_me_greet_text" :*/
fileprivate let constMoveK:String = "icon_me_lab model true a return"
fileprivate let notiTextUser:String = "greet_view block"

/*: "Photo greeting" :*/
fileprivate let notiStatusShow:[Character] = ["P","h","o","t","o"," "]
fileprivate let notiViewShow:String = "gindexe"

/*: "icon_me_greet_photo" :*/
fileprivate let notiPlayerK:String = "icshow"
fileprivate let appNameMain:String = "reedisk"
fileprivate let constQuantityKitNoti:[Character] = ["_","p","h","o","t","o"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AssetsThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/9.
//

//: import UIKit
import UIKit

//: class TalkingAutoGreetSettingVC: TalkingBaseViewController {
class AssetsThen: EmptyRecognizerDelegate {
	var systemOpen: Bool = false
	var cellSum: Double = 85.3
	var rowCount: Int = 7
	var serverEnable: Bool = false
	var viewQuantity: Double = 7.7
	var columnTotal: Int = 67

    //: var settingModel = TalkingSettingModel()
    var settingModel = ButtonEnvironsSettingModel()

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: appMakeK.reversed(), encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: getSettingData()
        hadith()
    
		if var errorValue = settingModel.isCupid { 
		
		//: if_extract_code "nameOutputStatus" begin
		
		var nameOutputStatus = false
		if !nameOutputStatus {
		    var isValue = false
		    if #available(iOS 13.0, *) {
		        isValue = self.editingInteractionConfiguration == .none
		    }
		    nameOutputStatus = isValue
		}
		
		//: if_extract_code "nameOutputStatus" end
		
	            if (nameOutputStatus) && (self.isEditing && self.editButtonItem.width == 3.70) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let doingTop = TelephoneReceiverController()
	            doingTop.phaseOfTheMoonQuantity = errorValue
	            
	            doingTop.byproductSwitch = { [self] titleDoing in
	            self.serverEnable = titleDoing
	            
	            var doingTop = self.hideNavi
	            doingTop = true
	            if doingTop != self.serverEnable {
	                self.serverEnable = doingTop
	            }
	            
	            self.serverEnable = true
	            return self.serverEnable
	            }
	            doingTop.titleButtonTotal = { [self] meWithInterval in
	            self.viewQuantity = meWithInterval
	            
	                self.viewQuantity += 1
	                if Int(self.viewQuantity) > -71 {
	                    self.viewQuantity = self.viewQuantity - 1
	                }
	            return self.viewQuantity
	            }
	                self.navigationController?.present(doingTop.self, animated: false) { [self] in
	            self.columnTotal /= 7
	                }
	            }
	
		}
	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
		if var insertValue = self.settingModel.textIsSet { 
		
		//: if_extract_code "contextOfUse" begin
		
		var contextOfUse = false
		if !contextOfUse {
		    var isValue = false
		    if #available(iOS 13.0, *) {
		        isValue = self.editingInteractionConfiguration == .none
		    }
		    contextOfUse = isValue
		}
		
		//: if_extract_code "contextOfUse" end
		
	            if (contextOfUse) && (self.isEditing && self.editButtonItem.width == 3.70) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let doingTop = TelephoneReceiverController()
	            doingTop.phaseOfTheMoonQuantity = insertValue
	            
	            doingTop.byproductSwitch = { [self] titleDoing in
	            self.systemOpen = titleDoing
	            
	            var doingTop = self.hideNavi
	            doingTop = true
	            if doingTop != self.systemOpen {
	                self.systemOpen = doingTop
	            }
	            
	            self.systemOpen = true
	            return self.systemOpen
	            }
	            doingTop.titleButtonTotal = { [self] meWithInterval in
	            self.cellSum = meWithInterval
	            
	                self.cellSum += 1
	                if Int(self.cellSum) > -71 {
	                    self.cellSum = self.cellSum - 1
	                }
	            return self.cellSum
	            }
	                self.navigationController?.present(doingTop.self, animated: false) { [self] in
	            self.rowCount /= 7
	                }
	            }
	
		}
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Greeting Message Settings".localized
        self.title = (String(userManagerKeyNoti.suffix(5)) + "ing M" + showTableMain.replacingOccurrences(of: "add", with: "ag") + "e Set" + showArrayApp.replacingOccurrences(of: "info", with: "g")).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.loadSize()
        //: setupSubviews()
        curfew()
        //: setupSubViewsConstraint()
        instance()
    }

    //: deinit {
    deinit {}

    // MARK: - Lazy load

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-TabBarViewHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: userDeviceKey, height: appNameMessage - constBeginScreenHeightKey), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: table.register(TalkingGreetingSetTableCell.self, forCellReuseIdentifier: "TalkingGreetingSetTableCell")
        table.register(FrameCellThen.self, forCellReuseIdentifier: (String(dataValueUser.prefix(5)) + String(notiPathConst.suffix(5)) + "etingS" + String(kAddShow.prefix(4)) + String(showMediumNoti)))
        //: table.separatorStyle = .none
        table.separatorStyle = .none
        //: table.dataSource = self
        table.dataSource = self
        //: table.delegate = self
        table.delegate = self
        //: return table
        return table
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingAutoGreetSettingVC {
extension AssetsThen {
    //: func getSettingData() {
    func hadith() {
        //: let dict = Dictionary<String, Any>()
        let dict = [String: Any]()
        //: TalkingMeRequestTool.req_GetGreetSetting(params: dict) { succeed, result, errorModel in
        CustomReactiveCompatible.addObserverCompletion(params: dict) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: self.settingModel = JSONDeserializer<TalkingSettingModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil)!
                self.settingModel = JSONDeserializer<ButtonEnvironsSettingModel>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil)!
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingAutoGreetSettingVC: UITableViewDelegate, UITableViewDataSource {
extension AssetsThen: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 3
        return 3
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return actualHeight(h: 50)
        return actualHeight(h: 50)
    }

    //: func tableView(_ tableView: UITableView, heightForHeaderInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForHeaderInSection _: Int) -> CGFloat {
        //: return 15
        return 15
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = "TalkingGreetingSetTableCell"
        let identifier = (String(dataValueUser.prefix(5)) + String(notiPathConst.suffix(5)) + "etingS" + String(kAddShow.prefix(4)) + String(showMediumNoti))
        //: let cell: TalkingGreetingSetTableCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetingSetTableCell
        let cell: FrameCellThen = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! FrameCellThen

        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: cell.setCellMessage(titile: "Voice greeting".localized, iconStr: "icon_me_greet_vioce", isShowDet: self.settingModel.audioIsSet != 0)
            cell.titile(titile: (String(showDismissRangeMain) + kBackgroundLiveAreaConst.replacingOccurrences(of: "info", with: "ng")).localized, iconStr: (constUserApp.replacingOccurrences(of: "size", with: "i") + String(showEditApp.suffix(4)) + "me_g" + String(appViewUser.suffix(6)) + "ioce"), isShowDet: self.settingModel.audioIsSet != 0)

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: cell.setCellMessage(titile: "Text greeting".localized, iconStr: "icon_me_greet_text", isShowDet: self.settingModel.textIsSet != 0)
            cell.titile(titile: (String(kNeedData) + String(appTempPathSharedNoti)).localized, iconStr: (String(constMoveK.prefix(8)) + String(notiTextUser.prefix(6)) + "text"), isShowDet: self.settingModel.textIsSet != 0)
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: cell.setCellMessage(titile: "Photo greeting".localized, iconStr: "icon_me_greet_photo", isShowDet: self.settingModel.intimatePhotoIsSet != 0)
            cell.titile(titile: (String(notiStatusShow) + notiViewShow.replacingOccurrences(of: "index", with: "re") + "ting").localized, iconStr: (notiPlayerK.replacingOccurrences(of: "show", with: "on") + "_me_g" + appNameMain.replacingOccurrences(of: "disk", with: "t") + String(constQuantityKitNoti)), isShowDet: self.settingModel.intimatePhotoIsSet != 0)
        }
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: let vc = TalkingGreetAudioVC.init()
            let vc = PathThen()
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let vc = TalkingGreetTextVC.init()
            let vc = SequenceThen()
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: let vc = TalkingGreetPhotoVC.init()
            let vc = MenuPhotomicrographRecognizerDelegate()
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
        }
    }

    //: func tableView(_ tableView: UITableView, viewForHeaderInSection section: Int) -> UIView? {
    func tableView(_: UITableView, viewForHeaderInSection _: Int) -> UIView? {
        //: let view = UIView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: 15))
        let view = UIView(frame: CGRect(x: 0, y: 0, width: userDeviceKey, height: 15))
        //: view.backgroundColor = UIColor.clear
        view.backgroundColor = UIColor.clear
        //: return view
        return view
    }

    //: func tableView(_ tableView: UITableView, viewForFooterInSection section: Int) -> UIView? {
    func tableView(_: UITableView, viewForFooterInSection _: Int) -> UIView? {
        //: return UIView.init()
        return UIView()
    }
}

// MARK: - Layout

//: extension TalkingAutoGreetSettingVC {
extension AssetsThen {
    //: private func setupSubviews() {
    private func curfew() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
    }

    //: private func setupSubViewsConstraint() {
    private func instance() {
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
        }
        //: MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
        MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
    }
}