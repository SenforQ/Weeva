
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let notiUserMoreK:[UInt8] = [0xbd,0xc2,0xbd,0xc8,0x7c,0xb7,0xc3,0xb8,0xb9,0xc6,0x8e,0x7d,0x74,0xbc,0xb5,0xc7,0x74,0xc2,0xc3,0xc8,0x74,0xb6,0xb9,0xb9,0xc2,0x74,0xbd,0xc1,0xc4,0xc0,0xb9,0xc1,0xb9,0xc2,0xc8,0xb9,0xb8]

fileprivate func listTitle(cut num: UInt8) -> UInt8 {
    let value = Int(num) - 84
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Voice greeting" :*/
fileprivate let constWithUser:String = "user after fromVoice"
fileprivate let dataSumShow:[Character] = [" ","g","r","e","e","t","i","n","g"]

/*: "Add" :*/
fileprivate let kManagerConst:[Character] = ["A","d","d"]

/*: "type" :*/
fileprivate let notiIndexMain:[Character] = ["t","y","p","e"]

/*: "addStatus" :*/
fileprivate let kFrameReaderNoti:[Character] = ["a","d","d","S","t"]
fileprivate let kViewComeApp:String = "atarrays"

/*: "list" :*/
fileprivate let showButtonApp:[Character] = ["l","i","s","t"]

/*: "audio" :*/
fileprivate let dataLayerMain:[Character] = ["a","u","d","i","o"]

/*: "length" :*/
fileprivate let showResignData:[Character] = ["l","e","n","g","t","h"]

/*: "url" :*/
fileprivate let notiViewDeleteApp:String = "uruser"

/*: "Successfully added, please wait for approval" :*/
fileprivate let notiTableModelData:[UInt8] = [0x77,0x99,0x87,0x87,0x89,0x97,0x97,0x8a,0x99,0x90,0x90,0x9d,0x44,0x85,0x88,0x88,0x89,0x88,0x50,0x44,0x94,0x90,0x89,0x85,0x97,0x89,0x44,0x9b,0x85,0x8d,0x98,0x44,0x8a,0x93,0x96,0x44,0x85,0x94,0x94,0x96,0x93,0x9a,0x85,0x90]

fileprivate func pointOfNumberersection(path num: UInt8) -> UInt8 {
    let value = Int(num) - 36
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Are you sure to delete this message？" :*/
fileprivate let kCellApp:[UInt8] = [0x9f,0xbc,0xef,0x65,0x67,0x61,0x73,0x73,0x65,0x6d,0x20,0x73,0x69,0x68,0x74,0x20,0x65,0x74,0x65,0x6c,0x65,0x64,0x20,0x6f,0x74,0x20,0x65,0x72,0x75,0x73,0x20,0x75,0x6f,0x79,0x20,0x65,0x72,0x41]

/*: "Cancel" :*/
fileprivate let mainDeleteApp:[Character] = ["C","a","n","c","e"]
fileprivate let constFrameUser:String = "L"

/*: "Delete" :*/
fileprivate let notiWindowData:String = "activity selfDelete"

/*: "id" :*/
fileprivate let dataNameK:String = "iwrap"

/*: "Please wait for the audit result" :*/
fileprivate let showColorData:[UInt8] = [0xb3,0xcf,0xc8,0xc4,0xd6,0xc8,0x83,0xda,0xc4,0xcc,0xd7,0x83,0xc9,0xd2,0xd5,0x83,0xd7,0xcb,0xc8,0x83,0xc4,0xd8,0xc7,0xcc,0xd7,0x83,0xd5,0xc8,0xd6,0xd8,0xcf,0xd7]

fileprivate func modelLabel(app num: UInt8) -> UInt8 {
    let value = Int(num) + 157
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "The upper limit has been reached" :*/
fileprivate let mainPingButtonCountervalNoti:[UInt8] = [0xb0,0x8c,0x81,0xc4,0x91,0x94,0x94,0x81,0x96,0xc4,0x88,0x8d,0x89,0x8d,0x90,0xc4,0x8c,0x85,0x97,0xc4,0x86,0x81,0x81,0x8a,0xc4,0x96,0x81,0x85,0x87,0x8c,0x81,0x80]

private func nextLayer(leading num: UInt8) -> UInt8 {
    return num ^ 228
}

/*: "filePath" :*/
fileprivate let showFeeMakeUser:[Character] = ["f","i","l","e","P"]
fileprivate let userDomainConst:[Character] = ["a","t","h"]

/*: "time" :*/
fileprivate let dataKitK:[Character] = ["t","i","m","e"]

/*: "OfViewCell" :*/
fileprivate let kSearchedUser:[Character] = ["T","a","l","k","i","n","g","G","r","e","e","t","A","u","d","i","o","C","e","l","l"]

/*: "#FF506D" :*/
fileprivate let showMainConst:String = "#FF506Dself color tab work interaction"

/*: "btn_message_delete_nor" :*/
fileprivate let dataOfMain:String = "top value layer path selfbtn_m"
fileprivate let mainCollectionTitleConst:String = "bye"
fileprivate let notiShowCenterUser:String = "task let to show hiddente_nor"

/*: "Currently mute" :*/
fileprivate let showAddMain:String = "let equal edit right dataCurre"
fileprivate let userToMeMain:String = "mutvalue"

/*: "Edit Title" :*/
fileprivate let kTargetUser:String = "Edit Tishare background var name"
fileprivate let notiMethodK:String = "tltop"

/*: "OK" :*/
fileprivate let mainMomentOnData:String = "translateK"

/*: "remark" :*/
fileprivate let kWorldConst:[Character] = ["r","e","m","a","r","k"]

/*: "Click the button below to add\ncontent" :*/
fileprivate let constOfNoti:[UInt8] = [0x53,0x7c,0x79,0x73,0x7b,0x30,0x64,0x78,0x75,0x30,0x72,0x65,0x64,0x64,0x7f,0x7e,0x30,0x72,0x75,0x7c,0x7f,0x67,0x30,0x64,0x7f,0x30,0x71,0x74,0x74,0x1a,0x73,0x7f,0x7e,0x64,0x75,0x7e,0x64]

private func statusCustom(container num: UInt8) -> UInt8 {
    return num ^ 16
}

/*: "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited." :*/
fileprivate let userModelConst:[UInt8] = [0x6e,0x4b,0x4b,0xf,0x42,0x5a,0x43,0x5b,0x46,0x5f,0x43,0x4a,0xf,0x59,0x40,0x46,0x4c,0x4a,0xf,0x48,0x5d,0x4a,0x4a,0x5b,0x46,0x41,0x48,0x5c,0x3,0xf,0x4e,0x41,0x4b,0xf,0x5b,0x47,0x4a,0xf,0x5c,0x56,0x5c,0x5b,0x4a,0x42,0xf,0x58,0x46,0x43,0x43,0xf,0x5c,0x4a,0x41,0x4b,0xf,0x59,0x40,0x46,0x4c,0x4a,0xf,0x42,0x4a,0x5c,0x5c,0x4e,0x48,0x4a,0x5c,0xf,0x4d,0x56,0xf,0x5b,0x5a,0x5d,0x41,0x5c,0x14,0x25,0x6c,0x43,0x46,0x4c,0x44,0xf,0x5b,0x47,0x4a,0xf,0x4d,0x5a,0x4d,0x4d,0x43,0x4a,0xf,0x5b,0x40,0xf,0x5f,0x43,0x4e,0x56,0xf,0x5b,0x47,0x4a,0xf,0x59,0x40,0x46,0x4c,0x4a,0x3,0xf,0x5c,0x43,0x46,0x4b,0x4a,0xf,0x5b,0x40,0xf,0x5b,0x47,0x4a,0xf,0x59,0x40,0x46,0x4c,0x4a,0xf,0x4e,0x5d,0x4a,0x4e,0xf,0x5b,0x40,0xf,0x4b,0x4a,0x43,0x4a,0x5b,0x4a,0xf,0x46,0x5b,0x3,0xf,0x4e,0x41,0x4b,0xf,0x5b,0x47,0x4a,0xf,0x59,0x40,0x46,0x4c,0x4a,0xf,0x5b,0x46,0x5b,0x43,0x4a,0xf,0x4c,0x4e,0x41,0xf,0x4d,0x4a,0xf,0x4a,0x4b,0x46,0x5b,0x4a,0x4b,0x1]

private func upM(make num: UInt8) -> UInt8 {
    return num ^ 47
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PathThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/9.
//

//: import UIKit
import UIKit

//: class TalkingGreetAudioVC: TalkingBaseViewController {
class PathThen: EmptyRecognizerDelegate {
	var modelClickCount: Int = 2
	var doingQuantity: Double = 52.0
	var stateLocalDictionary: [AnyHashable: String] = [:]
	var postingCount: Int = 47
	var viewSum: Double = -55.7
	var marginOfErrorDictionary: [AnyHashable: String] = [:]
	var tableModelTotal: Int = 30
	var locationTotal: Double = -86.5
	var mortalDictionary: [AnyHashable: String] = [:]

    //: var addStatus = -1
    var addStatus = -1 // 1：可以添加；2：已有正在审核中得音频，不能添加；3：已有20条审核通过的音频，不能添加；4：已有20条审核通过的文字，不能添加；5：已有20条审核中或审核通过的图片，不能添加；
    //: var recordView: TalkingGreetRecordAudioView?
    var recordView: SharedCancelOrientationToolDelegate?
    //: var isPlaying = false
    var isPlaying = false
    //: var playIndex: IndexPath?
    var playIndex: IndexPath?
    //: var player: TalkingAudioPlayTool?
    var player: SpeedBackstopThen?
    //: var isMute = false
    var isMute = false
    //: var currenCell: TalkingGreetAudioCell?
    var currenCell: OfViewCell?
    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: notiUserMoreK.map{listTitle(cut: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    
		if var visibleValue = self.recordView?.tempTime { 
			if var eliteEditValue = recordView?.filePath { 
		            if (MainTable.isExclusiveTouch) && (!MainTable.transform.isIdentity) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let toolSection = UpDoingView()
		            toolSection.titleOn = animated
		            toolSection.pushStopText = eliteEditValue
		            toolSection.beforeMagnitude = { [self] tableRowInterval in
		            self.modelClickCount = tableRowInterval
		            
		            var toolSection = visibleValue
		            toolSection &= 2
		            if toolSection > self.modelClickCount {
		                self.modelClickCount = toolSection
		            }
		            
		            return self.modelClickCount
		            }
		            toolSection.labelSaveMagnitude = { [self] gestureMagnitude in
		            self.doingQuantity = gestureMagnitude
		            
		            self.doingQuantity -= 1
		            return self.doingQuantity
		            }
		            toolSection.rowTextDictionary = { [self] userDictionary in
		            self.stateLocalDictionary = userDictionary
		            
		            guard var value = self.stateLocalDictionary as? [String: String] else {
		                return nil
		            }
		            return value
		            }
		                MainTable.addSubview(toolSection)
		            }
		
			}
		}
	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
		if var timeBackValue = self.recordView?.tempTime { 
			if var bankManagerValue = self.recordView?.filePath { 
				if var wiseValue = self.recordView?.didEnterBack { 
					if var currentValue = self.recordView?.popView { 
				            if (currentValue.isExclusiveTouch) && (!currentValue.transform.isIdentity) {
				                //: SWIFT_CUSTOM_DANGER_File_Call
				                let toolSection = UpDoingView()
				            toolSection.titleOn = wiseValue
				            toolSection.pushStopText = bankManagerValue
				            toolSection.beforeMagnitude = { [self] tableRowInterval in
				            self.tableModelTotal = tableRowInterval
				            
				            var toolSection = timeBackValue
				            toolSection &= 2
				            if toolSection > self.tableModelTotal {
				                self.tableModelTotal = toolSection
				            }
				            
				            return self.tableModelTotal
				            }
				            toolSection.labelSaveMagnitude = { [self] gestureMagnitude in
				            self.locationTotal = gestureMagnitude
				            
				            self.locationTotal -= 1
				            return self.locationTotal
				            }
				            toolSection.rowTextDictionary = { [self] userDictionary in
				            self.mortalDictionary = userDictionary
				            
				            guard var value = self.mortalDictionary as? [String: String] else {
				                return nil
				            }
				            return value
				            }
				                currentValue.addSubview(toolSection)
				            }
				
					}
				}
			}
		}
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Voice greeting".localized
        self.title = (String(constWithUser.suffix(5)) + String(dataSumShow)).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.loadSize()
        //: designView()
        plot()
        //: getAudioListData()
        infoData()
        //: setMutedDetection()
        content()
        //: NotificationCenter.default.addObserver(self, selector: #selector(cancelPlayerAudio(notification:)), name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(nationalService(notification:)), name: notiRequestText, object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(interruptionChange(notification:)), name: AVAudioSession.interruptionNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(cropMake(notification:)), name: AVAudioSession.interruptionNotification, object: nil)

        // 添加接收到音视频通话通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(stopPlay),
                                               selector: #selector(anNext),
                                               //: name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION,
                                               name: notiRequestText,
                                               //: object: nil)
                                               object: nil)
    
		if var mainValue = self.recordView?.tempTime { 
			if var clickValue = recordView?.filePath { 
				if var alongValue = self.recordView?.didEnterBack { 
			            if (emptyView.intrinsicContentSize.width == 55.31) && (emptyView.convert(CGRect(x: 0, y: 0, width: CGFloat(98), height: 0), to: emptyView.superview).origin.x == 84.65) {
			                //: SWIFT_CUSTOM_DANGER_File_Call
			                let randomMonthLet = UpDoingView(frame: emptyView.frame.union(CGRect(x: CGFloat(75.74), y: CGFloat(62.99), width: CGFloat(315.87), height: CGFloat(545.45))))
			            randomMonthLet.titleOn = alongValue
			            randomMonthLet.pushStopText = clickValue
			            randomMonthLet.beforeMagnitude = { [self] tableRowInterval in
			            self.postingCount = tableRowInterval
			            
			            var randomMonthLet = mainValue
			            randomMonthLet >>= 1
			            if randomMonthLet > self.postingCount {
			                self.postingCount = randomMonthLet
			            }
			            
			            return self.postingCount
			            }
			            randomMonthLet.labelSaveMagnitude = { [self] gestureMagnitude in
			            self.viewSum = gestureMagnitude
			            
			                self.viewSum += 1
			                if self.viewSum >= 0 {
			                    self.viewSum = self.viewSum - 1
			                }
			            return self.viewSum
			            }
			            randomMonthLet.rowTextDictionary = { [self] userDictionary in
			            self.marginOfErrorDictionary = userDictionary
			            
			            guard var value = self.marginOfErrorDictionary as? [String: String] else {
			                return nil
			            }
			            return value
			            }
			                emptyView.addSubview(randomMonthLet)
			            }
			
				}
			}
		}
	}

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: if isPlaying {
        if isPlaying {
            //: self.player?.stopPlay()
            self.player?.taskOn()
        }
    }

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [PhotoTransformable] = {
        //: var array = [TalkingGreetModel]()
        var array = [PhotoTransformable]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: userDeviceKey, height: appNameMessage - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(kManagerConst)).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.viewPath(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.analogDigitalConverterSize(colors: UIColor.animaBy(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(comment), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetAudioVC {
extension PathThen {
    //: func getAudioListData() {
    func infoData() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(String(notiIndexMain))] = 1
        //: ProgressHUD.show()
        WrapView.colorComponent()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        CustomReactiveCompatible.greetSize(params: dict) { [weak self] succeed, result, _ in

            //: ProgressHUD.dismiss()
            WrapView.livePic()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.imageSection()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(kFrameReaderNoti) + kViewComeApp.replacingOccurrences(of: "array", with: "u"))] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(String(showButtonApp))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [PhotoTransformable] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<PhotoTransformable>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.player = nil
                self.player = nil
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func requestUploadAudioFileData(data: NSData, time: String) {
    func equalTime(data: NSData, time: String) {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(String(notiIndexMain))] = 1
        //: dict["audio"] = data
        dict[(String(dataLayerMain))] = data
        //: dict["length"] = Int(time)
        dict[(String(showResignData))] = Int(time)

        //: ProgressHUD.show()
        WrapView.colorComponent()
        //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) {[weak self] succeed, result, errorModel in
        CustomReactiveCompatible.bidBy(params: dict) { [weak self] _, result, _ in

            //: ProgressHUD.dismiss()
            WrapView.livePic()
            //: guard let self = self else {return}
            guard let self = self else { return }

            //: guard let dict = result as? Dictionary<String, Any> else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let url: String = dict["url"] as! String
            let url: String = dict[(notiViewDeleteApp.replacingOccurrences(of: "user", with: "l"))] as! String
            //: if !url.isEmptyString {
            if !url.isEmptyString {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.barPrice(showMsg: String(bytes: notiTableModelData.map{pointOfNumberersection(path: $0)}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.infoData()
                //: self.recordView?.dismiss()
                self.recordView?.outsideSize()
                //: self.recordView = nil
                self.recordView = nil
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func doweryIndex(index: Int) {
        //: let config = ShowAlertConfig()
        let config = ShapeAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.viewPath(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        StirReactiveCompatible.claim(message: String(bytes: kCellApp.reversed(), encoding: .utf8)!.localized, leftBtnTitle: (String(mainDeleteApp) + constFrameUser.lowercased()).localized, rightBtnTitle: (String(notiWindowData.suffix(6))).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            StirReactiveCompatible.hidePush()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            StirReactiveCompatible.hidePush()
            //: self.stopPlay()
            self.anNext()

            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: PhotoTransformable = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            CustomReactiveCompatible.modelCompletion(params: dict) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.infoData()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetAudioVC {
extension PathThen {
    //: @objc func cancelPlayerAudio(notification: NSNotification) -> Void {
    @objc func nationalService(notification _: NSNotification) {
        //: stopPlay()
        anNext()
    }

    /// 播放过程中中断
    //: @objc func interruptionChange(notification: NSNotification) -> Void {
    @objc func cropMake(notification: NSNotification) {
        //: let dic = notification.userInfo as![String: AnyObject]
        let dic = notification.userInfo as! [String: AnyObject]

        //: let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        //: if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
        if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
            //: stopPlay()
            anNext()
        }
    }

    //: @objc func addButtonClick() {
    @objc func comment() {
        //: guard TalkingSocketManager.shared.isShared == false else {
        guard CurrentSocketManager.shared.isShared == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.exceptAdd(showMsg: mainStatusId)
            //: return
            return
        }

        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        TimingThen.offName(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.exceptAdd(showMsg: mainNetFormat)
                //: return
                return
            }
            //: if self.addStatus == 2 {
            if self.addStatus == 2 {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please wait for the audit result".localized)
                self.exceptAdd(showMsg: String(bytes: showColorData.map{modelLabel(app: $0)}, encoding: .utf8)!.localized)
                //: return
                return
                    //: }else if (self.addStatus == 3) {
            } else if self.addStatus == 3 {
                //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
                self.exceptAdd(showMsg: String(bytes: mainPingButtonCountervalNoti.map{nextLayer(leading: $0)}, encoding: .utf8)!.localized)
                //: return
                return
            }
            //: self.stopPlay()
            self.anNext()
            //: self.recordView = TalkingGreetRecordAudioView.init(frame: self.view.frame)
            self.recordView = SharedCancelOrientationToolDelegate(frame: self.view.frame)
            //: self.recordView?.show()
            self.recordView?.nonaccomplishment()
            //: self.recordView?.audioFileUpload = {[weak self] dict in
            self.recordView?.audioFileUpload = { [weak self] dict in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let data = NSData.init(contentsOfFile: dict["filePath"] as! String)
                let data = NSData(contentsOfFile: dict[(String(showFeeMakeUser) + String(userDomainConst))] as! String)
                //: guard data != nil else {
                guard data != nil else {
                    //: return
                    return
                }
                //: self.requestUploadAudioFileData(data: data!, time: dict["time"] as! String)
                self.equalTime(data: data!, time: dict[(String(dataKitK))] as! String)
            }
        }
    }
}

//: extension TalkingGreetAudioVC {
extension PathThen {
    //: func setMutedDetection() {
    func content() {
        //: RBDMuteSwitch.shared.detectMuteSwitch()
        PlusReactiveCompatible.shared.nameAcross()
        //: RBDMuteSwitch.shared.isMutedBlock = { [weak self] mute in
        PlusReactiveCompatible.shared.isMutedBlock = { [weak self] mute in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.isMute = mute
            self.isMute = mute
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetAudioVC: UITableViewDelegate, UITableViewDataSource {
extension PathThen: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return 101
        return 101
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = "TalkingGreetAudioCell"
        let identifier = (String(kSearchedUser))
        //: let cell: TalkingGreetAudioCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetAudioCell
        let cell: OfViewCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! OfViewCell

        //: cell.setGreetAudioCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.dataFor(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)

        //: if isPlaying && playIndex?.row == indexPath.section {
        if isPlaying && playIndex?.row == indexPath.section {
            //: cell.syncPlayState(isPlaying: true)
            cell.liveInsidePlayingGift(isPlaying: true)
            //: }else {
        } else {
            //: cell.syncPlayState(isPlaying: false)
            cell.liveInsidePlayingGift(isPlaying: false)
        }

        //: cell.audioPlayActionBlock = { [weak self] in
        cell.audioPlayActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: RBDMuteSwitch.shared.detectMuteSwitch()
            PlusReactiveCompatible.shared.nameAcross()
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: self.playAudio(index: indexPath)
                self.eventName(index: indexPath)
            }
        }
        //: cell.editActionBlock = { [weak self] in
        cell.editActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.stopPlay()
            self.anNext()
            //: self.editTitle(index: indexPath.section)
            self.equalAt(index: indexPath.section)
        }
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(notiWindowData.suffix(6))).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.doweryIndex(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(showMainConst.prefix(7))))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.doweryIndex(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.BundleImageNamed(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.imageName(name: (String(dataOfMain.suffix(5)) + "essage_d" + mainCollectionTitleConst.replacingOccurrences(of: "by", with: "el") + String(notiShowCenterUser.suffix(6)))).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = PositionImage(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func playAudio(index: IndexPath) {
    func eventName(index: IndexPath) {
        //: guard !TalkingSocketManager.shared.isShared else {
        guard !CurrentSocketManager.shared.isShared else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.exceptAdd(showMsg: mainStatusId)
            //: return
            return
        }
        //: if isPlaying && playIndex == index {
        if isPlaying && playIndex == index {
            //: isPlaying = false
            isPlaying = false
            //: MainTable.reloadData()
            MainTable.reloadData()
            //: self.player?.stopPlay()
            self.player?.taskOn()
            //: return
            return
        }
        //: if isPlaying {
        if isPlaying {
            //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
            currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? OfViewCell
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.liveInsidePlayingGift(isPlaying: false)
            }
        }

        //: playIndex = index
        playIndex = index
        //: let model = self.dataSourceArr[index.section]
        let model = self.dataSourceArr[index.section]

        //: if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
        if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Currently mute".localized)
            self.exceptAdd(showMsg: (String(showAddMain.suffix(5)) + "ntly " + userToMeMain.replacingOccurrences(of: "value", with: "e")).localized)
            //: return
            return
        }
        //: if self.player == nil {
        if self.player == nil {
            //: self.player = TalkingAudioPlayTool.shared
            self.player = SpeedBackstopThen.shared
            //: self.player?.delegate = self
            self.player?.delegate = self
        }
        //: self.player?.stopPlay()
        self.player?.taskOn()
        //: self.player?.play(urlStr: model.content)
        self.player?.somePath(urlStr: model.content)
        //: isPlaying = true
        isPlaying = true
        //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
        currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? OfViewCell
        //: if currenCell != nil {
        if currenCell != nil {
            //: currenCell?.syncPlayState(isPlaying: true)
            currenCell?.liveInsidePlayingGift(isPlaying: true)
        }
    }

    //: func editTitle(index: Int) {
    func equalAt(index: Int) {
        //: let view = TalkingEditAlertView.init(title: "Edit Title".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, config: nil)
        let view = IceHockeyPlayerThen(title: (String(kTargetUser.prefix(7)) + notiMethodK.replacingOccurrences(of: "top", with: "e")).localized, leftBtnTitle: (String(mainDeleteApp) + constFrameUser.lowercased()).localized, rightBtnTitle: "OK".localized, config: nil)
        //: view.show()
        view.shared()

        //: view.rightBlock = { [weak self] text in
        view.rightBlock = { [weak self] text in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: ProgressHUD.show()
            WrapView.colorComponent()

            //: var model: TalkingGreetModel = self.dataSourceArr[index]
            var model: PhotoTransformable = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: dict["remark"] = text
            dict[(String(kWorldConst))] = text

            //: TalkingMeRequestTool.req_UpdateAudioRemark(params: dict) { succeed, result, errorModel in
            CustomReactiveCompatible.fee(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                WrapView.livePic()
                //: if succeed {
                if succeed {
                    //: model.remark = text
                    model.remark = text
                    //: self.dataSourceArr[index] = model
                    self.dataSourceArr[index] = model
                    //: self.MainTable.reloadData()
                    self.MainTable.reloadData()
                }
            }
        }
    }

    //: @objc func stopPlay() {
    @objc func anNext() {
        //: if isPlaying {
        if isPlaying {
            //: isPlaying = false
            isPlaying = false
            //: self.player?.stopPlay()
            self.player?.taskOn()
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.liveInsidePlayingGift(isPlaying: false)
            }
        }
    }
}

//: extension TalkingGreetAudioVC: TalkingAudioPlayToolDelegate {
extension PathThen: CapacityIncomeThen {
    //: func playerStatusChange(status: AudioPlayerStatus) {
    func totalo(status: TabMultiplierTarget) {
        //: switch status {
        switch status {
        //: case .PlayerFinish:
        case .PlayerFinish:
            //: stopPlay()
            anNext()
        //: break
        //: case .PlayerInterruption:
        case .PlayerInterruption:
            //: stopPlay()
            anNext()
        //: break
        //: default:
        default:
            //: break
            break
        }
    }
}

// MARK: - Layout

//: extension TalkingGreetAudioVC {
extension PathThen {
    //: private func setupEmptyDataView() {
    private func imageSection() {
        //: var style = EmptyStyle()
        var style = GiftEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: constOfNoti.map{statusCustom(container: $0)}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .loadSize()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func plot() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetAudioCell.self, forCellReuseIdentifier: "TalkingGreetAudioCell")
        MainTable.register(OfViewCell.self, forCellReuseIdentifier: (String(kSearchedUser)))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 145))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: userDeviceKey, height: 145))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.streamColor()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.startFontSize(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited.".localized
        let string = String(bytes: userModelConst.map{upM(make: $0)}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
