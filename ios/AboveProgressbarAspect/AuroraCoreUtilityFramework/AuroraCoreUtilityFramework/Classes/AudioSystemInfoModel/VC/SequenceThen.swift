
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let constReadShow:[UInt8] = [0xb,0xc,0xb,0x16,0x4a,0x1,0xd,0x6,0x7,0x10,0x58,0x4b,0x42,0xa,0x3,0x11,0x42,0xc,0xd,0x16,0x42,0x0,0x7,0x7,0xc,0x42,0xb,0xf,0x12,0xe,0x7,0xf,0x7,0xc,0x16,0x7,0x6]

private func burnTing(to num: UInt8) -> UInt8 {
    return num ^ 98
}

/*: "Text greeting" :*/
fileprivate let appPushMakeK:[Character] = ["T","e"]
fileprivate let userColorLocalShow:String = "selected safetyxt gr"

/*: "Add" :*/
fileprivate let appPlayShow:String = "center infoAdd"

/*: "type" :*/
fileprivate let kCountMain:String = "typshare"

/*: "addStatus" :*/
fileprivate let mainAppViewNoti:String = "to marginaddSt"

/*: "list" :*/
fileprivate let kSendMain:[Character] = ["l","i","s","t"]

/*: "Are you sure to delete this message？" :*/
fileprivate let dataTopApp:[UInt8] = [0x89,0xba,0xad,0xe8,0xb1,0xa7,0xbd,0xe8,0xbb,0xbd,0xba,0xad,0xe8,0xbc,0xa7,0xe8,0xac,0xad,0xa4,0xad,0xbc,0xad,0xe8,0xbc,0xa0,0xa1,0xbb,0xe8,0xa5,0xad,0xbb,0xbb,0xa9,0xaf,0xad,0x27,0x74,0x57]

private func centerBlock(data num: UInt8) -> UInt8 {
    return num ^ 200
}

/*: "Cancel" :*/
fileprivate let userPhotoTheApp:String = "section app name imageCancel"

/*: "Delete" :*/
fileprivate let appViewUser:String = "Deletguard return image"
fileprivate let appShowMain:[Character] = ["e"]

/*: "id" :*/
fileprivate let userSubK:String = "ivideo"

/*: "The upper limit has been reached" :*/
fileprivate let userToWithApp:[UInt8] = [0xca,0xde,0xdb,0x96,0xeb,0xe6,0xe6,0xdb,0xe8,0x96,0xe2,0xdf,0xe3,0xdf,0xea,0x96,0xde,0xd7,0xe9,0x96,0xd8,0xdb,0xdb,0xe4,0x96,0xe8,0xdb,0xd7,0xd9,0xde,0xdb,0xda]

fileprivate func foulUp(data num: UInt8) -> UInt8 {
    let value = Int(num) - 118
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "text" :*/
fileprivate let kFieldShow:[Character] = ["t","e","x","t"]

/*: "Successfully added, please wait for approval" :*/
fileprivate let showClearUser:[UInt8] = [0x57,0x71,0x67,0x67,0x61,0x77,0x77,0x62,0x71,0x68,0x68,0x7d,0x24,0x65,0x60,0x60,0x61,0x60,0x28,0x24,0x74,0x68,0x61,0x65,0x77,0x61,0x24,0x73,0x65,0x6d,0x70,0x24,0x62,0x6b,0x76,0x24,0x65,0x74,0x74,0x76,0x6b,0x72,0x65,0x68]

private func toPath(bag num: UInt8) -> UInt8 {
    return num ^ 4
}

/*: "Any-Hex/Java" :*/
fileprivate let mainEnterApp:String = "self original request app selfAny-H"

/*: "MainReactiveCompatible" :*/
fileprivate let userRowData:[Character] = ["T","a","l","k","i","n","g","G","r","e","e","t","T","e","x","t","C","e","l","l"]

/*: "#FF506D" :*/
fileprivate let appViewNoti:String = "#Fmin list"
fileprivate let constColorMain:String = "Fpath6D"

/*: "btn_message_delete_nor" :*/
fileprivate let constBlockShow:[Character] = ["b","t","n","_","m","e","s","s","a","g","e","_","d","e","l","e","t","e","_","n","o"]
fileprivate let dataHomeUser:[Character] = ["r"]

/*: "Click the button below to add\ncontent" :*/
fileprivate let dataViewConst:[UInt8] = [0xc4,0xeb,0xee,0xe4,0xec,0xa7,0xf3,0xef,0xe2,0xa7,0xe5,0xf2,0xf3,0xf3,0xe8,0xe9,0xa7,0xe5,0xe2,0xeb,0xe8,0xf0,0xa7,0xf3,0xe8,0xa7,0xe6,0xe3,0xe3,0x8d,0xe4,0xe8,0xe9,0xf3,0xe2,0xe9,0xf3]

private func rawData(path num: UInt8) -> UInt8 {
    return num ^ 135
}

/*: "When adding multiple text message, the syst em will send them by turns;slide to the message to delete" :*/
fileprivate let notiPlayCurrentTableShow:[UInt8] = [0xa9,0x96,0x9b,0x90,0xde,0x9f,0x9a,0x9a,0x97,0x90,0x99,0xde,0x93,0x8b,0x92,0x8a,0x97,0x8e,0x92,0x9b,0xde,0x8a,0x9b,0x86,0x8a,0xde,0x93,0x9b,0x8d,0x8d,0x9f,0x99,0x9b,0xd2,0xde,0x8a,0x96,0x9b,0xde,0x8d,0x87,0x8d,0x8a,0xde,0x9b,0x93,0xde,0x89,0x97,0x92,0x92,0xde,0x8d,0x9b,0x90,0x9a,0xde,0x8a,0x96,0x9b,0x93,0xde,0x9c,0x87,0xde,0x8a,0x8b,0x8c,0x90,0x8d,0xc5,0x8d,0x92,0x97,0x9a,0x9b,0xde,0x8a,0x91,0xde,0x8a,0x96,0x9b,0xde,0x93,0x9b,0x8d,0x8d,0x9f,0x99,0x9b,0xde,0x8a,0x91,0xde,0x9a,0x9b,0x92,0x9b,0x8a,0x9b]

private func tableMode(comment num: UInt8) -> UInt8 {
    return num ^ 254
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SequenceThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/15.
//

//: import UIKit
import UIKit

//: class TalkingGreetTextVC: TalkingBaseViewController {
class SequenceThen: EmptyRecognizerDelegate {
	var postEveryDoing: Bool = true
	var clickMainCount: Int = 46
	var countGestureDictionary: [AnyHashable: String] = [:]
	var itemEnable: Bool = false
	var squeezeSum: Int = 80
	var cutDictionary: [AnyHashable: String] = [:]
	var laboratoryName: String = "arc"
	var glamourArray: [AnyHashable] = []
	var picDictionary: [AnyHashable: String] = [:]

    //: var addStatus = -1
    var addStatus = -1
    //: var recordView: TalkingGreetEditTextView?
    var recordView: HomeThen?

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: constReadShow.map{burnTing(to: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    
	if let recordView = recordView {

            if (recordView.subviews.count == 77) && (recordView.layer.anchorPoint.x != 0.5) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let centralArteryOfTheRetina = FirstView()
            
            
            centralArteryOfTheRetina.groundClose = { [self] pictureMeOpen in
            self.itemEnable = pictureMeOpen
            
            var centralArteryOfTheRetina = self.hideNavi
                centralArteryOfTheRetina = false
                centralArteryOfTheRetina = !centralArteryOfTheRetina
            if centralArteryOfTheRetina != self.itemEnable {
                self.itemEnable = centralArteryOfTheRetina
            }
            
            self.itemEnable = !self.itemEnable
            return self.itemEnable
            }
            centralArteryOfTheRetina.labQuantity = { [self] atConversationQuantity in
            self.squeezeSum = atConversationQuantity
            
            var centralArteryOfTheRetina = addStatus
            centralArteryOfTheRetina %= 3
            if centralArteryOfTheRetina > self.squeezeSum {
                self.squeezeSum = centralArteryOfTheRetina
            }
            
            return self.squeezeSum
            }
            centralArteryOfTheRetina.signatureDictionary = { [self] allowAreaDictionary in
            self.cutDictionary = allowAreaDictionary
            
            guard var value = self.cutDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                recordView.addSubview(centralArteryOfTheRetina)
            }

	}

	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
		if var rootMenuValue = recordView?.inputTView.placeholder { 
			if var withItValue = recordView?.inputTView.placeholder { 
		            if (self.view.bounds.origin.y == 3.19) && (self.isBeingPresented && !self.isViewLoaded) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let statusDown = FirstController()
		            statusDown.picEnable = animated
		
		            statusDown.tableTitle = withItValue
		            statusDown.removeTitle = { [self] doingName in
		            self.laboratoryName = doingName
		            
		            var statusDown = rootMenuValue
		            var manager: String.Encoding = .utf8
		            if let managerString = try? String(contentsOf: URL(string: "request.gift", relativeTo: URL(string: "/recommend"))!, usedEncoding: &manager) {
		                statusDown = managerString
		            }
		            if statusDown.hasSuffix(self.laboratoryName) {
		                self.laboratoryName = statusDown
		            }
		            
		            return self.laboratoryName
		            }
		            statusDown.offSubArray = { [self] replyArray in
		            self.glamourArray = replyArray
		            
		            guard var value = self.glamourArray as? [String] else {
		                return nil
		            }
		            return value
		            }
		            statusDown.sprechstimmeDictionary = { [self] allowAreaDictionary in
		            self.picDictionary = allowAreaDictionary
		            
		            guard var value = self.picDictionary as? [String: String] else {
		                return nil
		            }
		            return value
		            }
		                self.navigationController?.pushViewController(statusDown.self, animated: false)
		            }
		
			}
		}
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Text greeting".localized
        self.title = (String(appPushMakeK) + String(userColorLocalShow.suffix(5)) + "eeting").localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.loadSize()
        //: designView()
        doing()
        //: getAudioListData()
        voice()
    
		if var locationAppearValue = recordView?.popView?.isRemoveTapGes { 
		if let recordView = recordView {
	
	            if (recordView.contentView.subviews.count == 77) && (recordView.contentView.layer.anchorPoint.x != 0.5) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let centralArteryOfTheRetina = FirstView()
	            
	            
	            centralArteryOfTheRetina.groundClose = { [self] pictureMeOpen in
	            self.postEveryDoing = pictureMeOpen
	            
	            var centralArteryOfTheRetina = locationAppearValue
	                centralArteryOfTheRetina = false
	                centralArteryOfTheRetina = !centralArteryOfTheRetina
	            if centralArteryOfTheRetina != self.postEveryDoing {
	                self.postEveryDoing = centralArteryOfTheRetina
	            }
	            
	            self.postEveryDoing = !self.postEveryDoing
	            return self.postEveryDoing
	            }
	            centralArteryOfTheRetina.labQuantity = { [self] atConversationQuantity in
	            self.clickMainCount = atConversationQuantity
	            
	            var centralArteryOfTheRetina = addStatus
	            centralArteryOfTheRetina %= 3
	            if centralArteryOfTheRetina > self.clickMainCount {
	                self.clickMainCount = centralArteryOfTheRetina
	            }
	            
	            return self.clickMainCount
	            }
	            centralArteryOfTheRetina.signatureDictionary = { [self] allowAreaDictionary in
	            self.countGestureDictionary = allowAreaDictionary
	            
	            guard var value = self.countGestureDictionary as? [String: String] else {
	                return nil
	            }
	            return value
	            }
	                recordView.contentView.addSubview(centralArteryOfTheRetina)
	            }
	
		}
	
		}
	}

    //: deinit {
    deinit {}

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [PhotoTransformable] = {
        //: var array = [TalkingGreetModel]()
        var array = [PhotoTransformable]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: userDeviceKey, height: appNameMessage - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(appPlayShow.suffix(3))).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.viewPath(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.analogDigitalConverterSize(colors: UIColor.animaBy(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(naturalEvent), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetTextVC {
extension SequenceThen {
    //: func getAudioListData() {
    func voice() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 2
        dict[(kCountMain.replacingOccurrences(of: "share", with: "e"))] = 2
        //: ProgressHUD.show()
        WrapView.colorComponent()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        CustomReactiveCompatible.greetSize(params: dict) { [weak self] succeed, result, _ in
            //: ProgressHUD.dismiss()
            WrapView.livePic()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.noneInfo()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(mainAppViewNoti.suffix(5)) + "atus")] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(String(kSendMain))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [PhotoTransformable] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<PhotoTransformable>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func bindIndex(index: Int) {
        //: let config = ShowAlertConfig()
        let config = ShapeAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.viewPath(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        StirReactiveCompatible.claim(message: String(bytes: dataTopApp.map{centerBlock(data: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(userPhotoTheApp.suffix(6))).localized, rightBtnTitle: (String(appViewUser.prefix(5)) + String(appShowMain)).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            StirReactiveCompatible.hidePush()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            StirReactiveCompatible.hidePush()
            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: PhotoTransformable = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: ProgressHUD.show()
            WrapView.colorComponent()
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            CustomReactiveCompatible.modelCompletion(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                WrapView.livePic()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.voice()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetTextVC {
extension SequenceThen {
    //: @objc func addButtonClick() {
    @objc func naturalEvent() {
        //: if (self.addStatus == 4) {
        if self.addStatus == 4 {
            //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
            self.exceptAdd(showMsg: String(bytes: userToWithApp.map{foulUp(data: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.recordView = TalkingGreetEditTextView.init(frame: self.view.frame)
        self.recordView = HomeThen(frame: self.view.frame)
        //: self.recordView?.show()
        self.recordView?.less()
        //: self.recordView?.textUpload = {[weak self] str in
        self.recordView?.textUpload = { [weak self] str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let tempStr = self.unicodeString(str)
            let tempStr = self.packthread(str)
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["type"] = 2
            dict[(kCountMain.replacingOccurrences(of: "share", with: "e"))] = 2
            //: dict["text"] = tempStr
            dict[(String(kFieldShow))] = tempStr
            //: ProgressHUD.show()
            WrapView.colorComponent()
            //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) { succeed, result, errorModel in
            CustomReactiveCompatible.bidBy(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                WrapView.livePic()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.barPrice(showMsg: String(bytes: showClearUser.map{toPath(bag: $0)}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.voice()
                //: self.recordView?.dismiss()
                self.recordView?.cellTo()
            }
        }
    }

    //: func unicodeString(_ str: String) -> String {
    func packthread(_ str: String) -> String {
        //: let mutableStr = NSMutableString(string: str) as CFMutableString
        let mutableStr = NSMutableString(string: str) as CFMutableString
        //: CFStringTransform(mutableStr, nil, "Any-Hex/Java" as CFString, true)
        CFStringTransform(mutableStr, nil, (String(mainEnterApp.suffix(5)) + "ex/Java") as CFString, true)
        //: return mutableStr as String
        return mutableStr as String
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetTextVC: UITableViewDelegate, UITableViewDataSource {
extension SequenceThen: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: let model = self.dataSourceArr[indexPath.section]
        let model = self.dataSourceArr[indexPath.section]
        //: var height: CGFloat = 18+22
        var height: CGFloat = 18 + 22
        //: let rect = model.content.boundingRect(with: CGSize(width: ScreenWidth-60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.pingfangFont(type: .Medium, fontSize: 16)], context: nil)
        let rect = model.content.boundingRect(with: CGSize(width: userDeviceKey - 60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.viewPath(type: .Medium, fontSize: 16)], context: nil)
        //: height += rect.height
        height += rect.height
        //: if model.status == 0 {
        if model.status == 0 {
            //: height += 16+12
            height += 16 + 12
        }
        //: return height
        return height
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = "TalkingGreetTextCell"
        let identifier = (String(userRowData))
        //: let cell: TalkingGreetTextCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetTextCell
        let cell: MainReactiveCompatible = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! MainReactiveCompatible

        //: cell.setGreetTextCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.commonwealthStatus(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)
        //: return cell
        return cell
    }

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(appViewUser.prefix(5)) + String(appShowMain)).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.bindIndex(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(appViewNoti.prefix(2)) + constColorMain.replacingOccurrences(of: "path", with: "50")))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.bindIndex(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.BundleImageNamed(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.imageName(name: (String(constBlockShow) + String(dataHomeUser))).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = PositionImage(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.01
        return 0.01
    }
}

// MARK: - Layout

//: extension TalkingGreetTextVC {
extension SequenceThen {
    //: private func setupEmptyDataView() {
    private func noneInfo() {
        //: var style = EmptyStyle()
        var style = GiftEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: dataViewConst.map{rawData(path: $0)}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .loadSize()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func doing() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }

        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetTextCell.self, forCellReuseIdentifier: "TalkingGreetTextCell")
        MainTable.register(MainReactiveCompatible.self, forCellReuseIdentifier: (String(userRowData)))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 100))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: userDeviceKey, height: 100))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.streamColor()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.startFontSize(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "When adding multiple text message, the syst em will send them by turns;slide to the message to delete".localized
        let string = String(bytes: notiPlayCurrentTableShow.map{tableMode(comment: $0)}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
