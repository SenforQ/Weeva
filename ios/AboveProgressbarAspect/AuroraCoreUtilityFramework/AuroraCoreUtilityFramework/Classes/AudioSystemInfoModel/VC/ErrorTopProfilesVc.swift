
//: Declare String Begin

/*: "nil" :*/
fileprivate let userNamePriceK:[Character] = ["n","i","l"]

/*: "Edit profiles" :*/
fileprivate let appPushK:[Character] = ["E","d","i","t"," ","p","r","o"]
fileprivate let dataShareMain:[Character] = ["f","i","l","e","s"]

/*: "Done" :*/
fileprivate let appDateMain:[Character] = ["D","o","n","e"]

/*: "#999999" :*/
fileprivate let showPopLoadMain:[Character] = ["#","9","9","9","9","9","9"]

/*: "uid" :*/
fileprivate let appBeginData:[Character] = ["u","i","d"]

/*: "AddTextCell" :*/
fileprivate let userSearchedMain:[Character] = ["T","a","l","k","i","n","g","E"]
fileprivate let userActualShow:String = "time labelditNa"

/*: "PlusViewCell" :*/
fileprivate let showFormatApp:[Character] = ["T","a","l","k","i","n","g","E","d","i","t","S","i","g","n","C","e"]
fileprivate let constOfData:[Character] = ["l","l"]

/*: "Save the change?" :*/
fileprivate let notiLabConst:String = "as managerSave "
fileprivate let showImageData:String = "chaindex"
fileprivate let constAmNoti:String = "enable self show number returne?"

/*: "Cancel" :*/
fileprivate let notiEachUser:[Character] = ["C"]
fileprivate let userTextEqualMain:String = "contentncel"

/*: "Save" :*/
fileprivate let mainBackgroundUser:String = "self makeSave"

/*: "nickname" :*/
fileprivate let mainLabelRowK:String = "nikeyk"

/*: "birthday" :*/
fileprivate let appRedNoti:[Character] = ["b","i","r","t","h","d"]
fileprivate let showRecordNoti:String = "label"

/*: "desc" :*/
fileprivate let constViewShow:String = "depath"

/*: "Modify the success" :*/
fileprivate let appModeMain:[Character] = ["M","o","d","i","f","y"," ","t","h","e"]
fileprivate let notiErrorVideoTargetUser:[Character] = [" ","s","u","c","c","e","s","s"]

/*: "pic" :*/
fileprivate let kPicPlayShow:[Character] = ["p","i","c"]

/*: "[ :*/
fileprivate let constDirectionMain:String = "["

/*: ]" :*/
fileprivate let constFilterMain:[Character] = ["]"]

/*: "pids" :*/
fileprivate let notiImageConst:String = "pnords"

/*: "aboutMe" :*/
fileprivate let showButtonMain:String = "aboutMeuser image text have data"

/*: "interest" :*/
fileprivate let dataLicenseConst:String = "intfromest"

/*: "tagIds" :*/
fileprivate let showDataOfModelConst:[Character] = ["t"]
fileprivate let showVoiceNoti:[Character] = ["a","g","I","d","s"]

/*: "category" :*/
fileprivate let notiPhotoApp:[Character] = ["c","a","t","e","g"]
fileprivate let showSucceedMomentPreviousConst:String = "orflow"

/*: "An error occurred, please try again" :*/
fileprivate let mainEqualConst:[UInt8] = [0x4b,0x64,0x2a,0x6f,0x78,0x78,0x65,0x78,0x2a,0x65,0x69,0x69,0x7f,0x78,0x78,0x6f,0x6e,0x26,0x2a,0x7a,0x66,0x6f,0x6b,0x79,0x6f,0x2a,0x7e,0x78,0x73,0x2a,0x6b,0x6d,0x6b,0x63,0x64]

private func dataLocal(content num: UInt8) -> UInt8 {
    return num ^ 10
}

/*: "imgUrl" :*/
fileprivate let showTextData:String = "as float player player colorimgUr"
fileprivate let appViewGiftUser:String = "source"

/*: "videoUrl" :*/
fileprivate let appMakeConst:[Character] = ["v","i","d","e","o","U","r","l"]

/*: "Video cover submitted successfully" :*/
fileprivate let constLiveUser:[UInt8] = [0x57,0x68,0x65,0x64,0x6e,0x21,0x62,0x6e,0x77,0x64,0x73,0x21,0x72,0x74,0x63,0x6c,0x68,0x75,0x75,0x64,0x65,0x21,0x72,0x74,0x62,0x62,0x64,0x72,0x72,0x67,0x74,0x6d,0x6d,0x78]

private func modelMake(from num: UInt8) -> UInt8 {
    return num ^ 1
}

/*: "AlongView" :*/
fileprivate let mainLabNoti:[Character] = ["T","a","l","k","i","n","g","E","d"]
fileprivate let constColorShow:[Character] = ["i","t","V","i","d","e","o","C","e","l","l"]

/*: "video_url" :*/
fileprivate let kPictureApp:[Character] = ["v","i","d","e","o","_","u"]
fileprivate let kStyleMain:[Character] = ["r","l"]

/*: "img_url" :*/
fileprivate let userEmptyEqualMain:String = "img_urlremove video"

/*: "status" :*/
fileprivate let userViewData:String = "regulartaturegular"

/*: "LayerReactiveCompatible" :*/
fileprivate let notiErrorData:[Character] = ["T","a","l","k","i","n","g","E","d"]
fileprivate let appFemaleModeBlockShow:String = "itPhocell button content style fatal"

/*: "StateViewCell" :*/
fileprivate let kVerticalApp:String = "TalkingEmode image else"
fileprivate let notiGenderShow:String = "var name model data observerditB"
fileprivate let kManagerShow:String = "top make title height pathdayCell"

/*: "CharacteristicRootOfASquareMatrixReactiveCompatible" :*/
fileprivate let userSharedConst:[Character] = ["T","a","l","k","i","n","g","E","d","i","t"]
fileprivate let userEqualConst:[Character] = ["A","b","o","u","t","M","e","C","e","l","l"]

/*: "UITableViewCell" :*/
fileprivate let appListK:String = "UITaline self"
fileprivate let userToNoti:String = "wCellinside text if pro button"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ErrorTopProfilesVc.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/8/30.
//

//: import UIKit
import UIKit

//: class TalkingEditProfilesVC: TalkingBaseViewController {
class ErrorTopProfilesVc: EmptyRecognizerDelegate {
	var currentDoing: Bool = false
	var lastTextTotal: Int = 14
	var frameDictionary: [AnyHashable: String] = [:]
	var involvementEnable: Bool = false
	var laborResourcesCount: Int = 12
	var addInviteFinishDictionary: [AnyHashable: String] = [:]
	var kindSaveCount: Double = 84.6
	var toName: String = "bag"
	var viewArray: [AnyHashable] = []

    //: var seleteAboutme: [UserSeleteTagModel] = []
    var seleteAboutme: [MinDetailHandyJSON] = []
    //: var seleteInters: [UserSeleteTagModel] = []
    var seleteInters: [MinDetailHandyJSON] = []

    //: var isdeleteAboutme = false
    var isdeleteAboutme = false
    //: var isdeleteInters = false
    var isdeleteInters = false

    //: var isTips = false
    var isTips = false

    //: var PhotoArray: [TalkingUserInfoGalleryModel] = []
    var PhotoArray: [TempCustomModelType] = []
    //: var seletePhotoArray: [UIImage] = []
    var seletePhotoArray: [UIImage] = []
    //: var DeletePhotoArray: [String] = []
    var DeletePhotoArray: [String] = []

    //: var nameStr: String = ""
    var nameStr: String = ""
    //: var birthday: String = ""
    var birthday: String = ""
    //: var signStr: String = "nil"
    var signStr: String = (String(userNamePriceK))

    //: private var VideoIconPath = ""
    private var VideoIconPath = ""
    //: private var videoPath = ""
    private var videoPath = ""
    //: private var videoStatus = -3
    private var videoStatus = -3 // 视频审核状态：0待审核 1审核通过 -1审核拒绝 -3未上传
    //: private var isChangeVideo = false
    private var isChangeVideo = false

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = false
        self.hideNavi = false
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.loadSize()
        //: self.title = "Edit profiles".localized
        self.title = (String(appPushK) + String(dataShareMain)).localized

        //: let btn = UIButton(frame: CGRect(x: 0, y: 0, width: 40, height: 18))
        let btn = UIButton(frame: CGRect(x: 0, y: 0, width: 40, height: 18))
        //: btn.setTitle("Done".localized, for: .normal)
        btn.setTitle((String(appDateMain)).localized, for: .normal)
        //: btn.setTitleColor(UIColor.init(hex: "#999999"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(showPopLoadMain))), for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.viewPath(type: .Medium, fontSize: 15)
        //: btn.addTarget(self, action: #selector(doneBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(gildTheLilyClick), for: .touchUpInside)
        //: let item = UIBarButtonItem(customView: btn)
        let item = UIBarButtonItem(customView: btn)
        //: self.navigationItem.rightBarButtonItem=item
        self.navigationItem.rightBarButtonItem = item

        //: seleteAboutme = PositionLabelReactiveCompatible.share.loginUserMode.aboutMe!
        seleteAboutme = PositionLabelReactiveCompatible.share.loginUserMode.aboutMe!
        //: seleteInters = PositionLabelReactiveCompatible.share.loginUserMode.interest!
        seleteInters = PositionLabelReactiveCompatible.share.loginUserMode.interest!
        //: designView()
        naturalResources()
        //: GetGallery()
        collectionEqual()
        //: getVideoInfo()
        blockKey()

        //: NotificationCenter.default.addObserver(self, selector: #selector(keyboardShowBeHidden(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(presentDown(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
    
            if (btn.constraintsAffectingLayout(for: .vertical).count == 81) && (btn.inputViewController != nil) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let cellImage = AppView()
            cellImage.subTalkAtTotal = videoStatus
            cellImage.totalContent = self.signStr
            
            cellImage.labelInterval = { [self] liveGreetNumber in
            self.kindSaveCount = liveGreetNumber
            
            self.kindSaveCount -= 1
            return self.kindSaveCount
            }
            cellImage.tabTitle = { [self] plusTitle in
            self.toName = plusTitle
            
            var cellImage = videoPath
            cellImage = String.localizedName(of: .unicode)
            if cellImage.contains(self.toName) {
                self.toName = cellImage
            }
            
            if self.toName[self.toName.startIndex].isUppercase {
                self.toName = ""
            }
            return self.toName
            }
            cellImage.topArray = { [self] dominantArray in
            self.viewArray = dominantArray
            
            guard var value = self.viewArray as? [String] else {
                return nil
            }
            return value
            }
                btn.addSubview(cellImage)
            }

	}

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    
            if (self.childForStatusBarStyle != nil && self.childForStatusBarStyle!.edgesForExtendedLayout == .left) && (self.navigationController != nil && self.navigationController!.hidesBarsOnSwipe) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let hazeOver = AppController()

            hazeOver.featureText = VideoIconPath
            
            hazeOver.positionDoing = { [self] agentOff in
            self.involvementEnable = agentOff
            
            var hazeOver = isdeleteAboutme
            hazeOver = true
            if hazeOver != self.involvementEnable {
                self.involvementEnable = hazeOver
            }
            
                self.involvementEnable = true
                self.involvementEnable = !self.involvementEnable
            return self.involvementEnable
            }
            hazeOver.progressCount = { [self] dataCount in
            self.laborResourcesCount = dataCount
            
            var hazeOver = videoStatus
            hazeOver <<= 1
            if hazeOver < self.laborResourcesCount {
                self.laborResourcesCount = hazeOver
            }
            
            return self.laborResourcesCount
            }
            hazeOver.betweenDictionary = { [self] cancelDictionary in
            self.addInviteFinishDictionary = cancelDictionary
            
            guard var value = self.addInviteFinishDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                self.navigationController?.pushViewController(hazeOver.self, animated: true)
            }

	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
            if (self.disablesAutomaticKeyboardDismissal) && (self.extensionContext != nil) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let visiblePremium = AppController()

            visiblePremium.featureText = signStr
            
            visiblePremium.positionDoing = { [self] agentOff in
            self.currentDoing = agentOff
            
            var visiblePremium = isTips
            visiblePremium = !visiblePremium
            if visiblePremium != self.currentDoing {
                self.currentDoing = visiblePremium
            }
            
            self.currentDoing = false
            return self.currentDoing
            }
            visiblePremium.progressCount = { [self] dataCount in
            self.lastTextTotal = dataCount
            
            var visiblePremium = videoStatus
            visiblePremium = 0
            if visiblePremium > self.lastTextTotal {
                self.lastTextTotal = visiblePremium
            }
            
            return self.lastTextTotal
            }
            visiblePremium.betweenDictionary = { [self] cancelDictionary in
            self.frameDictionary = cancelDictionary
            
            guard var value = self.frameDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                self.navigationController?.present(visiblePremium.self, animated: true) { [self] in
                }
            }

	}

    //: deinit {
    deinit {
        //: ProgressHUD.dismiss()
        WrapView.livePic()
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy load

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: userDeviceKey, height: appNameMessage), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()
}

// MARK: - Laod data

//: extension TalkingEditProfilesVC {
extension ErrorTopProfilesVc {
    //: func GetGallery() {
    func collectionEqual() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["uid"] = PositionLabelReactiveCompatible.share.loginUserMode.userID
        dict[(String(appBeginData))] = PositionLabelReactiveCompatible.share.loginUserMode.userID
        //: TalkingMeRequestTool.req_GalleryByUid(params: dict) { succeed, result, errorModel in
        CustomReactiveCompatible.tingBy(params: dict) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: let array: Array =  result as! Array<Any>
                let array: Array = result as! [Any]
                //: if array.count>0 {
                if array.count > 0 {
                    //: self.PhotoArray.removeAll()
                    self.PhotoArray.removeAll()
                }
                //: if let datas = Array<TalkingUserInfoGalleryModel>.deserialize(from: array as? Array) {
                if let datas = Array<TempCustomModelType>.deserialize(from: array as? Array) {
                    //: self.PhotoArray.append(contentsOf: (datas as? [TalkingUserInfoGalleryModel])!)
                    self.PhotoArray.append(contentsOf: (datas as? [TempCustomModelType])!)
                }
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: override func naviPopback() {
    override func begin() {
        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(userSearchedMain) + String(userActualShow.suffix(5)) + "meCell") {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: AddTextCell = i as! AddTextCell
                //: namecell.nameTF.resignFirstResponder()
                namecell.nameTF.resignFirstResponder()
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(showFormatApp) + String(constOfData)) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: PlusViewCell = i as! PlusViewCell
                //: namecell.textView.resignFirstResponder()
                namecell.textView.resignFirstResponder()
            }
        }
        //: if (nameStr.count > 0&&PositionLabelReactiveCompatible.share.loginUserMode.nickname != nameStr)||( birthday.count > 0&&PositionLabelReactiveCompatible.share.loginUserMode.birthday != self.birthday)||(signStr != "nil" && PositionLabelReactiveCompatible.share.loginUserMode.signature != signStr)||self.seletePhotoArray.count>0||self.DeletePhotoArray.count>0||isdeleteAboutme||isdeleteInters || isChangeVideo {
        if (nameStr.count > 0 && PositionLabelReactiveCompatible.share.loginUserMode.nickname != nameStr) || (birthday.count > 0 && PositionLabelReactiveCompatible.share.loginUserMode.birthday != self.birthday) || (signStr != (String(userNamePriceK)) && PositionLabelReactiveCompatible.share.loginUserMode.signature != signStr) || self.seletePhotoArray.count > 0 || self.DeletePhotoArray.count > 0 || isdeleteAboutme || isdeleteInters || isChangeVideo {
            //: let config = ShowAlertConfig()
            let config = ShapeAlertConfig()
            //: config.alignment = .center
            config.alignment = .center
            //: TalkingAlertShow.customAlert(message: "Save the change?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Save".localized, leftBlock: { [weak self] in
            StirReactiveCompatible.claim(message: (String(notiLabConst.suffix(5)) + "the " + showImageData.replacingOccurrences(of: "index", with: "ng") + String(constAmNoti.suffix(2))).localized, leftBtnTitle: (String(notiEachUser) + userTextEqualMain.replacingOccurrences(of: "content", with: "a")).localized, rightBtnTitle: (String(mainBackgroundUser.suffix(4))).localized, leftBlock: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: TalkingAlertShow.hideAlert()
                StirReactiveCompatible.hidePush()
                //: self.navigationController?.popViewController(animated: true)
                self.navigationController?.popViewController(animated: true)

                //: }, rightBlock: { [weak self] in
            }, rightBlock: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: TalkingAlertShow.hideAlert()
                StirReactiveCompatible.hidePush()
                //: self.uploadTool(isBack: true)
                self.back(isBack: true)
                //: }, config: config)
            }, config: config)
            //: } else {
        } else {
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
    }

    //: @objc func doneBtnClick() {
    @objc func gildTheLilyClick() {
        //: uploadTool(isBack: false)
        back(isBack: false)
    }

    //: func uploadTool(isBack: Bool) {
    func back(isBack: Bool) {
        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(userSearchedMain) + String(userActualShow.suffix(5)) + "meCell") {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: AddTextCell = i as! AddTextCell
                //: namecell.nameTF.resignFirstResponder()
                namecell.nameTF.resignFirstResponder()
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(showFormatApp) + String(constOfData)) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: PlusViewCell = i as! PlusViewCell
                //: namecell.textView.resignFirstResponder()
                namecell.textView.resignFirstResponder()
            }
        }

        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()

        //: if nameStr.count > 0 && PositionLabelReactiveCompatible.share.loginUserMode.nickname != nameStr {
        if nameStr.count > 0, PositionLabelReactiveCompatible.share.loginUserMode.nickname != nameStr {
            //: dict["nickname"] = nameStr
            dict[(mainLabelRowK.replacingOccurrences(of: "key", with: "c") + "name")] = nameStr
        }
        //: if birthday.count > 0 && PositionLabelReactiveCompatible.share.loginUserMode.birthday != birthday {
        if birthday.count > 0, PositionLabelReactiveCompatible.share.loginUserMode.birthday != birthday {
            //: dict["birthday"] = birthday
            dict[(String(appRedNoti) + showRecordNoti.replacingOccurrences(of: "label", with: "ay"))] = birthday
        }
        //: if signStr != "nil" && PositionLabelReactiveCompatible.share.loginUserMode.signature != signStr {
        if signStr != (String(userNamePriceK)), PositionLabelReactiveCompatible.share.loginUserMode.signature != signStr {
            //: dict["desc"] = signStr
            dict[(constViewShow.replacingOccurrences(of: "path", with: "sc"))] = signStr
        }

        //: photoAndTagTool(BackT: isBack, isTips: dict.count == 0)
        indexOpen(BackT: isBack, isTips: dict.count == 0)

        //: if VideoIconPath.count>1 && videoPath.count>1 && isChangeVideo == true && PositionLabelReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
        if VideoIconPath.count > 1, videoPath.count > 1, isChangeVideo == true, PositionLabelReactiveCompatible.share.loginUserMode.sex == PetitionKeyRepresentable.female.rawValue {
            //: uploadVideo()
            message()
        }

        //: if dict.count == 0 {
        if dict.count == 0 {
            //: return
            return
        }

        //: ProgressHUD.show()
        WrapView.colorComponent()

        //: TalkingMeRequestTool.req_UpdateUserInfo(params: dict) { [weak self] succeed, result, errorModel in
        CustomReactiveCompatible.withTitle(params: dict) { [weak self] succeed, _, errorModel in
            //: ProgressHUD.dismiss()
            WrapView.livePic()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                self.barPrice(showMsg: (String(appModeMain) + String(notiErrorVideoTargetUser)).localized)

                //: if self.nameStr.count > 0 {
                if self.nameStr.count > 0 {
                    //: PositionLabelReactiveCompatible.share.loginUserMode.nickname = self.nameStr
                    PositionLabelReactiveCompatible.share.loginUserMode.nickname = self.nameStr
                }
                //: if self.birthday.count > 0 {
                if self.birthday.count > 0 {
                    //: PositionLabelReactiveCompatible.share.loginUserMode.birthday = self.birthday
                    PositionLabelReactiveCompatible.share.loginUserMode.birthday = self.birthday
                }
                //: PositionLabelReactiveCompatible.share.loginUserMode.signature = self.signStr
                PositionLabelReactiveCompatible.share.loginUserMode.signature = self.signStr

                //: if isBack {
                if isBack {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }

                //: } else {
            } else {
                //: if errorModel?.errorCode != 100 {
                if errorModel?.errorCode != 100 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg )
                    self.exceptAdd(showMsg: errorModel!.errorMsg)
                }
            }
        }
    }

    //: func photoAndTagTool(BackT: Bool, isTips: Bool) {
    func indexOpen(BackT: Bool, isTips: Bool) {
        //: self.isTips = isTips
        self.isTips = isTips
        //: if self.DeletePhotoArray.count>0 {
        if self.DeletePhotoArray.count > 0 {
            // 先删再加
            //: deletePhoto()
            photoMake()
            //: } else {
        } else {
            //: if self.seletePhotoArray.count>0 {
            if self.seletePhotoArray.count > 0 {
                //: uploadPhoto()
                destroy()
            }
        }

        //: if isdeleteAboutme {
        if isdeleteAboutme {
            //: deleteTag(type: 1)
            accumulation(type: 1)
        }
        //: if isdeleteInters {
        if isdeleteInters {
            //: deleteTag(type: 2)
            accumulation(type: 2)
        }
        //: if BackT {
        if BackT {
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
    }

    /// 上传相册
    //: func uploadPhoto() {
    func destroy() {
        //: for i in 0..<seletePhotoArray.count {
        for i in 0 ..< seletePhotoArray.count {
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: let resultData: NSData = seletePhotoArray[i].compressedImageData()! as NSData
            let resultData: NSData = seletePhotoArray[i].compressed()! as NSData
            //: dict["pic"] = resultData
            dict[(String(kPicPlayShow))] = resultData
            //: ProgressHUD.show()
            WrapView.colorComponent()
            //: TalkingMeRequestTool.req_UploadPic(params: dict) { [weak self] succeed, result, errorModel in
            CustomReactiveCompatible.adjustCompletion(params: dict) { [weak self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                WrapView.livePic()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if succeed {
                if succeed {
                    //: if  self.isTips {
                    if self.isTips {
                        //: self.isTips = false
                        self.isTips = false
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                        self.barPrice(showMsg: (String(appModeMain) + String(notiErrorVideoTargetUser)).localized)
                    }
                    //: self.seletePhotoArray.removeAll()
                    self.seletePhotoArray.removeAll()
                    //: self.GetGallery()
                    self.collectionEqual()
                }
            }
        }
    }

    /// 删除相册
    //: func deletePhoto() {
    func photoMake() {
        //: var str = DeletePhotoArray.joined(separator: ",")
        var str = DeletePhotoArray.joined(separator: ",")
        //: str = "[\(str)]"
        str = "[\(str)]"
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["pids"] = str
        dict[(notiImageConst.replacingOccurrences(of: "nor", with: "i"))] = str
        //: ProgressHUD.show()
        WrapView.colorComponent()
        //: TalkingMeRequestTool.req_DeletePic(params: dict) { [weak self] succeed, result, errorModel in
        CustomReactiveCompatible.fromCompletion(params: dict) { [weak self] succeed, _, _ in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if self.seletePhotoArray.count>0 {
            if self.seletePhotoArray.count > 0 {
                //: self.uploadPhoto()
                self.destroy()
                //: } else {
            } else {
                //: ProgressHUD.dismiss()
                WrapView.livePic()
            }
            //: if succeed {
            if succeed {
                //: if  self.isTips {
                if self.isTips {
                    //: self.isTips = false
                    self.isTips = false
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                    self.barPrice(showMsg: (String(appModeMain) + String(notiErrorVideoTargetUser)).localized)
                }

                //: self.DeletePhotoArray.removeAll()
                self.DeletePhotoArray.removeAll()
                //: self.GetGallery()
                self.collectionEqual()
            }
        }
    }

    /// 修改tag
    //: func deleteTag(type: Int) {
    func accumulation(type: Int) {
        //: var array: [UserSeleteTagModel] = []
        var array: [MinDetailHandyJSON] = []
        //: var category = ""
        var category = ""
        //: if type==1 {
        if type == 1 {
            //: array=seleteAboutme
            array = seleteAboutme
            //: category="aboutMe"
            category = (String(showButtonMain.prefix(7)))
            //: } else {
        } else {
            //: array=seleteInters
            array = seleteInters
            //: category="interest"
            category = (dataLicenseConst.replacingOccurrences(of: "from", with: "er"))
        }
        //: var str = ""
        var str = ""
        //: for i in 0..<array.count {
        for i in 0 ..< array.count {
            //: let model = array[i]
            let model = array[i]
            //: str += model.tag_id!
            str += model.tag_id!
            //: if i<array.count-1 {
            if i < array.count - 1 {
                //: str += ","
                str += ","
            }
        }
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["tagIds"] = str
        dict[(String(showDataOfModelConst) + String(showVoiceNoti))] = str
        //: dict["category"] = category
        dict[(String(notiPhotoApp) + showSucceedMomentPreviousConst.replacingOccurrences(of: "flow", with: "y"))] = category
        //: ProgressHUD.show()
        WrapView.colorComponent()
        //: TalkingMeRequestTool.req_EditTag(params: dict) { [weak self] succeed, result, errorModel in
        CustomReactiveCompatible.params(params: dict) { [weak self] succeed, _, _ in
            //: ProgressHUD.dismiss()
            WrapView.livePic()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: if  self.isTips {
                if self.isTips {
                    //: self.isTips = false
                    self.isTips = false
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                    self.barPrice(showMsg: (String(appModeMain) + String(notiErrorVideoTargetUser)).localized)
                }
                //: if type==1 {
                if type == 1 {
                    //: self.isdeleteAboutme = false
                    self.isdeleteAboutme = false
                    //: PositionLabelReactiveCompatible.share.loginUserMode.aboutMe = self.seleteAboutme
                    PositionLabelReactiveCompatible.share.loginUserMode.aboutMe = self.seleteAboutme
                    //: } else {
                } else {
                    //: self.isdeleteInters = false
                    self.isdeleteInters = false
                    //: PositionLabelReactiveCompatible.share.loginUserMode.interest = self.seleteInters
                    PositionLabelReactiveCompatible.share.loginUserMode.interest = self.seleteInters
                }
            }
        }
    }

    /// 上传视频
    //: func uploadVideo() {
    func message() {
        //: ProgressHUD.show()
        WrapView.colorComponent()
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 2, coverPath: self.VideoIconPath, videoPath: self.videoPath) { coverUrl, videoUrl, succeed in
        StateEqualSignatureProvider.shared.shopwindow(type: 2, coverPath: self.VideoIconPath, videoPath: self.videoPath) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: ProgressHUD.dismiss()
                WrapView.livePic()
                //: self.func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
                self.exceptAdd(showMsg: String(bytes: mainEqualConst.map{dataLocal(content: $0)}, encoding: .utf8)!.localized)
                //: return
                return
            }

            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["imgUrl"] = coverUrl
            dict[(String(showTextData.suffix(5)) + appViewGiftUser.replacingOccurrences(of: "source", with: "l"))] = coverUrl
            //: dict["videoUrl"] = videoUrl
            dict[(String(appMakeConst))] = videoUrl

            //: TalkingMeRequestTool.uploadVideoUserEdit(params: dict) { [weak self] succeed, result, errorModel in
            CustomReactiveCompatible.nudePerson(params: dict) { [weak self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                WrapView.livePic()
                //: guard let self = self else { return }
                guard let self = self else { return }

                //: guard succeed else {
                guard succeed else {
                    //: self.func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
                    self.exceptAdd(showMsg: String(bytes: mainEqualConst.map{dataLocal(content: $0)}, encoding: .utf8)!.localized)
                    //: return
                    return
                }
                //: self.VideoIconPath = ""
                self.VideoIconPath = ""
                //: self.videoPath = ""
                self.videoPath = ""
                //: self.isChangeVideo = false
                self.isChangeVideo = false
                //: self.videoStatus = 0
                self.videoStatus = 0
                //: self.func__showStatusBarSuccessMsg(showMsg: "Video cover submitted successfully".localized)
                self.barPrice(showMsg: String(bytes: constLiveUser.map{modelMake(from: $0)}, encoding: .utf8)!.localized)
                //: for i in self.MainTable.visibleCells {
                for i in self.MainTable.visibleCells {
                    //: if i.reuseIdentifier == "TalkingEditVideoCell" {
                    if i.reuseIdentifier == (String(mainLabNoti) + String(constColorShow)) {
                        //: let videocell: TalkingEditVideoCell = i as! TalkingEditVideoCell
                        let videocell: AlongView = i as! AlongView
                        //: videocell.upDateCellView(videoPlayPath: dict["videoUrl"] as! String)
                        videocell.quantityerpretation(videoPlayPath: dict[(String(appMakeConst))] as! String)
                        //: videocell.setVideoStatusLB(status: self.videoStatus)
                        videocell.stream(status: self.videoStatus)
                    }
                }
            }
        }
    }

    //: func getVideoInfo() {
    func blockKey() {
        //: TalkingMeRequestTool.getUploadVideoInfo { [weak self] succeed, result, errorModel in
        CustomReactiveCompatible.gen { [weak self] succeed, result, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {return}
                guard let dict = result as? [String: Any] else { return }
                //: self.videoPath = dict["video_url"] as! String
                self.videoPath = dict[(String(kPictureApp) + String(kStyleMain))] as! String
                //: self.VideoIconPath = dict["img_url"] as! String
                self.VideoIconPath = dict[(String(userEmptyEqualMain.prefix(7)))] as! String
                //: self.videoStatus = dict["status"] as? Int ?? -3
                self.videoStatus = dict[(userViewData.replacingOccurrences(of: "regular", with: "s"))] as? Int ?? -3
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }
}

// MARK: - Event

//: extension TalkingEditProfilesVC {
extension ErrorTopProfilesVc {
    //: @objc func keyboardShowBeHidden(notification: NSNotification) {
    @objc func presentDown(notification: NSNotification) {
        //: let info = notification.userInfo
        let info = notification.userInfo
        //: let keyBoardHeight = (info![UIResponder.keyboardFrameEndUserInfoKey] as! NSValue).cgRectValue.size.height
        let keyBoardHeight = (info![UIResponder.keyboardFrameEndUserInfoKey] as! NSValue).cgRectValue.size.height

        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(userSearchedMain) + String(userActualShow.suffix(5)) + "meCell") {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: AddTextCell = i as! AddTextCell
                //: if namecell.nameTF.isFirstResponder {
                if namecell.nameTF.isFirstResponder {
                    //: MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY-keyBoardHeight), animated: true)
                    MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY - keyBoardHeight), animated: true)
                }
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(showFormatApp) + String(constOfData)) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: PlusViewCell = i as! PlusViewCell
                //: if namecell.textView.isFirstResponder {
                if namecell.textView.isFirstResponder {
                    //: MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY-keyBoardHeight), animated: true)
                    MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY - keyBoardHeight), animated: true)
                }
            }
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingEditProfilesVC: UITableViewDelegate, UITableViewDataSource {
extension ErrorTopProfilesVc: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 7
        return 7
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: if PositionLabelReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
            if PositionLabelReactiveCompatible.share.loginUserMode.sex == PetitionKeyRepresentable.female.rawValue {
                //: return 226
                return 226
            }
            //: return 0
            return 0

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let sizewh = Int((ScreenWidth-30-12*2-9*2)/3)
            let sizewh = Int((userDeviceKey - 30 - 12 * 2 - 9 * 2) / 3)
            //: let Y = 46+sizewh*3+18
            let Y = 46 + sizewh * 3 + 18
            //: let cellheiht = Y+12+26+15
            let cellheiht = Y + 12 + 26 + 15
            //: return CGFloat(cellheiht)
            return CGFloat(cellheiht)
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: return 107+15
            return 107 + 15
            //: } else if indexPath.section == 3 {
        } else if indexPath.section == 3 {
            //: return 64
            return 64
            //: } else if indexPath.section == 4 {
        } else if indexPath.section == 4 {
            //: return 176
            return 176
            //: } else if indexPath.section == 5 || indexPath.section == 6 {
        } else if indexPath.section == 5 || indexPath.section == 6 {
            //: if indexPath.section == 5 && seleteAboutme.count>0 {
            if indexPath.section == 5 && seleteAboutme.count > 0 {
                //: return self.computeCellheight(type: 1)+64
                return self.location(type: 1) + 64
                //: } else if indexPath.section == 6 && seleteInters.count>0 {
            } else if indexPath.section == 6 && seleteInters.count > 0 {
                //: return self.computeCellheight(type: 2)+64
                return self.location(type: 2) + 64
            }
            //: return 64
            return 64
        }
        //: return 56
        return 56
    }

    //: func tableView(_ tableView: UITableView, estimatedHeightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, estimatedHeightForRowAt _: IndexPath) -> CGFloat {
        //: return 56
        return 56
    }

    //: func tableView(_ tableView: UITableView, heightForHeaderInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForHeaderInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: let identifier = "TalkingEditVideoCell"
            let identifier = (String(mainLabNoti) + String(constColorShow))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditVideoCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? AlongView
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditVideoCell(style: .default, reuseIdentifier: identifier)
                cell = AlongView(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.setVideoStatusLB(status: self.videoStatus)
            cell?.stream(status: self.videoStatus)
            //: if VideoIconPath.count>1 && videoPath.count>1 {
            if VideoIconPath.count > 1 && videoPath.count > 1 {
                //: cell?.setCellView(iconPath: VideoIconPath)
                cell?.icon(iconPath: VideoIconPath)
                //: cell?.upDateCellView(videoPlayPath: videoPath)
                cell?.quantityerpretation(videoPlayPath: videoPath)
            }
            //: cell?.seleteBlock = { [weak self] imagPath, VideoPath in
            cell?.seleteBlock = { [weak self] imagPath, VideoPath in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.VideoIconPath = imagPath
                self.VideoIconPath = imagPath
                //: self.videoPath = VideoPath
                self.videoPath = VideoPath
                //: self.isChangeVideo = true
                self.isChangeVideo = true
            }
            //: cell?.deleteBlock = { [weak self] in
            cell?.deleteBlock = { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.VideoIconPath = ""
                self.VideoIconPath = ""
                //: self.videoPath = ""
                self.videoPath = ""
                //: self.isChangeVideo = false
                self.isChangeVideo = false
            }
            //: return cell!
            return cell!

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let identifier = "TalkingEditPhotoCell"
            let identifier = (String(notiErrorData) + String(appFemaleModeBlockShow.prefix(5)) + "toCell")
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditPhotoCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? LayerReactiveCompatible
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditPhotoCell(style: .default, reuseIdentifier: identifier)
                cell = LayerReactiveCompatible(style: .default, reuseIdentifier: identifier)
            }
            //: if PhotoArray.count>0 {
            if PhotoArray.count > 0 {
                //: cell?.setPhoto(selete: PhotoArray)
                cell?.haphazard(selete: PhotoArray)
                //: } else {
            } else {
                //: cell?.resetPhoto()
                cell?.giftInfo()
            }
            //: cell?.delegate = self
            cell?.delegate = self
            //: return cell!
            return cell!
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: let identifier = "TalkingEditNameCell"
            let identifier = (String(userSearchedMain) + String(userActualShow.suffix(5)) + "meCell")
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditNameCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? AddTextCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditNameCell(style: .default, reuseIdentifier: identifier)
                cell = AddTextCell(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.nameBlock = { [weak self] name in
            cell?.nameBlock = { [weak self] name in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.nameStr = name
                self.nameStr = name
            }
            //: return cell!
            return cell!
            //: } else if indexPath.section == 3 {
        } else if indexPath.section == 3 {
            //: let identifier = "TalkingEditBirthdayCell"
            let identifier = (String(kVerticalApp.prefix(8)) + String(notiGenderShow.suffix(4)) + "irth" + String(kManagerShow.suffix(7)))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditBirthdayCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? StateViewCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditBirthdayCell(style: .default, reuseIdentifier: identifier)
                cell = StateViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.birthdayBlock = { [weak self] day in
            cell?.birthdayBlock = { [weak self] day in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.birthday = day
                self.birthday = day
            }
            //: return cell!
            return cell!
            //: } else if indexPath.section == 4 {
        } else if indexPath.section == 4 {
            //: let identifier = "TalkingEditSignCell"
            let identifier = (String(showFormatApp) + String(constOfData))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditSignCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? PlusViewCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditSignCell(style: .default, reuseIdentifier: identifier)
                cell = PlusViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.signBlock = { [weak self] sign in
            cell?.signBlock = { [weak self] sign in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.signStr = sign
                self.signStr = sign
            }

            //: return cell!
            return cell!
            //: } else if indexPath.section == 5 || indexPath.section == 6 {
        } else if indexPath.section == 5 || indexPath.section == 6 {
            //: let identifier = "TalkingEditAboutMeCell"
            let identifier = (String(userSharedConst) + String(userEqualConst))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditAboutMeCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? CharacteristicRootOfASquareMatrixReactiveCompatible
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditAboutMeCell(style: .default, reuseIdentifier: identifier)
                cell = CharacteristicRootOfASquareMatrixReactiveCompatible(style: .default, reuseIdentifier: identifier)
            }
            //: if indexPath.section == 5 {
            if indexPath.section == 5 {
                //: cell!.tagtype = .AboutMe
                cell!.tagtype = .AboutMe
                //: } else {
            } else {
                //: cell!.tagtype = .Interests
                cell!.tagtype = .Interests
            }
            //: cell?.setTitle()
            cell?.par()
            //: if seleteAboutme.count>0 && indexPath.section == 5 {
            if seleteAboutme.count > 0 && indexPath.section == 5 {
                //: cell?.setMessage(seleteAboutme)
                cell?.compartment(seleteAboutme)
            }
            //: if seleteInters.count>0 && indexPath.section == 6 {
            if seleteInters.count > 0 && indexPath.section == 6 {
                //: cell?.setMessage(seleteInters)
                cell?.compartment(seleteInters)
            }
            //: cell?.editBtnBlock = { [weak self] in
            cell?.editBtnBlock = { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: let vc = TalkingEditTagVC.init()
                let vc = PriceTagViewController()
                //: vc.delegate = self
                vc.delegate = self
                //: if indexPath.section == 5 {
                if indexPath.section == 5 {
                    //: vc.tagtype = .AboutMe
                    vc.tagtype = .AboutMe
                    //: } else {
                } else {
                    //: vc.tagtype = .Interests
                    vc.tagtype = .Interests
                }
                //: self.navigationController!.pushViewController(vc, animated: true)
                self.navigationController!.pushViewController(vc, animated: true)
            }
            //: cell?.deleteBlock = { [weak self] tag in
            cell?.deleteBlock = { [weak self] tag in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if indexPath.section == 5 {
                if indexPath.section == 5 {
                    //: if  self.seleteAboutme.contains(where: { $0.equals(compareTo: tag)}) {
                    if self.seleteAboutme.contains(where: { $0.upNow(compareTo: tag) }) {
                        //: self.seleteAboutme.removeAll(where: {$0.equals(compareTo: tag)})
                        self.seleteAboutme.removeAll(where: { $0.upNow(compareTo: tag) })
                        //: self.isdeleteAboutme = true
                        self.isdeleteAboutme = true
                    }
                    //: } else {
                } else {
                    //: if  self.seleteInters.contains(where: { $0.equals(compareTo: tag)}) {
                    if self.seleteInters.contains(where: { $0.upNow(compareTo: tag) }) {
                        //: self.seleteInters.removeAll(where: {$0.equals(compareTo: tag)})
                        self.seleteInters.removeAll(where: { $0.upNow(compareTo: tag) })
                        //: self.isdeleteInters = true
                        self.isdeleteInters = true
                    }
                }
            }
            //: return cell!
            return cell!
            //: } else {
        } else {
            //: let identifier = "UITableViewCell"
            let identifier = (String(appListK.prefix(4)) + "bleVie" + String(userToNoti.prefix(5)))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier)
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier)
            //: if cell == nil {
            if cell == nil {
                //: cell = UITableViewCell(style: .default, reuseIdentifier: identifier)
                cell = UITableViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: return cell!
            return cell!
        }
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    //: func computeCellheight(type: Int) -> CGFloat {
    func location(type: Int) -> CGFloat {
        //: let itemSpacing = CGFloat(8)
        let itemSpacing = CGFloat(8)
        //: var contentWidthInRow = CGFloat(0)
        var contentWidthInRow = CGFloat(0)
        //: var array = Array<UserSeleteTagModel>()
        var array = [MinDetailHandyJSON]()
        //: if type == 1 {
        if type == 1 {
            //: array = self.seleteAboutme
            array = self.seleteAboutme
            //: } else {
        } else {
            //: array = self.seleteInters
            array = self.seleteInters
        }
        //: var contentHeightInRow = CGFloat(30)
        var contentHeightInRow = CGFloat(30)

        //: for item in array {
        for item in array {
            //: var tag_name = ""
            var tag_name = ""
            //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue {
            if UpToDatenessReactiveCompatible.share.interfaceLang == DigitiserScalarLiteral.ar.rawValue {
                //: tag_name = item.tag_name_ar
                tag_name = item.tag_name_ar
                //: } else if TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue {
            } else if UpToDatenessReactiveCompatible.share.interfaceLang == DigitiserScalarLiteral.es.rawValue {
                //: tag_name = item.tag_name_es
                tag_name = item.tag_name_es
                //: } else if TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
            } else if UpToDatenessReactiveCompatible.share.interfaceLang == DigitiserScalarLiteral.pt.rawValue {
                //: tag_name = item.tag_name_pt
                tag_name = item.tag_name_pt
            }
            //: if tag_name.count <= 0 {
            if tag_name.count <= 0 {
                //: tag_name = item.tag_name
                tag_name = item.tag_name
            }
            //: let rect = (tag_name as NSString).boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.pingfangFont(type: .Regular, fontSize: 15)], context: nil)
            let rect = (tag_name as NSString).boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.viewPath(type: .Regular, fontSize: 15)], context: nil)
            //: let tagWidth = rect.width + 30
            let tagWidth = rect.width + 30
            //: if contentWidthInRow + tagWidth + itemSpacing > ScreenWidth-54 {
            if contentWidthInRow + tagWidth + itemSpacing > userDeviceKey - 54 {
                // 需要换行
                //: contentHeightInRow += (10 + 30)
                contentHeightInRow += (10 + 30)
                //: contentWidthInRow = itemSpacing
                contentWidthInRow = itemSpacing
                //: } else {
            } else {
                //: contentWidthInRow += (tagWidth+itemSpacing)
                contentWidthInRow += (tagWidth + itemSpacing)
            }
        }
        //: if array.count == 10 {
        if array.count == 10 {
            //: contentHeightInRow += 40
            contentHeightInRow += 40
        }
        //: return contentHeightInRow + 12
        return contentHeightInRow + 12
    }
}

// MARK: - DataTagDelegate

//: extension TalkingEditProfilesVC: EditTagDelegate {
extension ErrorTopProfilesVc: DataTagDelegate {
    //: func freshSeleteTag() {
    func item() {
        //: seleteAboutme =  PositionLabelReactiveCompatible.share.loginUserMode.aboutMe!
        seleteAboutme = PositionLabelReactiveCompatible.share.loginUserMode.aboutMe!
        //: seleteInters =  PositionLabelReactiveCompatible.share.loginUserMode.interest!
        seleteInters = PositionLabelReactiveCompatible.share.loginUserMode.interest!
        //: self.MainTable .reloadData()
        self.MainTable.reloadData()
    }
}

// MARK: - WellheadReactiveCompatible

//: extension TalkingEditProfilesVC: EditPhotoDelegate {
extension ErrorTopProfilesVc: WellheadReactiveCompatible {
    /// 添加本地图片
    //: func addPhoto(_ icon: [UIImage]) {
    func line(_ icon: [UIImage]) {
        //: for i in icon {
        for i in icon {
            //: self.seletePhotoArray.append(i)
            self.seletePhotoArray.append(i)
            //: var model = TalkingUserInfoGalleryModel.init()
            var model = TempCustomModelType()
            //: model.imagePic = i
            model.imagePic = i
            //: model.isLocal = true
            model.isLocal = true
            //: PhotoArray.append(model)
            PhotoArray.append(model)
        }
    }

    /// 删除本地相册选择的图片
    //: func deletePhoto(_ icon: UIImage) {
    func hidePhoto(_ icon: UIImage) {
        //: if  seletePhotoArray.contains(where: {$0 == icon}) {
        if seletePhotoArray.contains(where: { $0 == icon }) {
            //: seletePhotoArray.removeAll(where: {$0 == icon})
            seletePhotoArray.removeAll(where: { $0 == icon })
        }
        //: var seleteIndex = -1
        var seleteIndex = -1
        //: for i in 0..<PhotoArray.count {
        for i in 0 ..< PhotoArray.count {
            //: if icon == PhotoArray[i].imagePic {
            if icon == PhotoArray[i].imagePic {
                //: seleteIndex =  i
                seleteIndex = i
                //: break
                break
            }
        }
        //: if seleteIndex>=0 && seleteIndex<PhotoArray.count {
        if seleteIndex >= 0 && seleteIndex < PhotoArray.count {
            //: PhotoArray.remove(at: seleteIndex)
            PhotoArray.remove(at: seleteIndex)
            //: self.MainTable.reloadData()
            self.MainTable.reloadData()
        }
    }

    /// 删除后台相册的图片
    //: func deletePhotoWithUid(_ iconUid: String) {
    func playDrop(_ iconUid: String) {
        //: var seleteIndex = -1
        var seleteIndex = -1
        //: for i in 0..<PhotoArray.count {
        for i in 0 ..< PhotoArray.count {
            //: if iconUid == PhotoArray[i].uid {
            if iconUid == PhotoArray[i].uid {
                //: seleteIndex =  i
                seleteIndex = i
                //: self.DeletePhotoArray.append(iconUid)
                self.DeletePhotoArray.append(iconUid)
            }
        }
        //: if seleteIndex>=0 && seleteIndex<PhotoArray.count {
        if seleteIndex >= 0 && seleteIndex < PhotoArray.count {
            //: PhotoArray.remove(at: seleteIndex)
            PhotoArray.remove(at: seleteIndex)
            //: self.MainTable.reloadData()
            self.MainTable.reloadData()
        }
    }
}

// MARK: - UI

//: extension TalkingEditProfilesVC {
extension ErrorTopProfilesVc {
    //: private func designView() {
    private func naturalResources() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingEditVideoCell.self, forCellReuseIdentifier: "TalkingEditVideoCell")
        MainTable.register(AlongView.self, forCellReuseIdentifier: (String(mainLabNoti) + String(constColorShow)))
        //: MainTable.register(TalkingEditPhotoCell.self, forCellReuseIdentifier: "TalkingEditPhotoCell")
        MainTable.register(LayerReactiveCompatible.self, forCellReuseIdentifier: (String(notiErrorData) + String(appFemaleModeBlockShow.prefix(5)) + "toCell"))
        //: MainTable.register(TalkingEditNameCell.self, forCellReuseIdentifier: "TalkingEditNameCell")
        MainTable.register(AddTextCell.self, forCellReuseIdentifier: (String(userSearchedMain) + String(userActualShow.suffix(5)) + "meCell"))
        //: MainTable.register(TalkingEditBirthdayCell.self, forCellReuseIdentifier: "TalkingEditBirthdayCell")
        MainTable.register(StateViewCell.self, forCellReuseIdentifier: (String(kVerticalApp.prefix(8)) + String(notiGenderShow.suffix(4)) + "irth" + String(kManagerShow.suffix(7))))
        //: MainTable.register(TalkingEditSignCell.self, forCellReuseIdentifier: "TalkingEditSignCell")
        MainTable.register(PlusViewCell.self, forCellReuseIdentifier: (String(showFormatApp) + String(constOfData)))
        //: MainTable.register(TalkingEditAboutMeCell.self, forCellReuseIdentifier: "TalkingEditAboutMeCell")
        MainTable.register(CharacteristicRootOfASquareMatrixReactiveCompatible.self, forCellReuseIdentifier: (String(userSharedConst) + String(userEqualConst)))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(self.view)
            make.leading.trailing.bottom.equalTo(self.view)
            //: make.top.equalTo(self.view).offset(20)
            make.top.equalTo(self.view).offset(20)
        }
    }
}
