
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let appLevelMain:[UInt8] = [0x3e,0x39,0x3e,0x23,0x7f,0x34,0x38,0x33,0x32,0x25,0x6d,0x7e,0x77,0x3f,0x36,0x24,0x77,0x39,0x38,0x23,0x77,0x35,0x32,0x32,0x39,0x77,0x3e,0x3a,0x27,0x3b,0x32,0x3a,0x32,0x39,0x23,0x32,0x33]

/*: "Cancel" :*/
fileprivate let constAddData:String = "index main withCancel"

/*: "Submit" :*/
fileprivate let mainIconConst:String = "Submiview gesture gift"
fileprivate let constTableData:[Character] = ["t"]

/*: "Please tell us your problems                   " :*/
fileprivate let dataProgressApp:[UInt8] = [0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x73,0x6d,0x65,0x6c,0x62,0x6f,0x72,0x70,0x20,0x72,0x75,0x6f,0x79,0x20,0x73,0x75,0x20,0x6c,0x6c,0x65,0x74,0x20,0x65,0x73,0x61,0x65,0x6c,0x50]

/*: "You need to choose at least one." :*/
fileprivate let notiSourceAddMain:[UInt8] = [0xae,0x98,0x82,0xd7,0x99,0x92,0x92,0x93,0xd7,0x83,0x98,0xd7,0x94,0x9f,0x98,0x98,0x84,0x92,0xd7,0x96,0x83,0xd7,0x9b,0x92,0x96,0x84,0x83,0xd7,0x98,0x99,0x92,0xd9]

private func cornerApp(table num: UInt8) -> UInt8 {
    return num ^ 247
}

/*: "Please tell us your problems." :*/
fileprivate let kCountimateApp:String = "Pleaseto view post content"
fileprivate let constFemaleApp:String = "s yoarray leading"
fileprivate let userStyleK:String = "view type handle close tour pr"

/*: "app/userReport" :*/
fileprivate let mainItemData:String = "app/useron center"
fileprivate let showIndexUser:String = "digital manager false pageReport"

/*: "id" :*/
fileprivate let showNameApp:[UInt8] = [0x9b,0x96]

private func aLaCarte(name num: UInt8) -> UInt8 {
    return num ^ 242
}

/*: "reason" :*/
fileprivate let showKindUser:String = "makeason"

/*: "desc" :*/
fileprivate let userSharedConst:[UInt8] = [0xa7,0xa8,0xb6,0xa6]

fileprivate func colorReload(cell num: UInt8) -> UInt8 {
    let value = Int(num) - 67
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/moment/report" :*/
fileprivate let dataToShow:[Character] = ["m","f","/","m","o","m"]
fileprivate let kTipNoti:String = "insert count limit viewent/re"

/*: "momentId" :*/
fileprivate let showAlongMain:[UInt8] = [0x35,0x37,0x35,0x3d,0x36,0x2c,0x11,0x3c]

private func parameterShow(array num: UInt8) -> UInt8 {
    return num ^ 88
}

/*: "mf/momentReply/report" :*/
fileprivate let mainFileData:String = "clear error contentmf/mom"
fileprivate let dataApplyMain:String = "entReif color detail request"
fileprivate let appMemberNorNoti:String = "ply/reself gesture mode"

/*: "app/videoReport" :*/
fileprivate let notiViewDigitalToK:String = "block requestapp/v"
fileprivate let notiCellApp:[Character] = ["t"]

/*: "logId" :*/
fileprivate let userNameConst:[UInt8] = [0x2a,0x29,0x21,0xf,0x22]

private func sizeBottom(application num: UInt8) -> UInt8 {
    return num ^ 70
}

/*: "Submitted successfully" :*/
fileprivate let showInfoMain:String = "intimate left up media selfSubm"
fileprivate let showBlockUser:String = "d sucpath class"
fileprivate let mainKitK:String = "cemanagermanager"

/*: "Nudity" :*/
fileprivate let userBottomData:[Character] = ["N","u","d","i","t","y"]

/*: "Violence" :*/
fileprivate let notiToAddUser:String = "Violenelse view render gift"
fileprivate let notiViewUser:[Character] = ["c","e"]

/*: "Harassment" :*/
fileprivate let userHiddenMain:[Character] = ["H","a","r","a","s"]
fileprivate let appScaleConst:String = "smeto"

/*: "Suicide or Self-injury" :*/
fileprivate let mainPathErrorApp:[Character] = ["S","u","i","c","i","d","e"," ","o","r"," ","S","e","l","f","-","i","n","j","u","r","y"]

/*: "False Information" :*/
fileprivate let userLabApp:String = "info var section varFalse "
fileprivate let constStandardApp:[Character] = ["I","n","f","o","r","m","a","t","i","o","n"]

/*: "Spam" :*/
fileprivate let mainBackgroundConst:[Character] = ["S","p","a","m"]

/*: "Hate Speech" :*/
fileprivate let notiModeShow:String = "Hate view quote self false today"
fileprivate let userPlayData:String = "Speechvalue message string"

/*: "Terrorism" :*/
fileprivate let notiFilterK:[Character] = ["T","e","r","r","o","r","i","s","m"]

/*: "Sale of Illegal or Regulated Goods" :*/
fileprivate let notiShowApp:[UInt8] = [0xe9,0xf7,0x2,0xfb,0xb6,0x5,0xfc,0xb6,0xdf,0x2,0x2,0xfb,0xfd,0xf7,0x2,0xb6,0x5,0x8,0xb6,0xe8,0xfb,0xfd,0xb,0x2,0xf7,0xa,0xfb,0xfa,0xb6,0xdd,0x5,0x5,0xfa,0x9]

fileprivate func labMoment(center num: UInt8) -> UInt8 {
    let value = Int(num) - 150
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Sending disgusting messages" :*/
fileprivate let userTitleConst:[Character] = ["S","e","n","d","i","n","g"," ","d","i","s","g","u","s","t","i","n","g"," ","m","e","s"]
fileprivate let mainErrorApp:String = "saglog"

/*: "Using photos of nudity or drugs" :*/
fileprivate let showInsideData:[UInt8] = [0x29,0xf,0x15,0x12,0x1b,0x5c,0xc,0x14,0x13,0x8,0x13,0xf,0x5c,0x13,0x1a,0x5c,0x12,0x9,0x18,0x15,0x8,0x5,0x5c,0x13,0xe,0x5c,0x18,0xe,0x9,0x1b,0xf]

/*: "Harassing or sending ads" :*/
fileprivate let mainLuckGiftPersonApp:String = "let cell view make labelHaras"
fileprivate let kInfoData:[Character] = ["s","i","n","g"," ","o","r"," ","s","e","n","d"]
fileprivate let showBackgroundMain:[Character] = ["i","n","g"," ","a","d","s"]

/*: "Defraud or using fake info" :*/
fileprivate let constBubbleData:[Character] = ["D","e","f","r","a","u","d"," ","o","r"," ","u","s","i","n","g"," ","f","a"]
fileprivate let dataSelectedUser:[Character] = ["k","e"," ","i","n","f","o"]

/*: "Others" :*/
fileprivate let notiPathData:String = "Othersadd click message open month"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PositionTempAlertView.swift
//  AbroadTalking
//
//  Created by Bolo on 2022/9/14.
//

//: import Then
import Then
//: import UIKit
import UIKit

//: enum TalkingReportAlertType: CaseIterable {
enum ModelAlertType: CaseIterable {
    //: case reportCommentType
    case reportCommentType // 举报动态
    //: case reportDynamicType
    case reportDynamicType
    //: case reportUserType
    case reportUserType // 举报用户
    //: case reportVideo
    case reportVideo // 举报视频通话
}

// 是否举报成功block
//: typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void
typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void

//: class TalkingReportAlertView: UIView {
class PositionTempAlertView: UIView {
    //: var reportType: TalkingReportAlertType?
    var reportType: ModelAlertType?
    //: var dataArray: Array<TalkingReportModel> = []
    var dataArray: [CacheThen] = []
    //: var selectedModel = TalkingReportModel()
    var selectedModel = CacheThen()
    //: var tempId: String = ""
    var tempId: String = ""
    //: var reportHideBlock: ReportViewHideBlock?
    var reportHideBlock: ReportViewHideBlock?
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: public init(frame: CGRect, type: TalkingReportAlertType?, rId: String) {
    public init(frame: CGRect, type: ModelAlertType?, rId: String) {
        //: tempId = rId
        tempId = rId
        //: reportType = type
        reportType = type
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        button()
        //: setupSubViewsConstraint()
        receive()
        //: setupReasons()
        fromAppear()
        //: bindInteraction()
        female()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: appLevelMain.map{$0^87}, encoding: .utf8)!)
    }

    // MARK: - Lazy load

    //: lazy var cancelButton = UIButton().then {
    lazy var cancelButton = UIButton().then {
        //: $0.setTitle("Cancel".localized, for: .normal)
        $0.setTitle((String(constAddData.suffix(6))).localized, for: .normal)
        //: $0.setTitleColor(UIColor.appThemeColor(), for: .normal)
        $0.setTitleColor(UIColor.requestCardTitle(), for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.viewPath(type: .Medium, fontSize: 17)
        //: $0.backgroundColor = UIColor.white
        $0.backgroundColor = UIColor.white
        //: $0.layer.borderWidth = 1
        $0.layer.borderWidth = 1
        //: $0.layer.borderColor = UIColor.appThemeColor().cgColor
        $0.layer.borderColor = UIColor.requestCardTitle().cgColor
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerCancelAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(dataBlock), for: .touchUpInside)
    }

    //: lazy var submitButton = UIButton().then {
    lazy var submitButton = UIButton().then {
        //: $0.setTitle("Submit".localized, for: .normal)
        $0.setTitle((String(mainIconConst.prefix(5)) + String(constTableData)).localized, for: .normal)
        //: $0.setTitleColor(UIColor.white, for: .normal)
        $0.setTitleColor(UIColor.white, for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.viewPath(type: .Medium, fontSize: 17)
        //: $0.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        $0.setBackgroundImage(UIImage.analogDigitalConverterSize(colors: UIColor.animaBy(), size: CGSize(width: 126, height: 50)), for: .normal)
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerSubmitAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(postEnd), for: .touchUpInside)
    }

    //: lazy var tableFooterView = UIView().then {
    lazy var tableFooterView = UIView().then {
        //: $0.frame = CGRect(x: 0, y: 0, width: ScreenWidth-actualWidth(w: 108), height: actualHeight(h: 74))
        $0.frame = CGRect(x: 0, y: 0, width: userDeviceKey - actualWidth(w: 108), height: actualHeight(h: 74))
        //: $0.backgroundColor = UIColor.RGBA(245, 245, 248, 1)
        $0.backgroundColor = UIColor.eventWith(245, 245, 248, 1)
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 5
        $0.layer.cornerRadius = 5
    }

    //: lazy var otherTextView = PlaceholderTextView(placeholder: "Please tell us your problems                   ".localized, placeholderColor: UIColor.appValueDetailColor(), frame: CGRect(x: 10, y: 10, width: ScreenWidth-actualWidth(w: 128), height: actualHeight(h: 54))).then {
    lazy var otherTextView = DropView(placeholder: String(bytes: dataProgressApp.reversed(), encoding: .utf8)!.localized, placeholderColor: UIColor.streamColor(), frame: CGRect(x: 10, y: 10, width: userDeviceKey - actualWidth(w: 128), height: actualHeight(h: 54))).then {
        //: $0.backgroundColor = .clear
        $0.backgroundColor = .clear
        //: $0.placeholdertextView.backgroundColor = .clear
        $0.placeholdertextView.backgroundColor = .clear
        //: $0.limitWords = 200
        $0.limitWords = 200
        //: $0.isShowCountLabel = false
        $0.isShowCountLabel = false
        //: $0.isReturnHidden = true
        $0.isReturnHidden = true
        //: $0.plaleLabel.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.plaleLabel.font = .viewPath(type: .Regular, fontSize: 16)
        //: $0.placeholdertextView.textColor = UIColor.appTitleColor()
        $0.placeholdertextView.textColor = UIColor.read()
        //: $0.placeholdertextView.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.placeholdertextView.font = .viewPath(type: .Regular, fontSize: 16)
    }

    //: lazy var bgView = UIView().then {
    lazy var bgView = UIView().then {
        //: $0.backgroundColor = UIColor.RGBA(0, 0, 0, 0.6)
        $0.backgroundColor = UIColor.eventWith(0, 0, 0, 0.6)
    }

    //: lazy var containerView = UIView().then {
    lazy var containerView = UIView().then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 15
        $0.layer.cornerRadius = 15
    }

    //: lazy var tableView = UITableView.init(frame: .zero, style: .plain).then {
    lazy var tableView = UITableView(frame: .zero, style: .plain).then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.separatorStyle = .none
        $0.separatorStyle = .none
        //: $0.showsVerticalScrollIndicator = false
        $0.showsVerticalScrollIndicator = false
        //: $0.delegate = self
        $0.delegate = self
        //: $0.dataSource = self
        $0.dataSource = self
        //: if reportType != .reportCommentType, reportType != .reportVideo {
        if reportType != .reportCommentType, reportType != .reportVideo {
            //: $0.tableFooterView = self.tableFooterView
            $0.tableFooterView = self.tableFooterView
        }
        //: $0.register(TalkingReportViewCell.self, forCellReuseIdentifier: TalkingReportViewCell.className())
        $0.register(ToViewCell.self, forCellReuseIdentifier: ToViewCell.className())
    }
}

// MARK: - Bind && Event

//: extension TalkingReportAlertView {
extension PositionTempAlertView {
    //: @objc func func__TapGestureRecognizer() {
    @objc func detail() {
        //: self.endEditing(true)
        self.endEditing(true)
    }

    //: public func showReportViewIn(view: UIView?) {
    public func nameTo(view: UIView?) {
        //: if view == nil {
        if view == nil {
            //: let window = RowMacroDefine.getWindow()
            let window = RowMacroDefine.apply()
            //: window.addSubview(self)
            window.addSubview(self)
            //: return
            return
        }
        //: view?.addSubview(self)
        view?.addSubview(self)
    }

    /// 移除视图
    /// - Parameter succeed: 是否举报成功
    //: private func dismissReportView(succeed: Bool) {
    private func disregard(succeed: Bool) {
        //: reportHideBlock?(succeed)
        reportHideBlock?(succeed)
        //: self.removeFromSuperview()
        self.removeFromSuperview()
    }

    /// 取消
    //: @objc func registerCancelAction() {
    @objc func dataBlock() {
        //: self.dismissReportView(succeed: false)
        self.disregard(succeed: false)
    }

    /// 提交举报内容
    //: @objc func registerSubmitAction() {
    @objc func postEnd() {
        //: if selectedModel.selected == false {
        if selectedModel.selected == false {
            //: self.func__showStatusBarErrorMsg(showMsg: "You need to choose at least one.".localized)
            self.exceptAdd(showMsg: String(bytes: notiSourceAddMain.map{cornerApp(table: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
        if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please tell us your problems.".localized)
            self.exceptAdd(showMsg: (String(kCountimateApp.prefix(6)) + " tell u" + String(constFemaleApp.prefix(4)) + String(userStyleK.suffix(5)) + "oblems.").localized)
            //: return
            return
        }

        //: var param: [String: String] = [:]
        var param: [String: String] = [:]
        //: var path: String = ""
        var path = ""

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportUserType:
        case .reportUserType:
            //: path = "app/userReport"
            path = (String(mainItemData.prefix(8)) + String(showIndexUser.suffix(6)))
            //: param = ["id": tempId, "reason": selectedModel.type, "desc": otherTextView.placeholdertextView.text]
            param = ["id": tempId, (showKindUser.replacingOccurrences(of: "make", with: "re")): selectedModel.type, String(bytes: userSharedConst.map{colorReload(cell: $0)}, encoding: .utf8)!: otherTextView.placeholdertextView.text]

        //: case .reportDynamicType:
        case .reportDynamicType:
            //: path = "mf/moment/report"
            path = (String(dataToShow) + String(kTipNoti.suffix(6)) + "port")
            //: param = ["momentId": tempId, "reason": selectedModel.type]
            param = [String(bytes: showAlongMain.map{parameterShow(array: $0)}, encoding: .utf8)!: tempId, (showKindUser.replacingOccurrences(of: "make", with: "re")): selectedModel.type]

        //: case .reportCommentType:
        case .reportCommentType:
            //: path = "mf/momentReply/report"
            path = (String(mainFileData.suffix(6)) + String(dataApplyMain.prefix(5)) + String(appMemberNorNoti.prefix(6)) + "port")
            //: param = ["id": tempId, "reason": selectedModel.type]
            param = ["id": tempId, (showKindUser.replacingOccurrences(of: "make", with: "re")): selectedModel.type]

        //: case .reportVideo:
        case .reportVideo:
            //: path = "app/videoReport"
            path = (String(notiViewDigitalToK.suffix(5)) + "ideoRepor" + String(notiCellApp))
            //: param = ["logId": tempId, "reason": selectedModel.type]
            param = [String(bytes: userNameConst.map{sizeBottom(application: $0)}, encoding: .utf8)!: tempId, (showKindUser.replacingOccurrences(of: "make", with: "re")): selectedModel.type]

        //: default:
        default:
            //: break
            break
        }

        //: self.submitButton.isEnabled = false
        self.submitButton.isEnabled = false
        //: TalkingUserRequestManager.func__reportRequest(reqPath: path, reqParams: param) { succeed, result, errorModel in
        MenuRequestManager.topicConcern(reqPath: path, reqParams: param) { succeed, _, _ in
            //: self.submitButton.isEnabled = true
            self.submitButton.isEnabled = true
            //: if succeed == true {
            if succeed == true {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Submitted successfully".localized)
                self.barPrice(showMsg: (String(showInfoMain.suffix(4)) + "itte" + String(showBlockUser.prefix(5)) + mainKitK.replacingOccurrences(of: "manager", with: "s") + "fully").localized)
                //: self.dismissReportView(succeed: true)
                self.disregard(succeed: true)
            }
        }
    }
}

// MARK: - Delegate

//: extension TalkingReportAlertView: UITableViewDelegate, UITableViewDataSource {
extension PositionTempAlertView: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return dataArray.count
        return dataArray.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return actualHeight(h: 40)
        return actualHeight(h: 40)
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingReportViewCell.className()) as! TalkingReportViewCell
        let cell = tableView.dequeueReusableCell(withIdentifier: ToViewCell.className()) as! ToViewCell
        //: let model = dataArray[indexPath.item]
        let model = dataArray[indexPath.item]
        //: cell.updateReportCell(model: model)
        cell.doAt(model: model)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: for model in dataArray {
        for model in dataArray {
            //: model.selected = false
            model.selected = false
        }

        //: let itemModel = dataArray[indexPath.item]
        let itemModel = dataArray[indexPath.item]
        //: itemModel.selected = true
        itemModel.selected = true
        //: self.selectedModel = itemModel
        self.selectedModel = itemModel

        //: tableView.reloadData()
        tableView.reloadData()
    }
}

// MARK: - Layout

//: extension TalkingReportAlertView {
extension PositionTempAlertView {
    /// 添加视图
    //: private func setupSubviews() {
    private func button() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(containerView)
        addSubview(containerView)
        //: containerView.addSubview(tableView)
        containerView.addSubview(tableView)
        //: containerView.addSubview(cancelButton)
        containerView.addSubview(cancelButton)
        //: containerView.addSubview(submitButton)
        containerView.addSubview(submitButton)
        //: tableFooterView.addSubview(otherTextView)
        tableFooterView.addSubview(otherTextView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func receive() {
        //: var height = actualHeight(h: 325)
        var height = actualHeight(h: 325)
        //: if reportType == .reportUserType {
        if reportType == .reportUserType {
            //: height = actualHeight(h: 375)
            height = actualHeight(h: 375)
            //: } else if reportType == .reportVideo {
        } else if reportType == .reportVideo {
            //: height = actualHeight(h: 338)
            height = actualHeight(h: 338)
        }
        //: let topY = (self.frame.height - height) / 2.0
        let topY = (self.frame.height - height) / 2.0

        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.leading.top.trailing.bottom.equalTo(0)
            make.leading.top.trailing.bottom.equalTo(0)
        }
        //: containerView.snp.makeConstraints { make in
        containerView.snp.makeConstraints { make in
            //: make.top.equalTo(topY)
            make.top.equalTo(topY)
            //: make.leading.equalTo(actualWidth(w: 40))
            make.leading.equalTo(actualWidth(w: 40))
            //: make.trailing.equalTo(actualWidth(w: -40))
            make.trailing.equalTo(actualWidth(w: -40))
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.top.equalTo(actualHeight(h: 36))
            make.top.equalTo(actualHeight(h: 36))
            //: if reportType == .reportVideo {
            if reportType == .reportVideo {
                //: make.bottom.equalTo(actualHeight(h: -62))
                make.bottom.equalTo(actualHeight(h: -62))
                //: } else {
            } else {
                //: make.bottom.equalTo(actualHeight(h: -82))
                make.bottom.equalTo(actualHeight(h: -82))
            }
        }
        //: cancelButton.snp.makeConstraints { make in
        cancelButton.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
        //: submitButton.snp.makeConstraints { make in
        submitButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
    }

    /// 添加数据
    //: private func setupReasons() {
    private func fromAppear() {
        //: var reasonArray: [String] = []
        var reasonArray: [String] = []
        //: var type = 0
        var type = 0

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportDynamicType, .reportCommentType:
        case .reportDynamicType, .reportCommentType:
            //: type = 6
            type = 6
            //: reasonArray = ["Nudity".localized,
            reasonArray = [(String(userBottomData)).localized,
                           //: "Violence".localized,
                           (String(notiToAddUser.prefix(6)) + String(notiViewUser)).localized,
                           //: "Harassment".localized,
                           (String(userHiddenMain) + appScaleConst.replacingOccurrences(of: "to", with: "nt")).localized,
                           //: "Suicide or Self-injury".localized,
                           (String(mainPathErrorApp)).localized,
                           //: "False Information".localized,
                           (String(userLabApp.suffix(6)) + String(constStandardApp)).localized,
                           //: "Spam".localized,
                           (String(mainBackgroundConst)).localized,
                           //: "Hate Speech".localized,
                           (String(notiModeShow.prefix(5)) + String(userPlayData.prefix(6))).localized,
                           //: "Terrorism".localized,
                           (String(notiFilterK)).localized,
                           //: "Sale of Illegal or Regulated Goods".localized]
                           String(bytes: notiShowApp.map{labMoment(center: $0)}, encoding: .utf8)!.localized]

        //: case .reportUserType:
        case .reportUserType:
            //: type = 1
            type = 1
            //: reasonArray = ["Sending disgusting messages".localized,
            reasonArray = [(String(userTitleConst) + mainErrorApp.replacingOccurrences(of: "log", with: "es")).localized,
                           //: "Using photos of nudity or drugs".localized,
                           String(bytes: showInsideData.map{$0^124}, encoding: .utf8)!.localized,
                           //: "Harassing or sending ads".localized,
                           (String(mainLuckGiftPersonApp.suffix(5)) + String(kInfoData) + String(showBackgroundMain)).localized,
                           //: "Defraud or using fake info".localized,
                           (String(constBubbleData) + String(dataSelectedUser)).localized,
                           //: "Others".localized]
                           (String(notiPathData.prefix(6))).localized]

        //: case .reportVideo:
        case .reportVideo:
            //: if let reason = PositionLabelReactiveCompatible.share.appConfigMode.videoReport["reason"] {
            if let reason = PositionLabelReactiveCompatible.share.appConfigMode.videoReport[(showKindUser.replacingOccurrences(of: "make", with: "re"))] {
                //: reasonArray = reason as! [String]
                reasonArray = reason as! [String]
            }

        //: default:
        default:
            //: type = 0
            type = 0
            //: reasonArray = []
            reasonArray = []
        }

        //: for i in 0..<reasonArray.count {
        for i in 0 ..< reasonArray.count {
            //: let model = TalkingReportModel()
            let model = CacheThen()
            //: model.reason = reasonArray[i]
            model.reason = reasonArray[i]
            //: if self.reportType == .reportVideo {
            if self.reportType == .reportVideo {
                //: model.type = "\(i)"
                model.type = "\(i)"
                //: } else {
            } else {
                //: model.type = "\(i+type)"
                model.type = "\(i + type)"
            }
            //: dataArray.append(model)
            dataArray.append(model)
        }

        //: tableView.reloadData()
        tableView.reloadData()
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func female() {
        //: let tapGesture = UITapGestureRecognizer()
        let tapGesture = UITapGestureRecognizer()
        //: bgView.addGestureRecognizer(tapGesture)
        bgView.addGestureRecognizer(tapGesture)
        //: tapGesture.rx.event.bind { [weak self] recognizer in
        tapGesture.rx.event.bind { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.endEditing(true)
            self.endEditing(true)
        }
        //: .disposed(by: disposeBag)
        .disposed(by: disposeBag)
    }
}
