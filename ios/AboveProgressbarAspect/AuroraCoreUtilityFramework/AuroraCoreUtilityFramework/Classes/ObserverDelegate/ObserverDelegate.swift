
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let notiKeyData:[UInt8] = [0xda,0xdf,0xda,0xe5,0x99,0xd4,0xe0,0xd5,0xd6,0xe3,0xab,0x9a,0x91,0xd9,0xd2,0xe4,0x91,0xdf,0xe0,0xe5,0x91,0xd3,0xd6,0xd6,0xdf,0x91,0xda,0xde,0xe1,0xdd,0xd6,0xde,0xd6,0xdf,0xe5,0xd6,0xd5]

fileprivate func thoughtImage(bubble num: UInt8) -> UInt8 {
    let value = Int(num) - 113
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Load..." :*/
fileprivate let notiFileShow:[Character] = ["L","o","a","d",".",".","."]

/*: "Skip" :*/
fileprivate let userInfoNoti:String = "make user userSkip"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let dataModelShowApp:[UInt8] = [0x35,0x61,0x61,0x5d,0x41,0x5c,0x5c,0x59,0x1b,0x3b,0x4e,0x61,0x56,0x63,0x52,0x41,0x5c,0x37,0x60,0x15,0x14,0x5f,0x52,0x50,0x55,0x4e,0x5f,0x54,0x52,0x14,0x16]

fileprivate func upperArrayMake(index num: UInt8) -> UInt8 {
    let value = Int(num) + 19
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let kToData:[UInt8] = [0x7c,0x62,0x65,0x6f,0x64,0x7c,0x25,0x64,0x65,0x5b,0x6a,0x6c,0x6e,0x58,0x63,0x64,0x7c,0x2d,0x2d,0x64,0x65,0x5b,0x6a,0x6c,0x6e,0x58,0x63,0x64,0x7c,0x23,0x22,0x30]

private func sendBy(sex num: UInt8) -> UInt8 {
    return num ^ 11
}

/*: "jsEvent(onPageShow):  :*/
fileprivate let mainGiftConst:String = "data succeedjsEven"
fileprivate let mainEmptyMaxAccountShow:String = "send countageSh"
fileprivate let constChangeMain:String = "ow): array layer"

/*: --- :*/
fileprivate let appFieldSkipData:[Character] = ["-","-","-"]

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let notiIntervalConst:[UInt8] = [0x3b,0x29,0x28,0x65,0x64,0x69,0x48,0x65,0x67,0x61,0x50,0x6e,0x6f,0x26,0x26,0x65,0x64,0x69,0x48,0x65,0x67,0x61,0x50,0x6e,0x6f,0x2e,0x77,0x6f,0x64,0x6e,0x69,0x77]

/*: "jsEvent(onPageHide):  :*/
fileprivate let dataItemConst:String = "to i image makeup injsEve"
fileprivate let mainLessValueShow:String = "edge"
fileprivate let constManagerButtonWhiteUser:[Character] = ["H","i","d","e",")",":"," "]

/*: "title" :*/
fileprivate let constVideoApp:String = "closetle"

/*: "canGoBack" :*/
fileprivate let constLengthNoti:[Character] = ["c","a","n"]
fileprivate let showCoverConst:String = "GoBackmake item center"

/*: "PHPSESSID= :*/
fileprivate let showEventK:String = "phpse"

/*: ";uid= :*/
fileprivate let appImageShow:String = ";uid=model file share size"

/*: "Cookie" :*/
fileprivate let appHalfTitleK:String = "Cookiebar false return"

/*: "nav_back_black_nor" :*/
fileprivate let constStackMagnitudeervalNoti:String = "nav_view size click content"
fileprivate let kMaxApp:String = "black_novalue style equal false"
fileprivate let constToShow:String = "permission"

/*: "ico_intimate_list_sm" :*/
fileprivate let constCancelNoti:[Character] = ["i","c","o","_","i","n","t","i","m"]
fileprivate let kCountervalMain:String = "suite make to caseate_l"
fileprivate let userShareK:[Character] = ["i","s","t","_","s","m"]

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let appFlushConst:[UInt8] = [0x8b,0x96,0x8a,0x9c,0x94,0x8c,0x95,0x9b,0x55,0x8e,0x8c,0x9b,0x6c,0x93,0x8c,0x94,0x8c,0x95,0x9b,0x9a,0x69,0xa0,0x7b,0x88,0x8e,0x75,0x88,0x94,0x8c,0x4f,0x4e,0x89,0x96,0x8b,0xa0,0x4e,0x50,0x82,0x57,0x84,0x55,0x9a,0x9b,0xa0,0x93,0x8c,0x55,0x89,0x88,0x8a,0x92,0x8e,0x99,0x96,0x9c,0x95,0x8b,0x64,0x4e,0x99,0x8e,0x89,0x88,0x4f,0x57,0x53,0x57,0x53,0x57,0x53,0x57,0x50,0x4e]

fileprivate func queryIndex(head num: UInt8) -> UInt8 {
    let value = Int(num) + 217
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Alert" :*/
fileprivate let userProgressShow:String = "Alertfile let title menu"

/*: "OK" :*/
fileprivate let userWayNoti:String = "pathK"

/*: "Cancle" :*/
fileprivate let mainClickUser:String = "push let titleCancle"

/*: "Done" :*/
fileprivate let kLeadingUser:String = "Donesucceed array snap read"

/*: "js call method name =  :*/
fileprivate let notiColorData:String = "block file first tooljs cal"
fileprivate let dataClickConst:String = "app image user self fromthod n"
fileprivate let kExceptUser:String = "appear return colorame = "

/*: , message =  :*/
fileprivate let kValueData:String = ", messain key label device"
fileprivate let userModelDomainShow:[Character] = ["g","e"," ","="," "]

/*: "http://" :*/
fileprivate let notiAtUser:String = "http://actual new hidden head message"

/*: "https://" :*/
fileprivate let showKitMain:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let kPushUser:[UInt8] = [0xa,0x3b,0x29,0x20,0x7d,0x20,0x5d,0x30,0x5b,0x29,0x27,0x3d,0x27,0x28,0x74,0x69,0x6c,0x70,0x73,0x2e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x20,0x6e,0x72,0x75,0x74,0x65,0x72,0x20,0x7b,0x20,0x29,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x28,0x6e,0x6f,0x69,0x74,0x63,0x6e,0x75,0x66,0x28,0x70,0x61,0x6d,0x2e,0x29,0x27,0x20,0x3b,0x27,0x28,0x74,0x69,0x6c,0x70,0x73,0x2e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x2e,0x74,0x6e,0x65,0x6d,0x75,0x63,0x6f,0x64,0x20,0x3d,0x20,0x73,0x65,0x6d,0x61,0x4e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x20,0x72,0x61,0x76]

/*: "uid= :*/
fileprivate let dataEndUser:[Character] = ["u","i","d","="]

/*: ;domain= :*/
fileprivate let userLiveShow:[Character] = [";","d","o"]
fileprivate let showRowUsedNoti:String = "main=type explain item tap jump"

/*: ;path=/" :*/
fileprivate let showTimeMain:String = ";path=/player key bar"

/*: "document.cookie=' :*/
fileprivate let constFromShow:String = "DOCUM"
fileprivate let appMakeData:[Character] = ["o","o","k","i","e","=","\'"]

/*: ';\n" :*/
fileprivate let appNameModelConst:String = "\';\n"

/*: ;domin= :*/
fileprivate let appSizeShow:String = ";domin=model section self mode equal"

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let showViewWhiteNoti:[UInt8] = [0x67,0x64,0x1e,0x26,0x61,0x6d,0x6d,0x69,0x67,0x63,0x4c,0x5f,0x6b,0x63,0x71,0x2c,0x67,0x6c,0x62,0x63,0x76,0x4d,0x64,0x26,0x25,0x4e,0x46,0x4e,0x51,0x43,0x51,0x51,0x47,0x42,0x25,0x27,0x1e,0x3b,0x3b,0x1e,0x2b,0x2f,0x27,0x1e,0x79,0x1e,0x62,0x6d,0x61,0x73,0x6b,0x63,0x6c,0x72,0x2c,0x61,0x6d,0x6d,0x69,0x67,0x63,0x3b,0x25]

fileprivate func listValueInfo(normal num: UInt8) -> UInt8 {
    let value = Int(num) - 254
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: '; };\n" :*/
fileprivate let dataSoundNoti:[Character] = ["\'",";"," ","}",";","\n"]

/*: '" :*/
fileprivate let notiIndexMain:[Character] = ["\'"]

/*: "h5Domain =  :*/
fileprivate let showImageUser:String = "h5Domaview equal light super in"
fileprivate let constStyleEditMain:String = "name age a selfin = "

/*: "getAllCookies =  :*/
fileprivate let mainAddShow:String = "getAladd self with to self"
fileprivate let mainBeApp:String = "make"
fileprivate let constNameUser:String = "to view image height imageCookie"

/*: "PHPSESSID" :*/
fileprivate let notiEqualK:[Character] = ["P","H","P","S","E","S","S","I","D"]

/*: "uid" :*/
fileprivate let appArrayTextUser:[Character] = ["u","i","d"]

/*: "appCookies =  :*/
fileprivate let kStyleUser:[Character] = ["a","p","p","C","o","o","k","i","e","s"," "]
fileprivate let dataStartK:String = "= "

/*: "UID" :*/
fileprivate let showCommentConst:[Character] = ["U","I","D"]

/*: "navigator.userAgent" :*/
fileprivate let appButtonShow:[Character] = ["n","a","v","i","g","a","t","o","r",".","u","s","e","r","A","g"]
fileprivate let userModeStallMain:String = "stylent"

/*: "xunwu_iphone" :*/
fileprivate let dataPartyK:[Character] = ["x","u","n","w","u","_","i","p","h","o","n","e"]

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let notiCurrentUser:String = "keynw"
fileprivate let notiWhiteK:String = "custom mode feature xun"
fileprivate let kAppShow:String = "color item makersion/"

/*:  xunwu_package/ :*/
fileprivate let notiTableGiftHeightConst:String = "raw bar female let color xunw"
fileprivate let dataModelK:String = "me table beyond datackage/"

/*:  xunwu_bundleId/ :*/
fileprivate let kReadButtonData:String = "icon true xun"
fileprivate let dataViewApp:String = "wu_buself top group color current"

/*:   webkit/1" :*/
fileprivate let constFaceShow:String = "equal raw player  webk"

/*: "  :*/
fileprivate let kLogShow:String = "match"

/*: " xunwu_device_id/ :*/
fileprivate let mainTitleUser:[Character] = [" ","x","u","n","w","u","_"]
fileprivate let appLabelUser:[Character] = ["d","e","v","i","c","e","_","i","d","/"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ObserverDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class ObserverDelegate: LatterDayViewController {
	var jumpstartClose: Bool = true
	var viewMagnitude: Double = -2.3
	var toArray: [AnyHashable] = []

    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: CountShareConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: UserManagerNameConvertible?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: UserManagerNameConvertible?, webConfig: CountShareConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: notiKeyData.map{thoughtImage(bubble: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        recount()
    
		if var contriveValue = self.tranConfig?.visualBackAlpha { 
	            if (self.providesPresentationContextTransitionStyle) && (self.prefersStatusBarHidden) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let bringHome = PlayController()
	            bringHome.viewOn = isModal
	            
	            bringHome.buildClose = { [self] appearOn in
	            self.jumpstartClose = appearOn
	            
	            var bringHome = animated
	                bringHome = true
	                bringHome = false
	            if bringHome != self.jumpstartClose {
	                self.jumpstartClose = bringHome
	            }
	            
	            return self.jumpstartClose
	            }
	            bringHome.errorLiveTitleInterval = { [self] cuttingEdgeNumber in
	            self.viewMagnitude = cuttingEdgeNumber
	            
	            var bringHome = contriveValue
	                bringHome -= 1
	                if bringHome <= 0 {
	                    bringHome = bringHome + 1
	                }
	            if bringHome < self.viewMagnitude {
	                self.viewMagnitude = bringHome
	            }
	            
	            return self.viewMagnitude
	            }
	            bringHome.socialArray = { [self] capitateArray in
	            self.toArray = capitateArray
	            
	            guard var value = self.toArray as? [String] else {
	                return nil
	            }
	            return value
	            }
	                self.navigationController?.present(bringHome.self, animated: true) { [self] in
	            self.jumpstartClose = false
	                }
	            }
	
		}
	}

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        forme()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(notiFileShow))
        //: addNotification()
        valuePanNotification()
        //: func__checkConfig()
        witness()
        //: func__setUpWebView()
        closeEqual()
        //: func__registerObserver()
        funcWithoutAuditorUser()
        //: func__beginStartRequest()
        buttonRequest()
        //: func_checkAppleIAP()
        equilateral()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: appNetName, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all
        webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            numberimate(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = false
        w.allowsBackForwardNavigationGestures = false
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.requestCardTitle(), .font: UIFont.viewPath(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(userInfoNoti.suffix(4))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension ObserverDelegate {
    /// 添加通知
    //: private func addNotification() {
    private func valuePanNotification() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(ordinal),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: constAssetButtonData,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(recount),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func ordinal() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: dataModelShowApp.map{upperArrayMake(index: $0)}, encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func recount() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: kToData.map{sendBy(sex: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(mainGiftConst.suffix(6)) + "t(onP" + String(mainEmptyMaxAccountShow.suffix(5)) + String(constChangeMain.prefix(5))) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func forme() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: notiIntervalConst.reversed(), encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(dataItemConst.suffix(5)) + "nt(onPag" + mainLessValueShow.replacingOccurrences(of: "edge", with: "e") + String(constManagerButtonWhiteUser)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension ObserverDelegate {
    //: func func__registerObserver() {
    func funcWithoutAuditorUser() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (constVideoApp.replacingOccurrences(of: "close", with: "ti")))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(constLengthNoti) + String(showCoverConst.prefix(6))))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(targetAdd), name: userNoMsg, object: nil)
    }

    //: func func__setUpWebView() {
    func closeEqual() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        smallAgent()
        //: func__addBridgeMethod()
        bridge()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.loadSize()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            WrapView.effectOf(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(showDomainUrl)
            }
        }
    }

    //: func func__beginStartRequest() {
    func buttonRequest() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.shiner()
            }
            //: } else {
        } else {
            //: func__startRequest()
            shiner()
        }
    }

    //: func func__startRequest() {
    func shiner() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = PositionLabelReactiveCompatible.share.loginUid
        let uid = PositionLabelReactiveCompatible.share.loginUid
        //: let ssid = PositionLabelReactiveCompatible.share.loginSessionId
        let ssid = PositionLabelReactiveCompatible.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (showEventK.uppercased() + "SSID=") + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(appImageShow.prefix(5))) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(appHalfTitleK.prefix(6))))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        ofWithoutSpectralColourColor()
        //: func__initRightButton()
        requestVideo()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func equilateral() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
            SharedRequestDelegate.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if status == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.verity(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            SharedRequestDelegate.shared.searchMagnitudeo()
        }
    }

    //: func func__initLeftButton() {
    func laboratoryCoatHoldfastModel() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.imageName(name: (String(constStackMagnitudeervalNoti.prefix(4)) + "back_" + String(kMaxApp.prefix(8)) + constToShow.replacingOccurrences(of: "permission", with: "r"))), style: .plain, target: self, action: #selector(makeNameTo(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func requestVideo() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(UserManagerNameConvertible.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.imageName(name: (String(constCancelNoti) + String(kCountervalMain.suffix(5)) + String(userShareK))), style: .plain, target: self, action: #selector(miniCorner))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func makeNameTo(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        restate()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.underTitle() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: ObserverDelegate.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! ObserverDelegate).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! ObserverDelegate).recount()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func witness() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func makeSucess(sucess: Bool) {
        //: ProgressHUD.dismiss()
        WrapView.livePic()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func ofWithoutSpectralColourColor() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: appFlushConst.map{queryIndex(head: $0)}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func underApp(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: ViewPushManager.share.func__pushToWebVC(urlStr: url)
            ViewPushManager.share.pastUp(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func visualiser(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func targetAdd() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: userNoMsg, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func miniCorner() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = CacheModelViewDelegate(frame: CGRect(x: 0, y: 0, width: userDeviceKey, height: appNameMessage))
        //: view.show()
        view.constraintAddToShow()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension ObserverDelegate: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            WrapView.effectOf(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            makeSucess(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        laboratoryCoatHoldfastModel()
        //: func__clearJSBgColor()
        ofWithoutSpectralColourColor()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            makeSucess(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            makeSucess(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(userProgressShow.prefix(5))), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(mainClickUser.suffix(6))), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(kLeadingUser.prefix(4))).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension ObserverDelegate: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(notiColorData.suffix(6)) + "l me" + String(dataClickConst.suffix(6)) + String(kExceptUser.suffix(6))) + "\(message.name)" + (String(kValueData.prefix(7)) + String(userModelDomainShow)) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.addMessage(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func bridge() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in ResourceTermConvertible.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func restate() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in ResourceTermConvertible.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension ObserverDelegate {
    //: func func__addUserScript() {
    func saveVideo() {
        //: var htmlDomain: String = PositionLabelReactiveCompatible.share.appConfigMode.urlH5Domain
        var htmlDomain: String = PositionLabelReactiveCompatible.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(notiAtUser.prefix(7)))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(notiAtUser.prefix(7))), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((String(showKitMain))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(showKitMain)), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = PositionLabelReactiveCompatible.share.loginUserMode.userID
        let tempUid: String = PositionLabelReactiveCompatible.share.loginUserMode.userID
        //: let tempSessionID: String = PositionLabelReactiveCompatible.share.loginSessionId
        let tempSessionID: String = PositionLabelReactiveCompatible.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: kPushUser.reversed(), encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(dataEndUser)) + "\(tempUid)" + (String(userLiveShow) + String(showRowUsedNoti.prefix(5))) + "\(htmlDomain)" + (String(showTimeMain.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((constFromShow.lowercased() + "ent.c" + String(appMakeData)) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (showEventK.uppercased() + "SSID=") + "\(tempSessionID)" + (String(appSizeShow.prefix(7))) + "\(htmlDomain)" + (String(showTimeMain.prefix(7)))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((constFromShow.lowercased() + "ent.c" + String(appMakeData)) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: showViewWhiteNoti.map{listValueInfo(normal: $0)}, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(dataEndUser)) + "\(tempUid)" + (String(userLiveShow) + String(showRowUsedNoti.prefix(5))) + "\(htmlDomain)" + (String(showTimeMain.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((constFromShow.lowercased() + "ent.c" + String(appMakeData)) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(dataEndUser)) + "\(tempSessionID)" + (String(userLiveShow) + String(showRowUsedNoti.prefix(5))) + "\(htmlDomain)" + (String(showTimeMain.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((constFromShow.lowercased() + "ent.c" + String(appMakeData)) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func numberimate(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = PositionLabelReactiveCompatible.share.appConfigMode.urlH5Domain
            let htmlDomain: String = PositionLabelReactiveCompatible.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(showImageUser.prefix(6)) + String(constStyleEditMain.suffix(5))) + "\(htmlDomain)")
            //: let userID: String = PositionLabelReactiveCompatible.share.loginUserMode.userID
            let userID: String = PositionLabelReactiveCompatible.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(mainAddShow.prefix(5)) + mainBeApp.replacingOccurrences(of: "make", with: "l") + String(constNameUser.suffix(6)) + "s = ") + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (String(notiEqualK)) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (String(appArrayTextUser)), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.buttonRequest()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.clipDelete(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func clipDelete(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = PositionLabelReactiveCompatible.share.appConfigMode.urlH5Domain
                let htmlDomain: String = PositionLabelReactiveCompatible.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(showImageUser.prefix(6)) + String(constStyleEditMain.suffix(5))) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: UpToDatenessReactiveCompatible.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(kStyleUser) + dataStartK.capitalized) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (String(showCommentConst)), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (String(appArrayTextUser))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (String(notiEqualK)), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (String(notiEqualK))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.buttonRequest()
            }
        }
    }

    func getUserAgent() -> String {
        return (notiCurrentUser.replacingOccurrences(of: "key", with: "xu") + "u_iphone" + String(notiWhiteK.suffix(4)) + "wu_ve" + String(kAppShow.suffix(6))) + "\(mainNoName)" + (String(notiTableGiftHeightConst.suffix(5)) + "u_pa" + String(dataModelK.suffix(6))) + "\(notiBottomKeyValue)" + (String(kReadButtonData.suffix(4)) + String(dataViewApp.prefix(5)) + "ndleId/") + "\(notiTabPath)" + (String(constFaceShow.suffix(6)) + "it/1")
    }
    
    //: func func__addcustomUserAgent() {
    func smallAgent() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((String(appButtonShow) + userModeStallMain.replacingOccurrences(of: "style", with: "e"))) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (String(appButtonShow) + userModeStallMain.replacingOccurrences(of: "style", with: "e")))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((String(dataPartyK))) {
                //: let str = "xunwu_iphone xunwu_version/\(AppNetVersion) xunwu_package/\(PackageID) xunwu_bundleId/\(AppBundle)  webkit/1"
                let str = getUserAgent()
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.ta { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(mainTitleUser) + String(appLabelUser)) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }
}
