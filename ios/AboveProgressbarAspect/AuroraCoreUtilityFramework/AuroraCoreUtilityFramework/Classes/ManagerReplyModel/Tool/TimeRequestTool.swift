
//: Declare String Begin

/*: "mf/moment/list" :*/
fileprivate let appPageNoti:[Character] = ["m","f","/","m","o"]
fileprivate let constPostResultMain:String = "mix"
fileprivate let notiGestureShow:String = "ent/listname view false player"

/*: "mf/moment/moments" :*/
fileprivate let appUserNoti:String = "mf/mopath photo model of bottom"
fileprivate let kUpToData:[Character] = ["m","e","n","t","/","m"]
fileprivate let kLabelUser:String = "omencolor"

/*: "mf/moment/like" :*/
fileprivate let userPopConst:String = "time viewmf/mo"
fileprivate let dataPlayerShow:String = "model window/like"

/*: "mf/crush/send" :*/
fileprivate let dataEqualConst:[Character] = ["m","f","/","c","r","u","s","h","/","s"]
fileprivate let dataModeFilterVoiceMain:String = "alwaysnd"

/*: "mf/moment/view" :*/
fileprivate let notiMicK:String = "mf/momerange hidden next self"
fileprivate let appManagerShow:String = "nt/viewmodel post to"

/*: "mf/momentReply/list" :*/
fileprivate let dataRecordViewUser:String = "mf/momirror point raw let gender"
fileprivate let dataNameK:String = "eply/from type component true"
fileprivate let appGroupConst:String = "lishost"

/*: "mf/momentReply/save" :*/
fileprivate let constMatchData:String = "mf/mcount guard tag true in"
fileprivate let showViewLabUser:String = "epface"
fileprivate let notiWithUser:[Character] = ["/","s","a","v","e"]

/*: "mf/moment/videoSign" :*/
fileprivate let appTimeConst:String = "view viewmf/mom"
fileprivate let appIndexData:String = "at"
fileprivate let notiLayerApp:[Character] = ["n","t","/","v","i","d","e","o","S","i","g","n"]

/*: "dynamicPic.zip" :*/
fileprivate let userItemData:[Character] = ["d","y","n","a","m","i","c","P","i","c"]
fileprivate let dataButtonDirectionShow:[Character] = [".","z","i","p"]

/*: "pic" :*/
fileprivate let notiHiddenIconVarK:[UInt8] = [0x21,0x38,0x32]

private func keyStar(group num: UInt8) -> UInt8 {
    return num ^ 81
}

/*: "content" :*/
fileprivate let mainAddShow:String = "conmodelenmodel"

/*: "mf/moment/save" :*/
fileprivate let constDataShow:String = "mf/momrank view behavior"
fileprivate let notiBottomApp:String = "scontentve"

/*: "Publish Successful, Go to My Dynamics to view" :*/
fileprivate let notiInputK:[UInt8] = [0xb7,0xdc,0xc9,0xd3,0xd0,0xda,0xcf,0x87,0xba,0xdc,0xca,0xca,0xcc,0xda,0xda,0xcd,0xdc,0xd3,0x93,0x87,0xae,0xd6,0x87,0xdb,0xd6,0x87,0xb4,0xe0,0x87,0xab,0xe0,0xd5,0xc8,0xd4,0xd0,0xca,0xda,0x87,0xdb,0xd6,0x87,0xdd,0xd0,0xcc,0xde]

fileprivate func starTop(add num: UInt8) -> UInt8 {
    let value = Int(num) - 103
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/moment/saveVideo" :*/
fileprivate let userMakeData:String = "leading var height info centermf/mo"
fileprivate let mainTitleConst:[Character] = ["m","e","n","t","/","s","a","v","e","V","i","d","e","o"]

/*: .png" :*/
fileprivate let userMessageShow:[Character] = [".","p","n","g"]

/*: "dynamicPic" :*/
fileprivate let showExpressMain:String = "dcompareami"

/*: "freeImage" :*/
fileprivate let showBirthNoti:[Character] = ["f","r","e","e","I","m"]
fileprivate let constMakePopK:String = "agbroad"

/*: "mf/momentReply/replylist" :*/
fileprivate let showTargetBottomData:String = "view infomf/m"
fileprivate let dataMessageShow:[Character] = ["p","l","y","/","r","e","p"]
fileprivate let notiResultMain:String = "selectlist"

/*: "mf/moment/delete" :*/
fileprivate let constErrorK:[Character] = ["m","f","/","m","o"]
fileprivate let constColorToApp:[Character] = ["m","e","n","t","/","d","e","l","e","t","e"]

/*: "mf/momentReply/delete" :*/
fileprivate let mainLoopShow:String = "mf/moview name empty button"
fileprivate let userOffAppPanConst:String = "error centermentR"
fileprivate let kArrayData:[Character] = ["/","d","e","l","e","t","e"]

/*: "mf/moment/pin" :*/
fileprivate let userBackConst:String = "mf/mleading quote else"
fileprivate let showBackgroundData:String = "IN"

/*: "mf/moment/remindNum" :*/
fileprivate let notiPathUser:String = "mf/mobject with head item for"
fileprivate let appColorUserNameShow:String = "type to talk letmindNum"

/*: "mf/moment/remindList" :*/
fileprivate let showIconToUser:[Character] = ["m","f","/","m","o","m","e","n"]
fileprivate let showLineConst:String = "let color finish guard elset/r"
fileprivate let appPicConst:[Character] = ["e","m","i","n","d","L","i","s","t"]

/*: "page" :*/
fileprivate let notiShowHiddenK:[UInt8] = [0xeb,0xdc,0xe2,0xe0]

fileprivate func comeLabel(style num: UInt8) -> UInt8 {
    let value = Int(num) - 123
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cos/getCredential" :*/
fileprivate let appPriceData:String = "return textcos/"
fileprivate let appColorShow:String = "edemake"
fileprivate let appCameraSizeShow:String = "recordal"

/*: "type" :*/
fileprivate let constPathShow:[Character] = ["t","y","p","e"]

/*: "user/reportUserInfo" :*/
fileprivate let showInstanceNoti:[Character] = ["u","s","e","r","/","r","e","p","o","r","t","U","s","e","r"]
fileprivate let constMakeMain:String = "Infocount of title"

/*: "uid" :*/
fileprivate let constScreenTouchNoti:String = "imaged"

/*: "deviceId" :*/
fileprivate let dataTableApp:[Character] = ["d","e","v","i","c","e","I","d"]

/*: "deviceName" :*/
fileprivate let notiColorUser:String = "data range view selfdeviceN"
fileprivate let mainSizeConst:String = "makeme"

/*: "iphone" :*/
fileprivate let mainListConst:String = "ilabone"

/*: "fileUrl" :*/
fileprivate let dataGiftUser:String = "fileUrlmy my hidden table as"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TimeRequestTool.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/19.
//

//: import Adjust
import Adjust
//: import UIKit
import UIKit

//: class TalkingMomentRequestTool: NSObject {
class TimeRequestTool: NSObject {
    //: let _fm = FileManager.default
    let _fm = FileManager.default

    /// 动态列表接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func lastMinute(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/moment/list"
        reqModel.requestPath = (String(appPageNoti) + constPostResultMain.replacingOccurrences(of: "mix", with: "m") + String(notiGestureShow.prefix(8)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户动态接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UserMomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func doDeleteRow(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/moment/moments"
        reqModel.requestPath = (String(appUserNoti.prefix(5)) + String(kUpToData) + kLabelUser.replacingOccurrences(of: "color", with: "ts"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 点赞
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentLike(params: [String: Any], completion: @escaping FinishBlock) {
    class func vocalization(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/moment/like"
        reqModel.requestPath = (String(userPopConst.suffix(5)) + "ment" + String(dataPlayerShow.suffix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - 发送心动
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CrushSend(params: [String: Any], completion: @escaping FinishBlock) {
    class func doActionEqual(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/crush/send"
        reqModel.requestPath = (String(dataEqualConst) + dataModeFilterVoiceMain.replacingOccurrences(of: "always", with: "e"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态详情
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentDetail(params: [String: Any], completion: @escaping FinishBlock) {
    class func dateData(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/moment/view"
        reqModel.requestPath = (String(notiMicK.prefix(7)) + String(appManagerShow.prefix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func agendum(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/momentReply/list"
        reqModel.requestPath = (String(dataRecordViewUser.prefix(5)) + "mentR" + String(dataNameK.prefix(5)) + appGroupConst.replacingOccurrences(of: "host", with: "t"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 发表评论、回复
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SendComment(params: [String: Any], completion: @escaping FinishBlock) {
    class func isometrical(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/momentReply/save"
        reqModel.requestPath = (String(constMatchData.prefix(4)) + "omentR" + showViewLabUser.replacingOccurrences(of: "face", with: "ly") + String(notiWithUser))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取视频上传sign
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_VideoSign(params: [String: Any], completion: @escaping FinishBlock) {
    class func clue(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/moment/videoSign"
        reqModel.requestPath = (String(appTimeConst.suffix(6)) + appIndexData.replacingOccurrences(of: "at", with: "e") + String(notiLayerApp))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - photos: 图片
    ///   - content: 内容
    ///   - completion: 回调
    //: func releasePhotosMoment(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
    func createColor(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
        // 处理图片
        //: deleteFile()
        appPath()
        //: for (index, photo) in photos.enumerated() {
        for (index, photo) in photos.enumerated() {
            //: getImagePath(index: index, image: photo)
            mentalRepresentationNeed(index: index, image: photo)
        }
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let doc_path = (documentDictPath as NSString).appendingPathComponent("dynamicPic.zip")
        let doc_path = (documentDictPath as NSString).appendingPathComponent((String(userItemData) + String(dataButtonDirectionShow)))
        //: SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: let data = NSData(contentsOfFile: doc_path)
            let data = NSData(contentsOfFile: doc_path)
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                // 发布
                //: var params: [String: Any] = ["pic": data as Any]
                var params: [String: Any] = [String(bytes: notiHiddenIconVarK.map{keyStar(group: $0)}, encoding: .utf8)!: data as Any]
                //: if content.count > 0 {
                if content.count > 0 {
                    //: params["content"] = content
                    params[(mainAddShow.replacingOccurrences(of: "model", with: "t"))] = content
                }
                //: let reqModel = TalkingRequestModel()
                let reqModel = CurrentThen()
                //: reqModel.requestPath = "mf/moment/save"
                reqModel.requestPath = (String(constDataShow.prefix(6)) + "ent/" + notiBottomApp.replacingOccurrences(of: "content", with: "a"))
                //: reqModel.requestType = .POST
                reqModel.requestType = .POST
                //: reqModel.params = params
                reqModel.params = params
                //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
                showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Publish Successful, Go to My Dynamics to view".localized)
                        self.barPrice(showMsg: String(bytes: notiInputK.map{starTop(add: $0)}, encoding: .utf8)!.localized)
                        //: self.deleteFile()
                        self.appPath()
                    }
                    //: completion(succeed, result, errorModel)
                    completion(succeed, result, errorModel)
                }
            }
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - completion: 回调
    //: class func releaseVideoMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func beard(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/moment/saveVideo"
        reqModel.requestPath = (String(userMakeData.suffix(5)) + String(mainTitleConst))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: private func getImagePath(index: Int, image: UIImage) {
    private func mentalRepresentationNeed(index: Int, image: UIImage) {
        //: let data = image.compressedImageData()
        let data = image.compressed()
        //: let nameString = "\(index).png"
        let nameString = "\(index)" + (String(userMessageShow))
        //: let picPath = (filePath as NSString).appendingPathComponent("dynamicPic")
        let picPath = (filePath as NSString).appendingPathComponent((showExpressMain.replacingOccurrences(of: "compare", with: "yn") + "cPic"))
        //: if !_fm.fileExists(atPath: picPath) {
        if !_fm.fileExists(atPath: picPath) {
            //: try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
            try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
        }
        //: let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        //: _fm.createFile(atPath: imagePath, contents: data)
        _fm.createFile(atPath: imagePath, contents: data)
    }

    //: private func deleteFile() {
    private func appPath() {
        //: if _fm.fileExists(atPath: filePath) {
        if _fm.fileExists(atPath: filePath) {
            //: try? _fm.removeItem(atPath: filePath)
            try? _fm.removeItem(atPath: filePath)
        }
    }

    //: private lazy var filePath: String = {
    private lazy var filePath: String = {
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let imagePath = (documentDictPath as NSString).appendingPathComponent("freeImage")
        let imagePath = (documentDictPath as NSString).appendingPathComponent((String(showBirthNoti) + constMakePopK.replacingOccurrences(of: "broad", with: "e")))
        //: return imagePath
        return imagePath
        //: }()
    }()

    /// 子评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentDetailList(params: [String: Any], completion: @escaping FinishBlock) {
    class func firstName(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/momentReply/replylist"
        reqModel.requestPath = (String(showTargetBottomData.suffix(4)) + "omentRe" + String(dataMessageShow) + notiResultMain.replacingOccurrences(of: "select", with: "ly"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除动态
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func gestureCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/moment/delete"
        reqModel.requestPath = (String(constErrorK) + String(constColorToApp))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除评论
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMomentReply(params: [String: Any], completion: @escaping FinishBlock) {
    class func momentCompletionBarReplyParamsContent(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/momentReply/delete"
        reqModel.requestPath = (String(mainLoopShow.prefix(5)) + String(userOffAppPanConst.suffix(5)) + "eply" + String(kArrayData))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 置顶操作
    /// - Parameters:
    /// status=1表示置顶，0=取消置顶
    ///   - completion: 回调
    //: class func req_TopMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func imagePair(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/moment/pin"
        reqModel.requestPath = (String(userBackConst.prefix(4)) + "oment/p" + showBackgroundData.lowercased())
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户未读动态总数
    /// - Parameter completion: 回调
    //: class func req_getRemindNum(completion: @escaping FinishBlock) {
    class func endLayer(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/moment/remindNum"
        reqModel.requestPath = (String(notiPathUser.prefix(4)) + "oment/re" + String(appColorUserNameShow.suffix(7)))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前未读动态信息列表
    /// - Parameters:
    ///   - page: 分页
    ///   - completion: 回调
    //: class func req_getRemindList(page: Int, completion: @escaping (_ data: [TalkingRemindListModel]) -> Void) {
    class func creamOff(page: Int, completion: @escaping (_ data: [PositionModelType]) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "mf/moment/remindList"
        reqModel.requestPath = (String(showIconToUser) + String(showLineConst.suffix(3)) + String(appPicConst))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["page": page]
        reqModel.params = [String(bytes: notiShowHiddenK.map{comeLabel(style: $0)}, encoding: .utf8)!: page]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { _, result, _ in
            //: var dataArr: [TalkingRemindListModel] = []
            var dataArr: [PositionModelType] = []
            //: guard let arr = result as? [[String: Any]] else {
            guard let arr = result as? [[String: Any]] else {
                //: completion(dataArr)
                completion(dataArr)
                //: return
                return
            }
            //: if let datas = Array<TalkingRemindListModel>.deserialize(from: arr as? Array) as? [TalkingRemindListModel] {
            if let datas = Array<PositionModelType>.deserialize(from: arr as? Array) as? [PositionModelType] {
                //: dataArr.append(contentsOf: datas)
                dataArr.append(contentsOf: datas)
            }
            //: completion(dataArr)
            completion(dataArr)
        }
    }

    /// COS  视频存储
    /// - Parameters:
    ///   - type: 1=动态，2=视频封面，3=私信视频
    ///   - completion: 回调
    //: class func req_getCredential(type: Int, completion: @escaping FinishBlock) {
    class func sizeBreedCheck(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = CurrentThen()
        //: reqModel.requestPath = "cos/getCredential"
        reqModel.requestPath = (String(appPriceData.suffix(4)) + "getCr" + appColorShow.replacingOccurrences(of: "make", with: "nt") + appCameraSizeShow.replacingOccurrences(of: "record", with: "i"))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["type": type]
        reqModel.params = [(String(constPathShow)): type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传cos日志存储路径
    /// - Parameters:
    ///   - fileUrl: 日志路径
    ///   - completion: 回调
    //: class func req_uploadLogPath(fileUrl: String, completion: @escaping FinishBlock) {
    class func someLess(fileUrl: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = CurrentThen()
        //: reqModel.requestServer = PositionLabelReactiveCompatible.share.appConfigMode.reportDomain
        reqModel.requestServer = PositionLabelReactiveCompatible.share.appConfigMode.reportDomain
        //: reqModel.requestPath = "user/reportUserInfo"
        reqModel.requestPath = (String(showInstanceNoti) + String(constMakeMain.prefix(4)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var params = [String: Any]()
        var params = [String: Any]()
        //: params["uid"] = PositionLabelReactiveCompatible.share.loginUserMode.userID
        params[(constScreenTouchNoti.replacingOccurrences(of: "image", with: "ui"))] = PositionLabelReactiveCompatible.share.loginUserMode.userID
        //: params["deviceId"] = Adjust.adid() ?? ""
        params[(String(dataTableApp))] = Adjust.adid() ?? ""
        //: params["deviceName"] = "iphone"
        params[(String(notiColorUser.suffix(7)) + mainSizeConst.replacingOccurrences(of: "make", with: "a"))] = (mainListConst.replacingOccurrences(of: "lab", with: "ph"))
        //: params["type"] = 1
        params[(String(constPathShow))] = 1
        //: params["fileUrl"] = fileUrl
        params[(String(dataGiftUser.prefix(7)))] = fileUrl
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
