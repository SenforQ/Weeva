
//: Declare String Begin

/*: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION" :*/
fileprivate let notiGenderUser:[UInt8] = [0x71,0x60,0x60,0x7c,0x75,0x6f,0x79,0x71,0x60,0x6f,0x60,0x71,0x69,0x6f,0x63,0x65,0x73,0x73,0x75,0x75,0x74,0x6f,0x7e,0x7f,0x64,0x79,0x76,0x79,0x73,0x71,0x64,0x79,0x7f,0x7e]

/*: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION" :*/
fileprivate let appPointMain:[UInt8] = [0x6,0x17,0x17,0xb,0x2,0x18,0xe,0x6,0x17,0x18,0x14,0x12,0x5,0x14,0x4,0x15,0xe,0x5,0x2,0x18,0x14,0x12,0x4,0x4,0x2,0x2,0x3,0x18,0x9,0x8,0x13,0xe,0x1,0xe,0x4,0x6,0x13,0xe,0x8,0x9]

/*: "mf/recharge/createApplePay" :*/
fileprivate let showMakeConst:[Character] = ["m","f","/","r","e","c","h","a"]
fileprivate let userObserveApp:String = "image position picrge/c"
fileprivate let dataResultMain:String = "transform return view import namepplePay"

/*: "productId" :*/
fileprivate let showByK:[Character] = ["p","r","o","d","u","c","t","I","d"]

/*: "source" :*/
fileprivate let dataModelK:[Character] = ["s","o","u","r","c"]
fileprivate let appSizeK:String = "value"

/*: "IAP充值创建订单失败: :*/
fileprivate let appSharedMain:[Character] = ["I","A","P","充","值","创","建","\u{8ba2}","\u{5355}","失"]
fileprivate let dataSelectedNoti:String = "video:"

/*: , 参数: :*/
fileprivate let notiSnapUser:String = ", 参数:"

/*: ." :*/
fileprivate let constEqualInputUser:String = "model"

/*: "orderNum" :*/
fileprivate let appImageShow:[Character] = ["o","r","d","e","r","N","u","m"]

/*: "mf/recharge/applePayNotify" :*/
fileprivate let dataTitleUpSuperConst:[Character] = ["m","f","/","r","e","c","h"]
fileprivate let notiCapacityData:[Character] = ["a","r","g","e","/","a","p","p","l"]
fileprivate let userMakeNoti:[Character] = ["e","P","a","y","N","o","t","i","f","y"]

/*: "IAP充值校验失败: :*/
fileprivate let appFormatUser:String = "IAP充\u{503c}\u{6821}\u{9a8c}\u{5931}败"
fileprivate let kAvailableShow:String = ":"

/*: "transactionId" :*/
fileprivate let dataContentUser:String = "TRANSACT"
fileprivate let showCenterLibraryApp:[Character] = ["i","o","n","I","d"]

/*: "reportMoney" :*/
fileprivate let appFrameK:String = "REPORT"
fileprivate let notiTableK:[Character] = ["M","o","n","e","y"]

/*: "mf/AutoSub/AppleCreateOrder" :*/
fileprivate let mainDataConst:String = "to extension as imagemf/AutoSu"
fileprivate let showScreenUser:String = "b/Apimage color will"
fileprivate let userImageK:[Character] = ["e","a","t","e","O","r","d","e","r"]

/*: "IAP订阅创建订单失败: :*/
fileprivate let mainTableK:String = "IAP订menu top play click"
fileprivate let kKeyToMain:String = "location"

/*: "orderId" :*/
fileprivate let dataRequestCenterUser:[UInt8] = [0x4e,0x53,0x45,0x44,0x53,0x68,0x45]

/*: "mf/AutoSub/ApplePaySuccess" :*/
fileprivate let appTitleMain:String = "in self modelmf/A"
fileprivate let constFeaturePathRequestApp:[Character] = ["b","/","A","p","p","l","e","P"]
fileprivate let showCameraMain:String = "status name itemaySuccess"

/*: "IAP订阅校验失败: :*/
fileprivate let notiCellMain:[Character] = ["I","A","P","订","阅","校","验"]
fileprivate let mainPathK:String = "失\u{8d25}:"

/*: "App" :*/
fileprivate let showMakeNoti:[Character] = ["A","p","p"]

/*: "OrderTransactionInfo_ :*/
fileprivate let showViewData:String = "data image view bottomOrderTran"
fileprivate let kLoadData:String = "path"
fileprivate let dataUserShow:String = "acblocki"

/*: "OrderTransactionInfo_Subscribe_ :*/
fileprivate let userImageApp:[UInt8] = [0x6f,0x52,0x44,0x45,0x52,0x74,0x52,0x41,0x4e,0x53,0x41,0x43,0x54,0x49,0x4f,0x4e,0x69,0x4e,0x46,0x4f,0x7f,0x73,0x55,0x42,0x53,0x43,0x52,0x49,0x42,0x45,0x7f]

private func sharedVideo(active num: UInt8) -> UInt8 {
    return num ^ 32
}

/*: "verifyData" :*/
fileprivate let dataFilterApp:[UInt8] = [0x39,0x28,0x35,0x2c,0x29,0x3c,0x7,0x24,0x37,0x24]

fileprivate func managerExit(add num: UInt8) -> UInt8 {
    let value = Int(num) + 61
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "IAP查询内购商品失败, productId: :*/
fileprivate let kSourceFrameConst:[Character] = ["I","A","P","\u{67e5}","\u{8be2}","\u{5185}","购","商","品","失","败",","," ","p","r","o","d","u","c"]
fileprivate let appModelConst:String = "var gift point livetId:"

/*: "IAP查询内购商品失败, products.count为0." :*/
fileprivate let kTitleUser:String = "IAP查push to"
fileprivate let userFromLabelConst:String = "user add make min败, pro"
fileprivate let dataFrameApp:String = "s.cocenter as name gift"
fileprivate let kSplitData:String = "."

/*: "IAP查询内购商品失败, Error: :*/
fileprivate let showFileConst:[Character] = ["I","A","P","\u{67e5}","询","内","购","商"]
fileprivate let mainManagerConst:String = "品\u{5931}败, E"

/*: "IAP-purchased: :*/
fileprivate let userNumberNearbyData:[Character] = ["I","A","P","-","p"]
fileprivate let showByData:String = "make"
fileprivate let kBottomConst:String = "rchased:before model type"

/*: , 订单号: :*/
fileprivate let dataResourceShow:[Character] = [","," ","\u{8ba2}","单","\u{53f7}",":"]

/*: , 商品Id: :*/
fileprivate let kErrorLeadingInfoConst:String = ", 商品Id:hidden type make"

/*: , 苹果transId: :*/
fileprivate let constRowUser:String = ", 苹果"
fileprivate let constFaceNoti:String = "sId:height var path"

/*: "IAP-failed: :*/
fileprivate let constInstanceApp:[Character] = ["I","A","P","-","f"]
fileprivate let appWhiteMain:[Character] = ["a","i","l","e","d",":"]

/*: "IAP-deferred: :*/
fileprivate let mainMakeApp:String = "if collection forIAP-"
fileprivate let showCenterK:String = ":"

/*: "IAP未知交易类型: :*/
fileprivate let mainNameData:String = "IAP\u{672a}知交"
fileprivate let mainToApp:[Character] = ["易","类","型",":"]

/*: " 未知的交易类型" :*/
fileprivate let showTextKitData:[Character] = [""," ","未"]
fileprivate let notiFinishShow:String = "知的交易类layer"

/*: "IAP获取本地收据数据失败, transactionId: :*/
fileprivate let kSizeMain:String = "result else hidden model sizeIAP获取"
fileprivate let appCompareConst:String = "\u{636e}失\u{8d25},"
fileprivate let notiImageMain:String = "acdatao"

/*: , orderId: :*/
fileprivate let notiBackgroundVideoShow:String = "if button key var class, or"
fileprivate let showAppMain:[Character] = [":"]

/*: , payType: :*/
fileprivate let kNameConst:[Character] = [","," ","p","a","y","T"]
fileprivate let userServerMain:[Character] = ["y","p","e",":"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SharedRequestDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/9/2.
//

//: import StoreKit
import StoreKit
//: import SwiftyJSON
import SwiftyJSON
//: import UIKit
import UIKit

// 苹果支付成功通知
//: public let APPLE_IAP_PAY_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION")
public let dataBottomValue = NSNotification.Name(rawValue: String(bytes: notiGenderUser.map{$0^48}, encoding: .utf8)!)
// 苹果订阅成功通知
//: public let APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION")
public let constTabTitle = NSNotification.Name(rawValue: String(bytes: appPointMain.map{$0^71}, encoding: .utf8)!)
// 最大失败重试次数
//: let APPLE_IAP_MAX_RETRY_COUNT = 9
let mainDetailButtonKey = 9

/// 支付类型
//: enum ApplePayType {
enum BackgroundType {
    //: case Pay
    case Pay // 支付
    //: case Subscribe
    case Subscribe // 订阅
}

/// 支付状态
//: enum AppleIAPStatus: String {
enum MinViewTextLiteral: String {
    //: case unknow            = "未知类型"
    case unknow = "未知类型"
    //: case createOrderFail   = "创建订单失败"
    case createOrderFail = "创建订单失败"
    //: case notArrow          = "设备不允许"
    case notArrow = "设备不允许"
    //: case noProductId       = "缺少产品Id"
    case noProductId = "缺少产品Id"
    //: case failed            = "交易失败/取消"
    case failed = "交易失败/取消"
    //: case restored          = "已购买过该商品"
    case restored = "已购买过该商品"
    //: case deferred          = "交易延期"
    case deferred = "交易延期"
    //: case verityFail        = "服务器验证失败"
    case verityFail = "服务器验证失败"
    //: case veritySucceed     = "服务器验证成功"
    case veritySucceed = "服务器验证成功"
    //: case renewSucceed      = "自动续订成功"
    case renewSucceed = "自动续订成功"
}

//: typealias IAPcompletionHandle = (AppleIAPStatus, Double, ApplePayType) -> Void
typealias IAPcompletionHandle = (MinViewTextLiteral, Double, BackgroundType) -> Void

//: class AppleIAPManager: NSObject {
class SharedRequestDelegate: NSObject {
    //: var completionHandle: IAPcompletionHandle?
    var completionHandle: IAPcompletionHandle?
    //: private var productInfoReq: SKProductsRequest?
    private var productInfoReq: SKProductsRequest?
    //: private var reqRetryCountDict = [String: Int]()
    private var reqRetryCountDict = [String: Int]() // 记录每个交易请求重试次数
    //: private var payCacheList = [[String: String]]()
    private var payCacheList = [[String: String]]() // 【购买】缓存数据
    //: private var subscribeCacheList = [[String: String]]()
    private var subscribeCacheList = [[String: String]]() // 【订阅】缓存数据
    //: private var createOrderId: String?
    private var createOrderId: String? // 当前支付服务端创建的订单id
    //: private var currentPayType: ApplePayType = .Pay
    private var currentPayType: BackgroundType = .Pay // 当前支付类型

    // singleton
    //: static let shared = AppleIAPManager()
    static let shared = SharedRequestDelegate()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        // 监听应用将要销毁
        //: NotificationCenter.default.addObserver(self, selector: #selector(appWillTerminate),
        NotificationCenter.default.addObserver(self, selector: #selector(byEnd),
                                               //: name: UIApplication.willTerminateNotification,
                                               name: UIApplication.willTerminateNotification,
                                               //: object: nil)
                                               object: nil)
    }

    // MARK: - NotificationCenter

    //: @objc func appWillTerminate() {
    @objc func byEnd() {
        //: SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
    }
}

// MARK: - 【苹果购买】业务接口

//: extension AppleIAPManager {
private extension SharedRequestDelegate {
    /// 【购买】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_pay_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func carryOn(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/createApplePay"
        reqModel.requestPath = (String(showMakeConst) + String(userObserveApp.suffix(5)) + "reateA" + String(dataResultMain.suffix(7)))
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(String(showByK))] = productId
        //: dict["source"] = source
        dict[(String(dataModelK) + appSizeK.replacingOccurrences(of: "value", with: "e"))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP充值创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                NameReactiveCompatible.trademarkIndex(msg: (String(appSharedMain) + dataSelectedNoti.replacingOccurrences(of: "video", with: "败")) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderNum"].string
            let orderId = json[(String(appImageShow))].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【购买】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_pay_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func addBlock(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/applePayNotify"
        reqModel.requestPath = (String(dataTitleUpSuperConst) + String(notiCapacityData) + String(userMakeNoti))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                    //: UploadLogTool.writeLog(msg: "IAP充值校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    NameReactiveCompatible.trademarkIndex(msg: (appFormatUser + kAvailableShow.capitalized) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Pay)
                    self.addTranscationDigitizerSearch(transactionId, .Pay)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newPayCacheList = self.payCacheList.filter({$0["transactionId"] != transactionId})
            let newPayCacheList = self.payCacheList.filter { $0[(dataContentUser.lowercased() + String(showCenterLibraryApp))] != transactionId }
            //: let diskPath = self.getPayCachePath()
            let diskPath = self.pointOfSumersection()
            //: NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_PAY_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: dataBottomValue, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue, .Pay)
            self.completionHandle?(.veritySucceed, result[(appFrameK.lowercased() + String(notiTableK))].doubleValue, .Pay)
        }
    }
}

// MARK: - 【苹果订阅】业务接口

//: extension AppleIAPManager {
private extension SharedRequestDelegate {
    /// 【订阅】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_subscribe_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func signDataHandle(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/AppleCreateOrder"
        reqModel.requestPath = (String(mainDataConst.suffix(9)) + String(showScreenUser.prefix(4)) + "pleCr" + String(userImageK))
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(String(showByK))] = productId
        //: dict["source"] = source
        dict[(String(dataModelK) + appSizeK.replacingOccurrences(of: "value", with: "e"))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP订阅创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                NameReactiveCompatible.trademarkIndex(msg: (String(mainTableK.prefix(4)) + "阅创建订单失败" + kKeyToMain.replacingOccurrences(of: "location", with: ":")) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderId"].string
            let orderId = json[String(bytes: dataRequestCenterUser.map{$0^33}, encoding: .utf8)!].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【订阅】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_subscribe_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func todayParams(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = CurrentThen()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/ApplePaySuccess"
        reqModel.requestPath = (String(appTitleMain.suffix(4)) + "utoSu" + String(constFeaturePathRequestApp) + String(showCameraMain.suffix(9)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showViewFinishKey.beyondExplainCompletion(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                    //: UploadLogTool.writeLog(msg: "IAP订阅校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    NameReactiveCompatible.trademarkIndex(msg: (String(notiCellMain) + mainPathK) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Subscribe)
                    self.addTranscationDigitizerSearch(transactionId, .Subscribe)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newSubscribeCacheList = self.subscribeCacheList.filter({$0["transactionId"] != transactionId})
            let newSubscribeCacheList = self.subscribeCacheList.filter { $0[(dataContentUser.lowercased() + String(showCenterLibraryApp))] != transactionId }
            //: let diskPath = self.getSubscribeCachePath()
            let diskPath = self.sizePick()
            //: NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: constTabTitle, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue, .Subscribe)
            self.completionHandle?(.veritySucceed, result[(appFrameK.lowercased() + String(notiTableK))].doubleValue, .Subscribe)
        }
    }
}

// MARK: - Event

//: extension AppleIAPManager {
extension SharedRequestDelegate {
    /// 初始化数据
    //: private func iap_initData() {
    private func reason() {
        //: self.payCacheList = getLocalPayCacheList(payType: .Pay)
        self.payCacheList = getWithVariation(payType: .Pay)
        //: self.subscribeCacheList = getLocalPayCacheList(payType: .Subscribe)
        self.subscribeCacheList = getWithVariation(payType: .Subscribe)
        //: self.createOrderId = nil
        self.createOrderId = nil
    }

    /// 获取缓存列表
    /// - Parameter payType: 支付类型
    /// - Returns: 缓存列表
    //: private func getLocalPayCacheList(payType: ApplePayType) -> [[String: String]] {
    private func getWithVariation(payType: BackgroundType) -> [[String: String]] {
        //: var list: [[String: String]]?
        var list: [[String: String]]?
        //: var diskPath = ""
        var diskPath = ""
        //: if payType == .Pay {
        if payType == .Pay {
            //: diskPath = getPayCachePath()
            diskPath = pointOfSumersection()
            //: } else {
        } else {
            //: diskPath = getSubscribeCachePath()
            diskPath = sizePick()
        }

        //: if FileManager.default.fileExists(atPath: diskPath) {
        if FileManager.default.fileExists(atPath: diskPath) {
            //: list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            //: if list == nil {
            if list == nil {
                //: try? FileManager.default.removeItem(atPath: diskPath)
                try? FileManager.default.removeItem(atPath: diskPath)
            }
        }
        //: if list == nil {
        if list == nil {
            //: list = [[String: String]]()
            list = [[String: String]]()
        }
        //: return list!
        return list!
    }

    /// 获取【购买】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getPayCachePath() -> String {
    private func pointOfSumersection() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(showMakeNoti)))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = constSizeFormat.string(forKey: dataSampleUrl) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent((String(showViewData.suffix(9)) + kLoadData.replacingOccurrences(of: "path", with: "s") + dataUserShow.replacingOccurrences(of: "block", with: "t") + "onInfo_") + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取【订阅】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getSubscribeCachePath() -> String {
    private func sizePick() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(showMakeNoti)))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = constSizeFormat.string(forKey: dataSampleUrl) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_Subscribe_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent(String(bytes: userImageApp.map{sharedVideo(active: $0)}, encoding: .utf8)! + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取本地收据数据
    /// - Parameters:
    ///   - transactionId: 收据标识符
    ///   - payType: 支付类型
    /// - Returns: 收据数据
    //: fileprivate func getVerifyData(_ transactionId: String, _ payType: ApplePayType) -> String? {
    fileprivate func factorSelect(_ transactionId: String, _ payType: BackgroundType) -> String? {
        // 有未完成的订单，先取缓存
        //: var paramsArr = [[String: String]]()
        var paramsArr = [[String: String]]()
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.payCacheList.filter { $0[(dataContentUser.lowercased() + String(showCenterLibraryApp))] == transactionId }
        //: case .Subscribe:
        case .Subscribe:
            //: paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.subscribeCacheList.filter { $0[(dataContentUser.lowercased() + String(showCenterLibraryApp))] == transactionId }
        }
        //: if paramsArr.count > 0 && paramsArr.first!["verifyData"] != nil {
        if paramsArr.count > 0, paramsArr.first![String(bytes: dataFilterApp.map{managerExit(add: $0)}, encoding: .utf8)!] != nil {
            //: return paramsArr.first!["verifyData"]
            return paramsArr.first![String(bytes: dataFilterApp.map{managerExit(add: $0)}, encoding: .utf8)!]
        }

        // 取本地
        //: guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        //: let data = NSData(contentsOf: receiptUrl)
        let data = NSData(contentsOf: receiptUrl)
        //: let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        //: return receiptStr
        return receiptStr
    }
}

// MARK: - 失败重试流程

//: extension AppleIAPManager {
extension SharedRequestDelegate {
    /// 检测未完成的苹果支付【只会重试当前登录用户】
    //: func iap_checkUnfinishedTransactions() {
    func searchMagnitudeo() {
        //: iap_initData()
        reason()

        // 【购买】失败重试
        //: for dict in self.payCacheList {
        for dict in self.payCacheList {
            //: iap_failedRetry(dict["transactionId"], .Pay)
            militaryPost(dict[(dataContentUser.lowercased() + String(showCenterLibraryApp))], .Pay)
        }

        // 【订阅】失败重试
        //: for dict in self.subscribeCacheList {
        for dict in self.subscribeCacheList {
            //: iap_failedRetry(dict["transactionId"], .Subscribe)
            militaryPost(dict[(dataContentUser.lowercased() + String(showCenterLibraryApp))], .Subscribe)
        }
    }

    /// 失败重试
    /// - Parameters:
    ///   - transactionId: Id
    ///   - payType: 支付类型
    //: private func iap_failedRetry(_ transactionId: String?, _ payType: ApplePayType) {
    private func militaryPost(_ transactionId: String?, _ payType: BackgroundType) {
        //: guard let transactionId = transactionId else { return }
        guard let transactionId = transactionId else { return }
        // 初始化每个交易请求次数
        //: reqRetryCountDict[transactionId] = 0
        reqRetryCountDict[transactionId] = 0
        // 3. 服务端校验流程
        //: transcationPurchasedToCheck(transactionId, payType)
        addTranscationDigitizerSearch(transactionId, payType)
    }
}

// MARK: - 苹果正常支付流程

//: extension AppleIAPManager {
extension SharedRequestDelegate {
    /// 发起苹果支付【1.创建订单； 2.发起苹果支付； 3.服务端校验】
    /// - Parameters:
    ///   - purchID: 产品ID
    ///   - payType: 支付类型
    ///   - handle: 回调
    ///   - source: 0 常规充值 1 观看视频后充值或订阅
    //: func iap_startPurchase(productId: String, payType: ApplePayType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
    func pictureHandle(productId: String, payType: BackgroundType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
        //: iap_initData()
        reason()
        //: self.completionHandle = handle
        self.completionHandle = handle
        //: self.currentPayType = payType
        self.currentPayType = payType

        // 1. 根据类型创建订单
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: req_pay_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            carryOn(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0, .Pay)
                    self.completionHandle?(.createOrderFail, 0, .Pay)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.equalCell(productId)
            }

        //: case .Subscribe:
        case .Subscribe:
            //: req_subscribe_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            signDataHandle(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.equalCell(productId)
            }
        }
    }

    // 2 发起苹果支付，查询apple内购商品
    //: fileprivate func requestProductInfo(_ productId: String) {
    fileprivate func equalCell(_ productId: String) {
        //: guard SKPaymentQueue.canMakePayments() else {
        guard SKPaymentQueue.canMakePayments() else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, productId:\(productId).")
            NameReactiveCompatible.trademarkIndex(msg: (String(kSourceFrameConst) + String(appModelConst.suffix(4))) + "\(productId).")
            //: self.completionHandle?(.notArrow, 0, currentPayType)
            self.completionHandle?(.notArrow, 0, currentPayType)
            //: return
            return
        }

        // 销毁当前请求
        //: self.clearProductInfoRequest()
        self.tabRequest()
        // 查询apple内购商品
        //: let identifiers: Set<String> = [productId]
        let identifiers: Set<String> = [productId]
        //: productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        //: productInfoReq?.delegate = self
        productInfoReq?.delegate = self
        //: productInfoReq?.start()
        productInfoReq?.start()
    }

    // 销毁当前请求
    //: fileprivate func clearProductInfoRequest() {
    fileprivate func tabRequest() {
        //: guard productInfoReq != nil else { return }
        guard productInfoReq != nil else { return }
        //: productInfoReq?.delegate = nil
        productInfoReq?.delegate = nil
        //: productInfoReq?.cancel()
        productInfoReq?.cancel()
        //: productInfoReq = nil
        productInfoReq = nil
    }
}

// MARK: - SKProductsRequestDelegate【商品查询】

//: extension AppleIAPManager: SKProductsRequestDelegate {
extension SharedRequestDelegate: SKProductsRequestDelegate {
    // 查询apple内购商品成功回调
    //: func productsRequest(_ request: SKProductsRequest, didReceive response: SKProductsResponse) {
    func productsRequest(_: SKProductsRequest, didReceive response: SKProductsResponse) {
        //: guard response.products.count > 0 else {
        guard response.products.count > 0 else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, products.count为0.")
            NameReactiveCompatible.trademarkIndex(msg: (String(kTitleUser.prefix(4)) + "询\u{5185}\u{8d2d}商品失" + String(userFromLabelConst.suffix(6)) + "duct" + String(dataFrameApp.prefix(4)) + "unt为0" + kSplitData.capitalized))
            //: self.completionHandle?( .noProductId, 0, currentPayType)
            self.completionHandle?(.noProductId, 0, currentPayType)
            //: return
            return
        }

        //: let payment = SKPayment(product: response.products.first!)
        let payment = SKPayment(product: response.products.first!)
        //: SKPaymentQueue.default().add(payment)
        SKPaymentQueue.default().add(payment)
    }

    // 查询apple内购商品失败
    //: func request(_ request: SKRequest, didFailWithError error: Error) {
    func request(_: SKRequest, didFailWithError error: Error) {
        //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, Error:\(error).")
        NameReactiveCompatible.trademarkIndex(msg: (String(showFileConst) + mainManagerConst + "rror:") + "\(error).")
        //: self.completionHandle?( .noProductId, 0, currentPayType)
        self.completionHandle?(.noProductId, 0, currentPayType)
    }

    // 查询apple内购商品完成
    //: func requestDidFinish(_ request: SKRequest) {
    func requestDidFinish(_: SKRequest) {}
}

// MARK: - SKPaymentTransactionObserver【支付回调】

//: extension AppleIAPManager: SKPaymentTransactionObserver {
extension SharedRequestDelegate: SKPaymentTransactionObserver {
    /// 2.2 apple内购完成回调
    //: func paymentQueue(_ queue: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
    func paymentQueue(_: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
        //: for transaction in transactions {
        for transaction in transactions {
            //: switch transaction.transactionState {
            switch transaction.transactionState {
            //: case .purchasing:
            case .purchasing: // 交易中
                //: break
                break
            //: case .purchased:
            case .purchased: // 交易成功
                /**
                 original.transactionIdentifier 首次订阅时为nil，transaction.transactionIdentifier有值；
                 后续自动订阅、续订时，original.transactionIdentifier为首次订阅时生成的transaction.transactionIdentifier，值固定不变；
                 每次订阅transaction.transactionIdentifier都不一样，为当前交易的标识；
                 */
                //: if transaction.original != nil && createOrderId == nil {
                if transaction.original != nil && createOrderId == nil { // 启动自动续订时，不需要调用服务端验证接口
                    //: self.completionHandle?(.renewSucceed, 0, currentPayType)
                    self.completionHandle?(.renewSucceed, 0, currentPayType)
                    //: } else {
                } else { // 普通购买和订阅
                    //: UploadLogTool.writeLog(msg: "IAP-purchased:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                    NameReactiveCompatible.trademarkIndex(msg: (String(userNumberNearbyData) + showByData.replacingOccurrences(of: "make", with: "u") + String(kBottomConst.prefix(8))) + "\(currentPayType)" + (String(dataResourceShow)) + "\(String(describing: createOrderId))" + (String(kErrorLeadingInfoConst.prefix(7))) + "\(transaction.payment.productIdentifier)" + (constRowUser.capitalized + "tran" + String(constFaceNoti.prefix(4))) + "\(String(describing: transaction.transactionIdentifier)).")
                    // 初始化每个交易请求次数
                    //: reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    // 3. 服务端校验流程
                    //: transcationPurchasedToCheck(transaction.transactionIdentifier!, self.currentPayType)
                    addTranscationDigitizerSearch(transaction.transactionIdentifier!, self.currentPayType)
                }
                // 移除苹果支付系统缓存
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: createOrderId = nil
                createOrderId = nil
            //: case .failed:
            case .failed: // 交易失败/取消
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-failed:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                NameReactiveCompatible.trademarkIndex(msg: (String(constInstanceApp) + String(appWhiteMain)) + "\(currentPayType)" + (String(dataResourceShow)) + "\(String(describing: createOrderId))" + (String(kErrorLeadingInfoConst.prefix(7))) + "\(transaction.payment.productIdentifier)" + (constRowUser.capitalized + "tran" + String(constFaceNoti.prefix(4))) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.failed, 0, currentPayType)
                self.completionHandle?(.failed, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: case .restored:
            case .restored: // 已购买过该商品
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.restored, 0, currentPayType)
                self.completionHandle?(.restored, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: case .deferred:
            case .deferred: // 交易延期
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-deferred:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                NameReactiveCompatible.trademarkIndex(msg: (String(mainMakeApp.suffix(4)) + "deferred" + showCenterK.capitalized) + "\(currentPayType)" + (String(dataResourceShow)) + "\(String(describing: createOrderId))" + (String(kErrorLeadingInfoConst.prefix(7))) + "\(transaction.payment.productIdentifier)" + (constRowUser.capitalized + "tran" + String(constFaceNoti.prefix(4))) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.deferred, 0, currentPayType)
                self.completionHandle?(.deferred, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: @unknown default:
            @unknown default:
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.unknow, 0, currentPayType)
                self.completionHandle?(.unknow, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
                //: UploadLogTool.writeLog(msg: "IAP未知交易类型:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                NameReactiveCompatible.trademarkIndex(msg: (mainNameData + String(mainToApp)) + "\(currentPayType)" + (String(dataResourceShow)) + "\(String(describing: createOrderId))" + (String(kErrorLeadingInfoConst.prefix(7))) + "\(transaction.payment.productIdentifier)" + (constRowUser.capitalized + "tran" + String(constFaceNoti.prefix(4))) + "\(String(describing: transaction.transactionIdentifier)).")
                //: fatalError(" 未知的交易类型")
                fatalError((String(showTextKitData) + notiFinishShow.replacingOccurrences(of: "layer", with: "型")))
            }
        }
    }

    /// 3. 服务端校验流程
    /// - Parameters:
    ///   - transactionId: 交易唯一标识符
    ///   - payType: 支付类型
    //: fileprivate func transcationPurchasedToCheck(_ transactionId: String, _ payType: ApplePayType) {
    fileprivate func addTranscationDigitizerSearch(_ transactionId: String, _ payType: BackgroundType) {
        //: guard let receiptStr = getVerifyData(transactionId, payType) else {
        guard let receiptStr = factorSelect(transactionId, payType) else {
            //: UploadLogTool.writeLog(msg: "IAP获取本地收据数据失败, transactionId:\(transactionId), orderId:\(String(describing: createOrderId)), payType:\(payType).")
            NameReactiveCompatible.trademarkIndex(msg: (String(kSizeMain.suffix(5)) + "本地\u{6536}据数" + appCompareConst + " trans" + notiImageMain.replacingOccurrences(of: "data", with: "ti") + "nId:") + "\(transactionId)" + (String(notiBackgroundVideoShow.suffix(4)) + "derId" + String(showAppMain)) + "\(String(describing: createOrderId))" + (String(kNameConst) + String(userServerMain)) + "\(payType).")
            //: self.completionHandle?(.verityFail, 0, payType)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 缓存支付成功信息，防止接口校验失败
        //: if createOrderId != nil {
        if createOrderId != nil { // 正常支付流程
            //: switch(payType) {
            switch payType {
            //: case .Pay:
            case .Pay:
                //: if self.payCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 {  // 防止重复添加缓存数据

                if self.payCacheList.filter({ $0[(dataContentUser.lowercased() + String(showCenterLibraryApp))] == transactionId || $0[String(bytes: dataRequestCenterUser.map{$0^33}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(dataContentUser.lowercased() + String(showCenterLibraryApp)): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: dataRequestCenterUser.map{$0^33}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: dataFilterApp.map{managerExit(add: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.payCacheList.append(cacheDict)
                    self.payCacheList.append(cacheDict)
                    //: let diskPath = self.getPayCachePath()
                    let diskPath = self.pointOfSumersection()
                    //: NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                }

            //: case .Subscribe:
            case .Subscribe:
                //: if self.subscribeCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 { // 防止重复添加缓存数据

                if self.subscribeCacheList.filter({ $0[(dataContentUser.lowercased() + String(showCenterLibraryApp))] == transactionId || $0[String(bytes: dataRequestCenterUser.map{$0^33}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(dataContentUser.lowercased() + String(showCenterLibraryApp)): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: dataRequestCenterUser.map{$0^33}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: dataFilterApp.map{managerExit(add: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.subscribeCacheList.append(cacheDict)
                    self.subscribeCacheList.append(cacheDict)
                    //: let diskPath = self.getSubscribeCachePath()
                    let diskPath = self.sizePick()
                    //: NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                }
            }
        }

        // 限制交易重试最大次数
        //: var reqCount = reqRetryCountDict[transactionId] ?? 0
        var reqCount = reqRetryCountDict[transactionId] ?? 0
        //: reqCount += 1
        reqCount += 1
        //: reqRetryCountDict[transactionId] = reqCount
        reqRetryCountDict[transactionId] = reqCount
        //: if reqCount > APPLE_IAP_MAX_RETRY_COUNT {
        if reqCount > mainDetailButtonKey {
            //: self.completionHandle?(.verityFail, 0, payType)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 3.服务端校验，根据transactionId从缓存中取
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: let paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.payCacheList.filter { $0[(dataContentUser.lowercased() + String(showCenterLibraryApp))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_pay_uploadAppletransaction(transactionId, params: paramsArr.first!)
            addBlock(transactionId, params: paramsArr.first!)

        //: case .Subscribe:
        case .Subscribe:
            //: let paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.subscribeCacheList.filter { $0[(dataContentUser.lowercased() + String(showCenterLibraryApp))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_subscribe_uploadAppletransaction(transactionId, params: paramsArr.first!)
            todayParams(transactionId, params: paramsArr.first!)
        }
    }
}