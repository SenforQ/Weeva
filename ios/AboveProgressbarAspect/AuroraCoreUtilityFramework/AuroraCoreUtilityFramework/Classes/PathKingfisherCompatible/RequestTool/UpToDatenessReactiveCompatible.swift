
//: Declare String Begin

/*: "http://" :*/
fileprivate let dataValueColorApp:String = "view deal selectedhttp:"
fileprivate let notiLabMain:[Character] = ["/","/"]

/*: "https://" :*/
fileprivate let showControlConst:[Character] = ["h","t","t","p","s",":","/"]
fileprivate let mainAddNoti:String = "/"

/*: "http:// :*/
fileprivate let showMidTopViewConst:[Character] = ["h","t","t","p",":","/","/"]

/*: "serverSelctIndex =  :*/
fileprivate let constUserMain:String = "seliveve"
fileprivate let showMessageHiddenLetConst:String = "ctIndsearch component corner height to"
fileprivate let dataGiftK:[Character] = [" "]

/*: "serverUrlStr =  :*/
fileprivate let appQualityK:String = "smodel"
fileprivate let dataObjectNoti:String = "verUrme play show let"

/*: "app. :*/
fileprivate let appMessageData:String = "app.type none data"

/*: .com" :*/
fileprivate let kModelShow:String = ".comcenter target"

/*: "IM/getMyBlackList" :*/
fileprivate let dataVideoShow:String = "name extensionIM/ge"
fileprivate let userLastNoti:String = "data view name print valuetMyBl"

/*: "mf/business/sendMsg" :*/
fileprivate let showTableApp:String = "mf/busgift height return progress"
fileprivate let userExceptK:String = "aspect equalsendMs"
fileprivate let kSkinStatusDataMain:[Character] = ["g"]

/*: "mf/business/sendGift" :*/
fileprivate let appToConst:String = "else selfmf/busin"
fileprivate let userLabelCenterModelApp:String = "ecolor"
fileprivate let constMessageData:String = "live model equal product actual/send"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let showImageBorderApp:String = "view to timemf/c"
fileprivate let userMakeShow:String = "lab var explain left barm/sendMsg"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let notiToUser:String = "for top record linemf/cha"
fileprivate let constPinNoti:String = "/sendGby equal"
fileprivate let appOfFailConst:String = "replacementft"

/*: "recharge/applePayNotify" :*/
fileprivate let userPathShow:[Character] = ["r","e","c","h","a","r"]
fileprivate let showIndexConst:String = "ge/apmodel description gift video if"
fileprivate let appReloadNoti:[Character] = ["a","y","N","o","t","i","f","y"]

/*: "user/appleLogin" :*/
fileprivate let notiToMain:String = "circle name selfuser/"
fileprivate let constValueK:[Character] = ["i","n"]

/*: "en" :*/
fileprivate let mainAppPathNoti:String = "colorn"

/*: "ar" :*/
fileprivate let kStyleNoti:[Character] = ["a","r"]

/*: "es" :*/
fileprivate let mainMyNeedApp:String = "emake"

/*: "pt" :*/
fileprivate let userPinK:[Character] = ["p","t"]

/*: "GJ21FB" :*/
fileprivate let userHiddenRequestConst:[Character] = ["G","J","2","1","F","B"]

/*: "完成了检测,请求状态正常" :*/
fileprivate let showAfterAddNoti:String = "\u{5b8c}成了检"
fileprivate let showRawNoti:String = "状态正常"

/*: "接口请求失败" :*/
fileprivate let notiColorUser:String = "接口请求\u{5931}败"

/*: "完成了检测,需要更换server url" :*/
fileprivate let appCenterK:[Character] = ["完","成","了","检","测",","]
fileprivate let dataSizeArrayMain:[Character] = ["需","要","更","换","s"]
fileprivate let dataColorMain:String = "showve"

/*: "HTTPProxy" :*/
fileprivate let mainPhotoRegularUser:[Character] = ["H","T"]
fileprivate let userDataMain:[Character] = ["T","P","P","r","o","x","y"]

/*: "__SCOPED__" :*/
fileprivate let dataAddMain:[Character] = ["_","_","S","C","O","P","E","D","_","_"]

/*: "tap" :*/
fileprivate let showAFilterApp:String = "tcolor"

/*: "tun" :*/
fileprivate let showApplyUser:String = "TUN"

/*: "ipsec" :*/
fileprivate let notiInputShow:[Character] = ["i","p","s","e","c"]

/*: "ppp" :*/
fileprivate let userBackgroundApp:String = "tototo"

/*: "func__updateTalkingCookies =  :*/
fileprivate let showViewK:[Character] = ["f","u","n","c","_","_","u","p","d"]
fileprivate let kPriceConst:[Character] = ["a","t","e","T","a","l","k","i","n"]
fileprivate let notiDetailImageMain:String = "gCookname collection view gift"

/*: "https:// :*/
fileprivate let dataFillMain:String = "source"
fileprivate let mainCenterShow:[Character] = ["t","t","p","s",":","/","/"]

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let notiAgentUser:[UInt8] = [0x3b,0x28,0x33,0x3e,0x2,0x2,0x39,0x38,0x31,0x38,0x29,0x38,0xe,0x3c,0x30,0x38,0x1e,0x32,0x32,0x36,0x34,0x38,0x2e,0xa,0x34,0x29,0x35,0x8,0x2f,0x31,0x7d,0x60,0x7d]

private func topText(data num: UInt8) -> UInt8 {
    return num ^ 93
}

/*: "UID" :*/
fileprivate let kMessageUser:String = "Utemp"

/*: "PHPSESSID" :*/
fileprivate let notiLineConst:[Character] = ["P","H","P","S","E","S","S","I"]
fileprivate let userTitleShow:String = "model"

/*: "func__saveAllCookies =  :*/
fileprivate let dataCurrentUser:[Character] = ["f","u","n","c","_","_","s","a","v"]
fileprivate let constRetNoti:String = "eAllCoeffect type"
fileprivate let kTitleNoti:[Character] = ["o","k","i","e","s"," ","="," "]

/*: "func__saveAllCookies  error" :*/
fileprivate let showToApp:String = "fulivec"
fileprivate let notiMomentApp:String = "AllCheight player user"
fileprivate let kLabUser:String = "make text model videoookies  "
fileprivate let constMakeK:[Character] = ["e","r","r","o","r"]

/*: "🍪 删除 :*/
fileprivate let appDetailConst:String = "🍪 \u{5220}除"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  UpToDatenessReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum DigitiserScalarLiteral: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class UpToDatenessReactiveCompatible: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = UpToDatenessReactiveCompatible()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        oust()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(dataValueColorApp.suffix(5)) + String(notiLabMain))) || newValue.hasPrefix((String(showControlConst) + mainAddNoti.capitalized)) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if mainScaleMessage && !mainDeviceFormat {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(showMidTopViewConst)) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(showMidTopViewConst)) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    responseClick(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func oust() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = constSizeFormat.integer(forKey: notiDeviceTabPath)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (constUserMain.replacingOccurrences(of: "live", with: "r") + "rSel" + String(showMessageHiddenLetConst.prefix(5)) + "ex =" + String(dataGiftK)) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = outputWith()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(dataValueColorApp.suffix(5)) + String(notiLabMain))
        //: if Environment_Formal && !Encryption_Request {
        if mainScaleMessage && !mainDeviceFormat {
            //: serverPrefix = "http://"
            serverPrefix = (String(dataValueColorApp.suffix(5)) + String(notiLabMain))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (appQualityK.replacingOccurrences(of: "model", with: "er") + String(dataObjectNoti.prefix(5)) + "lStr = ") + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func outputWith() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func equalRecordStr(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(appMessageData.prefix(4))) + "\(appFormalStr)" + (String(kModelShow.prefix(4))), (String(appMessageData.prefix(4))) + "\(appFormalStr)" + (String(kModelShow.prefix(4)))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["IM/getMyBlackList",
        [(String(dataVideoShow.suffix(5)) + String(userLastNoti.suffix(5)) + "ackList"),
         //: "mf/business/sendMsg",
         (String(showTableApp.prefix(6)) + "iness/" + String(userExceptK.suffix(6)) + String(kSkinStatusDataMain)),
         //: "mf/business/sendGift",
         (String(appToConst.suffix(8)) + userLabelCenterModelApp.replacingOccurrences(of: "color", with: "ss") + String(constMessageData.suffix(5)) + "Gift")] + arr
    //: }()
        let arr = [         //: "mf/chatRoom/sendMsg",
            (String(showImageBorderApp.suffix(4)) + "hatRoo" + String(userMakeShow.suffix(9))),
            //: "mf/chatRoom/sendGift",
            (String(notiToUser.suffix(6)) + "tRoom" + String(constPinNoti.prefix(6)) + appOfFailConst.replacingOccurrences(of: "replacement", with: "i")),
            //: "recharge/applePayNotify",
            (String(userPathShow) + String(showIndexConst.prefix(5)) + "pleP" + String(appReloadNoti)),
            //: "user/appleLogin"]
            (String(notiToMain.suffix(5)) + "appleLog" + String(constValueK))]

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = RowMacroDefine.getSystemLangCode()
        let lang = RowMacroDefine.pastResume()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .birdSEyeViewFrom(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (String(userHiddenRequestConst)) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension UpToDatenessReactiveCompatible {
    //: func checkServerUrl() {
    func orderedSeries() {
        //: if PositionLabelReactiveCompatible.share.networkStatus != .Unavailable {
        if PositionLabelReactiveCompatible.share.networkStatus != .Unavailable {
            //: func_requestHost()
            red()
            //: } else {
        } else {
            //: func_starCodeTime()
            sharedJudge()
        }
    }

    //: func func_starCodeTime() {
    func sharedJudge() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.red()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func bottomList() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func red() {
        //: if PositionLabelReactiveCompatible.share.networkStatus == .Unavailable {
        if PositionLabelReactiveCompatible.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        bottomList()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        PhotoLabThen.makeAcross { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (showAfterAddNoti + "\u{6d4b},请求" + showRawNoti.capitalized))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (notiColorUser))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.sharedJudge()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (String(appCenterK) + String(dataSizeArrayMain) + dataColorMain.replacingOccurrences(of: "show", with: "er") + "r url"))
                    //: self.func__changeHostUrl()
                    self.wire()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func wire() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        bottomList()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        constSizeFormat.set(serverSelctIndex, forKey: notiDeviceTabPath)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(dataValueColorApp.suffix(5)) + String(notiLabMain))
        //: if Environment_Formal && !Encryption_Request {
        if mainScaleMessage, !mainDeviceFormat {
            //: serverPrefix = "http://"
            serverPrefix = (String(dataValueColorApp.suffix(5)) + String(notiLabMain))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension UpToDatenessReactiveCompatible {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func utilizedSize() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(String(mainPhotoRegularUser) + String(userDataMain))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func dowry() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(dataAddMain))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((showAFilterApp.replacingOccurrences(of: "color", with: "ap"))) || keyStr.contains((showApplyUser.lowercased())) || keyStr.contains((String(notiInputShow))) || keyStr.contains((userBackgroundApp.replacingOccurrences(of: "to", with: "p"))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension UpToDatenessReactiveCompatible {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func responseClick(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(showViewK) + String(kPriceConst) + String(notiDetailImageMain.prefix(5)) + "ies = ") + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = outputHide()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = PositionLabelReactiveCompatible.share.appConfigMode.urlH5Domain
        var urlDomain: String = PositionLabelReactiveCompatible.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(dataValueColorApp.suffix(5)) + String(notiLabMain)), with: (String(showControlConst) + mainAddNoti.capitalized))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((String(showControlConst) + mainAddNoti.capitalized)) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (dataFillMain.replacingOccurrences(of: "source", with: "h") + String(mainCenterShow)) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            dataSub(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        eachSearchSaveAdd()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func dataSub(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: notiAgentUser.map{topText(data: $0)}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func outputHide() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(dataValueColorApp.suffix(5)) + String(notiLabMain))
        //: if Environment_Formal && !Encryption_Request {
        if mainScaleMessage, !mainDeviceFormat {
            //: serverPrefix = "https://"
            serverPrefix = (String(showControlConst) + mainAddNoti.capitalized)
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(appMessageData.prefix(4))) + "\(appFormalStr)" + (String(kModelShow.prefix(4))))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func pseudonym(note _: Notification) {
        //: if PositionLabelReactiveCompatible.share.loginUid.isEmptyString {
        if PositionLabelReactiveCompatible.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            noneTableUp()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            html()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func noneTableUp() {
        //: var htmlDomain: String = PositionLabelReactiveCompatible.share.appConfigMode.urlH5Domain
        var htmlDomain: String = PositionLabelReactiveCompatible.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (kMessageUser.replacingOccurrences(of: "temp", with: "ID")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(kMessageUser.replacingOccurrences(of: "temp", with: "ID"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(notiLineConst) + userTitleShow.replacingOccurrences(of: "model", with: "D")) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(String(notiLineConst) + userTitleShow.replacingOccurrences(of: "model", with: "D"))] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (kMessageUser.replacingOccurrences(of: "temp", with: "ID")) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(kMessageUser.replacingOccurrences(of: "temp", with: "ID"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(notiLineConst) + userTitleShow.replacingOccurrences(of: "model", with: "D")) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(String(notiLineConst) + userTitleShow.replacingOccurrences(of: "model", with: "D"))] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((kMessageUser.replacingOccurrences(of: "temp", with: "ID"))), h5Cookie.keys.contains((kMessageUser.replacingOccurrences(of: "temp", with: "ID"))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(kMessageUser.replacingOccurrences(of: "temp", with: "ID"))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(kMessageUser.replacingOccurrences(of: "temp", with: "ID"))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((String(notiLineConst) + userTitleShow.replacingOccurrences(of: "model", with: "D"))), h5Cookie.keys.contains((String(notiLineConst) + userTitleShow.replacingOccurrences(of: "model", with: "D"))) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(String(notiLineConst) + userTitleShow.replacingOccurrences(of: "model", with: "D"))]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(String(notiLineConst) + userTitleShow.replacingOccurrences(of: "model", with: "D"))]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func html() {
        //: let htmlDomain: String = PositionLabelReactiveCompatible.share.appConfigMode.urlH5Domain
        let htmlDomain: String = PositionLabelReactiveCompatible.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == PositionLabelReactiveCompatible.share.loginUserMode.userID {
            if cookie.name == (kMessageUser.replacingOccurrences(of: "temp", with: "ID")), cookie.value == PositionLabelReactiveCompatible.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (String(notiLineConst) + userTitleShow.replacingOccurrences(of: "model", with: "D")) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (kMessageUser.replacingOccurrences(of: "temp", with: "ID")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(kMessageUser.replacingOccurrences(of: "temp", with: "ID"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(notiLineConst) + userTitleShow.replacingOccurrences(of: "model", with: "D")) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(String(notiLineConst) + userTitleShow.replacingOccurrences(of: "model", with: "D"))] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((kMessageUser.replacingOccurrences(of: "temp", with: "ID"))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(kMessageUser.replacingOccurrences(of: "temp", with: "ID"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((String(notiLineConst) + userTitleShow.replacingOccurrences(of: "model", with: "D"))) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(notiLineConst) + userTitleShow.replacingOccurrences(of: "model", with: "D"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func eachSearchSaveAdd() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(dataCurrentUser) + String(constRetNoti.prefix(6)) + String(kTitleNoti)) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            constSizeFormat.set(cookieData, forKey: showCountervalText)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (showToApp.replacingOccurrences(of: "live", with: "n") + "__save" + String(notiMomentApp.prefix(4)) + String(kLabUser.suffix(8)) + String(constMakeK)))
        }
    }

    //: func func__setAppCookies() {
    func constraintAppContentSet() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = constSizeFormat.object(forKey: showCountervalText)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func playCookies() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        constSizeFormat.removeObject(forKey: showCountervalText)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
