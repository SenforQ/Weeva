
//: Declare String Begin

/*: / :*/
fileprivate let kExtraStyleData:[Character] = ["/"]

/*:  ( :*/
fileprivate let dataFrameConst:String = " ("

/*: ; build: :*/
fileprivate let showInfoApp:[Character] = [";"," ","b","u","i","l"]
fileprivate let kViewModelConst:[Character] = ["d",":"]

/*: ; iOS  :*/
fileprivate let appMoreShow:[Character] = [";"," ","i","O","S"]
fileprivate let showToK:String = " "

/*: ;  :*/
fileprivate let constCellMain:String = "; "

/*: )" :*/
fileprivate let appGiftK:String = ")"

/*: "audio" :*/
fileprivate let notiDismissNameShow:[Character] = ["a","u","d","i","o"]

/*: + :*/
fileprivate let showKeyK:String = "share"

/*: .wav" :*/
fileprivate let dataBottomMain:String = "match model.wav"

/*: "audio/wav" :*/
fileprivate let mainTargetData:String = "ausignio"
fileprivate let kNumberApp:String = "data list to corner/wav"

/*: .jpg" :*/
fileprivate let mainManagerIconK:String = ".jpgbottom equal while section moment"

/*: "image/jpeg" :*/
fileprivate let notiPlayerAddK:String = "view key name element videoimage/"
fileprivate let notiEqualColorConst:[Character] = ["j","p","e","g"]

/*: "://app." :*/
fileprivate let showLastNoti:[Character] = [":","/","/","a","p","p","."]

/*: "data" :*/
fileprivate let mainMCompleteShow:String = "daview"

/*: ":null" :*/
fileprivate let notiTopK:String = "return lab bubble:null"

/*: ":{}" :*/
fileprivate let showReadConst:String = ":{video"

/*: "Request failed, Try again later" :*/
fileprivate let notiConversationItemToShow:[UInt8] = [0x8c,0xbb,0xaf,0xab,0xbb,0xad,0xaa,0xfe,0xb8,0xbf,0xb7,0xb2,0xbb,0xba,0xf2,0xfe,0x8a,0xac,0xa7,0xfe,0xbf,0xb9,0xbf,0xb7,0xb0,0xfe,0xb2,0xbf,0xaa,0xbb,0xac]

/*: "json error" :*/
fileprivate let kTitleRequestFillNoti:[Character] = ["j","s","o","n"," ","e","r","r"]
fileprivate let userToShow:String = "streamr"

/*: "platform=iphone&version= :*/
fileprivate let notiPathConst:String = "PLAT"
fileprivate let userTextApp:String = "phcurrente"
fileprivate let appNativeUser:[Character] = ["&"]
fileprivate let userReadyK:String = "ofrs"

/*: &packageId= :*/
fileprivate let constViewUser:String = "left self main var&pack"

/*: &bundleId= :*/
fileprivate let dataHiddenShow:String = "&bunadd else image as origin"

/*: &lang= :*/
fileprivate let appSelectedMain:[Character] = ["&","l","a","n","g","="]

/*: "?!@#$^&%*+,:;='" :*/
fileprivate let constTextApp:[Character] = ["?","!","@","#","$","^","&","%","*","+"]
fileprivate let showTimeConst:String = ",:;=\'"

/*: "`<>()[]{}/\\| " :*/
fileprivate let dataFleetApp:[Character] = ["`","<",">","(",")","[","]","{","}","/","\\","|"]
fileprivate let appPathUser:[Character] = [" "]

/*: "/route/slowindex" :*/
fileprivate let notiFrameTableTheApp:String = "in item tab/rout"
fileprivate let userIndexApp:String = "wifilterdex"

/*: "/route/index" :*/
fileprivate let userEqualK:String = "/routeelse false label guard path"

/*: "?path= :*/
fileprivate let kGameMain:String = "?path=self to view error"

/*: "&GJNonceStr= :*/
fileprivate let mainShareShow:String = "equal line case icon self&GJNo"
fileprivate let mainBackgroundData:String = "nceStr=reason return kit"

/*: "& :*/
fileprivate let appUserConst:[Character] = ["&"]

/*: = :*/
fileprivate let kBoldShow:String = "party"

/*: "&params= :*/
fileprivate let kErrorUser:String = "&pcount height"
fileprivate let userLabConst:[Character] = ["a","r","a","m","s","="]

/*: "/ :*/
fileprivate let notiToApp:String = "at"

/*: "? :*/
fileprivate let showDocumentData:[Character] = ["?"]

/*: "token" :*/
fileprivate let notiNumberFileK:String = "toreadn"

/*: "请求成功 :*/
fileprivate let constShowToolK:String = "\u{8bf7}求成功"

/*: "AAAAAAAA" :*/
fileprivate let dataActionNoti:[Character] = ["A","A","A"]
fileprivate let notiGiftUser:String = "offoffA"

/*: "AA" :*/
fileprivate let mainRequestData:String = "modelmodel"

/*: "BB" :*/
fileprivate let appTagK:String = "centercenter"

/*: "下载完成 -  :*/
fileprivate let constNameTimeTextUser:[Character] = ["下","载","完","成"," ","-"," "]

/*: "/Documents/" :*/
fileprivate let userEffectConst:String = "sound table to first/Docume"
fileprivate let mainExtraFileConst:String = "empty else return tonts/"

/*: ".mp4" :*/
fileprivate let mainLoadShow:String = "manager attribute.mp4"

/*: "移动地址: :*/
fileprivate let notiPathData:String = "移动\u{5730}\u{5740}:"

/*: "后台任务下载回来" :*/
fileprivate let notiWhiteStackFrameShow:String = "后台任务下载\u{56de}来"

/*: "下载成功 : :*/
fileprivate let notiResultMakeMain:[Character] = ["\u{4e0b}","载","成","\u{529f}"," ",":"]

/*: "Net Error" :*/
fileprivate let userImageData:[Character] = ["N","e","t"," ","E","r","r","o","r"]

/*: "下载失败 : :*/
fileprivate let userKindPopPushConst:String = "\u{4e0b}\u{8f7d}失败 :"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FatalDownloadDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let notiPackageMsg = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let dataFormatErrMessage = "\(mainAppStr)/\(kImageTitle) (\(notiTabPath)" + (String(showInfoApp) + String(kViewModelConst)) + "\(showQuantityervalTimeSenseFormat)" + (String(appMoreShow) + showToK.capitalized) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: GiftErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let showViewFinishKey = FatalDownloadDelegate()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class FatalDownloadDelegate: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func beyondExplainCompletion(model: CurrentThen, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.headAfter() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.upload(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.playCompletion(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func upload(model: CurrentThen, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.modelGift(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.fileModel(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    motilityConversation(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, GiftErrorResponse(errorCode: SubjectMatterSwitchsetTarget.NetError.rawValue, errorMsg: constEnvironmentKey))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        exceptAdd(showMsg: constEnvironmentKey)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (String(notiDismissNameShow)) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.withRemove())+\(notiBottomKeyValue)" + (String(dataBottomMain.suffix(4))), mimeType: (mainTargetData.replacingOccurrences(of: "sign", with: "d") + String(kNumberApp.suffix(4))))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(mainManagerIconK.prefix(4))), mimeType: (String(notiPlayerAddK.suffix(6)) + String(notiEqualColorConst)))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    motilityConversation(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, GiftErrorResponse(errorCode: SubjectMatterSwitchsetTarget.NetError.rawValue, errorMsg: constEnvironmentKey))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        exceptAdd(showMsg: constEnvironmentKey)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func playCompletion(model: CurrentThen, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.modelGift(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.fileModel(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: mainDeviceFormat ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = notiPackageMsg }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    motilityConversation(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, GiftErrorResponse(errorCode: SubjectMatterSwitchsetTarget.NetError.rawValue, errorMsg: constEnvironmentKey))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        exceptAdd(showMsg: constEnvironmentKey)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = notiPackageMsg }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    motilityConversation(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, GiftErrorResponse(errorCode: SubjectMatterSwitchsetTarget.NetError.rawValue, errorMsg: constEnvironmentKey))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        exceptAdd(showMsg: constEnvironmentKey)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func motilityConversation(model: CurrentThen, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            UpToDatenessReactiveCompatible.share.responseClick(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if mainDeviceFormat, model.requestServer.contains((String(showLastNoti))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.produce(key: UpToDatenessReactiveCompatible.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (mainMCompleteShow.replacingOccurrences(of: "view", with: "ta")) + "\"" + (String(notiTopK.suffix(5))), with: "" + "\"" + (mainMCompleteShow.replacingOccurrences(of: "view", with: "ta")) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !mainDeviceFormat {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(notiTopK.suffix(5))), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<ManagerGainBaseResponse>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == SubjectMatterSwitchsetTarget.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, GiftErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    exceptAdd(showMsg: responseModel.msg ?? String(bytes: notiConversationItemToShow.map{$0^222}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case SubjectMatterSwitchsetTarget.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: appDisplayKey, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, GiftErrorResponse(errorCode: SubjectMatterSwitchsetTarget.NetError.rawValue, errorMsg: (String(kTitleRequestFillNoti) + userToShow.replacingOccurrences(of: "stream", with: "o")).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func modelGift(model: CurrentThen) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (notiPathConst.lowercased() + "form=i" + userTextApp.replacingOccurrences(of: "current", with: "on") + String(appNativeUser) + userReadyK.replacingOccurrences(of: "of", with: "ve") + "ion=") + "\(mainNoName)" + (String(constViewUser.suffix(5)) + "ageId=") + "\(notiBottomKeyValue)" + (String(dataHiddenShow.prefix(4)) + "dleId=") + "\(notiTabPath)" + (String(appSelectedMain)) + "\(UpToDatenessReactiveCompatible.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if mainDeviceFormat, serverUrl.contains((String(showLastNoti))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if UpToDatenessReactiveCompatible.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(notiFrameTableTheApp.suffix(5)) + "e/slo" + userIndexApp.replacingOccurrences(of: "filter", with: "n")))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(userEqualK.prefix(6)) + "/index")) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(kGameMain.prefix(6))) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(mainShareShow.suffix(5)) + String(mainBackgroundData.prefix(7))) + "\(UpToDatenessReactiveCompatible.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.sow(key: UpToDatenessReactiveCompatible.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(kErrorUser.prefix(2)) + String(userLabConst)) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func fileModel(model: CurrentThen) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(dataFormatErrMessage)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (notiNumberFileK.replacingOccurrences(of: "read", with: "ke")), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func tillTable(model: CurrentThen) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.modelGift(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (constShowToolK) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (String(dataActionNoti) + notiGiftUser.replacingOccurrences(of: "off", with: "AA"))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (String(constNameTimeTextUser)) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(userEffectConst.suffix(7)) + String(mainExtraFileConst.suffix(4))) + NSDate.withRemove() + (String(mainLoadShow.suffix(4)))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (notiPathData) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (notiWhiteStackFrameShow))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = CancelDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func exaggerateCompletion(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: mainRequestFormat.name6decrypt(), with: appFormalStr)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(notiResultMakeMain)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, GiftErrorResponse(errorCode: SubjectMatterSwitchsetTarget.Normal.rawValue, errorMsg: (String(userImageData)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, GiftErrorResponse(errorCode: SubjectMatterSwitchsetTarget.NetError.rawValue, errorMsg: (String(userImageData)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (userKindPopPushConst) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func stopFile(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: mainRequestFormat.name6decrypt(), with: appFormalStr)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(notiResultMakeMain)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, GiftErrorResponse(errorCode: SubjectMatterSwitchsetTarget.Normal.rawValue, errorMsg: (String(userImageData)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, GiftErrorResponse(errorCode: SubjectMatterSwitchsetTarget.NetError.rawValue, errorMsg: (String(userImageData)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (userKindPopPushConst) + "\(errMsg)")
            }
        }
    }
}
