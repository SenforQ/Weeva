
//: Declare String Begin

/*: "nav_back_black_nor" :*/
fileprivate let userDataMain:[Character] = ["n","a","v","_","b","a","c","k","_","b","l","a"]
fileprivate let kTableUser:String = "ck_norheight image"

/*: "Email Address" :*/
fileprivate let mainSucceedButtonApp:[Character] = ["E","m","a","i","l"," ","A","d","d","r","e","s","s"]

/*: "F4F6FA" :*/
fileprivate let notiColorKeyData:String = "FuserF6FA"

/*: "Add an email address" :*/
fileprivate let appActionData:String = "Add annetwork data lock"
fileprivate let showCornerMain:String = "l addomain seat"

/*: "BBBBBB" :*/
fileprivate let constPositionData:String = "index"
fileprivate let constCanK:String = "leadingleadingB"

/*: "Only you will see your email on your profile." :*/
fileprivate let userCenterMain:[UInt8] = [0x2e,0x65,0x6c,0x69,0x66,0x6f,0x72,0x70,0x20,0x72,0x75,0x6f,0x79,0x20,0x6e,0x6f,0x20,0x6c,0x69,0x61,0x6d,0x65,0x20,0x72,0x75,0x6f,0x79,0x20,0x65,0x65,0x73,0x20,0x6c,0x6c,0x69,0x77,0x20,0x75,0x6f,0x79,0x20,0x79,0x6c,0x6e,0x4f]

/*: "Send verification email" :*/
fileprivate let constFrameUserData:String = "Send vvalue to video text manager"
fileprivate let showPlayerData:String = "atiname"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TimingReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/1.
//

//: import UIKit
import UIKit

//: class TalkingLoginBindEmailVC: TalkingBaseViewController {
class TimingReactiveCompatible: EmptyRecognizerDelegate {
	var showDoing: Bool = false
	var priceCount: Int = 69
	var beyondTitle: String = "cell"
	var modeSmallArray: [AnyHashable] = []
	var sectionLabDictionary: [AnyHashable: String] = [:]
	var withClose: Bool = false
	var keySum: Int = 72
	var makeName: String = "down"
	var tableArray: [AnyHashable] = []
	var noDictionary: [AnyHashable: String] = [:]

    // 是否展示返回按钮
    //: var isBack = true
    var isBack = true
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if isBack == false {
        if isBack == false {
            //: popGesture(isOpen: false)
            everyNameEqual(isOpen: false)
        }
    
            if (commitButton.layer.mask != nil) && (commitButton.constraints.count == 160) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let nameIn = WillView()
            nameIn.chessPlayerLineSwitch = isBack



            
            nameIn.stackOff = { [self] observeOn in
            self.showDoing = observeOn
            
            var nameIn = isBack
            nameIn = true
            if nameIn != self.showDoing {
                self.showDoing = nameIn
            }
            
            return self.showDoing
            }
            nameIn.pathCount = { [self] soyaTotal in
            self.priceCount = soyaTotal
            
            return self.priceCount
            }
            nameIn.spellManagerText = { [self] hideTaskText in
            self.beyondTitle = hideTaskText
            
            let netModelString = self.beyondTitle.uppercased() + "click"
            let netModelData = netModelString.data(using: String.Encoding.utf8)!
            self.beyondTitle = String(data: netModelData, encoding: .utf8) ?? self.beyondTitle.lowercased() + "dialog"
            return self.beyondTitle
            }
            nameIn.playerMessageArray = { [self] itButtonArray in
            self.modeSmallArray = itButtonArray
            
            guard var value = self.modeSmallArray as? [String] else {
                return nil
            }
            return value
            }
            nameIn.visualPropertyDictionary = { [self] indexDictionary in
            self.sectionLabDictionary = indexDictionary
            
            guard var value = self.sectionLabDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                commitButton.addSubview(nameIn)
            }

	}

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        //: if isBack == false {
        if isBack == false {
            //: popGesture(isOpen: true)
            everyNameEqual(isOpen: true)
        }
    
            if (self.modalPresentationCapturesStatusBarAppearance) && (self.viewIfLoaded != nil && self.viewIfLoaded!.frame.origin.y == 8.18) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let startMy = WillController()



            
                self.navigationController?.pushViewController(startMy.self, animated: true)
            }

	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: self.view.backgroundColor = .white
        self.view.backgroundColor = .white
        //: self.setupSubviews()
        self.exitTo()
        //: self.setupSubViewsConstraint()
        self.table()
        //: self.bindInteraction()
        self.sumeraction()
        //: addTapGestureRecognizer()
        greet()
    
		
		//: if_extract_code "lightCollection" begin
		
		var lightCollection = false
		if !lightCollection {
		    var isValue = false
		    if let nextResponder = emailTipLabel.next {
		        isValue = nextResponder.inputView != nil
		    }
		    lightCollection = isValue
		}
		
		//: if_extract_code "lightCollection" end
		
            if (emailTipLabel.tintColor != nil && emailTipLabel.tintColor.cgColor == UIColor(red: 0.29, green: 0.63, blue: 0.36, alpha: 0.92).cgColor) && (lightCollection) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let nameTitle = WillView(frame: emailTipLabel.bounds)
            nameTitle.chessPlayerLineSwitch = isBack



            
            nameTitle.stackOff = { [self] observeOn in
            self.withClose = observeOn
            
            var nameTitle = self.isBack
                nameTitle = true
                nameTitle = !nameTitle
            if nameTitle != self.withClose {
                self.withClose = nameTitle
            }
            
            return self.withClose
            }
            nameTitle.pathCount = { [self] soyaTotal in
            self.keySum = soyaTotal
            
            return self.keySum
            }
            nameTitle.spellManagerText = { [self] hideTaskText in
            self.makeName = hideTaskText
            
            var popPrice: String.Encoding = .utf8
            if let popPriceString = try? String(contentsOf: URL(string: "system.begin")!, usedEncoding: &popPrice) {
                self.makeName = popPriceString
            }
            return self.makeName
            }
            nameTitle.playerMessageArray = { [self] itButtonArray in
            self.tableArray = itButtonArray
            
            guard var value = self.tableArray as? [String] else {
                return nil
            }
            return value
            }
            nameTitle.visualPropertyDictionary = { [self] indexDictionary in
            self.noDictionary = indexDictionary
            
            guard var value = self.noDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                emailTipLabel.addSubview(nameTitle)
            }

	}

    //: deinit {
    deinit {}

    // MARK: - Lazy Load

    //: private lazy var backBtn: UIButton = {
    private lazy var backBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "nav_back_black_nor"), for: .normal)
        btn.setImage(UIImage.imageName(name: (String(userDataMain) + String(kTableUser.prefix(6)))), for: .normal)
        //: btn.addTarget(self, action: #selector(backBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(video), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var titleLabel: UILabel = {
    private lazy var titleLabel: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Semibold, fontSize: 17.0)
        label.font = .viewPath(type: .Semibold, fontSize: 17.0)
        //: label.textColor = .appTitleColor()
        label.textColor = .read()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.text = "Email Address".localized
        label.text = (String(mainSucceedButtonApp)).localized
        //: return label
        return label
        //: }()
    }()

    //: private lazy var emailInputView: UITextField = {
    private lazy var emailInputView: UITextField = {
        //: let textField = UITextField()
        let textField = UITextField()
        //: textField.backgroundColor = UIColor(hex: "F4F6FA")
        textField.backgroundColor = UIColor(hex: (notiColorKeyData.replacingOccurrences(of: "user", with: "4")))
        //: textField.font = .pingfangFont(type: .Medium, fontSize: 17)
        textField.font = .viewPath(type: .Medium, fontSize: 17)
        //: textField.textColor = .appTitleColor()
        textField.textColor = .read()
        //: textField.layer.cornerRadius = 25
        textField.layer.cornerRadius = 25
        //: textField.textAlignment = .center
        textField.textAlignment = .center
        //: textField.clipsToBounds = true
        textField.clipsToBounds = true
        //: textField.attributedPlaceholder = NSAttributedString(string: "Add an email address".localized, attributes: [NSAttributedString.Key.font: UIFont.pingfangFont(type: .Regular, fontSize: 15), NSAttributedString.Key.foregroundColor: UIColor(hex: "BBBBBB")!])
        textField.attributedPlaceholder = NSAttributedString(string: (String(appActionData.prefix(6)) + " emai" + String(showCornerMain.prefix(4)) + "dress").localized, attributes: [NSAttributedString.Key.font: UIFont.viewPath(type: .Regular, fontSize: 15), NSAttributedString.Key.foregroundColor: UIColor(hex: (constPositionData.replacingOccurrences(of: "index", with: "B") + constCanK.replacingOccurrences(of: "leading", with: "BB")))!])
        //: textField.keyboardType = .default
        textField.keyboardType = .default
        //: textField.returnKeyType = .done
        textField.returnKeyType = .done
        //: textField.clearButtonMode = .whileEditing
        textField.clearButtonMode = .whileEditing
        //: return textField
        return textField
        //: }()
    }()

    //: private lazy var emailTipLabel: UILabel = {
    private lazy var emailTipLabel: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 15.0)
        label.font = .viewPath(type: .Regular, fontSize: 15.0)
        //: label.textColor = .appValueDetailColor()
        label.textColor = .streamColor()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: label.text = "Only you will see your email on your profile.".localized
        label.text = String(bytes: userCenterMain.reversed(), encoding: .utf8)!.localized
        //: return label
        return label
        //: }()
    }()

    //: private lazy var commitButton: UIButton = {
    private lazy var commitButton: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 18)
        btn.titleLabel?.font = UIFont.viewPath(type: .Regular, fontSize: 18)
        //: btn.setTitle("Send verification email".localized, for: .normal)
        btn.setTitle((String(constFrameUserData.prefix(6)) + "erific" + showPlayerData.replacingOccurrences(of: "name", with: "on") + " email").localized, for: .normal)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: ScreenWidth-60, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.analogDigitalConverterSize(colors: UIColor.animaBy(), size: CGSize(width: userDeviceKey - 60, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
        if UpToDatenessReactiveCompatible.share.interfaceLang == DigitiserScalarLiteral.es.rawValue || UpToDatenessReactiveCompatible.share.interfaceLang == DigitiserScalarLiteral.pt.rawValue {
            //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 16)
            btn.titleLabel?.font = UIFont.viewPath(type: .Regular, fontSize: 16)
        }
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Public Event

//: extension TalkingLoginBindEmailVC {
extension TimingReactiveCompatible {
    //: @objc func backBtnClick() {
    @objc func video() {
        //: self.navigationController?.popViewController(animated: true)
        self.navigationController?.popViewController(animated: true)
    }

    //: func checkCommitButtonState() {
    func appellativeTo() {
        //: if emailInputView.text!.isEmptyString {
        if emailInputView.text!.isEmptyString {
            //: commitButton.isEnabled = false
            commitButton.isEnabled = false
            //: } else {
        } else {
            //: commitButton.isEnabled = true
            commitButton.isEnabled = true
        }
    }

    //: func func__getEmailCodeAction() {
    func doFail() {
        //: view.endEditing(true)
        view.endEditing(true)
        //: ProgressHUD.show(superView: view)
        WrapView.effectOf(superView: view)
        //: TalkingLoginRequestTool.req_getBindEmailCode(emailStr: emailInputView.text!, isBind: true) { succeed, result, errorModel in
        RowThen.paintTheLily(emailStr: emailInputView.text!, isBind: true) { succeed, _, _ in
            //: ProgressHUD.dismiss()
            WrapView.livePic()
            //: if succeed {
            if succeed {
                //: self.func__pushToPhoneCodeVC()
                self.viewer()
            }
        }
    }

    //: func func__pushToPhoneCodeVC() {
    func viewer() {
        //: let vc = TalkingLoginBindInputCodeVC()
        let vc = SystemVc()
        //: vc.phoneOrEmailStr = emailInputView.text!
        vc.phoneOrEmailStr = emailInputView.text!
        //: vc.viewType = .BindEmail
        vc.viewType = .BindEmail
        //: navigationController?.pushViewController(vc, animated: true)
        navigationController?.pushViewController(vc, animated: true)
    }
}

// MARK: - Layout

//: extension TalkingLoginBindEmailVC {
extension TimingReactiveCompatible {
    // 添加视图
    //: private func setupSubviews() {
    private func exitTo() {
        //: backBtn.isHidden = !isBack
        backBtn.isHidden = !isBack
        //: view.addSubview(backBtn)
        view.addSubview(backBtn)
        //: view.bringSubviewToFront(backBtn)
        view.bringSubviewToFront(backBtn)
        //: view.addSubview(titleLabel)
        view.addSubview(titleLabel)
        //: view.addSubview(emailInputView)
        view.addSubview(emailInputView)
        //: view.addSubview(commitButton)
        view.addSubview(commitButton)
        //: view.addSubview(emailTipLabel)
        view.addSubview(emailTipLabel)
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func table() {
        //: backBtn.snp.makeConstraints { make in
        backBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(6)
            make.leading.equalTo(6)
            //: make.top.equalTo(StatusBarHeight)
            make.top.equalTo(showDomainUrl)
            //: make.size.equalTo(44)
            make.size.equalTo(44)
        }
        //: titleLabel.snp.makeConstraints { make in
        titleLabel.snp.makeConstraints { make in
            //: make.top.equalTo(backBtn.snp.bottom).offset(44)
            make.top.equalTo(backBtn.snp.bottom).offset(44)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
        }
        //: emailInputView.snp.makeConstraints { make in
        emailInputView.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.top.equalTo(titleLabel.snp.bottom).offset(40)
            make.top.equalTo(titleLabel.snp.bottom).offset(40)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
        }
        //: commitButton.snp.makeConstraints { make in
        commitButton.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
            //: make.top.equalTo(emailInputView.snp.bottom).offset(40)
            make.top.equalTo(emailInputView.snp.bottom).offset(40)
        }
        //: emailTipLabel.snp.makeConstraints { make in
        emailTipLabel.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.top.equalTo(commitButton.snp.bottom).offset(24)
            make.top.equalTo(commitButton.snp.bottom).offset(24)
        }
    }

    // 添加事件
    //: private func bindInteraction() {
    private func sumeraction() {
        //: emailInputView.rx.text.orEmpty.asObservable()
        emailInputView.rx.text.orEmpty.asObservable()
            //: .subscribe(onNext: { [weak self] _ in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.checkCommitButtonState()
                self.appellativeTo()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: commitButton.rx.tap
        commitButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func__getEmailCodeAction()
                self.doFail()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}