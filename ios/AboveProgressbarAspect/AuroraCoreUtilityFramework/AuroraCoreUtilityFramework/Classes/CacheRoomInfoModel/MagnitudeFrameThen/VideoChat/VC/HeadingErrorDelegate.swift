
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kTitleConst:[UInt8] = [0x13,0x14,0x13,0xe,0x52,0x19,0x15,0x1e,0x1f,0x8,0x40,0x53,0x5a,0x12,0x1b,0x9,0x5a,0x14,0x15,0xe,0x5a,0x18,0x1f,0x1f,0x14,0x5a,0x13,0x17,0xa,0x16,0x1f,0x17,0x1f,0x14,0xe,0x1f,0x1e]

private func modelRange(receive num: UInt8) -> UInt8 {
    return num ^ 122
}

/*: "uid" :*/
fileprivate let dataFaceRawFilterShow:[UInt8] = [0xd2,0xce,0xc3]

/*: "headPic" :*/
fileprivate let userMaxMain:String = "bottom"
fileprivate let notiToConst:String = "eadPictype index version count"

/*: "nickname" :*/
fileprivate let appBelowConst:String = "NICKNAME"

/*: "birthday" :*/
fileprivate let showTitleApp:[Character] = ["b","i","r","t","h","d"]
fileprivate let userBottomNoti:String = "atool"

/*: "videoUrl" :*/
fileprivate let userEqualNoti:[Character] = ["v","i","d","e","o","U","r","l"]

/*: "cover" :*/
fileprivate let showSucceedMain:String = "COVER"

/*: "location" :*/
fileprivate let mainVideoK:[Character] = ["l"]
fileprivate let notiRawShow:String = "oplayeration"

/*: "cmd" :*/
fileprivate let showFemaleData:[UInt8] = [0xc7,0xd1,0xc8]

fileprivate func topPath(record num: UInt8) -> UInt8 {
    let value = Int(num) + 156
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "refuseCall" :*/
fileprivate let userSucceedShow:String = "REFU"

/*: "data" :*/
fileprivate let showConstraintMain:[UInt8] = [0x4c,0x49,0x5c,0x49]

private func firstMake(path num: UInt8) -> UInt8 {
    return num ^ 40
}

/*: "Call interruption" :*/
fileprivate let appShowK:String = "var hidden let rangeCall in"
fileprivate let mainSendConst:String = "modele"
fileprivate let kOmitScreenNoti:String = "rruptmessagen"

/*: "stopCall" :*/
fileprivate let showLabelShareData:[Character] = ["s","t","o","p","C","a","l","l"]

/*: "logId" :*/
fileprivate let notiAreaConst:[UInt8] = [0x2c,0x2f,0x27,0x9,0x24]

fileprivate func dataHidden(fail num: UInt8) -> UInt8 {
    let value = Int(num) - 192
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let notiPriceConst:String = "ctranslate"
fileprivate let constEqualShow:String = "regular inncelCall"

/*: "No answer" :*/
fileprivate let kTextData:[Character] = ["N","o"," ","a","n","s","w","e","r"]

/*: "duration" :*/
fileprivate let notiArrayModelConst:[Character] = ["d","u","r","a","t","i","o","n"]

/*: "She has hung up" :*/
fileprivate let dataBarShow:[Character] = ["S","h","e"," ","h","a","s"," ","h"]
fileprivate let showEqualMain:[Character] = ["u","n","g"," ","u","p"]

/*: "cancelSex" :*/
fileprivate let appContentData:[Character] = ["c","a","n","c","e","l","S","e","x"]

/*: "He has hung up" :*/
fileprivate let showPopConst:String = "He has request owner equal"
fileprivate let notiEqualData:String = "to make to data userhung up"

/*: "endType" :*/
fileprivate let dataStyleApp:String = "endTypemodel cookie model let"

/*: "stopSex" :*/
fileprivate let kImageColorShow:[Character] = ["s","t","o","p","S","e","x"]

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let showLibraryMain:[UInt8] = [0x8a,0xad,0xb0,0xb6,0xa5,0xa5,0xaa,0xa0,0xaa,0xa6,0xad,0xb7,0xe3,0xa4,0xac,0xaf,0xa7,0xe3,0xa0,0xac,0xaa,0xad,0xb0,0xef,0xe3,0xa0,0xa2,0xaf,0xaf,0xe3,0xab,0xa2,0xb0,0xe3,0xa6,0xad,0xa7,0xa6,0xa7]

private func reIndex(content num: UInt8) -> UInt8 {
    return num ^ 195
}

/*: "The other hung up the phone" :*/
fileprivate let showAtUser:[Character] = ["T","h","e"," ","o","t","h","e","r"]
fileprivate let constBagK:String = "drop data hung"
fileprivate let constKitToVarApp:String = "intimate key varthe p"

/*: "type" :*/
fileprivate let kGroupMain:String = "typrow"

/*: "jumpUrl" :*/
fileprivate let notiResultConst:[Character] = ["j","u","m","p","U","r","l"]

/*: "Cancel" :*/
fileprivate let appViewShow:String = "icon var to mediaCancel"

/*: "Get gold coins" :*/
fileprivate let mainDigitalK:String = "view make click extensionGet"
fileprivate let mainBarData:String = "less return show gol"

/*: "addBean" :*/
fileprivate let kTalkConst:String = "model type backaddBean"

/*: "refuseSex" :*/
fileprivate let kRequestApp:String = "needefus"

/*: "onAcceptCall" :*/
fileprivate let userOfApp:String = "make data return videoonAcc"
fileprivate let constBlockData:String = "click"

/*: "acceptCall" :*/
fileprivate let showTotalData:String = "ACCEPT"
fileprivate let showTextNoti:String = "layer centerCall"

/*: "Calls are cancelled" :*/
fileprivate let notiMomentUser:String = "Callskey layer let"
fileprivate let notiCollectionConst:String = "ncetotoed"

/*: "The call has ended" :*/
fileprivate let appWrapData:String = "The color view remove do"
fileprivate let dataServiceUser:String = "price extensionhas "
fileprivate let notiWithLeadingShow:String = "enofficiald"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  HeadingErrorDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let dataStatusName = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class HeadingErrorDelegate: EmptyRecognizerDelegate {
	var keyListEnable: Bool = true
	var secondCount: Int = 41
	var imageEnable: Bool = false
	var languageMagnitude: Int = 89

    //: var chatModel: TalkingVideoChatModel?
    var chatModel: EnableChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: EqualPlayer? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: MenuThen? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: BackgroundReactiveCompatible? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        CurrentSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        CurrentSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        CurrentSocketManager.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kTitleConst.map{modelRange(receive: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! TimeNavigationController
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.addBgm()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            equalCenter()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            makePath(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            CurrentSocketManager.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.evenData()
    
		if var videoValue = self.chatModel?.pairAge { 
			if var reinValue = self.chatModel?.points { 
				if var fillValue = videoManager.bgVideoView { 
			            if (fillValue.convert(CGPoint(x: 0, y: CGFloat(0)), to: fillValue.superview).y == 20.66) && (fillValue.undoManager != nil) {
			                //: SWIFT_CUSTOM_DANGER_File_Call
			                let buttonLoadLet = TelephoneReceiverView(frame: fillValue.bounds.integral)
			            buttonLoadLet.sexMagnitude = interactionView.giftView.contentHeight
			            buttonLoadLet.dataModelText = reinValue
			            
			            buttonLoadLet.viewOff = { [self] titleDoing in
			            self.keyListEnable = titleDoing
			            
			            var buttonLoadLet = animated
			            buttonLoadLet = true
			            if buttonLoadLet != self.keyListEnable {
			                self.keyListEnable = buttonLoadLet
			            }
			            
			            return self.keyListEnable
			            }
			            buttonLoadLet.talkLabInterval = { [self] bringHomeCount in
			            self.secondCount = bringHomeCount
			            
			            var buttonLoadLet = videoValue
			            buttonLoadLet &= 3
			            if buttonLoadLet > self.secondCount {
			                self.secondCount = buttonLoadLet
			            }
			            
			            return self.secondCount
			            }
			                fillValue.addSubview(buttonLoadLet)
			            }
			
				}
			}
		}
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        outspoken()
        //: setupSubViewsConstraint()
        detailMake()
        //: initNotificationObserver()
        featureObserver()
        //: req_callGetUserInfo()
        finishBackground()
        //: timeoutRules()
        timeout()
        //: setUpBGMPlayer()
        listBilliardPlayer()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.beginWith()
        }
    
		if var clickLabelValue = self.chatModel?.iUid { 
		if let reportView = reportView {
	
	            if (reportView.convert(CGPoint(x: 0, y: CGFloat(0)), to: reportView.superview).y == 20.66) && (reportView.undoManager != nil) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let buttonLoadLet = TelephoneReceiverView(frame: reportView.bounds.integral)
	            buttonLoadLet.sexMagnitude = interactionView.giftView.contentHeight
	            buttonLoadLet.dataModelText = clickLabelValue
	            
	            buttonLoadLet.viewOff = { [self] titleDoing in
	            self.imageEnable = titleDoing
	            
	            var buttonLoadLet = isFromMiniView
	            buttonLoadLet = true
	            if buttonLoadLet != self.imageEnable {
	                self.imageEnable = buttonLoadLet
	            }
	            
	            return self.imageEnable
	            }
	            buttonLoadLet.talkLabInterval = { [self] bringHomeCount in
	            self.languageMagnitude = bringHomeCount
	            
	            var buttonLoadLet = interactionView.giftView.keyborHeight
	            buttonLoadLet &= 3
	            if buttonLoadLet > self.languageMagnitude {
	                self.languageMagnitude = buttonLoadLet
	            }
	            
	            return self.languageMagnitude
	            }
	                reportView.addSubview(buttonLoadLet)
	            }
	
		}
	
		}
	}

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if PositionLabelReactiveCompatible.share.appConfigMode.disableShootScreen {
            if PositionLabelReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = FrameThen(frame: CGRect(x: 0, y: 0, width: userDeviceKey, height: appNameMessage))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isShared = false
        CurrentSocketManager.shared.isShared = false
        //: self.stopVCAllActivties()
        self.sumActivties()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: HumanActivityReactiveCompatible = {
        //: let v = TalkingBGVideoView()
        let v = HumanActivityReactiveCompatible()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.toLevel(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: BackingView = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = BackingView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: NonnegativeThen = {
        //: let v = TalkingVideoWindowView()
        let v = NonnegativeThen()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = userDeviceKey - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if ManagerReactiveCompatible.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: showDomainUrl + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.accelerationEqual()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: ObjectThen = {
        //: let m = TalkingVideoChatManager()
        let m = ObjectThen()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension HeadingErrorDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func finishBackground() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        ObjectThen.reside(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: dataFaceRawFilterShow.map{$0^167}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(userMaxMain.replacingOccurrences(of: "bottom", with: "h") + String(notiToConst.prefix(6)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(appBelowConst.lowercased())].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(String(showTitleApp) + userBottomNoti.replacingOccurrences(of: "tool", with: "y"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(userEqualNoti))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(showSucceedMain.lowercased())].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(mainVideoK) + notiRawShow.replacingOccurrences(of: "player", with: "c"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.smallness()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.videoUponSocialStation()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func videoUponSocialStation() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = SpecsCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = SpecsCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.progressCamera(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.labelContent(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension HeadingErrorDelegate {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func equalCenter() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        TimingThen.atomicNumber85 { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.exceptAdd(showMsg: mainNetFormat)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                CurrentSocketManager.shared.towardAdd(info: [String(bytes: showFemaleData.map{topPath(record: $0)}, encoding: .utf8)!: (userSucceedShow.lowercased() + "seCall"), String(bytes: showConstraintMain.map{firstMake(path: $0)}, encoding: .utf8)!: [String(bytes: dataFaceRawFilterShow.map{$0^167}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.onSub()
                //: return
                return
            }

            //: self.startPreview()
            self.beginWith()
            //: self.stopBGM()
            self.areaEqual()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = EqualPlayer.tableBar(type: .Response)
            //: self.startBGM()
            self.addBgm()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func beginWith() {
        //: self.videoManager.startPreview()
        self.videoManager.reMagnitude()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func sumActivties() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.areaEqual()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        TrifleThen.wallpaper()
        //: TalkingSocketManager.shared.callerDelegate = nil
        CurrentSocketManager.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        CurrentSocketManager.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        CurrentSocketManager.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func onSub(animated: Bool = true) {
        //: self.stopBGM()
        self.areaEqual()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func accelerationEqual() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.indexFor()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.videoUponSocialStation()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func imageMagnitudeerest() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.makeNeed()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.videoUponSocialStation()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.fortifyFit()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func fortifyFit() {
        //: guard PositionLabelReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard PositionLabelReactiveCompatible.share.loginUserMode.sex == PetitionKeyRepresentable.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = BackgroundReactiveCompatible()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.lotFile()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension HeadingErrorDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func featureObserver() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(tiptoeClickAllow(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(beyondValue(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: notiViewKey,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(storage(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: mainTabKey,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        dataStatusName.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func tiptoeClickAllow(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            onSub()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func storage(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard CurrentSocketManager.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        CurrentSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        CurrentSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        CurrentSocketManager.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.someEvent()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func beyondValue(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.videoUponSocialStation()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension HeadingErrorDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.exceptAdd(showMsg: (String(appShowK.suffix(7)) + mainSendConst.replacingOccurrences(of: "model", with: "t") + kOmitScreenNoti.replacingOccurrences(of: "message", with: "io")))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                CurrentSocketManager.shared.towardAdd(info: [String(bytes: showFemaleData.map{topPath(record: $0)}, encoding: .utf8)!: (String(showLabelShareData)), String(bytes: showConstraintMain.map{firstMake(path: $0)}, encoding: .utf8)!: [String(bytes: notiAreaConst.map{dataHidden(fail: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                CurrentSocketManager.shared.towardAdd(info: [String(bytes: showFemaleData.map{topPath(record: $0)}, encoding: .utf8)!: (notiPriceConst.replacingOccurrences(of: "translate", with: "a") + String(constEqualShow.suffix(8))), String(bytes: showConstraintMain.map{firstMake(path: $0)}, encoding: .utf8)!: [String(bytes: notiAreaConst.map{dataHidden(fail: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                CurrentSocketManager.shared.towardAdd(info: [String(bytes: showFemaleData.map{topPath(record: $0)}, encoding: .utf8)!: (userSucceedShow.lowercased() + "seCall"), String(bytes: showConstraintMain.map{firstMake(path: $0)}, encoding: .utf8)!: [String(bytes: dataFaceRawFilterShow.map{$0^167}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.bucculaEnd()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                CurrentSocketManager.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            onSub()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension HeadingErrorDelegate {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func listBilliardPlayer() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? ButtonScalar.Call : ButtonScalar.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = EqualPlayer.tableBar(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func addBgm() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.pushUp()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func areaEqual() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.groupStop()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension HeadingErrorDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func timeout() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? mainCleanData : self.chatModel?.invite()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            titleDown()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.titleDown()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func titleDown() {
        //: guard !TalkingSocketManager.shared.isShared else { return }
        guard !CurrentSocketManager.shared.isShared else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.exceptAdd(showMsg: (String(kTextData)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: showFemaleData.map{topPath(record: $0)}, encoding: .utf8)!: (notiPriceConst.replacingOccurrences(of: "translate", with: "a") + String(constEqualShow.suffix(8))), String(bytes: showConstraintMain.map{firstMake(path: $0)}, encoding: .utf8)!: [String(bytes: notiAreaConst.map{dataHidden(fail: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: showFemaleData.map{topPath(record: $0)}, encoding: .utf8)!: (userSucceedShow.lowercased() + "seCall"), String(bytes: showConstraintMain.map{firstMake(path: $0)}, encoding: .utf8)!: [String(bytes: dataFaceRawFilterShow.map{$0^167}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        CurrentSocketManager.shared.towardAdd(info: socketDict)
        //: popCurrentViewController()
        onSub()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension HeadingErrorDelegate {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func examine(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: notiAreaConst.map{dataHidden(fail: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(notiArrayModelConst))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.marginOfErrorMake(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.boxLoad()
            //: createTalkRoom()
            imageMagnitudeerest()
            //: self.stopBGM()
            self.areaEqual()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func makePath(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.indexFor()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.toLevel(show: false)
        //: self.stopBGM()
        self.areaEqual()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.boxLoad()
        //: self.createTalkRoom()
        self.imageMagnitudeerest()
    }
}

// MARK: - MinCurrentObjectProtocol【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension HeadingErrorDelegate: MinCurrentObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func telecastingEvent(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(dataBarShow) + String(showEqualMain)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(appContentData))].stringValue == PetitionKeyRepresentable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(showPopConst.prefix(7)) + String(notiEqualData.suffix(7))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.exceptAdd(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.bucculaEnd()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            CurrentSocketManager.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        onSub()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func observerGift(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        makePath(logId: json[String(bytes: notiAreaConst.map{dataHidden(fail: $0)}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func upServer(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(dataStyleApp.prefix(7)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(dataBarShow) + String(showEqualMain)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(kImageColorShow))].stringValue == PetitionKeyRepresentable.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(showPopConst.prefix(7)) + String(notiEqualData.suffix(7))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if PositionLabelReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if PositionLabelReactiveCompatible.share.appStatus == ValueHashableRepresentation.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: showLibraryMain.map{reIndex(content: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(showAtUser) + String(constBagK.suffix(5)) + " up " + String(constKitToVarApp.suffix(5)) + "hone").localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.exceptAdd(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.bucculaEnd()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            CurrentSocketManager.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.hemAndHawDismiss()
        //: popCurrentViewController()
        onSub()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func solidus(msg: String, data: [String: Any]) {
        //: guard PositionLabelReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard PositionLabelReactiveCompatible.share.appStatus == ValueHashableRepresentation.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(kGroupMain.replacingOccurrences(of: "row", with: "e"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(notiResultConst))] as? String else { return }
            //: let httpsUrl = PositionLabelReactiveCompatible.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = PositionLabelReactiveCompatible.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = CountShareConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: ViewPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            ViewPushManager.share.pastUp(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            StirReactiveCompatible.leftConfig(title: nil,
                                        //: message: msg,
                                        message: msg,
                                        //: leftBtnTitle: "Cancel".localized,
                                        leftBtnTitle: (String(appViewShow.suffix(6))).localized,
                                        //: rightBtnTitle: "Get gold coins".localized) {
                                        rightBtnTitle: (String(mainDigitalK.suffix(3)) + String(mainBarData.suffix(4)) + "d coins").localized)
            {
                //: TalkingAlertShow.hideAlert()
                StirReactiveCompatible.hidePush()
                //: } rightBlock: {
            } rightBlock: {
                //: ViewPushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                ViewPushManager.share.blockRequestEnd(clickEvent: kLineValue, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func giftHorse(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.loadArr(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func listener(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(kTalkConst.suffix(7)))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.modelPoint(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.formatConversation(point: addBean)
        }
    }
}

// MARK: - TardinessObjectProtocol【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension HeadingErrorDelegate: TardinessObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func withPlay(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(dataBarShow) + String(showEqualMain)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(kRequestApp.replacingOccurrences(of: "need", with: "r") + "eSex")].stringValue == PetitionKeyRepresentable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(showPopConst.prefix(7)) + String(notiEqualData.suffix(7))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.exceptAdd(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.bucculaEnd()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            CurrentSocketManager.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        onSub()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func beatBackData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        makePath(logId: json[String(bytes: notiAreaConst.map{dataHidden(fail: $0)}, encoding: .utf8)!].intValue)
    }
}

// MARK: - LabelErrorDelegate【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension HeadingErrorDelegate: LabelErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func ping(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (userSucceedShow.lowercased() + "seCall") { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.exceptAdd(showMsg: (String(kTextData)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.bucculaEnd()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                CurrentSocketManager.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            onSub()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(userOfApp.suffix(5)) + "eptCal" + constBlockData.replacingOccurrences(of: "click", with: "l")) && errorNo == 101 { // 接听通话时，余额不足
            //: guard PositionLabelReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard PositionLabelReactiveCompatible.share.appStatus == ValueHashableRepresentation.normal.rawValue else { return }
            //: ViewPushManager.share.func__jumpToWebRecharge(sufficient: false)
            ViewPushManager.share.blockRequestEnd(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(userOfApp.suffix(5)) + "eptCal" + constBlockData.replacingOccurrences(of: "click", with: "l")) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.exceptAdd(showMsg: msg)
            //: popCurrentViewController()
            onSub()
        }
    }
}

// MARK: - TextViewDelegate【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension HeadingErrorDelegate: TextViewDelegate {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func popDate() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        CurrentSocketManager.shared.towardAdd(info: [String(bytes: showFemaleData.map{topPath(record: $0)}, encoding: .utf8)!: (showTotalData.lowercased() + String(showTextNoti.suffix(4))), String(bytes: showConstraintMain.map{firstMake(path: $0)}, encoding: .utf8)!: [String(bytes: dataFaceRawFilterShow.map{$0^167}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func secondMatch() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.exceptAdd(showMsg: (String(notiMomentUser.prefix(5)) + " are ca" + notiCollectionConst.replacingOccurrences(of: "to", with: "l")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            CurrentSocketManager.shared.towardAdd(info: [String(bytes: showFemaleData.map{topPath(record: $0)}, encoding: .utf8)!: (notiPriceConst.replacingOccurrences(of: "translate", with: "a") + String(constEqualShow.suffix(8))), String(bytes: showConstraintMain.map{firstMake(path: $0)}, encoding: .utf8)!: [String(bytes: notiAreaConst.map{dataHidden(fail: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.exceptAdd(showMsg: (String(notiMomentUser.prefix(5)) + " are ca" + notiCollectionConst.replacingOccurrences(of: "to", with: "l")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            CurrentSocketManager.shared.towardAdd(info: [String(bytes: showFemaleData.map{topPath(record: $0)}, encoding: .utf8)!: (userSucceedShow.lowercased() + "seCall"), String(bytes: showConstraintMain.map{firstMake(path: $0)}, encoding: .utf8)!: [String(bytes: dataFaceRawFilterShow.map{$0^167}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.exceptAdd(showMsg: (String(appWrapData.prefix(4)) + "call " + String(dataServiceUser.suffix(4)) + notiWithLeadingShow.replacingOccurrences(of: "official", with: "de")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            CurrentSocketManager.shared.towardAdd(info: [String(bytes: showFemaleData.map{topPath(record: $0)}, encoding: .utf8)!: (String(showLabelShareData)), String(bytes: showConstraintMain.map{firstMake(path: $0)}, encoding: .utf8)!: [String(bytes: notiAreaConst.map{dataHidden(fail: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        onSub()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func viewFinder(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.colorPlay(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func weltanschauungFrom(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.toLevel(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.photomosaic(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.videoUponSocialStation()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func behindMatch() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        CurrentSocketManager.shared.currTalkingVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = MenuThen.orientationLive()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.filterWith()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.submit()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.underTitle()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        onSub()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func intervaleraction() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension HeadingErrorDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func outspoken() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.informationSystem(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func detailMake() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
