
//: Declare String Begin

/*: "msgType" :*/
fileprivate let userModelShow:[Character] = ["m","s","g","T","y","p","e"]

/*: "tips" :*/
fileprivate let notiPopUser:String = "tippath"

/*: "extra" :*/
fileprivate let constPastApp:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let dataItemNoti:String = "let height show fatalmsgInfo"

/*: "user" :*/
fileprivate let appSectionModelNoti:String = "useat"

/*: "uid" :*/
fileprivate let kLastConst:String = "ulab"

/*: "nickname" :*/
fileprivate let mainFemaleShow:[UInt8] = [0x65,0x6d,0x61,0x6e,0x6b,0x63,0x69,0x6e]

/*: "headPic" :*/
fileprivate let constNameK:[UInt8] = [0x63,0x69,0x50,0x64,0x61,0x65,0x68]

/*: "age" :*/
fileprivate let notiSmartDateShow:[UInt8] = [0xb3,0xb5,0xb7]

private func userPosition(read num: UInt8) -> UInt8 {
    return num ^ 210
}

/*: "gift" :*/
fileprivate let appLeaveUser:String = "gtitlet"

/*: "isGroup" :*/
fileprivate let appTopUser:String = "isGroufill red return in"
fileprivate let userModelEqualNoti:[Character] = ["p"]

/*: "WantGift" :*/
fileprivate let notiTitleNeedLiveData:[Character] = ["W","a","n","t","G","i","f","t"]

/*: "txt" :*/
fileprivate let constMeK:String = "tleading"

/*: "img" :*/
fileprivate let constAddShow:[Character] = ["i","m","g"]

/*: "video" :*/
fileprivate let notiSelectUnderScaleApp:String = "vitempo"

/*: "audio" :*/
fileprivate let dataMenuConst:String = "audlab"

/*: "messageType" :*/
fileprivate let dataPlayerK:[Character] = ["m","e","s","s","a"]
fileprivate let appPicK:String = "direction value modelgeType"

/*: "contentType" :*/
fileprivate let showAreaToUser:String = "clabntent"
fileprivate let dataListUser:[Character] = ["T","y","p","e"]

/*: "ImgMsg" :*/
fileprivate let dataLimitConst:String = "you string makeImgMsg"

/*: "imageUri" :*/
fileprivate let userSizeMain:[Character] = ["i","m","a","g","e","U","r","i"]

/*: "type" :*/
fileprivate let appSkinK:String = "tmediumpe"

/*: "pic" :*/
fileprivate let appBeautyShow:String = "statec"

/*: "isBurn" :*/
fileprivate let dataRequestMain:String = "I"
fileprivate let appToNumberryShow:[Character] = ["s","B","u","r","n"]

/*: "giftId" :*/
fileprivate let dataImageUser:String = "giftIdstyle error"

/*: "params" :*/
fileprivate let notiSharedConst:String = "pastreamms"

/*: "audioLength" :*/
fileprivate let showToConst:[Character] = ["a","u","d","i","o"]
fileprivate let constDeviceUser:String = "import color commit self countLength"

/*: "len" :*/
fileprivate let appAddConst:String = "intervalen"

/*: "audioSandbox" :*/
fileprivate let appLineConst:String = "audioSlet add var"

/*: "senderId" :*/
fileprivate let appPathConst:String = "tool click makesenderId"

/*: "AudioMsg" :*/
fileprivate let notiTitleKeyFrameK:String = "AudioMview intimate first gift"
fileprivate let showDefineUser:String = "sframe"

/*: "content" :*/
fileprivate let constAtMain:[Character] = ["c","o"]
fileprivate let appFatalNoti:[Character] = ["n","t","e","n","t"]

/*: "TxtMsg" :*/
fileprivate let appViewMain:String = "TxtMsself make self"
fileprivate let showToNoti:String = "follow"

/*: "message" :*/
fileprivate let showContainerTextMain:[Character] = ["m","e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let constIndexEqualShow:[Character] = ["q","u","o"]
fileprivate let notiImageSourceData:String = "language model darkteMsgId"

/*: "quoteMsgInfo" :*/
fileprivate let appAddK:String = "size extension let typequoteM"
fileprivate let kBackgroundApp:String = "raw icon stringsgInfo"

/*: "rYMsgType" :*/
fileprivate let mainAppK:String = "super model stringrYMsgTy"
fileprivate let showBorderData:[Character] = ["p","e"]

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let notiPostData:[Character] = ["G","J",":","S","e","s","s",":","A","u","t","o","G","r","e"]
fileprivate let mainNameNoti:String = "frame succeedet:Tx"
fileprivate let mainLeadingApp:[Character] = ["t","C","u","s","t","o","m"]

/*: "changeDirection" :*/
fileprivate let kReplyConst:String = "inputan"
fileprivate let mainStatusApp:String = "rectsizen"

/*: "system" :*/
fileprivate let kMakeData:String = "systebottom"

/*: "title" :*/
fileprivate let notiLabelApp:String = "nameinamele"

/*: "id" :*/
fileprivate let appUserFinishLoadNoti:String = "icollection"

/*: "icon" :*/
fileprivate let constColorNoti:String = "iequaln"

/*: "all" :*/
fileprivate let userOpenShow:[Character] = ["a","l","l"]

/*: "icon_chats_pcr" :*/
fileprivate let mainAddValueUser:String = "catch let selected make succeedicon_"
fileprivate let kBlockConst:String = "phalfr"

/*: "status" :*/
fileprivate let dataStatusShow:String = "statusers"

/*: "msgIncome" :*/
fileprivate let showTipApp:String = "video aspect intervalmsgIncome"

/*: "msgId" :*/
fileprivate let kItemData:String = "msgIdtable app color let data"

/*: "SequenceTotalLiteral" :*/
fileprivate let dataIndexK:String = "TMsgTany as self"
fileprivate let constScaleApp:String = "teStatas class list color"
fileprivate let mainTextK:String = "close"

/*: "TMsgTranslatedContent" :*/
fileprivate let appDataInfoNoti:[Character] = ["T","M","s","g","T","r","a","n","s","l"]
fileprivate let userMiniNoti:[Character] = ["a","t","e","d","C","o","n","t","e","n","t"]

/*: "formatType" :*/
fileprivate let dataToNoti:String = "formatTimage image"
fileprivate let appTrackModelK:[Character] = ["y","p","e"]

/*: "plain" :*/
fileprivate let kCellNoti:String = "playerain"

/*: "imageData" :*/
fileprivate let showModeApp:String = "imageDaction text hidden gift"
fileprivate let appLiveShow:[Character] = ["a","t","a"]

/*: "VideoMsg" :*/
fileprivate let kStyleContainerConst:String = "color icon valueVideoMsg"

/*: "match" :*/
fileprivate let mainPriceConst:[Character] = ["m","a","t","c","h"]

/*: "mf" :*/
fileprivate let appFrameBlockUser:String = "mno"

/*: "userIntro" :*/
fileprivate let kKeyGroupTimeMain:[Character] = ["u","s","e","r","I"]
fileprivate let mainDateApp:String = "ntrcolor"

/*: "targetId" :*/
fileprivate let notiInputConst:String = "total heighttargetI"
fileprivate let userGreenNoti:[Character] = ["d"]

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let userWhiteReloadShow:String = "app recording successyyyy-"
fileprivate let showAddUser:[Character] = ["M","M","-","d","d"," ","H","H",":","m","m",":","s","s"]

/*: "sendTime" :*/
fileprivate let dataPlayerApp:String = "for channelsendT"
fileprivate let mainFilterK:String = "ikeye"

/*: "isRead" :*/
fileprivate let userWrapNoti:[Character] = ["i","s","R","e","a","d"]

/*: "audioData" :*/
fileprivate let notiEqualK:[Character] = ["a","u","d","i","o","D","a","t","a"]

/*: "audioUri" :*/
fileprivate let constVideoK:[Character] = ["a","u","d","i","o","U","r","i"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SocietalChatManager.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum AddressBinaryQuantity: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - SocietalChatManager

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class SocietalChatManager: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func infoTime() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> MessageThen? {
    public class func mail(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> MessageThen? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = rejoin(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: TabThen.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            TabThen.shared.appearMake(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> MessageThen? {
    public class func rejoin(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> MessageThen? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(userModelShow))] = (notiPopUser.replacingOccurrences(of: "path", with: "s"))
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(String(constPastApp)): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = observerTing(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = PhotoGainCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = TodayModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? ChatModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(dataItemNoti.suffix(7)))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(appSectionModelNoti.replacingOccurrences(of: "at", with: "r"))] = [
            //: "uid": PositionLabelReactiveCompatible.share.loginUserMode.userID,
            (kLastConst.replacingOccurrences(of: "lab", with: "id")): PositionLabelReactiveCompatible.share.loginUserMode.userID,
            //: "nickname": PositionLabelReactiveCompatible.share.loginUserMode.nickname,
            String(bytes: mainFemaleShow.reversed(), encoding: .utf8)!: PositionLabelReactiveCompatible.share.loginUserMode.nickname,
            //: "headPic": PositionLabelReactiveCompatible.share.loginUserMode.headPic,
            String(bytes: constNameK.reversed(), encoding: .utf8)!: PositionLabelReactiveCompatible.share.loginUserMode.headPic,
            //: "age": PositionLabelReactiveCompatible.share.loginUserMode.age,
            String(bytes: notiSmartDateShow.map{userPosition(read: $0)}, encoding: .utf8)!: PositionLabelReactiveCompatible.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(String(constPastApp)): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = observerTing(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(userModelShow))] as? String == (appLeaveUser.replacingOccurrences(of: "title", with: "if")) {
            //: var data: MessageThen
            var data: MessageThen
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(appTopUser.prefix(6)) + String(userModelEqualNoti))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = DropCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = ViewCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ViewCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? TodayModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(userModelShow))] as? String == (String(notiTitleNeedLiveData)) {
            //: let data = SourceThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = SourceThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? TodayModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(userModelShow))] as? String == (constMeK.replacingOccurrences(of: "leading", with: "xt")) {
            //: var data: MessageThen
            var data: MessageThen
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(appTopUser.prefix(6)) + String(userModelEqualNoti))] as? String == "1" { // 群聊
                //: data = WhiteCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = WhiteCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = NumberReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = NumberReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? TodayModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(userModelShow))] as? String == (String(constAddShow)) {
            //: let data = LayerSharedReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = LayerSharedReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? TodayModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(userModelShow))] as? String == (notiSelectUnderScaleApp.replacingOccurrences(of: "temp", with: "de")) {
            //: let data = CradleThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = CradleThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? TodayModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(userModelShow))] as? String == (dataMenuConst.replacingOccurrences(of: "lab", with: "io")) {
            //: let data = ChatRunningReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ChatRunningReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? TodayModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(appLeaveUser.replacingOccurrences(of: "title", with: "if"))] != nil && (msgInfo[(String(dataPlayerK) + String(appPicK.suffix(6)))] as? Int) == 4 {
            //: let data = NumberReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = NumberReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? TodayModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: MessageThen) -> MessageThen? {
    public class func sizeSearched(withData cellData: MessageThen) -> MessageThen? {
        // 图片
        //: if let reCellData = cellData as? LayerSharedReactiveCompatible {
        if let reCellData = cellData as? LayerSharedReactiveCompatible {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(RowMacroDefine.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(RowMacroDefine.exist())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(showAreaToUser.replacingOccurrences(of: "lab", with: "o") + String(dataListUser))] = (String(dataLimitConst.suffix(6)))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(userModelShow))] = (String(constAddShow))
            //: msgInfo["imageUri"] = filePath
            msgInfo[(String(userSizeMain))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(dataItemNoti.suffix(7)))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(appSkinK.replacingOccurrences(of: "medium", with: "y"))] = (String(dataLimitConst.suffix(6)))
            //: params["pic"] = filePath
            params[(appBeautyShow.replacingOccurrences(of: "state", with: "pi"))] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(dataRequestMain.lowercased() + String(appToNumberryShow))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(dataRequestMain.lowercased() + String(appToNumberryShow))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(String(dataImageUser.prefix(6)))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(String(dataImageUser.prefix(6)))] = "0"
            }

            //: dic["params"] = params
            dic[(notiSharedConst.replacingOccurrences(of: "stream", with: "ra"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = rejoin(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? CradleThen {
        if let reCellData = cellData as? CradleThen {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? ChatRunningReactiveCompatible {
        if let reCellData = cellData as? ChatRunningReactiveCompatible {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = CurrentViewThen.getMessage(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(String(showToConst) + String(constDeviceUser.suffix(6)))] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(appAddConst.replacingOccurrences(of: "interval", with: "l"))] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(String(appLineConst.prefix(6)) + "andbox")] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(String(appLineConst.prefix(6)) + "andbox")] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(userModelShow))] = (dataMenuConst.replacingOccurrences(of: "lab", with: "io"))
            //: msgInfo["senderId"] = PositionLabelReactiveCompatible.share.loginUserMode.userID
            msgInfo[(String(appPathConst.suffix(8)))] = PositionLabelReactiveCompatible.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(dataItemNoti.suffix(7)))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(appSkinK.replacingOccurrences(of: "medium", with: "y"))] = (String(notiTitleKeyFrameK.prefix(6)) + showDefineUser.replacingOccurrences(of: "frame", with: "g"))
            //: dic["params"] = params
            dic[(notiSharedConst.replacingOccurrences(of: "stream", with: "ra"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = rejoin(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? NumberReactiveCompatible {
        if let reCellData = cellData as? NumberReactiveCompatible {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(String(constAtMain) + String(appFatalNoti))] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(userModelShow))] = (constMeK.replacingOccurrences(of: "leading", with: "xt"))
            //: dic["msgInfo"] = msgInfo
            dic[(String(dataItemNoti.suffix(7)))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(appSkinK.replacingOccurrences(of: "medium", with: "y"))] = (String(appViewMain.prefix(5)) + showToNoti.replacingOccurrences(of: "follow", with: "g"))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(String(showContainerTextMain))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.exceptStatus() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(String(constIndexEqualShow) + String(notiImageSourceData.suffix(7)))] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(String(appAddK.suffix(6)) + String(kBackgroundApp.suffix(6)))] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: dic["params"] = params
            dic[(notiSharedConst.replacingOccurrences(of: "stream", with: "ra"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = rejoin(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> MessageThen? {
    public class func exceptViewStyle(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> MessageThen? {
        //: var cellData: MessageThen?
        var cellData: MessageThen?
        //: guard let extraDict = dic["extra"] as? [String: Any],
        let extraDict = dic[(String(constPastApp))] as? [String: Any]
              //: let msgInfoDict = extraDict["msgInfo"] as? [String: Any] else {
        let msgInfoDict = extraDict?[(String(dataItemNoti.suffix(7)))] as? [String: Any]

        //: let msgInfo = msgInfoDict
        let msgInfo = msgInfoDict
        //: let user = extraDict["user"] as? [String: Any] ?? [:]
        let user = dic[(appSectionModelNoti.replacingOccurrences(of: "at", with: "r"))] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? TodayModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == PositionLabelReactiveCompatible.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == PositionLabelReactiveCompatible.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == PositionLabelReactiveCompatible.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == PositionLabelReactiveCompatible.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(mainAppK.suffix(7)) + String(showBorderData))] as? String == (String(notiPostData) + String(mainNameNoti.suffix(5)) + String(mainLeadingApp)) {
            //: isSelf = msgInfo["changeDirection"] as? Bool ?? false
            isSelf = msgInfo?[(kReplyConst.replacingOccurrences(of: "input", with: "ch") + "geDi" + mainStatusApp.replacingOccurrences(of: "size", with: "io"))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = available(dic)

        // 系统消息
        //: if extraDict["msgType"] as? String == "system" ||
        if extraDict?[(String(userModelShow))] as? String == (kMakeData.replacingOccurrences(of: "bottom", with: "m")) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = TodayModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? ChatModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = PhotoGainCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(String(constAtMain) + String(appFatalNoti))] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = LayerSharedReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = LayerSharedReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = CradleThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = CradleThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = DropCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = ViewCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ViewCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = example(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(String(constPastApp))] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(dataItemNoti.suffix(7)))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? LoadImageModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = SourceThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = SourceThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = ChatRunningReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ChatRunningReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? ChatRunningReactiveCompatible {
            if let audioCellData = cellData as? ChatRunningReactiveCompatible {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = buttonDestroy(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = AudileReactiveCompatible.atWith(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = AudileReactiveCompatible.dismissDb(atImage(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = FrameCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict["title"] as? String ?? ""
            let title = extraDict?[(notiLabelApp.replacingOccurrences(of: "name", with: "t"))] as? String ?? ""
            //: let content = extraDict["content"] as? String ?? ""
            let content = extraDict?[(String(constAtMain) + String(appFatalNoti))] as? String ?? ""
            //: let img = extraDict["img"] as? String ?? ""
            let img = extraDict?[(String(constAddShow))] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = FootballPlayReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = WhiteCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = WhiteCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = NumberReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = NumberReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfo) { (_, new) in new }
            userDict.merge(msgInfo ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? FeatureModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(constColorNoti.replacingOccurrences(of: "equal", with: "co"))] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: PositionLabelReactiveCompatible.share.loginUserMode.headPic ?? ""
                        PositionLabelReactiveCompatible.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = LoadImageModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(String(constAtMain) + String(appFatalNoti))] as? String ?? ""
            //: msgInfoModel.jumps = extraDict["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(String(constPastApp))] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = ByBindModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (String(userOpenShow))
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (String(mainAddValueUser.suffix(5)) + "chats_" + kBlockConst.replacingOccurrences(of: "half", with: "c"))
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.addVideo(withJsonString: customStr) ?? [:]
        //: let status = TMsgStatus(rawValue: UInt(customDic["status"] as? Int ?? 0)) ?? .Msg_Status_Init
        let statusInt = customDic["status"] as? String ?? "0"
        let status = TMsgStatus(rawValue: UInt(statusInt) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(String(showTipApp.suffix(9)))] as? Float ?? 0)
        //: cellData?.messageType = msgInfo["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfo?[(String(dataPlayerK) + String(appPicK.suffix(6)))] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(kItemData.prefix(5)))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(String(dataIndexK.prefix(5)) + "ransla" + String(constScaleApp.prefix(6)) + mainTextK.replacingOccurrences(of: "close", with: "us"))] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = SequenceTotalLiteral(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(appDataInfoNoti) + String(userMiniNoti))] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = SocialModel.message()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = SocialModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = TrafficModel.imageTime()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = TrafficModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = KeyModel.create()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func available(_ extraDic: [String: Any]) -> AddressBinaryQuantity {
        //: guard let dic = extraDic["extra"] as? [String: Any],
        let dic = extraDic[(String(constPastApp))] as? [String: Any]
              //: let msgInfo = dic["msgInfo"] as? [String: Any],
        let msgInfo = dic?[(String(dataItemNoti.suffix(7)))] as? [String: Any]
        guard let model = try? TodayModel(dictionary: dic) else {
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic["formatType"] as? String == "plain" {
        if dic?[(String(dataToNoti.prefix(7)) + String(appTrackModelK))] as? String == (kCellNoti.replacingOccurrences(of: "player", with: "pl")) {
            //: let imageUri = dic["imageUri"] as? String ?? ""
            let imageUri = dic?[(String(userSizeMain))] as? String ?? ""
            //: let imageData = dic["imageData"] as? String ?? ""
            let imageData = dic?[(String(showModeApp.prefix(6)) + String(appLiveShow))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(kStyleContainerConst.suffix(8))) {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo)
        let isVoiceStyle = atMsginfo(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic["match"] as? Int) == 1 || (msgInfo["match"] as? Int) == 1 {
        if (dic?[(String(mainPriceConst))] as? Int) == 1 || (msgInfo?[(String(mainPriceConst))] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic["msgType"] as? String == "tips" || (dic["messageType"] as? Int) == 17 {
        if dic?[(String(userModelShow))] as? String == (notiPopUser.replacingOccurrences(of: "path", with: "s")) || (dic?[(String(dataPlayerK) + String(appPicK.suffix(6)))] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic["formatType"] as? String == "mf") && ((dic["messageType"] as? Int) == 15 || (dic["messageType"] as? Int) == 16) {
        if (dic?[(String(dataToNoti.prefix(7)) + String(appTrackModelK))] as? String == "mf") && ((dic?[(String(dataPlayerK) + String(appPicK.suffix(6)))] as? Int) == 15 || (dic?[(String(dataPlayerK) + String(appPicK.suffix(6)))] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic["msgType"] as? String == "userIntro" {
        if dic?[(String(userModelShow))] as? String == (String(kKeyGroupTimeMain) + mainDateApp.replacingOccurrences(of: "color", with: "o")) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo["msgType"] as? String == "WantGift" || (msgInfo["messageType"] as? Int) == 25 {
        if msgInfo?[(String(userModelShow))] as? String == (String(notiTitleNeedLiveData)) || (msgInfo?[(String(dataPlayerK) + String(appPicK.suffix(6)))] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func atImage(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.addVideo(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(String(constPastApp))] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(dataItemNoti.suffix(7)))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(appSectionModelNoti.replacingOccurrences(of: "at", with: "r"))] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(String(appPathConst.suffix(8)))] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == PositionLabelReactiveCompatible.share.loginUserMode.userID {
        if senderId == PositionLabelReactiveCompatible.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(String(appPathConst.suffix(8)))] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(String(notiInputConst.suffix(7)) + String(userGreenNoti))] = message.userID ?? (user?[(String(notiInputConst.suffix(7)) + String(userGreenNoti))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(appPathConst.suffix(8)))] = "\(msgInfo?[(kLastConst.replacingOccurrences(of: "lab", with: "id"))] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(notiInputConst.suffix(7)) + String(userGreenNoti))] = "\(msgInfo?[(kLastConst.replacingOccurrences(of: "lab", with: "id"))] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(kItemData.prefix(5)))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(kItemData.prefix(5)))] = "\(msgInfo?[(String(kItemData.prefix(5)))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.addVideo(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(kItemData.prefix(5)))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(kItemData.prefix(5)))] = "\(customDic[(String(kItemData.prefix(5)))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(kItemData.prefix(5)))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((String(appLineConst.prefix(6)) + "andbox")) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(String(appLineConst.prefix(6)) + "andbox")] = "\(msgInfo?[(String(appLineConst.prefix(6)) + "andbox")] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((String(showToConst) + String(constDeviceUser.suffix(6)))) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(String(showToConst) + String(constDeviceUser.suffix(6)))] = "\(msgInfo?[(String(showToConst) + String(constDeviceUser.suffix(6)))] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (String(userWhiteReloadShow.suffix(5)) + String(showAddUser))
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(String(dataPlayerApp.suffix(5)) + mainFilterK.replacingOccurrences(of: "key", with: "m"))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == PositionLabelReactiveCompatible.share.loginUserMode.userID ? "1" : "0"
        info?[(String(userWrapNoti))] = senderId == PositionLabelReactiveCompatible.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func buttonDestroy(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = atImage(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(kItemData.prefix(5)))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func example(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func observerTing(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func atMsginfo(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(userModelShow))] as? String, msgType == (dataMenuConst.replacingOccurrences(of: "lab", with: "io")) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(showAreaToUser.replacingOccurrences(of: "lab", with: "o") + String(dataListUser))] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(notiTitleKeyFrameK.prefix(6)) + showDefineUser.replacingOccurrences(of: "frame", with: "g"))
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(String(notiEqualK))] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(String(constVideoK))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
