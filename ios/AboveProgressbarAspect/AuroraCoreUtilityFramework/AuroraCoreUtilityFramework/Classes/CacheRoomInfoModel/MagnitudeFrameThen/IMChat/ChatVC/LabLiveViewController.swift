
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let mainWhoTableShow:[UInt8] = [0x36,0x31,0x36,0x2b,0x77,0x3c,0x30,0x3b,0x3a,0x2d,0x65,0x76,0x7f,0x37,0x3e,0x2c,0x7f,0x31,0x30,0x2b,0x7f,0x3d,0x3a,0x3a,0x31,0x7f,0x36,0x32,0x2f,0x33,0x3a,0x32,0x3a,0x31,0x2b,0x3a,0x3b]

/*: "F5F5F5" :*/
fileprivate let constNumberTrack:String = "f5f5f5"

/*: "TalkingChatTextMsgCellID" :*/
fileprivate let userTimeWhiteApp:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","T","e","x","t","M","s","g","C","e"]
fileprivate let constIconK:String = "llIDcorner text key"

/*: "TalkingChatImageMsgCellID" :*/
fileprivate let dataEndNoti:String = "player var normal var topTalkin"
fileprivate let showEmptyRequestNoti:String = "mageMsfront for guard size"

/*: "TalkingChatVideoMsgCellID" :*/
fileprivate let showPlayerHiddenK:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","V","i"]
fileprivate let appProgressModeShow:[Character] = ["d","e","o","M","s","g","C","e","l","l","I","D"]

/*: "TalkingChatGiftMsgCellID" :*/
fileprivate let mainExpressConst:String = "Talkshow lab"
fileprivate let kTimeTableRetData:String = "tGiferror to"
fileprivate let kModelShow:String = "style label if righttMsg"
fileprivate let showMakeK:String = "CellIDelse height image status table"

/*: "ImageMessageCell" :*/
fileprivate let dataCenterNoti:String = "today item selfTalk"
fileprivate let notiObjectConst:String = "info add datatAskF"
fileprivate let kLabelMain:String = "ftMsguser app"

/*: "TalkingChatTipsMsgCellID" :*/
fileprivate let notiLabMain:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","T","i","p","s","M","s","g","C","e","l","l","I","D"]

/*: "TalkingChatMatchMsgCellID" :*/
fileprivate let showFillMain:String = "Talkinpath gender in for"
fileprivate let appResultUser:[Character] = ["t","M","a","t"]
fileprivate let mainMakeData:String = "chMsginput case"

/*: "TalkingChatAudioMsgCellID" :*/
fileprivate let userSucceedMain:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","A","u","d","i","o","M","s"]
fileprivate let dataUserMain:String = "gCellIDheight image"

/*: "TalkingChatSystemNotifJumpMsgCellID" :*/
fileprivate let dataPathMain:[UInt8] = [0x44,0x49,0x6c,0x6c,0x65,0x43,0x67,0x73,0x4d,0x70,0x6d,0x75,0x4a,0x66,0x69,0x74,0x6f,0x4e,0x6d,0x65,0x74,0x73,0x79,0x53,0x74,0x61,0x68,0x43,0x67,0x6e,0x69,0x6b,0x6c,0x61,0x54]

/*: "TSystemMessageCell_ReuseId" :*/
fileprivate let mainStreamEqualRightUser:[Character] = ["T","S","y","s","t","e","m","M","e","s","s","a"]
fileprivate let userErrorImageShow:String = "info progress usergeCell"
fileprivate let notiUserData:String = "_Reuview text pop color add"

/*: "#128CFF" :*/
fileprivate let dataSharedApp:[Character] = ["#","1","2","8","C","F","F"]

/*: "Feedback" :*/
fileprivate let notiTitleShow:String = "color source up type colorFeedback"

/*: "#8370FF" :*/
fileprivate let appPhotoConst:String = "#8370"
fileprivate let dataCountUser:String = "fromfrom"

/*: "Quick Greeting" :*/
fileprivate let showVoiceNoti:[Character] = ["Q","u","i","c","k"," "]
fileprivate let appCornerNoti:String = "empty return viewGr"
fileprivate let constVideoValueShow:String = "eemanagerng"

/*: "Beauty Photos" :*/
fileprivate let appKitMain:[Character] = ["B"]
fileprivate let dataTakeNoti:String = "EAUTY"

/*: "url" :*/
fileprivate let mainGiftData:String = "urmake"

/*: "unlockGift" :*/
fileprivate let constGlobalK:String = "UNL"
fileprivate let mainAddLabMatchApp:[Character] = ["o","c","k","G","i","f","t"]

/*: "%d" :*/
fileprivate let userEraseMain:String = "%dlet let with string"

/*: "isBurn" :*/
fileprivate let kModelNoti:[UInt8] = [0x88,0x92,0xa3,0x94,0x93,0x8f]

private func succeedUserWhiteCell(error num: UInt8) -> UInt8 {
    return num ^ 225
}

/*: "giftId" :*/
fileprivate let constToMain:String = "giftIuser menu error"
fileprivate let appViewShow:String = "video"

/*: "rYMsgType" :*/
fileprivate let constExplainData:[Character] = ["r","Y","M","s","g","T","y","p","e"]

/*: "GJ:CallCustom" :*/
fileprivate let appInputConst:[Character] = ["G","J",":","C","a","l","l","C","u"]
fileprivate let notiToApp:String = "stoview"

/*: "changeDirection" :*/
fileprivate let notiCardData:[Character] = ["c","h","a","n","g","e","D","i","r","e","c","t","i","o","n"]

/*: "extra" :*/
fileprivate let notiGiftMakeUser:String = "exstylera"

/*: "msgInfo" :*/
fileprivate let showPlayerMain:[Character] = ["m"]
fileprivate let constEndData:[Character] = ["s","g","I","n","f","o"]

/*: "toUid" :*/
fileprivate let mainSendEffectNoti:[Character] = ["t","o","U","i","d"]

/*: "wantGift" :*/
fileprivate let showPressUser:String = "wantGuser lab filter"
fileprivate let showToK:[Character] = ["i","f","t"]

/*: "jumpKey" :*/
fileprivate let notiLengthData:[Character] = ["j"]
fileprivate let userEqualNoti:[Character] = ["u","m","p","K","e","y"]

/*: "truePersonAuth" :*/
fileprivate let kTotaleractionUser:[Character] = ["t","r","u","e","P","e","r","s","o","n","A"]
fileprivate let mainReplyK:String = "ushare"

/*: "logType" :*/
fileprivate let notiPathConst:[Character] = ["l","o","g","T","y","p","e"]

/*: "msgType" :*/
fileprivate let notiTitleUser:[UInt8] = [0x65,0x70,0x79,0x54,0x67,0x73,0x6d]

/*: "txt" :*/
fileprivate let notiLiveUserMain:String = "treasont"

/*: "type" :*/
fileprivate let appColorMain:String = "tviewe"

/*: "QuickMsg" :*/
fileprivate let constOfShow:String = "QuickMcontent format rank"
fileprivate let kMenuMain:String = "SG"

/*: "params" :*/
fileprivate let showColorData:[UInt8] = [0x38,0x29,0x3a,0x29,0x35,0x3b]

fileprivate func viewTable(content num: UInt8) -> UInt8 {
    let value = Int(num) - 200
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "content" :*/
fileprivate let showViolationK:[UInt8] = [0x2c,0x38,0x37,0x3d,0x2e,0x37,0x3d]

fileprivate func extraName(effect num: UInt8) -> UInt8 {
    let value = Int(num) - 201
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "id" :*/
fileprivate let showNameConst:String = "statusd"

/*: "txtId" :*/
fileprivate let appPastConst:[Character] = ["t","x","t","I","d"]

/*: "message" :*/
fileprivate let showNowFilterK:[UInt8] = [0x65,0x67,0x61,0x73,0x73,0x65,0x6d]

/*: "TxtMsg" :*/
fileprivate let constLeadingShow:String = "TxtMsgselected type editor"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let mainAppActionData:[UInt8] = [0xcc,0xf5,0xf3,0xf9,0xff,0xba,0xf2,0xff,0xf6,0xf6,0xf5,0xba,0xfe,0xf5,0xed,0xf4,0xf6,0xf5,0xfb,0xfe,0xba,0xfc,0xfb,0xf3,0xf6,0xff,0xfe,0xb4,0xba,0xca,0xe8,0xf5,0xf9,0xff,0xfe,0xef,0xe8,0xff]

private func userSize(seek num: UInt8) -> UInt8 {
    return num ^ 154
}

/*: "length" :*/
fileprivate let userPostApp:String = "LENGTH"

/*: "audio" :*/
fileprivate let dataProgressNoti:String = "AUDIO"

/*: "audioId" :*/
fileprivate let userMainEqualShow:String = "audioIremove item string me view"
fileprivate let notiSizeApp:String = "make"

/*: "audioLength" :*/
fileprivate let showIconK:[UInt8] = [0x68,0x74,0x67,0x6e,0x65,0x4c,0x6f,0x69,0x64,0x75,0x61]

/*: "audioSandbox" :*/
fileprivate let notiPermissionPanShow:String = "AUDI"

/*: "senderId" :*/
fileprivate let showThoughtEmptyQuickApp:[UInt8] = [0x23,0x35,0x3e,0x34,0x35,0x22,0x19,0x34]

private func nameListLayer(user num: UInt8) -> UInt8 {
    return num ^ 80
}

/*: "AudioMsg" :*/
fileprivate let appCoverShow:[Character] = ["A","u","d","i","o","M"]
fileprivate let constDismissMain:String = "scontact"

/*: "Can't send blank message" :*/
fileprivate let notiDeviceInsideK:String = "Can\'t "
fileprivate let mainValueNoti:String = "day pic var blank"
fileprivate let showPlayMain:String = "birthe"

/*: "quoteMsgId" :*/
fileprivate let dataLiveByConst:[Character] = ["q","u","o","t","e","M","s","g","I","d"]

/*: "quoteMsgInfo" :*/
fileprivate let constStartNoti:String = "list group path main emptyquoteMsg"
fileprivate let dataAddMain:[Character] = ["I","n","f","o"]

/*: "contentType" :*/
fileprivate let showColorMain:[UInt8] = [0x85,0x89,0x88,0x92,0x83,0x88,0x92,0xb2,0x9f,0x96,0x83]

private func loadBurn(status num: UInt8) -> UInt8 {
    return num ^ 230
}

/*: "ImgMsg" :*/
fileprivate let userFrameApp:String = "name"
fileprivate let appPushData:String = "self view the return selfmgMsg"

/*: "img" :*/
fileprivate let appToK:[Character] = ["i","m","g"]

/*: "imageUri" :*/
fileprivate let notiMakeTitleApp:[UInt8] = [0x4f,0x4b,0x47,0x41,0x43,0x73,0x54,0x4f]

private func textInput(succeed num: UInt8) -> UInt8 {
    return num ^ 38
}

/*: "customSend" :*/
fileprivate let userButtonValueMain:[UInt8] = [0xd5,0xc3,0xc5,0xc2,0xd9,0xdb,0xe5,0xd3,0xd8,0xd2]

private func giftDefine(year num: UInt8) -> UInt8 {
    return num ^ 182
}

/*: "pic" :*/
fileprivate let appPlayerNoti:String = "pobserverc"

/*: "VideoMsg" :*/
fileprivate let mainEqualK:[Character] = ["V","i","d","e","o","M","s","g"]

/*: "video" :*/
fileprivate let notiBlockShow:String = "videerror"

/*: "coverImg" :*/
fileprivate let notiIconDataMain:[UInt8] = [0x94,0x98,0x81,0x92,0x85,0xbe,0x9a,0x90]

/*: "videoUrl" :*/
fileprivate let constColorData:[UInt8] = [0x5d,0x42,0x4f,0x4e,0x44,0x7e,0x59,0x47]

/*: "duration" :*/
fileprivate let showModelConst:[UInt8] = [0x9e,0x8f,0x88,0x9b,0x8e,0x93,0x95,0x94]

private func sleepingTablet(point num: UInt8) -> UInt8 {
    return num ^ 250
}

/*: "len" :*/
fileprivate let showMinData:[UInt8] = [0xa3,0xaa,0xa1]

/*: "Documents/User/voice/" :*/
fileprivate let notiOnConst:String = "Documemodel category"
fileprivate let kBeanMain:String = "ser/equal bottom title name"
fileprivate let dataSuccessUser:String = "add"
fileprivate let constOfApp:[Character] = ["o","i","c","e","/"]

/*: "Documents/User/record/" :*/
fileprivate let kEqualUser:[Character] = ["D","o","c","u","m","e","n","t","s","/","U","s","e","r","/"]
fileprivate let userManagerData:String = "record/let self content to feature"

/*: "msgIncome" :*/
fileprivate let showGroupRegularData:String = "height labmsgIncom"
fileprivate let constModelNoti:String = "active"

/*: "mfBean" :*/
fileprivate let notiGiftInfoShow:[UInt8] = [0x9e,0x95,0xb1,0x96,0x92,0x9d]

/*: "messageType" :*/
fileprivate let mainValueHiddenApp:[Character] = ["m","e","s","s","a"]
fileprivate let dataTopK:[Character] = ["g","e","T","y","p","e"]

/*: "WantGift" :*/
fileprivate let appCountData:String = "add video true cellWan"
fileprivate let notiTagConst:String = "tGiftleading app"

/*: _PrivateChatTipsTitle_ :*/
fileprivate let mainSeaNoti:String = "equal i action_Priva"
fileprivate let mainMakeK:String = "make true asatTi"
fileprivate let notiPlayConst:String = "e_source image normal top"

/*: "msgId" :*/
fileprivate let notiPathShow:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let appMakeViewData:[Character] = ["t","o","t","a","l","M","f","C","o","i","n"]

/*: "attachSysMsg" :*/
fileprivate let userElementShow:String = "awithwithach"

/*: "intimatePhoto" :*/
fileprivate let userTotalMain:String = "index"
fileprivate let userPubliclyK:String = "actiontim"

/*: _ :*/
fileprivate let mainCenterNoti:String = "equal"

/*: "totalIntimate" :*/
fileprivate let constStatusServerK:[Character] = ["t","o","t","a","l","I","n","t","i","m","a","t","e"]

/*: "uid" :*/
fileprivate let showSectionPathConst:[UInt8] = [0xb6,0xaa,0xa5]

fileprivate func varTime(show num: UInt8) -> UInt8 {
    let value = Int(num) - 65
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "intimate" :*/
fileprivate let dataValueK:[UInt8] = [0x65,0x74,0x61,0x6d,0x69,0x74,0x6e,0x69]

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let notiImageK:[UInt8] = [0x2e,0x6c,0x65,0x76,0x65,0x6c,0x20,0x79,0x63,0x61,0x6d,0x69,0x74,0x6e,0x69,0x20,0x66,0x6f,0x20,0x6b,0x63,0x61,0x6c,0x20,0x72,0x75,0x6f,0x79,0x20,0x6f,0x74,0x20,0x65,0x75,0x64,0x20,0x6e,0x6f,0x69,0x74,0x63,0x6e,0x75,0x66,0x20,0x65,0x68,0x74,0x20,0x65,0x73,0x75,0x20,0x74,0x27,0x6e,0x61,0x43]

/*: "giftNum" :*/
fileprivate let notiFilterApp:[Character] = ["g","i","f","t","N"]
fileprivate let mainValueUser:String = "umake"

/*: "pkgItemsetId" :*/
fileprivate let notiLabK:String = "image name time nowpkgItem"
fileprivate let appWithCenterShow:String = "setIdlength clear pop return"

/*: "from" :*/
fileprivate let showManagerSizeUser:[Character] = ["f","r","o","m"]

/*: "wantGiftId" :*/
fileprivate let notiCollectShow:[Character] = ["w","a"]
fileprivate let notiItemMain:String = "false selected manager dayntGiftId"

/*: "WantGiftMsg" :*/
fileprivate let notiGiftLayerApp:String = "interaction whereWantG"
fileprivate let mainHiddenUser:[Character] = ["g"]

/*: "gift" :*/
fileprivate let appTableRequestUser:String = "GIFT"

/*: "View info" :*/
fileprivate let dataViewMain:String = "image before true view pathView"

/*: "Notes" :*/
fileprivate let showEdgeK:String = "photo view center count viewNotes"

/*: "Report" :*/
fileprivate let mainCostData:[Character] = ["R","e","p","o","r","t"]

/*: "View read receipts?" :*/
fileprivate let appCenterK:String = "View rerror true page self"
fileprivate let appAppearConst:String = "eipts?string for change view log"

/*: "He" :*/
fileprivate let userPriceConst:[Character] = ["H","e"]

/*: "She" :*/
fileprivate let showDeleteApp:String = "Shetrue make self permission shadow"

/*: "%@ is using too low a version" :*/
fileprivate let constNetworkPriceApp:String = "self false super%@ i"
fileprivate let constTransitionUser:[Character] = ["g"," ","t","o","o"," ","l","o","w"," ","a"," ","v","e","r","s","i","o","n"]

/*: "Shoot video at least 5 seconds" :*/
fileprivate let showEndConst:[Character] = ["S","h","o","o","t"," ","v","i","d","e","o"," "]
fileprivate let kAddApp:[Character] = ["a","t"," ","l","e","a","s","t"," ","5"," ","s","e","c","o","n","d","s"]

/*: "Upload failed" :*/
fileprivate let constEqualK:String = "Upload model name index"
fileprivate let kAtDataNoti:String = "facreateled"

/*: "播放状态: :*/
fileprivate let appCellK:String = "\u{64ad}放状\u{6001}:"

/*: "c2c_" :*/
fileprivate let mainUserData:String = "c2c_list else index"

/*: "msg" :*/
fileprivate let userAppImageK:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let mainTapConst:String = "tinet"

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let notiCenterApp:[UInt8] = [0x70,0x46,0x5c,0x9,0x4a,0x48,0x47,0x9,0x5c,0x5a,0x4c,0x9,0x5d,0x41,0x4c,0x9,0x5f,0x40,0x4d,0x4c,0x46,0x9,0x4a,0x41,0x48,0x5d,0x9,0x4f,0x4c,0x48,0x5d,0x5c,0x5b,0x4c,0x9,0x5d,0x46,0x9,0x4e,0x40,0x5f,0x4c,0x9,0xc,0x69,0x9,0x48,0x9,0x4a,0x48,0x45,0x45]

/*: "cellData" :*/
fileprivate let constMoveCornerNoti:String = "bottom tap mask image selfcellData"

/*: "System notification" :*/
fileprivate let showShareAppUser:String = "Systemcase lab self view"
fileprivate let notiActionMain:String = "FICATION"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LabLiveViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum UserRequestFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class LabLiveViewController: EmptyRecognizerDelegate {
	var conversationOff: Bool = false
	var dataTotal: Int = 40
	var headerName: String = "selected"
	var actionArray: [AnyHashable] = []
	var colorOff: Bool = false
	var quickShowTotal: Int = 93
	var collectionTitle: String = "last"
	var endArray: [AnyHashable] = []
	var achromaticColorOn: Bool = true
	var haveNumber: Int = 86
	var priceSectionText: String = "gift"
	var styleTopArray: [AnyHashable] = []

    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var isHaveSession = false
    var isHaveSession = false
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: CacheIMChatType = .private
    private var conversationType: CacheIMChatType = .private
    //: private var userWrap = EventMeasurable()
    private var userWrap = EventMeasurable() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = UserRequestFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: EventMeasurable = EventMeasurable()) {
    init(chatID: String, isFrom: UserRequestFromEnum = .Normal, userWrap: EventMeasurable = EventMeasurable()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        LabLiveViewController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        ViewChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == RowMacroDefine.getXiaoMiID() {
        if targetId == RowMacroDefine.tillAge() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == RowMacroDefine.getCustomerServiceID() {
        } else if targetId == RowMacroDefine.menagerie() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: mainWhoTableShow.map{$0^95}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if PositionLabelReactiveCompatible.share.appConfigMode.disableShootScreen {
            if PositionLabelReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = FrameThen(frame: CGRect(x: 0, y: 0, width: userDeviceKey, height: appNameMessage))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        toAnimation()
        //: createUI()
        mention()
        //: createUIConstraints()
        tack()
        //: bindInteraction()
        join()
        //: addTapGestureRecognizer()
        greet()
        //: func__requestChatInfo()
        dataAppear()
        //: func__getFillDraftText()
        polePosition()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        StateBankManagerPlayerDelegate.shared.nameStatus()
        //: func__addVoiceNotification()
        subsequently()
        //: addReadPeceiptNotification()
        toNotification()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            manager()
            //: func__TipsNotification()
            design()
            //: if PositionLabelReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
            if PositionLabelReactiveCompatible.share.loginUserMode.sex == PetitionKeyRepresentable.male.rawValue {
                //: func__setReplyTipsView()
                say()
            }
        }
    
            if (feedbackBtn.superview != nil && feedbackBtn.superview!.isHidden) && (feedbackBtn.layoutGuides.count == 76) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let carefulCount = PhonationView(frame: feedbackBtn.bounds.integral)
            carefulCount.showCount = msgInputView.giftView.contentHeight
            
            carefulCount.constraintOn = { [self] colorOn in
            self.colorOff = colorOn
            
            var carefulCount = userWrap.tpAuth
                carefulCount = true
                carefulCount = !carefulCount
            if carefulCount != self.colorOff {
                self.colorOff = carefulCount
            }
            
            return self.colorOff
            }
            carefulCount.promptQuantity = { [self] atAcrossNumber in
            self.quickShowTotal = atAcrossNumber
            
            var carefulCount = userWrap.onlineStatus
                carefulCount += 1
                if Int(carefulCount) > -74 {
                    carefulCount = carefulCount - 1
                }
            if carefulCount < self.quickShowTotal {
                self.quickShowTotal = carefulCount
            }
            
            return self.quickShowTotal
            }
            carefulCount.tableSumName = { [self] netText in
            self.collectionTitle = netText
            
            var carefulCount = targetId
            carefulCount = ""
            if carefulCount.contains(self.collectionTitle) {
                self.collectionTitle = carefulCount
            }
            
            do {
                self.collectionTitle = try String(contentsOfFile: self.collectionTitle.capitalized + "player", encoding: .utf8)
            } catch {
                self.collectionTitle = error.localizedDescription
            }
            return self.collectionTitle
            }
            carefulCount.byArray = { [self] tableAllArray in
            self.endArray = tableAllArray
            
            guard var value = self.endArray as? [String] else {
                return nil
            }
            return value
            }
                feedbackBtn.addSubview(carefulCount)
            }

	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        viewContainer(isAdd: true)
    
            if (bgView.superview != nil && bgView.superview!.isHidden) && (bgView.layoutGuides.count == 76) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let carefulCount = PhonationView(frame: bgView.bounds.integral)
            carefulCount.showCount = msgInputView.inputTextView.placeholderLeftOffset
            
            carefulCount.constraintOn = { [self] colorOn in
            self.conversationOff = colorOn
            
            var carefulCount = giftEffectView.giftAnimatView.needStopAll
                carefulCount = true
                carefulCount = !carefulCount
            if carefulCount != self.conversationOff {
                self.conversationOff = carefulCount
            }
            
            return self.conversationOff
            }
            carefulCount.promptQuantity = { [self] atAcrossNumber in
            self.dataTotal = atAcrossNumber
            
            var carefulCount = self.msgInputView.duration
                carefulCount += 1
                if Int(carefulCount) > -74 {
                    carefulCount = carefulCount - 1
                }
            if carefulCount < self.dataTotal {
                self.dataTotal = carefulCount
            }
            
            return self.dataTotal
            }
            carefulCount.tableSumName = { [self] netText in
            self.headerName = netText
            
            var carefulCount = targetId
            carefulCount = ""
            if carefulCount.contains(self.headerName) {
                self.headerName = carefulCount
            }
            
            do {
                self.headerName = try String(contentsOfFile: self.headerName.capitalized + "player", encoding: .utf8)
            } catch {
                self.headerName = error.localizedDescription
            }
            return self.headerName
            }
            carefulCount.byArray = { [self] tableAllArray in
            self.actionArray = tableAllArray
            
            guard var value = self.actionArray as? [String] else {
                return nil
            }
            return value
            }
                bgView.addSubview(carefulCount)
            }

	}

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        viewContainer(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            wideAwakeTo()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    
		if var withItUserValue = videoCallManager.uid { 
	            if (contentView.superview != nil && contentView.superview!.isHidden) && (contentView.layoutGuides.count == 76) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let carefulCount = PhonationView(frame: contentView.bounds.integral)
	            carefulCount.showCount = msgInputView.inputTextView.placeholderLeftOffset
	            
	            carefulCount.constraintOn = { [self] colorOn in
	            self.achromaticColorOn = colorOn
	            
	            var carefulCount = floatScreenView.needStopAll
	                carefulCount = true
	                carefulCount = !carefulCount
	            if carefulCount != self.achromaticColorOn {
	                self.achromaticColorOn = carefulCount
	            }
	            
	            return self.achromaticColorOn
	            }
	            carefulCount.promptQuantity = { [self] atAcrossNumber in
	            self.haveNumber = atAcrossNumber
	            
	            var carefulCount = userWrap.vipSkinId
	                carefulCount += 1
	                if Int(carefulCount) > -74 {
	                    carefulCount = carefulCount - 1
	                }
	            if carefulCount < self.haveNumber {
	                self.haveNumber = carefulCount
	            }
	            
	            return self.haveNumber
	            }
	            carefulCount.tableSumName = { [self] netText in
	            self.priceSectionText = netText
	            
	            var carefulCount = withItUserValue
	            carefulCount = ""
	            if carefulCount.contains(self.priceSectionText) {
	                self.priceSectionText = carefulCount
	            }
	            
	            do {
	                self.priceSectionText = try String(contentsOfFile: self.priceSectionText.capitalized + "player", encoding: .utf8)
	            } catch {
	                self.priceSectionText = error.localizedDescription
	            }
	            return self.priceSectionText
	            }
	            carefulCount.byArray = { [self] tableAllArray in
	            self.styleTopArray = tableAllArray
	            
	            guard var value = self.styleTopArray as? [String] else {
	                return nil
	            }
	            return value
	            }
	                contentView.addSubview(carefulCount)
	            }
	
		}
	}

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: LocalScreenTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = LocalScreenTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (constNumberTrack.uppercased()))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: "TalkingChatTextMsgCellID")
        msgVC.tableView.register(GainObserverViewCell.self, forCellReuseIdentifier: (String(userTimeWhiteApp) + String(constIconK.prefix(4))))
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: "TalkingChatImageMsgCellID")
        msgVC.tableView.register(DataEmptyViewCell.self, forCellReuseIdentifier: (String(dataEndNoti.suffix(6)) + "gChatI" + String(showEmptyRequestNoti.prefix(6)) + "gCellID"))
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: "TalkingChatVideoMsgCellID")
        msgVC.tableView.register(OnMsgCell.self, forCellReuseIdentifier: (String(showPlayerHiddenK) + String(appProgressModeShow)))
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: "TalkingChatGiftMsgCellID")
        msgVC.tableView.register(ContentMsgCell.self, forCellReuseIdentifier: "ContentMsgCell")
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: "TalkingChatAskForGiftMsgCell")
        msgVC.tableView.register(ImageMessageCell.self, forCellReuseIdentifier: "ImageMessageCell")
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: "TalkingChatTipsMsgCellID")
        msgVC.tableView.register(TipsMinThen.self, forCellReuseIdentifier: (String(notiLabMain)))
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: "TalkingChatMatchMsgCellID")
        msgVC.tableView.register(VideoOnView.self, forCellReuseIdentifier: "VideoOnView")
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: "TalkingChatAudioMsgCellID")
        msgVC.tableView.register(WhiteViewCell.self, forCellReuseIdentifier: (String(userSucceedMain) + String(dataUserMain.prefix(7))))
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: "TalkingChatSystemNotifJumpMsgCellID")
        msgVC.tableView.register(PhotoSpeedViewCell.self, forCellReuseIdentifier: String(bytes: dataPathMain.reversed(), encoding: .utf8)!)
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: "TSystemMessageCell_ReuseId")
        msgVC.tableView.register(WhiteView.self, forCellReuseIdentifier: (String(mainStreamEqualRightUser) + String(userErrorImageShow.suffix(6)) + String(notiUserData.prefix(4)) + "seId"))
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: ButtonThen = {
        //: let m = TalkingCallMenuManager()
        let m = ButtonThen()
        //: m.uid = targetId
        m.uid = targetId
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: TempInputView = {
        //: var type: InputViewType = .PrivateChat
        var type: TopViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = TempInputView(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(dataSharedApp)))!, .font: UIFont.viewPath(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(notiTitleShow.suffix(8))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(feedbackWithColor), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: SourceReactiveCompatible = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = SourceReactiveCompatible()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: StraightAndNarrowThen = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = StraightAndNarrowThen(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.viewPath(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (appPhotoConst.capitalized + dataCountUser.replacingOccurrences(of: "from", with: "F"))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(showVoiceNoti) + String(appCornerNoti.suffix(2)) + constVideoValueShow.replacingOccurrences(of: "manager", with: "ti")).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.requestCardTitle().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(chiaroscuro), for: .touchUpInside)
        //: btn.isHidden = !PositionLabelReactiveCompatible.share.appUserConfigMode.showDirectGreet || PositionLabelReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue || PositionLabelReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !PositionLabelReactiveCompatible.share.appUserConfigMode.showDirectGreet || PositionLabelReactiveCompatible.share.loginUserMode.sex == PetitionKeyRepresentable.male.rawValue || PositionLabelReactiveCompatible.share.appStatus == ValueHashableRepresentation.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: OfThen? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = OfThen()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: WeltanschauungView = {
        //: let v = TalkingSelectTranslationView()
        let v = WeltanschauungView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.quantity(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: SwitchgoingResultView = {
        //: let v = TalkingTranslationResultView()
        let v = SwitchgoingResultView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.funcGift(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.viewPath(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (appPhotoConst.capitalized + dataCountUser.replacingOccurrences(of: "from", with: "F"))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(appKitMain) + dataTakeNoti.lowercased() + " Photos").localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.requestCardTitle().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(toView), for: .touchUpInside)
        //: btn.isHidden = PositionLabelReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue || PositionLabelReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = PositionLabelReactiveCompatible.share.loginUserMode.sex == PetitionKeyRepresentable.male.rawValue || PositionLabelReactiveCompatible.share.appStatus == ValueHashableRepresentation.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: CouncilChamberCardGamesView = {
        //: let v = TalkingLiveRoomGamesView()
        let v = CouncilChamberCardGamesView()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: CurrentBannerView = {
        //: let view = TalkingPrivateBannerView.init()
        let view = CurrentBannerView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: ReportReactiveCompatible = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = ReportReactiveCompatible()
        //: v.isHidden = (PositionLabelReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (PositionLabelReactiveCompatible.share.appStatus == ValueHashableRepresentation.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: CapTitleView = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = CapTitleView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.principal()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension LabLiveViewController: ShareSumThen {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func outpost(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if PositionLabelReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
                if PositionLabelReactiveCompatible.share.loginUserMode.sex == PetitionKeyRepresentable.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if PositionLabelReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
                if PositionLabelReactiveCompatible.share.loginUserMode.sex == PetitionKeyRepresentable.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.towardTo(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension LabLiveViewController: ValueKeepDelegate {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func chiaroscuro() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        ViewChatManager.duringSign(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.exceptAdd(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.finishResign(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func toView() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        ViewChatManager.titleUp(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = MenuPhotomicrographRecognizerDelegate()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().windowFinish(urlStr: dict?[(mainGiftData.replacingOccurrences(of: "make", with: "l"))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(constGlobalK.lowercased() + String(mainAddLabMatchApp))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = BarModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: kModelNoti.map{succeedUserWhiteCell(error: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(constToMain.prefix(5)) + appViewShow.replacingOccurrences(of: "video", with: "d"))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.betweenBar(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> MessageThen! {
    func quote(_: LocalScreenTableViewController!, run data: V2TIMMessage!) -> MessageThen! {
        //: let dic = ArtefactChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = ArtefactChatManager.pull(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = SocietalChatManager.exceptViewStyle(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: FrameCellData.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(constExplainData))].stringValue == (String(appInputConst) + notiToApp.replacingOccurrences(of: "view", with: "m")) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(String(notiCardData))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(notiGiftMakeUser.replacingOccurrences(of: "style", with: "t"))][(String(showPlayerMain) + String(constEndData))][(String(mainSendEffectNoti))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = NumberReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = NumberReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: ChatRunningReactiveCompatible.self) == true ||
        if cellData?.isKind(of: ChatRunningReactiveCompatible.self) == true ||
            //: cellData?.isKind(of: LayerSharedReactiveCompatible.self) == true ||
            cellData?.isKind(of: LayerSharedReactiveCompatible.self) == true ||
            //: cellData?.isKind(of: NumberReactiveCompatible.self) == true ||
            cellData?.isKind(of: NumberReactiveCompatible.self) == true ||
            //: cellData?.isKind(of: CradleThen.self) == true {
            cellData?.isKind(of: CradleThen.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            ViewChatManager.dataTo(cellData: cellData, msgView: self.msgView)
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
            ViewChatManager.listRefresh(cellData.msgIncome, data.timestamp, msgView)
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: MessageThen!) -> DetailReactiveCompatible! {
    func circumferenceSession(_: LocalScreenTableViewController!, roundNumber data: MessageThen!) -> DetailReactiveCompatible! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = telephoneMessage(data: data)

        //: if data.isKind(of: NumberReactiveCompatible.self) {
        if data.isKind(of: NumberReactiveCompatible.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTextMsgCellID")
            let cell = GainObserverViewCell(style: .default, reuseIdentifier: (String(userTimeWhiteApp) + String(constIconK.prefix(4))))
            //: let cellData = data as! NumberReactiveCompatible
            let cellData = data as! NumberReactiveCompatible

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.exceptStatus() {
                //: if cellData.msgModel.quoteMsgInfo.uid == PositionLabelReactiveCompatible.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == PositionLabelReactiveCompatible.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = PositionLabelReactiveCompatible.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = PositionLabelReactiveCompatible.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.equalWith(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: LayerSharedReactiveCompatible.self) {
        if data.isKind(of: LayerSharedReactiveCompatible.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: "TalkingChatImageMsgCellID")
            let cell = DataEmptyViewCell(style: .default, reuseIdentifier: (String(dataEndNoti.suffix(6)) + "gChatI" + String(showEmptyRequestNoti.prefix(6)) + "gCellID"))
            //: let cellData = data as! LayerSharedReactiveCompatible
            let cellData = data as! LayerSharedReactiveCompatible
            //: cell.fill(with: cellData)
            cell.equalWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: CradleThen.self) {
        if data.isKind(of: CradleThen.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: "TalkingChatVideoMsgCellID")
            let cell = OnMsgCell(style: .default, reuseIdentifier: (String(showPlayerHiddenK) + String(appProgressModeShow)))
            //: let cellData = data as! CradleThen
            let cellData = data as! CradleThen
            //: cell.fill(with: cellData)
            cell.equalWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: ViewCellData.self) {
        if data.isKind(of: ViewCellData.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingChatGiftMsgCellID")
            let cell = ContentMsgCell(style: .default, reuseIdentifier: "ContentMsgCell")
            //: let cellData = data as! ViewCellData
            let cellData = data as! ViewCellData
            //: cell.fill(with: cellData)
            cell.equalWith(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: SourceThen.self) {
        if data.isKind(of: SourceThen.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingChatAskForGiftMsgCell")
            let cell = ImageMessageCell(style: .default, reuseIdentifier: "ImageMessageCell")
            //: let cellData = data as! SourceThen
            let cellData = data as! SourceThen
            //: cell.fill(with: cellData)
            cell.equalWith(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = GiftMeasurable()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.withList(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(showPressUser.prefix(5)) + String(showToK)))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: ChatRunningReactiveCompatible.self) {
        if data.isKind(of: ChatRunningReactiveCompatible.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: "TalkingChatAudioMsgCellID")
            let cell = WhiteViewCell(style: .default, reuseIdentifier: (String(userSucceedMain) + String(dataUserMain.prefix(7))))
            //: let cellData = data as! ChatRunningReactiveCompatible
            let cellData = data as! ChatRunningReactiveCompatible
            //: cell.fill(with: cellData)
            cell.equalWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: FrameCellData.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: "TalkingChatMatchMsgCellID")
            let cell = VideoOnView(style: .default, reuseIdentifier: "VideoOnView")
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! FrameCellData
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.equalWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: PhotoGainCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTipsMsgCellID")
            let cell = TipsMinThen(style: .default, reuseIdentifier: (String(notiLabMain)))
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! PhotoGainCellData
            //: cell.fill(with: cellData)
            cell.equalWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: FootballPlayReactiveCompatible.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: "TalkingChatSystemNotifJumpMsgCellID")
            let cell = PhotoSpeedViewCell(style: .default, reuseIdentifier: String(bytes: dataPathMain.reversed(), encoding: .utf8)!)
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! FootballPlayReactiveCompatible
            //: cell.fill(with: cellData)
            cell.equalWith(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: DetailReactiveCompatible!) {
    func value(_: LocalScreenTableViewController!, length cell: DetailReactiveCompatible!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != RowMacroDefine.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != RowMacroDefine.menagerie() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: ViewPushManager.share.func__pushToUserDetailVC(uid: String(uid))
            ViewPushManager.share.digitalAnalogConverter(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: DetailReactiveCompatible!) {
    func handle(_: LocalScreenTableViewController!, packetOrAggregation cell: DetailReactiveCompatible!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: DataEmptyViewCell.self) { // 图片
            //: let imageCellData = cell.messageData as! LayerSharedReactiveCompatible
            let imageCellData = cell.messageData as! LayerSharedReactiveCompatible
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId)
                let vc = LayerViewController(imageCellData: imageCellData, toUid: self.targetId)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.burn(of: imageCellData, layerModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.boner(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: showPlainUrl)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.fileMake(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.clickTool(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                viewController()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(userReloadName)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = PlayerTransformable()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = TitleMomentModel()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [PlayerTransformable] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = PhotosViewDelegate(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                viewController()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: OnMsgCell.self) { // 视频
            //: let videoCellData = cell.messageData as! CradleThen
            let videoCellData = cell.messageData as! CradleThen
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = CoordinateViewController(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                viewController()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId)
                let vc = ListViewDelegate(videoCellData: videoCellData, toUid: self.targetId)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.model(videoCellData, strength: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.boner(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: showPlainUrl)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.fileMake(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.clickTool(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                viewController()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: WhiteViewCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! WhiteViewCell
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! ChatRunningReactiveCompatible, cellView: audioCell)
            no(cellData: audioCell.cellData as! ChatRunningReactiveCompatible, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: GainObserverViewCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! GainObserverViewCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = ArtefactChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = ArtefactChatManager.pull(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(notiGiftMakeUser.replacingOccurrences(of: "style", with: "t"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(notiLengthData) + String(userEqualNoti))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(notiLengthData) + String(userEqualNoti))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (String(kTotaleractionUser) + mainReplyK.replacingOccurrences(of: "share", with: "th")) {
                    //: if PositionLabelReactiveCompatible.share.loginUserMode.isTPAuth == TPUserAuth.isSuccessed.rawValue {
                    if PositionLabelReactiveCompatible.share.loginUserMode.isTPAuth == CreateVideoTermConvertible.isSuccessed.rawValue {
                        //: return
                        return
                            //: } else if PositionLabelReactiveCompatible.share.loginUserMode.isTPAuth == TPUserAuth.unknown.rawValue {
                    } else if PositionLabelReactiveCompatible.share.loginUserMode.isTPAuth == CreateVideoTermConvertible.unknown.rawValue {
                        //: ViewPushManager.share.func__pushUserVerifyController(toast: nil)
                        ViewPushManager.share.authorSName(toast: nil)
                        //: } else if PositionLabelReactiveCompatible.share.loginUserMode.isTPAuth == TPUserAuth.isOnGoing.rawValue {
                    } else if PositionLabelReactiveCompatible.share.loginUserMode.isTPAuth == CreateVideoTermConvertible.isOnGoing.rawValue {
                        //: let vc = TalkingFinalVerificationVC()
                        let vc = MemoryImageThen()
                        //: ViewPushManager.share.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        ViewPushManager.share.dataModel()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: ViewPushManager.share.func__pushUserVerifyController(toast: nil)
                        ViewPushManager.share.authorSName(toast: nil)
                    }
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<ControlSystemModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: ViewPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    ViewPushManager.share.labelScreen(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(constExplainData))] as? String == (String(appInputConst) + notiToApp.replacingOccurrences(of: "view", with: "m")) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(showPlayerMain) + String(constEndData))][(String(notiPathConst))].intValue == 1 {
                    //: checkAndCallVoice()
                    toPlayer()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    equalVideo()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: TipsMinThen.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! TipsMinThen
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = ArtefactChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = ArtefactChatManager.pull(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(notiGiftMakeUser.replacingOccurrences(of: "style", with: "t"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(notiLengthData) + String(userEqualNoti))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<ControlSystemModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: ViewPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    ViewPushManager.share.labelScreen(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: VideoOnView.self) {
            //: ViewPushManager.share.func__pushToUserDetailVC(uid: targetId)
            ViewPushManager.share.digitalAnalogConverter(uid: targetId)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: DetailReactiveCompatible!) {
    func message(_: LocalScreenTableViewController!, title cell: DetailReactiveCompatible!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: GainReactiveCompatible?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: DataEmptyViewCell.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! DataEmptyViewCell
            //: cellData = imageCell.cellData as! LayerSharedReactiveCompatible
            cellData = imageCell.cellData as! LayerSharedReactiveCompatible
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: OnMsgCell.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! OnMsgCell
            //: cellData = videoCell.cellData as! CradleThen
            cellData = videoCell.cellData as! CradleThen
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: WhiteViewCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! WhiteViewCell
            //: cellData = audioCell.cellData as! ChatRunningReactiveCompatible
            cellData = audioCell.cellData as! ChatRunningReactiveCompatible
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: GainObserverViewCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! GainObserverViewCell
            //: cellData = textCell.cellData as! NumberReactiveCompatible
            cellData = textCell.cellData as! NumberReactiveCompatible
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: MessageThen? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: MessageThen? = SocietalChatManager.sizeSearched(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: CradleThen.self) {
                if reCellData!.isKind(of: CradleThen.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    reportSuccess(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    clickBottom(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: MessageThen!) {
    func alongOf(_: LocalScreenTableViewController!, giftAlong cellData: MessageThen!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = ViewChatManager.disable(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: MessageThen!) {
    func upwardly(_: LocalScreenTableViewController!, motionFor cellData: MessageThen!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        ViewChatManager.nameCamera(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: MessageThen) -> MessageThen {
    private func telephoneMessage(data: MessageThen) -> MessageThen {
        //: let isSelf = data.msgModel.user.uid == Int(PositionLabelReactiveCompatible.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(PositionLabelReactiveCompatible.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if PositionLabelReactiveCompatible.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if PositionLabelReactiveCompatible.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = PositionLabelReactiveCompatible.share.loginUserMode.headPic!
                data.msgModel.user.headPic = PositionLabelReactiveCompatible.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = PositionLabelReactiveCompatible.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = PositionLabelReactiveCompatible.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension LabLiveViewController {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func magnitudeeraction() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: notiTitleUser.reversed(), encoding: .utf8)!: (notiLiveUserMain.replacingOccurrences(of: "reason", with: "x"))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(appColorMain.replacingOccurrences(of: "view", with: "yp")): (String(constOfShow.prefix(6)) + kMenuMain.lowercased())]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(showPlayerMain) + String(constEndData)): msgInfo, String(bytes: showColorData.map{viewTable(content: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = SocietalChatManager.rejoin(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.clickBottom(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func finishResign(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: showViolationK.map{extraName(effect: $0)}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: showViolationK.map{extraName(effect: $0)}, encoding: .utf8)!: content, (String(appPastConst)): greetId, String(bytes: notiTitleUser.reversed(), encoding: .utf8)!: (notiLiveUserMain.replacingOccurrences(of: "reason", with: "x"))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: showNowFilterK.reversed(), encoding: .utf8)!: content, (String(appPastConst)): greetId, (appColorMain.replacingOccurrences(of: "view", with: "yp")): (String(constLeadingShow.prefix(6)))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(showPlayerMain) + String(constEndData)): msgInfo, String(bytes: showColorData.map{viewTable(content: $0)}, encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = SocietalChatManager.rejoin(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.clickBottom(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            VideoObjectProtocol.shared.audio(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.exceptAdd(showMsg: String(bytes: mainAppActionData.map{userSize(seek: $0)}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        ViewChatManager.auditoryImage(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(userPostApp.lowercased())]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: notiTitleUser.reversed(), encoding: .utf8)!: (dataProgressNoti.lowercased()),
                               //: "audioId": greetId,
                               (String(userMainEqualShow.prefix(6)) + notiSizeApp.replacingOccurrences(of: "make", with: "d")): greetId,
                               //: "audioLength": length,
                               String(bytes: showIconK.reversed(), encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (notiPermissionPanShow.lowercased() + "oSandbox"): model.unZipFilePath,
                               //: "senderId": PositionLabelReactiveCompatible.share.loginUserMode.userID]
                               String(bytes: showThoughtEmptyQuickApp.map{nameListLayer(user: $0)}, encoding: .utf8)!: PositionLabelReactiveCompatible.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(userMainEqualShow.prefix(6)) + notiSizeApp.replacingOccurrences(of: "make", with: "d")): greetId,
                                 //: "audioLength": length,
                                 String(bytes: showIconK.reversed(), encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (notiPermissionPanShow.lowercased() + "oSandbox"): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (appColorMain.replacingOccurrences(of: "view", with: "yp")): (String(appCoverShow) + constDismissMain.replacingOccurrences(of: "contact", with: "g"))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(showPlayerMain) + String(constEndData)): msgInfo, String(bytes: showColorData.map{viewTable(content: $0)}, encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = SocietalChatManager.rejoin(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.clickBottom(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func funcGift(msgStr: String, quoteModel: DeadlineModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            exceptAdd(showMsg: (notiDeviceInsideK + "send" + String(mainValueNoti.suffix(6)) + " messa" + showPlayMain.replacingOccurrences(of: "birth", with: "g")).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: showViolationK.map{extraName(effect: $0)}, encoding: .utf8)!: temStr, String(bytes: notiTitleUser.reversed(), encoding: .utf8)!: (notiLiveUserMain.replacingOccurrences(of: "reason", with: "x"))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: showNowFilterK.reversed(), encoding: .utf8)!: temStr, (appColorMain.replacingOccurrences(of: "view", with: "yp")): (String(constLeadingShow.prefix(6)))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(showPlayerMain) + String(constEndData)): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(String(dataLiveByConst))] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(constStartNoti.suffix(8)) + String(dataAddMain))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: showColorData.map{viewTable(content: $0)}, encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = SocietalChatManager.rejoin(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? DeadlineModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            clickBottom(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func betweenBar(img: UIImage, sendModel: BarModel) {
        //: let imagePath = LayerSharedReactiveCompatible.getChatImagePath(image: img)
        let imagePath = LayerSharedReactiveCompatible.exaggerate(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: showColorMain.map{loadBurn(status: $0)}, encoding: .utf8)!: (userFrameApp.replacingOccurrences(of: "name", with: "I") + String(appPushData.suffix(5))), String(bytes: notiTitleUser.reversed(), encoding: .utf8)!: (String(appToK)), String(bytes: notiMakeTitleApp.map{textInput(succeed: $0)}, encoding: .utf8)!: imagePath, String(bytes: userButtonValueMain.map{giftDefine(year: $0)}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(appPlayerNoti.replacingOccurrences(of: "observer", with: "i")): imagePath,
                                        //: "type": "ImgMsg",
                                        (appColorMain.replacingOccurrences(of: "view", with: "yp")): (userFrameApp.replacingOccurrences(of: "name", with: "I") + String(appPushData.suffix(5))),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: kModelNoti.map{succeedUserWhiteCell(error: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(constToMain.prefix(5)) + appViewShow.replacingOccurrences(of: "video", with: "d")): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(showPlayerMain) + String(constEndData)): msgInfo, String(bytes: showColorData.map{viewTable(content: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = SocietalChatManager.rejoin(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            clickBottom(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: MessageThen?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func reportSuccess(msgData: MessageThen?, sendModel: BarModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: showColorMain.map{loadBurn(status: $0)}, encoding: .utf8)!: (String(mainEqualK)), String(bytes: notiTitleUser.reversed(), encoding: .utf8)!: (notiBlockShow.replacingOccurrences(of: "error", with: "o")), String(bytes: userButtonValueMain.map{giftDefine(year: $0)}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(appColorMain.replacingOccurrences(of: "view", with: "yp")): (String(mainEqualK)),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: notiIconDataMain.map{$0^247}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: constColorData.map{$0^43}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: showModelConst.map{sleepingTablet(point: $0)}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: kModelNoti.map{succeedUserWhiteCell(error: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(constToMain.prefix(5)) + appViewShow.replacingOccurrences(of: "video", with: "d")): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(showPlayerMain) + String(constEndData)): msgInfo, String(bytes: showColorData.map{viewTable(content: $0)}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = SocietalChatManager.rejoin(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.title(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.betweenMake(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.minute(cellData, device: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.betweenMake(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.burnTitle(dict, compartment: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        clickBottom(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func funcQueryPiece(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: notiTitleUser.reversed(), encoding: .utf8)!: (dataProgressNoti.lowercased()),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: showIconK.reversed(), encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (notiPermissionPanShow.lowercased() + "oSandbox"): voicePath,
                       //: "senderId": PositionLabelReactiveCompatible.share.loginUserMode.userID]
                       String(bytes: showThoughtEmptyQuickApp.map{nameListLayer(user: $0)}, encoding: .utf8)!: PositionLabelReactiveCompatible.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(notiPermissionPanShow.lowercased() + "oSandbox"): voicePath,
                         //: "type": "AudioMsg",
                         (appColorMain.replacingOccurrences(of: "view", with: "yp")): (String(appCoverShow) + constDismissMain.replacingOccurrences(of: "contact", with: "g")),
                         //: "len": "\(voiceTime)"]
                         String(bytes: showMinData.map{$0^207}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(showPlayerMain) + String(constEndData)): msgInfo, String(bytes: showColorData.map{viewTable(content: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = SocietalChatManager.rejoin(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            clickBottom(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: MessageThen, reSend: Bool) {
    private func clickBottom(msgData: MessageThen, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            exceptAdd(showMsg: constEnvironmentKey)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(mainSendEffectNoti))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: CradleThen.self) == false && msgData.isKind(of: SourceThen.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: CradleThen.self) == false, msgData.isKind(of: SourceThen.self) == false, params[(appColorMain.replacingOccurrences(of: "view", with: "yp"))] as? String != (String(constOfShow.prefix(6)) + kMenuMain.lowercased()) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.title(msgData)
        }
        //: let recordTime = ArtefactChatManager.getMessageInsertTime()
        let recordTime = ArtefactChatManager.giftByTime()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((appPlayerNoti.replacingOccurrences(of: "observer", with: "i"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(appPlayerNoti.replacingOccurrences(of: "observer", with: "i"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(appPlayerNoti.replacingOccurrences(of: "observer", with: "i"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(appColorMain.replacingOccurrences(of: "view", with: "yp"))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(appCoverShow) + constDismissMain.replacingOccurrences(of: "contact", with: "g")) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((notiPermissionPanShow.lowercased() + "oSandbox")) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(notiPermissionPanShow.lowercased() + "oSandbox")] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (notiPermissionPanShow.lowercased() + "oSandbox"))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(notiOnConst.prefix(6)) + "nts/U" + String(kBeanMain.prefix(4)) + dataSuccessUser.replacingOccurrences(of: "add", with: "v") + String(constOfApp))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = dataScreenTitleVersionStr + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(kEqualUser) + String(userManagerData.prefix(7)))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = appPartyStr + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(userMainEqualShow.prefix(6)) + notiSizeApp.replacingOccurrences(of: "make", with: "d"))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(dataProgressNoti.lowercased())] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: TitleRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        TitleRequestTool.priveteFromData(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(constOfShow.prefix(6)) + kMenuMain.lowercased()) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.titleure(msgData, textDevice: nil, camera: NSNumber(value: 0), video: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.boner(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: dataInfoId)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(appPastConst))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    ViewChatManager.auditoryImage(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.size(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(showPlayerMain) + String(constEndData))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(showGroupRegularData.suffix(8)) + constModelNoti.replacingOccurrences(of: "active", with: "e"))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(showGroupRegularData.suffix(8)) + constModelNoti.replacingOccurrences(of: "active", with: "e"))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if MortalLiveManager.viewShared().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: appVersionText, object: nil, userInfo: [String(bytes: notiGiftInfoShow.map{$0^243}, encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.constraintInfo(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(String(mainValueHiddenApp) + String(dataTopK))] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(constOfShow.prefix(6)) + kMenuMain.lowercased()) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: notiTitleUser.reversed(), encoding: .utf8)!] = (String(appCountData.suffix(3)) + String(notiTagConst.prefix(5)))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    constTimeStatusValue.fileStr(eventID: kInfoShowData)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(constOfShow.prefix(6)) + kMenuMain.lowercased()) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: notiTitleUser.reversed(), encoding: .utf8)!] = (notiLiveUserMain.replacingOccurrences(of: "reason", with: "x"))
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(showPlayerMain) + String(constEndData)): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = SocietalChatManager.rejoin(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.title(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(PositionLabelReactiveCompatible.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(PositionLabelReactiveCompatible.share.loginUserMode.userID)" + (String(mainSeaNoti.suffix(6)) + "teCh" + String(mainMakeK.suffix(4)) + "psTitl" + String(notiPlayConst.prefix(2))) + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if PositionLabelReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if PositionLabelReactiveCompatible.share.loginUserMode.sex == PetitionKeyRepresentable.male.rawValue, type != (String(constOfShow.prefix(6)) + kMenuMain.lowercased()) {
                //: self.removeReplyTipsView()
                self.inputBy()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.titleure(updateMsgData, textDevice: msgInfo[(String(notiPathShow))] as? String, camera: msgIncome, video: .Msg_Status_Succ)

            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            ViewChatManager.listRefresh(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(appMakeViewData))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(appMakeViewData))] as! NSNumber
                //: PositionLabelReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                PositionLabelReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((userElementShow.replacingOccurrences(of: "with", with: "t") + "SysMsg")) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(userElementShow.replacingOccurrences(of: "with", with: "t") + "SysMsg")] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.eventStr(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((userTotalMain.replacingOccurrences(of: "index", with: "i") + userPubliclyK.replacingOccurrences(of: "action", with: "n") + "atePhoto")) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(userTotalMain.replacingOccurrences(of: "index", with: "i") + userPubliclyK.replacingOccurrences(of: "action", with: "n") + "atePhoto")] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? SocialModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.burn(of: updateMsgData, layerModel: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((notiBlockShow.replacingOccurrences(of: "error", with: "o"))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(notiBlockShow.replacingOccurrences(of: "error", with: "o"))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? TrafficModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.model(updateMsgData, strength: model)

                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        anima()

        //: updateSession()
        voiceSession()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func voiceSession() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        CurrentViewThen.formatting(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func boner(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard PositionLabelReactiveCompatible.share.loginUserMode.status != 1 else {
            guard PositionLabelReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    exceptAdd(showMsg: errorStr)
                }
                //: return
                return
            }

            //: ViewPushManager.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            ViewPushManager.share.blockRequestEnd(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.showToView()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = AlbescentView(frame: CGRect(x: 0, y: 0, width: userDeviceKey, height: appNameMessage))
            //: view.show()
            view.moreName()
            //: } else if errorCode == 60003, !PositionLabelReactiveCompatible.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !PositionLabelReactiveCompatible.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            WrapThen.shared.discard()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                exceptAdd(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension LabLiveViewController {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func clickTool(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(showPlayerMain) + String(constEndData))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.constraintInfo(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func constituent() {
        //: if self.userWrap.intimate >= PositionLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= PositionLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.worldViewMode(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(PositionLabelReactiveCompatible.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(PositionLabelReactiveCompatible.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (constSizeFormat.array(forKey: showErrorBusyPath)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.phoneDoing()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                constSizeFormat.set(intimateCacheData, forKey: showErrorBusyPath)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func uncolored(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = ArtefactChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = ArtefactChatManager.pull(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(notiGiftMakeUser.replacingOccurrences(of: "style", with: "t"))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(showPlayerMain) + String(constEndData))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(constStatusServerK))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.colorCurrenDisappear(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func constraintInfo(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(constStatusServerK))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(constStatusServerK))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.colorCurrenDisappear(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func colorCurrenDisappear(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = CurrentViewThen.eventId(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= PositionLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= PositionLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.worldViewMode(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.sumimateTotalimate(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.clickSumimate(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.clickSumimate(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        CurrentViewThen.formatTime(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: showSectionPathConst.map{varTime(show: $0)}, encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: dataValueK.reversed(), encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: mainAppValue,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension LabLiveViewController: ImageViewDelegate {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func input() {
        //: guard PositionLabelReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard PositionLabelReactiveCompatible.share.loginUserMode.sex == PetitionKeyRepresentable.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.makeGift(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func tip(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func equalType(actionType: DataInsertActionType) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= PositionLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= PositionLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    telegram()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    digitiserWill()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                telegram()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if PositionLabelReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
            if PositionLabelReactiveCompatible.share.loginUserMode.sex == PetitionKeyRepresentable.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if PositionLabelReactiveCompatible.share.loginUserMode.loungePlus {
                if PositionLabelReactiveCompatible.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= PositionLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= PositionLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.scaleValue(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                digitiserWill()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= PositionLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= PositionLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    allCamera()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    digitiserWill()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                allCamera()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.colorDoing(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func digitiserWill() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.exceptAdd(showMsg: String(bytes: notiImageK.reversed(), encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func tap(giftModel _: GiftMeasurable, giftNum _: String, model _: OfMainHandyJSON) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func devise(giftModel: GiftMeasurable, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        withList(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: SourceThen?, fromStr: String?) {
    func withList(giftModel: GiftMeasurable, giftNum: String, cellData: SourceThen?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            exceptAdd(showMsg: constEnvironmentKey)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(mainSendEffectNoti))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(constToMain.prefix(5)) + appViewShow.replacingOccurrences(of: "video", with: "d"))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(notiFilterApp) + mainValueUser.replacingOccurrences(of: "make", with: "m"))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(notiLabK.suffix(7)) + String(appWithCenterShow.prefix(5)))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(String(showManagerSizeUser))] = fromStr
        }
        // 送礼接口
        //: TitleRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        TitleRequestTool.targetCompletion(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.boner(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: dataInputCameraKey)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.sumerest(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.fileMake(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.voiceSession()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(showPlayerMain) + String(constEndData))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(constStatusServerK))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(constStatusServerK))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.colorCurrenDisappear(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.last(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                constTimeStatusValue.fileStr(eventID: dataExploreErrValue)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func tag(giftModel: GiftMeasurable, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        constTimeStatusValue.fileStr(eventID: notiDomainKey)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            exceptAdd(showMsg: constEnvironmentKey)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= PositionLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= PositionLabelReactiveCompatible.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            digitiserWill()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(notiCollectShow) + String(notiItemMain.suffix(8)))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(appColorMain.replacingOccurrences(of: "view", with: "yp"))] = (String(notiGiftLayerApp.suffix(5)) + "iftMs" + String(mainHiddenUser))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: notiTitleUser.reversed(), encoding: .utf8)!: (String(appCountData.suffix(3)) + String(notiTagConst.prefix(5)))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(showPlayerMain) + String(constEndData)): msgInfo, String(bytes: showColorData.map{viewTable(content: $0)}, encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = SocietalChatManager.rejoin(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            clickBottom(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func modify(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(mainSelectImageName - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.heel(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func sizeSearch(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.heel(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func sizeLive(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.sizeLive(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                modelAnimation(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func bid() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if StateBankManagerPlayerDelegate.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            StateBankManagerPlayerDelegate.shared.allEqual()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension LabLiveViewController {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func fileMake(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(showPlayerMain) + String(constEndData))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: notiTitleUser.reversed(), encoding: .utf8)!] = (appTableRequestUser.lowercased())
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(showPlayerMain) + String(constEndData))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = SocietalChatManager.rejoin(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.title(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(showGroupRegularData.suffix(8)) + constModelNoti.replacingOccurrences(of: "active", with: "e"))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(showGroupRegularData.suffix(8)) + constModelNoti.replacingOccurrences(of: "active", with: "e"))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.titleure(cellData, textDevice: msgInfoDic[(String(notiPathShow))] as? String, camera: msgIncome, video: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        currentDic(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(appMakeViewData))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(appMakeViewData))] as! NSNumber
            //: PositionLabelReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            PositionLabelReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: PositionLabelReactiveCompatible.share.loginUserMode.mf_coin)
            msgInputView.giftView.imageEnable(needReload: false, mf_coin: PositionLabelReactiveCompatible.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func manager() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.levelId(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func currentDic(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.connectSize(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func object(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.instrumentality(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func selectGlomClout() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension LabLiveViewController {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func toPlayer() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.phonationFor { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.voiceSession()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func equalVideo() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.outputGift { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.voiceSession()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension LabLiveViewController {
    //: @objc private func feedbackBtnClick() {
    @objc private func feedbackWithColor() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = CapablenessViewDelegate()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        viewController()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func equalFile() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = ImagePopReactiveCompatible(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.compartmentList(cellTitleList: [(String(dataViewMain.suffix(4)) + " info").localized, (String(showEdgeK.suffix(5))).localized, (String(mainCostData)).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .read()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: ViewPushManager.share.func__pushToUserDetailVC(uid: self.targetId)
                ViewPushManager.share.digitalAnalogConverter(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                principal()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = PositionTempAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.nameTo(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func principal() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = PrivateViewDelegate()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.itemMini(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.local(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func panoramicView() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            modelAnimation(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            viewController()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func jumpNotification(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = CurrentScreenMeasurable.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.imageLoad(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension LabLiveViewController {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func toNotification() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(makeNotification(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: appLiveFormat,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(funcPlayListReload),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: dataDeviceBottomValue,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func makeNotification(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard PositionLabelReactiveCompatible.share.loginUserMode.loungePlus == true else { return }
        guard PositionLabelReactiveCompatible.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func funcPlayListReload() {
        //: PositionLabelReactiveCompatible.share.loginUserMode.loungePlus = true
        PositionLabelReactiveCompatible.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func colorMsg() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? MessageThen else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? MessageThen else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if ViewChatManager.theoreticalAccount(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.addCannulize(tipStr: (String(appCenterK.prefix(6)) + "ead rec" + String(appAppearConst.prefix(6))).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension LabLiveViewController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func telegram() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        TimingThen.someFunc(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = TempThen.single(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.firstImg(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func groupInstance() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        TimingThen.statusBlock(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.firstImg(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func firstImg(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: PositionLabelReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else {
            PositionLabelReactiveCompatible.share.appStatus == ValueHashableRepresentation.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = BarModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.betweenBar(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = PhotosViewDelegate(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = BarModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.betweenBar(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        viewController()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension LabLiveViewController {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func allCamera() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard ViewChatManager.selectAdd(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == PetitionKeyRepresentable.male.rawValue ? "He".localized : (String(showDeleteApp.prefix(3))).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.exceptAdd(showMsg: (String(constNetworkPriceApp.suffix(4)) + "s usin" + String(constTransitionUser)).targetPost(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.barBy()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func barBy() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = TempThen.single(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            TempThen.wombTombGarment(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.compressedLikeVideo(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.exceptAdd(showMsg: (String(showEndConst) + String(kAddApp)).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func compressedLikeVideo(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: VideoTalkingEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = GiftViewController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = BarModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.reportSuccess(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        WrapView.livePic()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        viewController()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: MessageThen, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func betweenMake(msgData: MessageThen, sendModel: BarModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        StateEqualSignatureProvider.shared.shopwindow(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.exceptAdd(showMsg: (String(constEqualK.prefix(7)) + kAtDataNoti.replacingOccurrences(of: "create", with: "i")).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.titleure(msgData, textDevice: nil, camera: NSNumber(value: 0), video: .Msg_Status_Fail)
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.reportSuccess(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension LabLiveViewController {
    //: func func__addVoiceNotification() {
    func subsequently() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(atActive),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(countBack),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(equilateralPic(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(subUp(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: kInfoShowNetUrl,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(uncolored(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: kScreenDiskValue,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func viewContainer(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            StateBankManagerPlayerDelegate.shared.nameStatus()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            StateBankManagerPlayerDelegate.shared.fleetAt()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func atActive() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        viewContainer(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func countBack() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        viewContainer(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func equilateralPic(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            viewContainer(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            viewContainer(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: ChatRunningReactiveCompatible, cellView: TalkingChatAudioMsgCell) {
    func no(cellData: ChatRunningReactiveCompatible, cellView: WhiteViewCell) {
        //: guard TalkingSocketManager.shared.isShared == false else {
        guard CurrentSocketManager.shared.isShared == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.exceptAdd(showMsg: mainStatusId)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? MessageThen)!.isKind(of: ChatRunningReactiveCompatible.self) == false {
            if (indexModel as? MessageThen)!.isKind(of: ChatRunningReactiveCompatible.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! ChatRunningReactiveCompatible
            let audioModel = indexModel as! ChatRunningReactiveCompatible
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = CurrentViewThen.getMessage(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        StateBankManagerPlayerDelegate.shared.allEqual()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if StateBankManagerPlayerDelegate.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (appCellK) + "\(StateBankManagerPlayerDelegate.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if StateBankManagerPlayerDelegate.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                StateBankManagerPlayerDelegate.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        StateBankManagerPlayerDelegate.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        StateBankManagerPlayerDelegate.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: MessageThen = self.msgView.uiMsgs.object(at: index) as! MessageThen
                let subModel: MessageThen = self.msgView.uiMsgs.object(at: index) as! MessageThen
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: ChatRunningReactiveCompatible.self) {
                if subModel.isKind(of: ChatRunningReactiveCompatible.self) {
                    //: let audioModel = subModel as! ChatRunningReactiveCompatible
                    let audioModel = subModel as! ChatRunningReactiveCompatible
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = CurrentViewThen.getMessage(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    AudileReactiveCompatible.awakeObject(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        StateBankManagerPlayerDelegate.shared.versatileViewTouchTo(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension LabLiveViewController {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func dataAppear() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.addWrap()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.finishResign(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? MessageThen
                let indexModel = model as? MessageThen
                //: if indexModel?.isKind(of: ChatRunningReactiveCompatible.self) == true || indexModel?.isKind(of: LayerSharedReactiveCompatible.self) == true || indexModel?.isKind(of: NumberReactiveCompatible.self) == true || indexModel?.isKind(of: CradleThen.self) == true {
                if indexModel?.isKind(of: ChatRunningReactiveCompatible.self) == true || indexModel?.isKind(of: LayerSharedReactiveCompatible.self) == true || indexModel?.isKind(of: NumberReactiveCompatible.self) == true || indexModel?.isKind(of: CradleThen.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.anima()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.appearanceView()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.colorMsg()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func addWrap() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.degas(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.dropDeal(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.constituent()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == PositionLabelReactiveCompatible.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == PositionLabelReactiveCompatible.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == PositionLabelReactiveCompatible.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == PositionLabelReactiveCompatible.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.local(userWrap)
    }

    //: func func__getFillDraftText() {
    func polePosition() {
        //: let userID = "c2c_"+targetId
        let userID = (String(mainUserData.prefix(4))) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.input()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func wideAwakeTo() {
        //: let userID = "c2c_"+targetId
        let userID = (String(mainUserData.prefix(4))) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension LabLiveViewController {
    //: func func__TipsNotification() {
    func design() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(buildNotificationTips(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: constListId,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func buildNotificationTips(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: PhotoGainCellData = userinfo[(String(userAppImageK))] as! PhotoGainCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.heel(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func addCannulize(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: showViolationK.map{extraName(effect: $0)}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: notiTitleUser.reversed(), encoding: .utf8)!: (mainTapConst.replacingOccurrences(of: "net", with: "ps"))]
        //: var message: MessageThen?
        var message: MessageThen?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = SocietalChatManager.mail(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = SocietalChatManager.rejoin(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.heel(true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func appearanceView() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        ViewChatManager.referencePointHandler { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.addCannulize(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func eventStr(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.addCannulize(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func anima() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        ViewChatManager.compound(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.addCannulize(tipStr: String(bytes: notiCenterApp.map{$0^41}, encoding: .utf8)!.targetPost(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func subUp(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: MessageThen = dict?["cellData"] as! MessageThen
        let cellData: MessageThen = dict?[(String(constMoveCornerNoti.suffix(8)))] as! MessageThen
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.heel(true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension LabLiveViewController: SouvenirThen {
    //: func func__setReplyTipsView() {
    func say() {
        //: let key =  "\(PositionLabelReactiveCompatible.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(PositionLabelReactiveCompatible.share.loginUserMode.userID)" + (String(mainSeaNoti.suffix(6)) + "teCh" + String(mainMakeK.suffix(4)) + "psTitl" + String(notiPlayConst.prefix(2))) + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && PositionLabelReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && PositionLabelReactiveCompatible.share.appStatus == ValueHashableRepresentation.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.planOfAction()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            outpost(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            inputBy()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func nameSystem(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        funcGift(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        inputBy()
    }

    //: func removeReplyTipsView() {
    func inputBy() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            outpost(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension LabLiveViewController {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func toAnimation() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: constConversationData, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: appNameMessage - mainSelectImageName, width: userDeviceKey, height: appNameMessage)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: userDeviceKey,
                                     //: height: ScreenHeight)
                                     height: appNameMessage)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func modelAnimation(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.underTitle() {
            //: if TalkingVoiceRoomManager.shared().isParty == true {
            if LoungeEventHandler.imageShared().isParty == true {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: notiFailFormat, object: nil, userInfo: nil)
            }
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: MinViewDelegate.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: appNameMessage - mainSelectImageName,
                                     //: width: ScreenWidth,
                                     width: userDeviceKey,
                                     //: height: ScreenHeight)
                                     height: appNameMessage)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
            //: if TalkingVoiceRoomManager.shared().isParty == false {
            if LoungeEventHandler.imageShared().isParty == false {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: notiFailFormat, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension LabLiveViewController {
    /// 创建UI
    //: private func createUI() {
    private func mention() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(showShareAppUser.prefix(6)) + " noti" + notiActionMain.lowercased()).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func tack() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? mainSelectImageName : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.willRadii(width: contentView.width,
                                  //: height: contentView.height,
                                  height: contentView.height,
                                  //: corners: [.topLeft, .topRight],
                                  corners: [.topLeft, .topRight],
                                  //: cornerRadii: .init(width: 8, height: 8))
                                  cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? kButtonPath : constFailAppData)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + showNameData)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? kButtonPath : constFailAppData)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + showNameData)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + showNameData)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func join() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.panoramicView()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.equalFile()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.equalType(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                constTimeStatusValue.fileStr(eventID: mainTransformStr, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
