
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let constMessageMain:String = "var return like count stringIM退出"
fileprivate let notiFeeConst:String = "ode:manager return effect show self"

/*: , desc: :*/
fileprivate let notiEqualApp:String = ", desc:user right make add name"

/*: ." :*/
fileprivate let showNearK:String = "."

/*: "IM登录失败，code: :*/
fileprivate let kOthersUser:String = "IM登\u{5f55}失"
fileprivate let showFirstContentApp:[Character] = ["败","，","c","o","d","e",":"]

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let notiIndexTapData:[Character] = ["I","M","获","取"]
fileprivate let constMediumNoti:String = "未读消\u{606f}总"

/*: "crushBadge" :*/
fileprivate let kDoingUser:[Character] = ["c","r","u","s"]
fileprivate let dataGiftConst:String = "hBadgeequal height why day title"

/*: "badNumber" :*/
fileprivate let showLiveConst:[UInt8] = [0x8a,0x89,0x8c,0xa6,0x9d,0x85,0x8a,0x8d,0x9a]

private func viewWhite(content num: UInt8) -> UInt8 {
    return num ^ 232
}

/*: "plaza" :*/
fileprivate let dataHideShow:[Character] = ["p","l","a","z","a"]

/*: "roomNum" :*/
fileprivate let notiProcedureMain:String = "content trueroomNum"

/*: "num" :*/
fileprivate let showTabTotalMain:[Character] = ["n","u","m"]

/*: "onlyOneRoomId" :*/
fileprivate let mainAutomaticArrayUser:String = "onllab"
fileprivate let userToK:String = "oomIdmodel catch manager nor true"

/*: "LIVE-CHAT" :*/
fileprivate let appInputConst:[Character] = ["L","I","V","E","-","C"]
fileprivate let appMakeShow:String = "rawT"

/*: "PARTY-CHAT" :*/
fileprivate let dataFilterShow:[Character] = ["P","A","R","T","Y","-","C","H","A"]
fileprivate let appTimeShow:String = "temp"

/*: "extra" :*/
fileprivate let showToTapMain:String = "extrbar"

/*: "msgType" :*/
fileprivate let userImageNoti:[Character] = ["m","s","g","T","y","p","e"]

/*: "newPkgGift" :*/
fileprivate let constModelIndexUser:String = "section uniform for librarynewP"

/*: "MF:ChatMentionMsg" :*/
fileprivate let dataDetailConst:[Character] = ["M","F",":","C","h","a","t","M","e"]
fileprivate let userStyleNoti:[Character] = ["n","t"]
fileprivate let appErrorShow:[Character] = ["i","o","n","M","s","g"]

/*: "uid" :*/
fileprivate let showColorData:[UInt8] = [0x84,0x98,0x95]

private func contentPush(start num: UInt8) -> UInt8 {
    return num ^ 241
}

/*: "onlineStatus" :*/
fileprivate let kStyleCellData:[UInt8] = [0x77,0x76,0x74,0x71,0x76,0x7d,0x4b,0x6c,0x79,0x6c,0x6d,0x6b]

/*: "isNewUser" :*/
fileprivate let kAcceptGiftConst:[UInt8] = [0x92,0x88,0xb5,0x9e,0x8c,0xae,0x88,0x9e,0x89]

private func dataTo(change num: UInt8) -> UInt8 {
    return num ^ 251
}

/*: "c2c_ :*/
fileprivate let kCenterApp:[Character] = ["c","2","c","_"]

/*: "IM获取会话失败，convId: :*/
fileprivate let kSumConst:String = "IM\u{83b7}取会话"
fileprivate let mainVoiceWhiteShow:String = "nvId:left make interaction class"

/*: , code: :*/
fileprivate let kCellMain:String = "app collect, code:"

/*: "IM插入消息失败，targetId: :*/
fileprivate let appManagerNoti:String = "IM\u{63d2}入消"
fileprivate let dataStyleHiddenUser:[Character] = ["息","\u{5931}","败","，","t","a","r","g"]
fileprivate let kFirstShow:String = "etId:m first size hide model"

/*: "changeDirection" :*/
fileprivate let constSizeShow:String = "chanback"
fileprivate let kAppTitleNoti:[Character] = ["o","n"]

/*: "rYMsgType" :*/
fileprivate let mainModelColorApp:String = "if aspectrYMsgT"
fileprivate let mainShowUser:String = "YPE"

/*: "GJ:CallCustom" :*/
fileprivate let userHiddenK:[Character] = ["G","J",":","C","a","l"]
fileprivate let mainReadK:[Character] = ["l","C","u","s","t","o","m"]

/*: "IM获取会话列表失败，code: :*/
fileprivate let kSumAddMain:String = "IM获取index color var video"
fileprivate let kLiveMain:String = "，code:error professional if succeed camera"

/*: "msgInfo" :*/
fileprivate let userCreateApp:String = "msgInfoself var"

/*: "needFold" :*/
fileprivate let constValueRefreshUser:String = "needFview if"
fileprivate let appTopConst:String = "blockld"

/*: "group_ :*/
fileprivate let showMoreK:String = "type manager add apply rangegroup_"

/*: "IM删除会话失败，convID: :*/
fileprivate let appTentRequestUser:[Character] = ["I","M","\u{5220}","除","会","话","失","败","\u{ff0c}","c","o","n","v","I","D"]
fileprivate let mainModelUser:String = ":"

/*: "IM置顶会话失败，convId: :*/
fileprivate let dataTimeApp:String = "video business asIM置顶会话"
fileprivate let userObjectK:String = "label equalnvId"
fileprivate let notiTableK:String = "action"

/*: "SDK 正在连接到服务器" :*/
fileprivate let userViewApp:String = "rank selfSDK 正在连"
fileprivate let dataModelUser:[Character] = ["接","到","服","务","器"]

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let constPathLabelK:String = "like beyond manager layerSDK 已"
fileprivate let mainTextNoti:String = "到服务器"

/*: "IM连接服务器失败，code: :*/
fileprivate let dataAllApp:String = "IM\u{8fde}接\u{670d}"
fileprivate let showVideoFormalViewConst:String = "，codeself else bottom false left"
fileprivate let mainTooData:String = ":"

/*: , error: :*/
fileprivate let kShowConst:String = "color shared app output, er"

/*: "IM账号被挤下线。" :*/
fileprivate let userStatusData:[Character] = ["I","M","账","号","被","挤","下","线","\u{3002}"]

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let dataAfterK:[UInt8] = []

/*: "Offline Notification" :*/
fileprivate let kVideoApp:[Character] = ["O","f","f","l","i","n","e"," ","N","o","t","i","f","i","c"]
fileprivate let appActionEmptyBackgroundMain:[Character] = ["a","t","i","o","n"]

/*: "Cancel" :*/
fileprivate let userMessageShow:[Character] = ["C","a","n","c","e","l"]

/*: "Re Login" :*/
fileprivate let notiGreenPostK:String = "Re Logsucceed path center index"
fileprivate let notiAreaApp:String = "ipath"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TabThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol StateManagerDelegate: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func sessionMailingList(data: [FrameReactiveCompatible])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func addTime(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func revolutionaryFinish(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum LabDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class TabThen: NSObject {
@objcMembers public class TabThen: NSObject {
    //: public static let shared = TabThen()
    public static let shared = TabThen()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [FrameReactiveCompatible] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [FrameReactiveCompatible] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [FrameReactiveCompatible] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [FrameReactiveCompatible] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [FrameReactiveCompatible] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: FrameReactiveCompatible]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.nameDoing()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func orientationChange() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(appFirstTitle, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func nameDoing() {
        //: func__removeAllDelegate()
        byDelegate()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.fromThan()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func fromThan() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = FrameReactiveCompatible.outMethod(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = FrameReactiveCompatible.outMethod(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func enableicialControl() -> [FrameReactiveCompatible] {
        // 审核模式，在消息列表移除官方客服
        //: if PositionLabelReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue {
        if PositionLabelReactiveCompatible.share.appStatus == ValueHashableRepresentation.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == RowMacroDefine.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == RowMacroDefine.menagerie() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension TabThen {
extension TabThen {
    //: func func__removeAllDelegate() {
    func byDelegate() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func valueNeed(_ delegate: StateManagerDelegate) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func permit(_ delegate: StateManagerDelegate) {
        //: self.func__invokeDelegate {
        self.cling {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func cling(_ invocation: (StateManagerDelegate) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! StateManagerDelegate)
        }
    }
}

// MARK: - IM登录态管理

//: extension TabThen {
extension TabThen {
    /// 退出登录IM
    //: func func__LogingOut() {
    func toOut() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            NameReactiveCompatible.trademarkIndex(msg: (String(constMessageMain.suffix(4)) + "\u{767b}录\u{5931}败\u{ff0c}c" + String(notiFeeConst.prefix(4))) + "\(code)" + (String(notiEqualApp.prefix(7))) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.nameDoing()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func atFunc(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = PositionLabelReactiveCompatible.share.appUserConfigMode.uSig
        let uSigStr = PositionLabelReactiveCompatible.share.appUserConfigMode.uSig
        //: let uidStr = PositionLabelReactiveCompatible.share.loginUserMode.userID
        let uidStr = PositionLabelReactiveCompatible.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.tillStar()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            NameReactiveCompatible.trademarkIndex(msg: (kOthersUser + String(showFirstContentApp)) + "\(code)" + (String(notiEqualApp.prefix(7))) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func representationList() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || PositionLabelReactiveCompatible.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || PositionLabelReactiveCompatible.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.exceptAdd(showMsg: constEnvironmentKey)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension TabThen {
extension TabThen {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func tillStar() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
            NameReactiveCompatible.trademarkIndex(msg: (String(notiIndexTapData) + constMediumNoti + "\u{6570}失败，code:") + "\(code)" + (String(notiEqualApp.prefix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func level() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: TitleRequestTool.req_sysMessageList { _, result in
        TitleRequestTool.cookie { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(String(kDoingUser) + String(dataGiftConst.prefix(6)))] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: constDomainValue, object: self, userInfo: [String(bytes: showLiveConst.map{viewWhite(content: $0)}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func benefit(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: TitleRequestTool.req_getChatRoomInfo { succeed, result, _ in
        TitleRequestTool.allShared { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(String(dataHideShow))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(notiProcedureMain.suffix(7)))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = FrameReactiveCompatible.outMethod(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(String(showTabTotalMain))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(mainAutomaticArrayUser.replacingOccurrences(of: "lab", with: "y") + "OneR" + String(userToK.prefix(5)))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension TabThen: V2TIMAdvancedMsgListener {
extension TabThen: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT") || msg.groupID.contains("PARTY-CHAT")) { // 开播/语聊房 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(appInputConst) + appMakeShow.replacingOccurrences(of: "raw", with: "HA"))) || msg.groupID.contains((String(dataFilterShow) + appTimeShow.replacingOccurrences(of: "temp", with: "T")))) { // 开播/语聊房 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            ToponymThen.clickWith().rawDataCorner(msg: msg)
            //: return
            return
        }
        //: if msg.sender == PositionLabelReactiveCompatible.share.loginUserMode.userID { return }
        if msg.sender == PositionLabelReactiveCompatible.share.loginUserMode.userID { return }

        //: let dic = ArtefactChatManager.parseTXMessageData(data: msg.customElem.data)
        let dic = ArtefactChatManager.pull(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(showToTapMain.replacingOccurrences(of: "bar", with: "a"))] as! [AnyHashable: Any]
        //: let msgModel = try? AbTalkingChatMsgBaseModel(dictionary: extraDic)
        let msgModel = try? TodayModel(dictionary: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: kScreenDiskValue, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(userImageNoti))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(constModelIndexUser.suffix(4)) + "kgGift") { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: kNamePath, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if ArtefactChatManager.checkVoiceMessage(message: msg) {
            if ArtefactChatManager.topFinish(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = SocietalChatManager.atImage(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                AudileReactiveCompatible.dismissDb(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.detailEqual(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            SharePlayerThen.shared.equalShow(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel?.msgInfo.messageType == .MessageTypeGift {
        if msgModel?.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            SharePlayerThen.shared.chat(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(dataDetailConst) + String(userStyleNoti) + String(appErrorShow)) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            SchemeErrorThen.share.showIn(msg: msg, extraInfo: extraDic)
        }

        //: if msgModel?.gift.unlockMsgId.count ?? 0 > 0 {
        if msgModel?.gift.unlockMsgId.count ?? 0 > 0 { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: ViewChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel!.gift.unlockMsgId)
            array.append(msgModel!.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: ViewChatManager.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != RowMacroDefine.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != RowMacroDefine.tillAge() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.cling { $0.revolutionaryFinish?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            MinReactiveCompatible.shared.dominant(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: showColorData.map{contentPush(start: $0)}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: kStyleCellData.map{$0^24}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                                String(bytes: kAcceptGiftConst.map{dataTo(change: $0)}, encoding: .utf8)!: msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: constErrorMinMessage,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            CurrentViewThen.deal(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            ViewChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: appLiveFormat, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: MessageThen, isPrivete: Bool) {
    @objc public func appearMake(targetId: String, msgCellData: MessageThen, isPrivete: Bool) {
        //: if let imMsg = msgCellData.innerMessage {
        if let imMsg = msgCellData.innerMessage {
            //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
            self.half(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        }
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(kCenterApp)) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.panFor(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.priceList(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
                NameReactiveCompatible.trademarkIndex(msg: (kSumConst + "\u{5931}败，co" + String(mainVoiceWhiteShow.prefix(5))) + "\(convId)" + (String(kCellMain.suffix(7))) + "\(code)" + (String(notiEqualApp.prefix(7))) + "\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func half(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = PositionLabelReactiveCompatible.share.loginUserMode.userID
        let senderStr = PositionLabelReactiveCompatible.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
                NameReactiveCompatible.trademarkIndex(msg: (appManagerNoti + String(dataStyleHiddenUser) + String(kFirstShow.prefix(5))) + "\(targetId)" + (String(kCellMain.suffix(7))) + "\(code)" + (String(notiEqualApp.prefix(7))) + "\(String(describing: desc)).")
            }
        }
    }

    func getString() -> String {
        return (String(userHiddenK) + String(mainReadK))
    }
    
    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func detailEqual(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(constSizeShow.replacingOccurrences(of: "back", with: "g") + "eDirecti" + String(kAppTitleNoti))].boolValue
        //: if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && ViewPushManager.share.func__getCurrentActivityVC()!.isKind(of: TalkingPrivateChatController.self) == false {
        if jsonDic[("rY"+"MsgType")].stringValue == getString() && isSelf && ViewPushManager.share.dataModel()!.isKind(of: LabLiveViewController.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: dataTitlePath) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: dataTitlePath)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension TabThen: V2TIMConversationListener {
extension TabThen: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.panFor(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.priceList(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.panFor(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.priceList(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: PositionLabelReactiveCompatible.share.unreadMessageNum = Int(totalUnreadCount)
        PositionLabelReactiveCompatible.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.cling {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.addTime?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func priceList(conVersationList: [FrameReactiveCompatible]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if CurrentViewThen.containerShared().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                CurrentViewThen.containerShared().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.cling {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.sessionMailingList?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func fitNext(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [FrameReactiveCompatible] = []

        // V2TIMConversation->FrameReactiveCompatible
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = FrameReactiveCompatible(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.edit(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [FrameReactiveCompatible] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = TitleInfoTable.terms(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.face(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension TabThen {
extension TabThen {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func rootCompletion(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.fitNext(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.priceList(conVersationList: arr as? [FrameReactiveCompatible] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
            NameReactiveCompatible.trademarkIndex(msg: (String(kSumAddMain.prefix(4)) + "会话列表失败" + String(kLiveMain.prefix(6))) + "\(code)" + (String(notiEqualApp.prefix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func imageResume(targetId: String) -> FrameReactiveCompatible? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func panFor(convList: [V2TIMConversation]) -> [FrameReactiveCompatible] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [FrameReactiveCompatible] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = FrameReactiveCompatible(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.edit(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.face(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func face(convModel: FrameReactiveCompatible, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.fromThan()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: LabDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - PositionLabelReactiveCompatible.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - PositionLabelReactiveCompatible.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = CurrentViewThen.eventId(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = ArtefactChatManager.parseTXMessageData(data: elemData)
            let dict = ArtefactChatManager.pull(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(showToTapMain.replacingOccurrences(of: "bar", with: "a"))][(String(userCreateApp.prefix(7)))][(String(constValueRefreshUser.prefix(5)) + appTopConst.replacingOccurrences(of: "block", with: "o"))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.accountingDataEnable(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.accountingDataEnable(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func accountingDataEnable(convList: [FrameReactiveCompatible], dataType: LabDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func poolPlayer(targetId: String, dataType: LabDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(kCenterApp)) + "\(targetId)" : (String(showMoreK.suffix(6))) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            CurrentViewThen.illegitimacy(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(PositionLabelReactiveCompatible.share.loginUserMode.userID, toUid: targetId)
            AudileReactiveCompatible.ok(PositionLabelReactiveCompatible.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.tillStar()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
            self.writeData(convID: convID, code: code, desc: desc)
        }
    }
    
    func writeData(convID: String, code: Int32, desc: String?) {
        NameReactiveCompatible.trademarkIndex(msg: (String(appTentRequestUser) + mainModelUser.capitalized) + "\(convID)" + (String(kCellMain.suffix(7))) + "\(code)" + (String(notiEqualApp.prefix(7))) + "\(String(describing: desc)).")
    }

    func getString(convModel: FrameReactiveCompatible, code: Int32, desc: String?) {
        NameReactiveCompatible.trademarkIndex(msg: (String(dataTimeApp.suffix(6)) + "失败，co" + String(userObjectK.suffix(4)) + notiTableK.replacingOccurrences(of: "action", with: ":")) + "\(convModel.conversationID)" + (String(kCellMain.suffix(7))) + "\(code)" + (String(notiEqualApp.prefix(7))) + "\(String(describing: desc)).")
    }
    
    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func atEqual(convModel: FrameReactiveCompatible, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { [self] code, desc in
            //: UploadLogTool.writeLog(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
            getString(convModel: convModel, code: code, desc: desc)
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.accountingDataEnable(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.accountingDataEnable(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension TabThen: V2TIMSDKListener {
extension TabThen: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(userViewApp.suffix(7)) + String(dataModelUser)))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(constPathLabelK.suffix(5)) + "经成功连接" + mainTextNoti.capitalized))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        NameReactiveCompatible.trademarkIndex(msg: (dataAllApp + "务器\u{5931}败" + String(showVideoFormalViewConst.prefix(5)) + mainTooData.capitalized) + "\(code)" + (String(kShowConst.suffix(4)) + "ror:") + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        NameReactiveCompatible.trademarkIndex(msg: (String(userStatusData)))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        CurrentSocketManager.shared.periodicTableWindow(userLoginOut: true)
        //: TabThen.shared.isConnection = false
        TabThen.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (PathKingfisherCompatible.shared as! PathKingfisherCompatible).listDestroy()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: dataAfterK.reversed(), encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = ShapeAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if UpToDatenessReactiveCompatible.share.interfaceLang == DigitiserScalarLiteral.ar.rawValue || UpToDatenessReactiveCompatible.share.interfaceLang == DigitiserScalarLiteral.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            UpToDatenessReactiveCompatible.share.interfaceLang == DigitiserScalarLiteral.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.liveBackground(fontSize: 13)
            config.rightFont = UIFont.liveBackground(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        StirReactiveCompatible.leftConfig()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        StirReactiveCompatible.leftConfig(title: (String(kVideoApp) + String(appActionEmptyBackgroundMain)).localized, message: msg, leftBtnTitle: (String(userMessageShow)).localized, rightBtnTitle: (String(notiGreenPostK.prefix(6)) + notiAreaApp.replacingOccurrences(of: "path", with: "n")).localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            MortalLiveManager.viewShared().appearShow()
            // 销毁语聊房
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            LoungeEventHandler.imageShared().toResource()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            RowThen.adjust { t in
                //: ProgressHUD.dismiss()
                WrapView.livePic()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: appDisplayKey, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            RowThen.reloginView { t in
                //: if t {
                if t {
                    //: TabThen.shared.func__LogingIn { _ in
                    TabThen.shared.atFunc { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if MortalLiveManager.viewShared().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(MortalLiveManager.viewShared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    CurrentSocketManager.shared.capture()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
