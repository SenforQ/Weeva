
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let showAddNoti:[UInt8] = [0xa,0xf,0xa,0x15,0xc9,0x4,0x10,0x5,0x6,0x13,0xdb,0xca,0xc1,0x9,0x2,0x14,0xc1,0xf,0x10,0x15,0xc1,0x3,0x6,0x6,0xf,0xc1,0xa,0xe,0x11,0xd,0x6,0xe,0x6,0xf,0x15,0x6,0x5]

fileprivate func equalYou(show num: UInt8) -> UInt8 {
    let value = Int(num) - 161
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}
 
/*: "TalkingChatImageMsgCellID" :*/
fileprivate let appModeNoti:String = "add view player statusTalk"
fileprivate let showRecordConst:String = "hatImagindex else"
fileprivate let kFilePlayerData:[Character] = ["e","M","s","g"]
fileprivate let showValueApp:String = "CellIDerror element layer let exist"
 
/*: "TalkingChatTipsMsgCellID" :*/
fileprivate let kViewMarginSectionUser:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","T","i"]
fileprivate let appTalkUser:String = "to image selfpsMsg"

/*: "TSystemMessageCell_ReuseId" :*/
fileprivate let constTitleK:String = "return bindTSyste"
fileprivate let notiPicViewK:String = "ageCeelse path view var remind"
fileprivate let dataModeCornerTipMain:String = "pathuspath"
fileprivate let appAppearLeadingConst:[Character] = ["I","d"]

/*: "roomId" :*/
fileprivate let constMessageShow:String = "roomIdmodel white as var the"

/*: "type" :*/
fileprivate let userTableContentK:[UInt8] = [0xc,0x1,0x8,0x1d]

private func nameUp(fail num: UInt8) -> UInt8 {
    return num ^ 120
}

/*: "extra" :*/
fileprivate let constLabToNoti:[Character] = ["e","x","t","r","a"]

/*: "jumpKey" :*/
fileprivate let userLayerData:[UInt8] = [0x41,0x4c,0x44,0x47,0x22,0x3c,0x50]

fileprivate func fullPhaseOfTheMoon(color num: UInt8) -> UInt8 {
    let value = Int(num) - 215
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Send Gift" :*/
fileprivate let kTimeNoti:[Character] = ["S","e","n","d"," "]
fileprivate let mainPathNoti:[Character] = ["G","i","f","t"]

/*: "View info" :*/
fileprivate let dataBackgroundApp:String = "let text voice thanView info"

/*: "Report" :*/
fileprivate let kItemApp:String = "icon source label guard priceReport"

/*: "uid" :*/
fileprivate let kQuoteShow:[UInt8] = [0x81,0x9d,0x90]

private func indexLabel(equal num: UInt8) -> UInt8 {
    return num ^ 244
}

/*: "name" :*/
fileprivate let constActualNoti:[UInt8] = [0x55,0x48,0x54,0x4c]

fileprivate func upgradeBlock(video num: UInt8) -> UInt8 {
    let value = Int(num) + 25
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Can't send blank message" :*/
fileprivate let constLeadingMain:[Character] = ["C","a","n","\'","t"," ","s","e","n","d"," ","b","l","a","n","k"," ","m","e","s","s","a"]
fileprivate let kModelNorUser:String = "social"

/*: "user" :*/
fileprivate let mainAddNoti:String = "contentser"

/*: "content" :*/
fileprivate let showMakeMain:[UInt8] = [0x4b,0x47,0x46,0x5c,0x4d,0x46,0x5c]

private func screenRelease(path num: UInt8) -> UInt8 {
    return num ^ 40
}

/*: "msgType" :*/
fileprivate let showVideoMain:[Character] = ["m","s","g","T","y","p","e"]

/*: "txt" :*/
fileprivate let showTextNoti:String = "tsessiont"

/*: "isGroup" :*/
fileprivate let kCellConst:String = "IS"
fileprivate let showAtNoti:String = "Groupmanager data shared"

/*: "jumps" :*/
fileprivate let notiDonationConst:String = "juupps"

/*: "message" :*/
fileprivate let dataPicConst:[UInt8] = [0x65,0x67,0x61,0x73,0x73,0x65,0x6d]

/*: "TxtMsg" :*/
fileprivate let constFormatData:[Character] = ["T"]
fileprivate let notiAtUser:String = "me corner letxtMsg"

/*: "toUid" :*/
fileprivate let notiNoseUser:[Character] = ["t","o","U","i","d"]

/*: "msgInfo" :*/
fileprivate let constReloadNoti:String = "path pathmsgInfo"

/*: "params" :*/
fileprivate let appContentData:[UInt8] = [0x9b,0x8a,0x99,0x8a,0x86,0x98]

/*: "contentType" :*/
fileprivate let mainScaleK:[UInt8] = [0x65,0x70,0x79,0x54,0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "ImgMsg" :*/
fileprivate let mainAboutData:String = "ImgMsgerror equal true"

/*: "img" :*/
fileprivate let notiConfirmShow:[Character] = ["i","m","g"]

/*: "imageUri" :*/
fileprivate let showPathMain:[UInt8] = [0x89,0x8d,0x81,0x87,0x85,0x75,0x92,0x89]

fileprivate func pointMake(pool num: UInt8) -> UInt8 {
    let value = Int(num) + 224
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "pic" :*/
fileprivate let appManagerShow:String = "nameic"

/*: "msgId" :*/
fileprivate let mainExceptApp:String = "msgIdsucceed self self"

/*: "totalMfCoin" :*/
fileprivate let constIconIndexK:[Character] = ["t","o","t","a","l","M","f","C","o","i","n"]

/*: "giftId" :*/
fileprivate let dataStyleNoti:String = "giftIdshadow color effect model"

/*: "giftNum" :*/
fileprivate let kModePriceData:String = "control array cellgiftNum"

/*: "pkgItemsetId" :*/
fileprivate let userManagerMain:[Character] = ["p","k","g","I","t","e"]
fileprivate let mainModelGiftHeightConst:[Character] = ["m","s","e","t","I","d"]

/*: "msg" :*/
fileprivate let constArrayNoti:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let constColorFlowLabApp:[Character] = ["t","i","p","s"]

/*: "gift" :*/
fileprivate let userGestureApp:String = "bottomift"

/*: "nickname" :*/
fileprivate let notiLeadingRawUser:String = "titleicktitle"
fileprivate let userScopeMain:String = "AME"

/*: "headPic" :*/
fileprivate let appUnknownK:[UInt8] = [0x86,0x8b,0x8f,0x8a,0xbe,0x87,0x8d]

private func statusLocation(strength num: UInt8) -> UInt8 {
    return num ^ 238
}

/*: "All Numbers" :*/
fileprivate let kLabelConst:String = "action pointAll Num"
fileprivate let mainRunData:String = "BERS"

/*: "sendNum" :*/
fileprivate let showFollowingApp:String = "handle wrap appsendNum"

/*: "toUser" :*/
fileprivate let notiAppK:String = "toUserpic self nor"

/*: "msgIncome" :*/
fileprivate let appModeVideoK:[Character] = ["m","s","g","I","n","c","o","m","e"]

/*: "isConnection" :*/
fileprivate let constRefreshToK:[Character] = ["i","s","C","o","n","n","e","c","t","i","o"]
fileprivate let kBackNoti:String = "available"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ValueControllerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class ValueControllerDelegate: EmptyRecognizerDelegate {
	var regardArray: [AnyHashable] = []
	var pathMaxEnable: Bool = true
	var draftName: String = "cell"
	var iconTextArray: [AnyHashable] = []
	var produceLiveDictionary: [AnyHashable: String] = [:]

    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = RequestTransformable() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = FrameReactiveCompatible() // 会话cellData
    //: private var conversationType: CacheIMChatType = .group
    private var conversationType: CacheIMChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = RequestTransformable.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: showAddNoti.map{equalYou(show: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if PositionLabelReactiveCompatible.share.appConfigMode.disableShootScreen {
            if PositionLabelReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = FrameThen(frame: CGRect(x: 0, y: 0, width: userDeviceKey, height: appNameMessage))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        velleityUi()
        //: createUIConstraints()
        underBy()
        //: bindInteraction()
        noLineNumbereraction()
        //: addTapGestureRecognizer()
        greet()
        //: func__chatRoom_insertTipMsg()
        bringHome()
        //: chatSettings()
        requestSettings()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            veryWell()
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
	}

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
	}

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.versoTo(join: false)
        //: self.destroyTipsTimer()
        self.addWith()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: LocalScreenTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = LocalScreenTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: "TalkingGroupChatTextMsgCellID")
        msgVC.tableView.register(HadithThen.self, forCellReuseIdentifier: "HadithThen")
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: "TalkingChatImageMsgCellID")
        msgVC.tableView.register(DataEmptyViewCell.self, forCellReuseIdentifier: "DataEmptyViewCell")
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: "TalkingGroupChatGiftMsgCell")
        msgVC.tableView.register(RequestSourceThen.self, forCellReuseIdentifier: "RequestSourceThen")
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: "TalkingChatTipsMsgCellID")
        msgVC.tableView.register(TipsMinThen.self, forCellReuseIdentifier: "TipsMinThen")
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: "TSystemMessageCell_ReuseId")
        msgVC.tableView.register(WhiteView.self, forCellReuseIdentifier: (String(constTitleK.suffix(6)) + "mMess" + String(notiPicViewK.prefix(5)) + "ll_R" + dataModeCornerTipMain.replacingOccurrences(of: "path", with: "e") + String(appAppearLeadingConst)))
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: TempInputView = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = TempInputView(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: SourceReactiveCompatible = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = SourceReactiveCompatible()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: StepsThen = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = StepsThen()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: StraightAndNarrowThen = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = StraightAndNarrowThen(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: IncomeReactiveCompatible = {
        //: let v = TalkingSomeoneAtMeView()
        let v = IncomeReactiveCompatible()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension ValueControllerDelegate {
    /// 群聊设置
    //: private func chatSettings() {
    private func requestSettings() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        SchemeErrorThen.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        versoTo(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func versoTo(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(constMessageShow.prefix(6))): self.conversationData.groupID, String(bytes: userTableContentK.map{nameUp(fail: $0)}, encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: TitleRequestTool.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        TitleRequestTool.passePartout(params: params) { succeed, _, _ in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - ValueKeepDelegate

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension ValueControllerDelegate: ValueKeepDelegate {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> MessageThen! {
    func quote(_: LocalScreenTableViewController!, run data: V2TIMMessage!) -> MessageThen! {
        //: let dic = ArtefactChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = ArtefactChatManager.pull(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = SocietalChatManager.exceptViewStyle(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: MessageThen!) -> DetailReactiveCompatible! {
    func circumferenceSession(_: LocalScreenTableViewController!, roundNumber data: MessageThen!) -> DetailReactiveCompatible! {
        //: if data.isKind(of: WhiteCellData.self) {
        if data.isKind(of: WhiteCellData.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: "TalkingGroupChatTextMsgCellID")
            let cell = HadithThen(style: .default, reuseIdentifier: "HadithThen")
            //: let cellData = data as! WhiteCellData
            let cellData = data as! WhiteCellData
            //: cell.fill(with: cellData)
            cell.equalWith(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.infoUid(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.gutter(cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: LayerSharedReactiveCompatible.self) {
        if data.isKind(of: LayerSharedReactiveCompatible.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: "TalkingChatImageMsgCellID")
            let cell = DataEmptyViewCell(style: .default, reuseIdentifier: "DataEmptyViewCell")
            //: let cellData = data as! LayerSharedReactiveCompatible
            let cellData = data as! LayerSharedReactiveCompatible
            //: cell.fill(with: cellData)
            cell.equalWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: DropCellData.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingGroupChatGiftMsgCell")
            let cell = RequestSourceThen(style: .default, reuseIdentifier: "RequestSourceThen")
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! DropCellData
            //: cell.fill(with: cellData)
            cell.equalWith(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: PhotoGainCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTipsMsgCellID")
            let cell = TipsMinThen(style: .default, reuseIdentifier: "TipsMinThen")
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! PhotoGainCellData
            //: cell.fill(with: cellData)
            cell.equalWith(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: DetailReactiveCompatible!) {
    func value(_: LocalScreenTableViewController!, length cell: DetailReactiveCompatible!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(PositionLabelReactiveCompatible.share.loginUid) {
            if userModel.uid == Int(PositionLabelReactiveCompatible.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                infoUid(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                domino(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: DetailReactiveCompatible!) {
    func handle(_: LocalScreenTableViewController!, packetOrAggregation cell: DetailReactiveCompatible!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: DataEmptyViewCell.self) { // 图片
            //: let imageCellData = cell.messageData as! LayerSharedReactiveCompatible
            let imageCellData = cell.messageData as! LayerSharedReactiveCompatible
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(userReloadName)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = PlayerTransformable()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = TitleMomentModel()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [PlayerTransformable] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = PhotosViewDelegate(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.underTitle()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: HadithThen.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! HadithThen
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = ArtefactChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = ArtefactChatManager.pull(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(constLabToNoti))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: userLayerData.map{fullPhaseOfTheMoon(color: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<ControlSystemModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: ViewPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    ViewPushManager.share.labelScreen(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: TipsMinThen.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! TipsMinThen
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = ArtefactChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = ArtefactChatManager.pull(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(constLabToNoti))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: userLayerData.map{fullPhaseOfTheMoon(color: $0)}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<ControlSystemModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: ViewPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    ViewPushManager.share.labelScreen(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: DetailReactiveCompatible!) {
    func message(_: LocalScreenTableViewController!, title cell: DetailReactiveCompatible!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        harbour(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension ValueControllerDelegate {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func addWith() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func domino(user: FeatureModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = ImagePopReactiveCompatible(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.compartmentList(cellTitleList: [(String(kTimeNoti) + String(mainPathNoti)).localized, "@", (String(dataBackgroundApp.suffix(9))).localized, (String(kItemApp.suffix(6))).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.bitty(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.ginMill(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.infoUid(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.pushUpUid(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func bitty(user: FeatureModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.chorus()
        //: let model = TalkingChatRoomMemberModel()
        let model = OfMainHandyJSON()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.computerSimulation(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func ginMill(user: FeatureModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: kQuoteShow.map{indexLabel(equal: $0)}, encoding: .utf8)!: String(user.uid), String(bytes: constActualNoti.map{upgradeBlock(video: $0)}, encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.dateLabDict(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func infoUid(uid: Int) {
        //: ViewPushManager.share.func__pushToUserDetailVC(uid: String(uid))
        ViewPushManager.share.digitalAnalogConverter(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func pushUpUid(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = PositionTempAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.nameTo(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension ValueControllerDelegate {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func funcGift(msgStr: String, quoteModel _: DeadlineModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            exceptAdd(showMsg: (String(constLeadingMain) + kModelNorUser.replacingOccurrences(of: "social", with: "ge")).localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: userLayerData.map{fullPhaseOfTheMoon(color: $0)}, encoding: .utf8)!: (mainAddNoti.replacingOccurrences(of: "content", with: "u")), String(bytes: kQuoteShow.map{indexLabel(equal: $0)}, encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: showMakeMain.map{screenRelease(path: $0)}, encoding: .utf8)!: temStr, (String(showVideoMain)): (showTextNoti.replacingOccurrences(of: "session", with: "x")), (kCellConst.lowercased() + String(showAtNoti.prefix(5))): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(notiDonationConst.replacingOccurrences(of: "up", with: "m"))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: dataPicConst.reversed(), encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: userTableContentK.map{nameUp(fail: $0)}, encoding: .utf8)!: (String(constFormatData) + String(notiAtUser.suffix(5))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(constMessageShow.prefix(6))): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(notiNoseUser))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(constReloadNoti.suffix(7))): msgInfo, String(bytes: appContentData.map{$0^235}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = SocietalChatManager.rejoin(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            harbour(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func sizeOf(img: UIImage) {
        //: let imagePath = LayerSharedReactiveCompatible.getChatImagePath(image: img)
        let imagePath = LayerSharedReactiveCompatible.exaggerate(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: mainScaleK.reversed(), encoding: .utf8)!: (String(mainAboutData.prefix(6))), (String(showVideoMain)): (String(notiConfirmShow)), String(bytes: showPathMain.map{pointMake(pool: $0)}, encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(appManagerShow.replacingOccurrences(of: "name", with: "p")): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: userTableContentK.map{nameUp(fail: $0)}, encoding: .utf8)!: (String(mainAboutData.prefix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(constMessageShow.prefix(6))): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(constReloadNoti.suffix(7))): msgInfo, String(bytes: appContentData.map{$0^235}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = SocietalChatManager.rejoin(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            harbour(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: MessageThen, reSend: Bool) {
    private func harbour(msgData: MessageThen, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            exceptAdd(showMsg: constEnvironmentKey)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.title(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((appManagerShow.replacingOccurrences(of: "name", with: "p"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(appManagerShow.replacingOccurrences(of: "name", with: "p"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(appManagerShow.replacingOccurrences(of: "name", with: "p"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: TitleRequestTool.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        TitleRequestTool.playDestroyText(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.titleure(msgData, textDevice: nil, camera: NSNumber(value: 0), video: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.aboveTouch(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(constReloadNoti.suffix(7)))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.titleure(msgData, textDevice: msgInfo[(String(mainExceptApp.prefix(5)))] as? String, camera: NSNumber(value: 0), video: .Msg_Status_Succ)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(constIconIndexK))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(constIconIndexK))] as! NSNumber
                //: PositionLabelReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                PositionLabelReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func aboveTouch(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard PositionLabelReactiveCompatible.share.loginUserMode.status != 1 else {
            guard PositionLabelReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    exceptAdd(showMsg: errorStr)
                }
                //: return
                return
            }

            //: ViewPushManager.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            ViewPushManager.share.blockRequestEnd(clickEvent: dataInputCameraKey, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.showToView()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            editInput(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = AlbescentView(frame: CGRect(x: 0, y: 0, width: userDeviceKey, height: appNameMessage))
            //: view.show()
            view.moreName()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                exceptAdd(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension ValueControllerDelegate: ImageViewDelegate {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func tag(giftModel _: GiftMeasurable, giftNum _: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func input() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func tip(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = ManagerListController()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.dateLabDict(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func equalType(actionType: DataInsertActionType) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            quit()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func devise(giftModel _: GiftMeasurable, giftNum _: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func tap(giftModel: GiftMeasurable, giftNum: String, model: OfMainHandyJSON) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            exceptAdd(showMsg: constEnvironmentKey)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(notiNoseUser))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(dataStyleNoti.prefix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(kModePriceData.suffix(7)))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(constMessageShow.prefix(6)))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(userManagerMain) + String(mainModelGiftHeightConst))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: TitleRequestTool.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        TitleRequestTool.notSizeStatus(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.aboveTouch(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.sumerest(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.stem(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func modify(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.heel(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func sizeSearch(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.heel(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func sizeLive(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.sizeLive(tap)
    }

    //: func func__starRecordBtnDown() {}
    func bid() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func funcQueryPiece(voicePath _: String, voiceTime _: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension ValueControllerDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func quit() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        TimingThen.someFunc(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = TempThen.single(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: ViewPushManager.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                ViewPushManager.share.dataModel()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.sizeOf(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func outValue() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        TimingThen.statusBlock(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.sizeOf(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension ValueControllerDelegate {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func sufficient(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(constMessageShow.prefix(6)))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension ValueControllerDelegate {
    //: func func__TipsNotification() {
    func constraintAddTipsNotification() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(rhythm(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: constListId,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func rhythm(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: PhotoGainCellData = userinfo[(String(constArrayNoti))] as! PhotoGainCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.heel(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func editInput(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: showMakeMain.map{screenRelease(path: $0)}, encoding: .utf8)!: tipStr, (String(showVideoMain)): (String(constColorFlowLabApp))]
        //: var message: MessageThen?
        var message: MessageThen?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = SocietalChatManager.rejoin(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.heel(true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func bringHome() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard PositionLabelReactiveCompatible.share.appUserConfigMode.rTips.count > 0 else { return }
            guard PositionLabelReactiveCompatible.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: PositionLabelReactiveCompatible.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.editInput(tipStr: PositionLabelReactiveCompatible.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension ValueControllerDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func stem(extral: [String: Any?], model: OfMainHandyJSON, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(constReloadNoti.suffix(7)))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(kCellConst.lowercased() + String(showAtNoti.prefix(5)))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(showVideoMain))] = (userGestureApp.replacingOccurrences(of: "bottom", with: "g"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(constReloadNoti.suffix(7)))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: kQuoteShow.map{indexLabel(equal: $0)}, encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (notiLeadingRawUser.replacingOccurrences(of: "title", with: "n") + userScopeMain.lowercased()): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: appUnknownK.map{statusLocation(strength: $0)}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(kLabelConst.suffix(7)) + mainRunData.lowercased()).localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((userGestureApp.replacingOccurrences(of: "bottom", with: "g"))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(userGestureApp.replacingOccurrences(of: "bottom", with: "g"))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(notiLeadingRawUser.replacingOccurrences(of: "title", with: "n") + userScopeMain.lowercased())] = "\(giftdic[(String(showFollowingApp.suffix(7)))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(notiAppK.prefix(6)))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = SocietalChatManager.rejoin(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.title(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(appModeVideoK))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(appModeVideoK))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.titleure(cellData, textDevice: msgInfoDic[(String(mainExceptApp.prefix(5)))] as? String, camera: msgIncome, video: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        efffecct(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(constIconIndexK))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(constIconIndexK))] as! NSNumber
            //: PositionLabelReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            PositionLabelReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: PositionLabelReactiveCompatible.share.loginUserMode.mf_coin)
            msgInputView.giftView.imageEnable(needReload: false, mf_coin: PositionLabelReactiveCompatible.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.colorMembers()
    }

    //: func initCachEffectData() {
    func veryWell() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.levelId(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func efffecct(giftDic: [String: Any], model: OfMainHandyJSON) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.success(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in SpecsReactiveCompatible.share.appVoice() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [SpeedMeasurable]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.sh(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.connectSize(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func viewIn(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in SpecsReactiveCompatible.share.appVoice() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [SpeedMeasurable]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.sh(model: giftModel as! ShareMeasurable)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.instrumentality(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension ValueControllerDelegate {
    /// 创建UI
    //: private func createUI() {
    private func velleityUi() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func underBy() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(constFailAppData)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(constFailAppData)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - showNameData)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func noLineNumbereraction() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = ManagerListController()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! MessageThen).isKind(of: WhiteCellData.self) {
                    if (cellData as! MessageThen).isKind(of: WhiteCellData.self) {
                        //: let groupCellData = cellData as! WhiteCellData
                        let groupCellData = cellData as! WhiteCellData
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(mainExceptApp.prefix(5)))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: TabThen.shared.rx
        TabThen.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(constRefreshToK) + kBackNoti.replacingOccurrences(of: "available", with: "n")))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
