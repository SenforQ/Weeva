
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kAddApp:[UInt8] = [0x93,0x94,0x93,0x8e,0xd2,0x99,0x95,0x9e,0x9f,0x88,0xc0,0xd3,0xda,0x92,0x9b,0x89,0xda,0x94,0x95,0x8e,0xda,0x98,0x9f,0x9f,0x94,0xda,0x93,0x97,0x8a,0x96,0x9f,0x97,0x9f,0x94,0x8e,0x9f,0x9e]

private func rubricShared(gift num: UInt8) -> UInt8 {
    return num ^ 250
}

/*: "icon_home_v" :*/
fileprivate let appCurrentMain:String = "ifile"
fileprivate let notiColorConst:String = "on_hoadd to enable"

/*: "icon_lounge" :*/
fileprivate let constFrameworkApp:String = "if disk lineicon_"

/*: "icon_jianbian_back" :*/
fileprivate let mainEventConst:String = "iaddn"
fileprivate let userClickMakeTextData:String = "ishow"
fileprivate let dataLiveShow:[Character] = ["n","_","b","a","c","k"]

/*: "Like&Chat" :*/
fileprivate let mainNameShow:String = "Like&local head content"

/*: "#EFEDFF" :*/
fileprivate let appQuantityFactorMain:String = "to data plan gift any#EFEDFF"

/*: "Quick Greeting" :*/
fileprivate let constPathUser:String = "let content typeQuic"
fileprivate let dataDetailUser:[Character] = ["e","e","t","i","n","g"]

/*: "Nope" :*/
fileprivate let appTabConst:[Character] = ["N","o","p","e"]

/*: "icon_ender_boy" :*/
fileprivate let dataCornerUser:String = "let leticon_"
fileprivate let mainVoiceConst:String = "olayer"

/*: "icon_ender_girl" :*/
fileprivate let appIconUser:String = "mode cornericon_end"
fileprivate let userSizeK:String = "elab"
fileprivate let mainSearchedBottomTempNoti:[Character] = ["_","g","i","r","l"]

/*: "   " :*/
fileprivate let constToUser:[Character] = [" "," "," "]

/*: "crushId" :*/
fileprivate let kManagerData:[Character] = ["c"]
fileprivate let constPageData:[Character] = ["r","u","s","h","I","d"]

/*: "her" :*/
fileprivate let kKeyNoti:String = "detailer"

/*: "him" :*/
fileprivate let constEqualK:[Character] = ["h","i","m"]

/*: "content" :*/
fileprivate let notiToK:[Character] = ["c","o","n","t","e","n","t"]

/*: "Why don't you say hello to %@?" :*/
fileprivate let constAdjustmentUser:String = "target show info makeWhy d"
fileprivate let notiMinimizeShow:String = "to succeed guard not you s"
fileprivate let mainJoinSizeBottomShow:String = "ay heview type"
fileprivate let appUserConst:String = "view cell if image positionllo "

/*: "msgType" :*/
fileprivate let userViewK:String = "msgTypetitle app finish"

/*: "txt" :*/
fileprivate let mainLengthConst:[Character] = ["t","x","t"]

/*: "msg" :*/
fileprivate let kCardData:[UInt8] = [0x80,0x9e,0x8a]

private func bottomBasic(moment num: UInt8) -> UInt8 {
    return num ^ 237
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CompartmentReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/13.
//

//: import UIKit
import UIKit

//: public protocol WhoLikeDelegate: NSObject {
public protocol ViewCancelThen: NSObject {
    //: func quickGreetingSeleteIndex(_ index: IndexPath )
    func metadata(_ index: IndexPath)
    //: func likeSeleteIndex(_ index: IndexPath )
    func marginOfSafetyWith(_ index: IndexPath)
}

//: class TalkingWhoLikeCell: UITableViewCell {
class CompartmentReactiveCompatible: UITableViewCell {
	var chemLabOn: Bool = false
	var iconStopNumber: Int = 49
	var atDictionary: [AnyHashable: String] = [:]
	var occurrentEnable: Bool = true
	var tableCount: Int = 99
	var applyDictionary: [AnyHashable: String] = [:]

    //: var currenModel = TalkingWhoLikeMeModel()
    var currenModel = SourceHandyJSON()
    //: var seleteIndex: IndexPath?
    var seleteIndex: IndexPath?
    //: open weak var delegate: WhoLikeDelegate?
    open weak var delegate: ViewCancelThen?

    //: override func awakeFromNib() {
    override func awakeFromNib() {
        //: super.awakeFromNib()
        super.awakeFromNib()
    
		if var digitalAnalogConverterColorValue = self.currenModel.age { 
			if var dismissValue = self.currenModel.isTPAuth { 
				if var beamyValue = currenModel.message { 
			            if (IconImageV.layoutMargins.right == 5.61) && (IconImageV.layer.zPosition == 19.15) {
			                //: SWIFT_CUSTOM_DANGER_File_Call
			                let fullMoon = AnalogDigitalConverterView()
			            fullMoon.toContent = beamyValue
			            
			            fullMoon.atEnable = { [self] cellOff in
			            self.occurrentEnable = cellOff
			            
			            var fullMoon = dismissValue
			                fullMoon = true
			                fullMoon = true
			            if fullMoon != self.occurrentEnable {
			                self.occurrentEnable = fullMoon
			            }
			            
			            self.occurrentEnable = false
			            return self.occurrentEnable
			            }
			            fullMoon.constraintStackCount = { [self] backTotal in
			            self.tableCount = backTotal
			            
			            var fullMoon = digitalAnalogConverterColorValue
			                fullMoon -= 1
			                if Int(fullMoon) > -25 {
			                    fullMoon = fullMoon + 1
			                }
			            if fullMoon < self.tableCount {
			                self.tableCount = fullMoon
			            }
			            
			            return self.tableCount
			            }
			            fullMoon.modeDictionary = { [self] bottomDictionary in
			            self.applyDictionary = bottomDictionary
			            
			            guard var value = self.applyDictionary as? [String: String] else {
			                return nil
			            }
			            return value
			            }
			                IconImageV.addSubview(fullMoon)
			            }
			
				}
			}
		}
	}

    //: override func setSelected(_ selected: Bool, animated: Bool) {
    override func setSelected(_ selected: Bool, animated: Bool) {
        //: super.setSelected(selected, animated: animated)
        super.setSelected(selected, animated: animated)
    
		if var worldValue = self.currenModel.status { 
			if var componentValue = self.currenModel.likeId { 
		            if (nameLabel.contentMode == .bottomLeft) && (nameLabel.gestureRecognizers != nil && nameLabel.gestureRecognizers!.count == 10) {
		                //: SWIFT_CUSTOM_DANGER_File_Call
		                let betweenPrivacy = AnalogDigitalConverterView(frame: nameLabel.frame.offsetBy(dx: CGFloat(68), dy: CGFloat(335.77)))
		            betweenPrivacy.toContent = componentValue
		            
		            betweenPrivacy.atEnable = { [self] cellOff in
		            self.chemLabOn = cellOff
		            
		            var betweenPrivacy = currenModel.loungePlus
		            betweenPrivacy = !betweenPrivacy
		            if betweenPrivacy != self.chemLabOn {
		                self.chemLabOn = betweenPrivacy
		            }
		            
		                self.chemLabOn = true
		                self.chemLabOn = !self.chemLabOn
		            return self.chemLabOn
		            }
		            betweenPrivacy.constraintStackCount = { [self] backTotal in
		            self.iconStopNumber = backTotal
		            
		            var betweenPrivacy = worldValue
		            betweenPrivacy = 0
		            if betweenPrivacy > self.iconStopNumber {
		                self.iconStopNumber = betweenPrivacy
		            }
		            
		            return self.iconStopNumber
		            }
		            betweenPrivacy.modeDictionary = { [self] bottomDictionary in
		            self.atDictionary = bottomDictionary
		            
		            guard var value = self.atDictionary as? [String: String] else {
		                return nil
		            }
		            return value
		            }
		                nameLabel.addSubview(betweenPrivacy)
		            }
		
			}
		}
	}

    //: override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
    override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
        //: super.init(style: style, reuseIdentifier: reuseIdentifier)
        super.init(style: style, reuseIdentifier: reuseIdentifier)
        //: selectionStyle = UITableViewCell.SelectionStyle.none
        selectionStyle = UITableViewCell.SelectionStyle.none
        //: self.backgroundColor = UIColor.clear
        self.backgroundColor = UIColor.clear
        //: self.contentView.backgroundColor = UIColor.clear
        self.contentView.backgroundColor = UIColor.clear
        //: contentView.addSubview(backView)
        contentView.addSubview(backView)
        //: backView.addSubview(IconImageV)
        backView.addSubview(IconImageV)
        //: backView.addSubview(iconBorder)
        backView.addSubview(iconBorder)
        //: backView.addSubview(nameLabel)
        backView.addSubview(nameLabel)
        //: backView.addSubview(cardImg)
        backView.addSubview(cardImg)
        //: backView.addSubview(sexBtn)
        backView.addSubview(sexBtn)
        //: backView.addSubview(loungeImgV)
        backView.addSubview(loungeImgV)
        //: backView.addSubview(timeLabel)
        backView.addSubview(timeLabel)
        //: backView.addSubview(messageLabel)
        backView.addSubview(messageLabel)
        //: backView.addSubview(quickGreetingBtn)
        backView.addSubview(quickGreetingBtn)
        //: backView.addSubview(chatBtn)
        backView.addSubview(chatBtn)
        //: layoutSubViewsConstraints()
        willBy()
    }

    //: required init?(coder aDecoder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kAddApp.map{rubricShared(gift: $0)}, encoding: .utf8)!)
    }

    //: override func layoutSubviews() {
    override func layoutSubviews() {
        //: super.layoutSubviews()
        super.layoutSubviews()
    }

    // MARK: - Lazy load

    //: lazy var backView: UIView = {
    lazy var backView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = UIColor.white
        view.backgroundColor = UIColor.white
        //: view.layer.shadowColor = UIColor(red: 0.8, green: 0.8, blue: 0.8, alpha: 0.5).cgColor
        view.layer.shadowColor = UIColor(red: 0.8, green: 0.8, blue: 0.8, alpha: 0.5).cgColor
        //: view.layer.shadowOffset = CGSize(width: 0, height: 1)
        view.layer.shadowOffset = CGSize(width: 0, height: 1)
        //: view.layer.shadowOpacity = 1
        view.layer.shadowOpacity = 1
        //: view.layer.shadowRadius = 4
        view.layer.shadowRadius = 4
        //: view.layer.cornerRadius = 9
        view.layer.cornerRadius = 9
        //: return view
        return view
        //: }()
    }()

    //: lazy var IconImageV: UIImageView = {
    lazy var IconImageV: UIImageView = {
        //: let imageV = UIImageView.init()
        let imageV = UIImageView()
        //: imageV.layer.cornerRadius = 40/2
        imageV.layer.cornerRadius = 40 / 2
        //: imageV.layer.masksToBounds = true
        imageV.layer.masksToBounds = true
        //: imageV.contentMode = .scaleAspectFill
        imageV.contentMode = .scaleAspectFill
        //: return imageV
        return imageV
        //: }()
    }()

    //: lazy var iconBorder: UIButton = {
    lazy var iconBorder: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.isUserInteractionEnabled = false
        btn.isUserInteractionEnabled = false
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var nameLabel: UILabel = {
    lazy var nameLabel: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 16)
        label.font = .viewPath(type: .Medium, fontSize: 16)
        //: label.textColor = .appTitleColor()
        label.textColor = .read()
        //: return label
        return label
        //: }()
    }()

    //: lazy var cardImg: UIImageView = {
    lazy var cardImg: UIImageView = {
        //: let img = UIImageView.init()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_home_v")
        img.image = UIImage.imageName(name: (appCurrentMain.replacingOccurrences(of: "file", with: "c") + String(notiColorConst.prefix(5)) + "me_v"))
        //: return img
        return img
        //: }()
    }()

    //: lazy var sexBtn: UIButton = {
    lazy var sexBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.isUserInteractionEnabled = false
        btn.isUserInteractionEnabled = false
        //: btn.titleLabel?.font = .pingfangFont(type: .Medium, fontSize: 10)
        btn.titleLabel?.font = .viewPath(type: .Medium, fontSize: 10)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var loungeImgV: UIImageView = {
    lazy var loungeImgV: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.imageName(name: (String(constFrameworkApp.suffix(5)) + "lounge"))
        //: return img
        return img
        //: }()
    }()

    //: lazy var timeLabel: UILabel = {
    lazy var timeLabel: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 14)
        label.font = .viewPath(type: .Medium, fontSize: 14)
        //: label.textColor = .appValueDetailColor()
        label.textColor = .streamColor()
        //: return label
        return label
        //: }()
    }()

    //: lazy var chatBtn: UIButton = {
    lazy var chatBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = .pingfangFont(type: .Medium, fontSize: 16)
        btn.titleLabel?.font = .viewPath(type: .Medium, fontSize: 16)
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "icon_jianbian_back"), for: .normal)
        btn.setBackgroundImage(UIImage.imageName(name: (mainEventConst.replacingOccurrences(of: "add", with: "co") + "_jianb" + userClickMakeTextData.replacingOccurrences(of: "show", with: "a") + String(dataLiveShow))), for: .normal)
        //: btn.setTitle(" " + "Like&Chat".localized + " ", for: .normal)
        btn.setTitle(" " + (String(mainNameShow.prefix(5)) + "Chat").localized + " ", for: .normal)
        //: btn.addTarget(self, action: #selector(ChatBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(pathEnable), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var quickGreetingBtn: UIButton = {
    lazy var quickGreetingBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitleColor(UIColor.appThemeColor(), for: .normal)
        btn.setTitleColor(UIColor.requestCardTitle(), for: .normal)
        //: btn.layer.cornerRadius = 37/2
        btn.layer.cornerRadius = 37 / 2
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.titleLabel?.font = .pingfangFont(type: .Medium, fontSize: 16)
        btn.titleLabel?.font = .viewPath(type: .Medium, fontSize: 16)
        //: btn.setBackgroundColor(color: UIColor.init(hex: "#EFEDFF")!, forState: .normal)
        btn.showCorner(color: UIColor(hex: (String(appQuantityFactorMain.suffix(7))))!, forState: .normal)
        //: btn.addTarget(self, action: #selector(quickGreetingBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(fast), for: .touchUpInside)
        //: if PositionLabelReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
        if PositionLabelReactiveCompatible.share.loginUserMode.sex == PetitionKeyRepresentable.female.rawValue { // 女性
            //: btn.setTitle("Quick Greeting".localized, for: .normal)
            btn.setTitle((String(constPathUser.suffix(4)) + "k Gr" + String(dataDetailUser)).localized, for: .normal)
            //: } else {
        } else {
            //: btn.setTitle("Nope".localized, for: .normal)
            btn.setTitle((String(appTabConst)).localized, for: .normal)
        }
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var messageLabel: UILabel = {
    lazy var messageLabel: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Medium, fontSize: 16)
        label.font = .viewPath(type: .Medium, fontSize: 16)
        //: label.textColor = .appTitleColor()
        label.textColor = .read()
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: return label
        return label
        //: }()
    }()
}

// MARK: - Event

//: extension TalkingWhoLikeCell {
extension CompartmentReactiveCompatible {
    //: func setCell(model: TalkingWhoLikeMeModel, index: IndexPath) {
    func refer(model: SourceHandyJSON, index: IndexPath) {
        //: currenModel = model
        currenModel = model
        //: seleteIndex = index
        seleteIndex = index
        //: IconImageV.setUrlImage(urlStr: model.headPic ?? "", placeImg: UIImage.placeHolderImage(sex: String(model.sex ?? 1 )))
        IconImageV.windowFinish(urlStr: model.headPic ?? "", placeImg: UIImage.buttonInfoArea(sex: String(model.sex ?? 1)))
        //: if !model.headPicFrame.isEmptyString {
        if !model.headPicFrame.isEmptyString {
            //: IconImageV.snp.remakeConstraints { make in
            IconImageV.snp.remakeConstraints { make in
                //: make.leading.equalTo(backView).offset(12)
                make.leading.equalTo(backView).offset(12)
                //: make.top.equalTo(backView).offset(12)
                make.top.equalTo(backView).offset(12)
                //: make.width.height.equalTo(36)
                make.width.height.equalTo(36)
            }
            //: iconBorder.isHidden = false
            iconBorder.isHidden = false
            //: iconBorder.setHeadFrameUrlImage(urlStr: model.headPicFrame)
            iconBorder.addEnable(urlStr: model.headPicFrame)
            //: }else {
        } else {
            //: iconBorder.isHidden = true
            iconBorder.isHidden = true
        }

        //: nameLabel.textColor = model.loungePlus ? .userVipColor():.appTitleColor()
        nameLabel.textColor = model.loungePlus ? .endUserForColoration() : .read()
        //: nameLabel.text = model.nickname
        nameLabel.text = model.nickname
        //: if !(model.isTPAuth ?? false) {
        if !(model.isTPAuth ?? false) {
            //: cardImg.isHidden = true
            cardImg.isHidden = true
            //: cardImg.snp.updateConstraints { make in
            cardImg.snp.updateConstraints { make in
                //: make.width.equalTo(1)
                make.width.equalTo(1)
                //: make.leading.equalTo(nameLabel.snp.trailing).offset(0)
                make.leading.equalTo(nameLabel.snp.trailing).offset(0)
            }
            //: } else {
        } else {
            //: cardImg.isHidden = false
            cardImg.isHidden = false
            //: cardImg.snp.updateConstraints { make in
            cardImg.snp.updateConstraints { make in
                //: make.width.equalTo(16)
                make.width.equalTo(16)
                //: make.leading.equalTo(nameLabel.snp.trailing).offset(4)
                make.leading.equalTo(nameLabel.snp.trailing).offset(4)
            }
        }

        //: switch model.sex {
        switch model.sex {
        //: case 1:
        case 1:
            //: sexBtn.setBackgroundImage(UIImage.BundleImageNamed(name: "icon_ender_boy"), for: .normal)
            sexBtn.setBackgroundImage(UIImage.imageName(name: (String(dataCornerUser.suffix(5)) + "ender_b" + mainVoiceConst.replacingOccurrences(of: "layer", with: "y"))), for: .normal)
        //: break
        //: case 2:
        case 2:
            //: sexBtn.setBackgroundImage(UIImage.BundleImageNamed(name: "icon_ender_girl"), for: .normal)
            sexBtn.setBackgroundImage(UIImage.imageName(name: (String(appIconUser.suffix(8)) + userSizeK.replacingOccurrences(of: "lab", with: "r") + String(mainSearchedBottomTempNoti))), for: .normal)
        //: break
        //: default:
        default:
            //: break
            break
        }
        //: sexBtn.setTitle("   " + String(model.age ?? 0), for: .normal)
        sexBtn.setTitle("   " + String(model.age ?? 0), for: .normal)
        //: timeLabel.text = model.timeFormat
        timeLabel.text = model.timeFormat
        //: messageLabel.text = model.message
        messageLabel.text = model.message

        //: loungeImgV.isHidden = !model.loungePlus
        loungeImgV.isHidden = !model.loungePlus
        //: if loungeImgV.isHidden == false {
        if loungeImgV.isHidden == false {
            //: loungeImgV.snp.makeConstraints { make in
            loungeImgV.snp.makeConstraints { make in
                //: make.centerY.equalTo(nameLabel)
                make.centerY.equalTo(nameLabel)
                //: make.leading.equalTo(sexBtn.snp.trailing).offset(3)
                make.leading.equalTo(sexBtn.snp.trailing).offset(3)
                //: make.size.equalTo(CGSize(width: 20, height: 20))
                make.size.equalTo(CGSize(width: 20, height: 20))
                //: make.trailing.lessThanOrEqualToSuperview().offset(-10)
                make.trailing.lessThanOrEqualToSuperview().offset(-10)
            }
        }
    }

    /// quick Greeting 按钮点击事件（女性）
    //: @objc func quickGreetingBtnClick() {
    @objc func fast() {
        //: let dict: [String: Any] = ["crushId": currenModel.likeId ?? ""]
        let dict: [String: Any] = [(String(kManagerData) + String(constPageData)): currenModel.likeId ?? ""]
        //: if PositionLabelReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
        if PositionLabelReactiveCompatible.share.loginUserMode.sex == PetitionKeyRepresentable.female.rawValue { // 女性
            //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: currenModel.uid) { errorCode, errorMsg, textDict, audioDict in
            ViewChatManager.duringSign(toUserId: currenModel.uid) { errorCode, errorMsg, textDict, audioDict in
                //: if (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) {
                if (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) { // 跳转私聊，发送一键打招呼
                    //: ViewPushManager.share.func__pushToPriveteChatVC(chatID: self.currenModel.uid) { vc in
                    ViewPushManager.share.giftForCompletion(chatID: self.currenModel.uid) { vc in
                        //: vc.quickGreetingTextDict = textDict
                        vc.quickGreetingTextDict = textDict
                        //: vc.quickGreetingAudioDict = audioDict
                        vc.quickGreetingAudioDict = audioDict
                    }
                    //: } else {
                } else {
                    //: if errorMsg?.count ?? 0 > 0 {
                    if errorMsg?.count ?? 0 > 0 {
                        //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                        self.exceptAdd(showMsg: errorMsg!)
                    }
                    //: if errorCode == 2 { return }
                    if errorCode == 2 { return }
                }

                // 删除当前cell
                //: TitleRequestTool.req_whoLikeMeIgnore(params: dict) { succeed, result, errorModel in
                TitleRequestTool.emptyEffect(params: dict) { succeed, _, _ in
                    //: if succeed {
                    if succeed {
                        //: if self.delegate != nil {
                        if self.delegate != nil {
                            //: self.delegate?.quickGreetingSeleteIndex(self.seleteIndex!)
                            self.delegate?.metadata(self.seleteIndex!)
                        }
                    }
                }
            }

            //: } else {
        } else { // 男性只有删除
            //: TitleRequestTool.req_whoLikeMeIgnore(params: dict) { succeed, result, errorModel in
            TitleRequestTool.emptyEffect(params: dict) { succeed, _, _ in
                //: if succeed {
                if succeed {
                    //: if self.delegate != nil {
                    if self.delegate != nil {
                        //: self.delegate?.quickGreetingSeleteIndex(self.seleteIndex!)
                        self.delegate?.metadata(self.seleteIndex!)
                    }
                }
            }
        }
    }

    /// Like&Chat 按钮点击事件
    //: @objc func ChatBtnClick() {
    @objc func pathEnable() {
        //: if currenModel.status == 0 {
        if currenModel.status == 0 {
            //: ViewPushManager.share.func__pushToPriveteChatVC(chatID: currenModel.uid)
            ViewPushManager.share.giftForCompletion(chatID: currenModel.uid)
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["crushId"] = currenModel.likeId
            dict[(String(kManagerData) + String(constPageData))] = currenModel.likeId
            //: ProgressHUD.show()
            WrapView.colorComponent()
            //: TitleRequestTool.req_whoLikeMelike(params: dict) { succeed, result, errorModel in
            TitleRequestTool.withTop(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                WrapView.livePic()
                //: if succeed {
                if succeed {
                    //: self.currenModel.status = 1
                    self.currenModel.status = 1
                    //: if self.delegate != nil {
                    if self.delegate != nil {
                        //: self.delegate?.likeSeleteIndex(self.seleteIndex!)
                        self.delegate?.marginOfSafetyWith(self.seleteIndex!)
                    }
                }
            }

            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now()+1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                let msgInfo = self.getInfo()
                //: let message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo,
                let message = SocietalChatManager.mail(withExtral: msgInfo,
                                                        //: toUid: self.currenModel.uid,
                                                        toUid: self.currenModel.uid,
                                                        //: isSender: true,
                                                        isSender: true,
                                                        //: isTip: true)
                                                        isTip: true)
                //: NotificationCenter.default.post(name: CHAT_TIPS_TEXT_NOTIFICATION, object: self, userInfo: ["msg": message])
                NotificationCenter.default.post(name: constListId, object: self, userInfo: [String(bytes: kCardData.map{bottomBasic(moment: $0)}, encoding: .utf8)!: message])
            }
        }
    }
    
    func getInfo() -> [String: Any] {
        var msgInfo = [String: Any]()
        //: let gender = self.currenModel.sex == Int(Gender.female.rawValue) ? "her".localized:"him".localized
        let gender = self.currenModel.sex == Int(PetitionKeyRepresentable.female.rawValue) ? (kKeyNoti.replacingOccurrences(of: "detail", with: "h")).localized : (String(constEqualK)).localized
        //: msgInfo["content"] = "Why don't you say hello to %@?".localizedArguments(gender)
        msgInfo[(String(notiToK))] = (String(constAdjustmentUser.suffix(5)) + "on\'t" + String(notiMinimizeShow.suffix(6)) + String(mainJoinSizeBottomShow.prefix(5)) + String(appUserConst.suffix(4)) + "to %@?").targetPost(gender)
        //: msgInfo["msgType"] = "txt"
        msgInfo[(String(userViewK.prefix(7)))] = (String(mainLengthConst))
        return msgInfo
    }
}

// MARK: - UI

//: extension TalkingWhoLikeCell {
extension CompartmentReactiveCompatible {
    //: func layoutSubViewsConstraints() {
    func willBy() {
        //: backView.snp.makeConstraints { make in
        backView.snp.makeConstraints { make in
            //: make.leading.equalTo(15)
            make.leading.equalTo(15)
            //: make.trailing.equalTo(-15)
            make.trailing.equalTo(-15)
            //: make.top.equalToSuperview()
            make.top.equalToSuperview()
            //: make.bottom.equalTo(-12)
            make.bottom.equalTo(-12)
        }
        //: IconImageV.snp.makeConstraints { make in
        IconImageV.snp.makeConstraints { make in
            //: make.leading.equalTo(backView).offset(10)
            make.leading.equalTo(backView).offset(10)
            //: make.top.equalTo(backView).offset(10)
            make.top.equalTo(backView).offset(10)
            //: make.width.height.equalTo(40)
            make.width.height.equalTo(40)
        }
        //: iconBorder.snp.makeConstraints { make in
        iconBorder.snp.makeConstraints { make in
            //: make.leading.equalTo(backView).offset(7)
            make.leading.equalTo(backView).offset(7)
            //: make.top.equalTo(backView).offset(7)
            make.top.equalTo(backView).offset(7)
            //: make.width.height.equalTo(46)
            make.width.height.equalTo(46)
        }

        //: nameLabel.snp.makeConstraints { make in
        nameLabel.snp.makeConstraints { make in
            //: make.leading.equalTo(IconImageV.snp.trailing).offset(10)
            make.leading.equalTo(IconImageV.snp.trailing).offset(10)
            //: make.top.equalTo(IconImageV.snp.top)
            make.top.equalTo(IconImageV.snp.top)
        }
        //: cardImg.snp.makeConstraints { make in
        cardImg.snp.makeConstraints { make in
            //: make.leading.equalTo(nameLabel.snp.trailing).offset(4)
            make.leading.equalTo(nameLabel.snp.trailing).offset(4)
            //: make.centerY.equalTo(nameLabel)
            make.centerY.equalTo(nameLabel)
            //: make.height.equalTo(16)
            make.height.equalTo(16)
            //: make.width.equalTo(16)
            make.width.equalTo(16)
        }
        //: sexBtn.snp.makeConstraints { make in
        sexBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(cardImg.snp.trailing).offset(3)
            make.leading.equalTo(cardImg.snp.trailing).offset(3)
            //: make.centerY.equalTo(nameLabel)
            make.centerY.equalTo(nameLabel)
            //: make.height.equalTo(16)
            make.height.equalTo(16)
            //: make.width.equalTo(33)
            make.width.equalTo(33)
            //: make.trailing.lessThanOrEqualToSuperview().offset(-10)
            make.trailing.lessThanOrEqualToSuperview().offset(-10)
        }
        //: timeLabel.snp.makeConstraints { make in
        timeLabel.snp.makeConstraints { make in
            //: make.leading.equalTo(IconImageV.snp.trailing).offset(10)
            make.leading.equalTo(IconImageV.snp.trailing).offset(10)
            //: make.top.equalTo(nameLabel.snp.bottom).offset(2)
            make.top.equalTo(nameLabel.snp.bottom).offset(2)
        }
        //: messageLabel.snp.makeConstraints { make in
        messageLabel.snp.makeConstraints { make in
            //: make.top.equalTo(IconImageV.snp.bottom).offset(10)
            make.top.equalTo(IconImageV.snp.bottom).offset(10)
            //: make.leading.equalTo(backView).offset(10)
            make.leading.equalTo(backView).offset(10)
            //: make.trailing.equalTo(backView.snp.trailing).offset(-10)
            make.trailing.equalTo(backView.snp.trailing).offset(-10)
        }

        //: chatBtn.snp.makeConstraints { make in
        chatBtn.snp.makeConstraints { make in
            //: make.top.equalTo(messageLabel.snp.bottom).offset(10)
            make.top.equalTo(messageLabel.snp.bottom).offset(10)
            //: make.trailing.equalTo(backView.snp.trailing).offset(-10)
            make.trailing.equalTo(backView.snp.trailing).offset(-10)
            //: make.height.equalTo(37)
            make.height.equalTo(37)
            //: make.bottom.equalTo(backView.snp.bottom).offset(-10)
            make.bottom.equalTo(backView.snp.bottom).offset(-10)
        }
        //: quickGreetingBtn.snp.makeConstraints { make in
        quickGreetingBtn.snp.makeConstraints { make in
            //: make.centerY.equalTo(chatBtn)
            make.centerY.equalTo(chatBtn)
            //: make.trailing.equalTo(chatBtn.snp.leading).offset(-12)
            make.trailing.equalTo(chatBtn.snp.leading).offset(-12)
            //: make.height.equalTo(37)
            make.height.equalTo(37)
            //: make.width.equalTo((PositionLabelReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue) ? 148:76)
            make.width.equalTo((PositionLabelReactiveCompatible.share.loginUserMode.sex == PetitionKeyRepresentable.female.rawValue) ? 148 : 76)
        }
    }
}
