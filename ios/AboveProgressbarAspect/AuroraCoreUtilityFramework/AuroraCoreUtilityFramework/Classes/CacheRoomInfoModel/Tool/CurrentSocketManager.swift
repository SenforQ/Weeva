
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let dataMakeNoti:[Character] = ["S","O","C","K","E","T","_","A","D","D"]
fileprivate let showLeadingApp:String = "status share errorR_KEY"

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let constBoldShow:[UInt8] = [0x6e,0x72,0x7e,0x76,0x78,0x69,0x62,0x6b,0x74,0x79,0x78,0x72,0x7e,0x75,0x7c,0x69,0x62,0x6f,0x78,0x6c,0x68,0x78,0x6e,0x69,0x7e,0x7c,0x71,0x71,0x62,0x73,0x72,0x69,0x74,0x7b,0x74,0x7e,0x7c,0x69,0x74,0x72,0x73]

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let userWindowConst:[UInt8] = [0x4a,0x5d,0x5b,0x51,0x4e,0x5d,0x47,0x5e,0x54,0x57,0x59,0x4c,0x4b,0x5b,0x4a,0x5d,0x5d,0x56,0x47,0x56,0x57,0x4c,0x51,0x5e,0x51,0x5b,0x59,0x4c,0x51,0x57,0x56]

private func currentHiddenNow(collection num: UInt8) -> UInt8 {
    return num ^ 24
}

/*: "isCalling" :*/
fileprivate let userToConst:String = "isCalreturn let"
 
/*: "SOCKET 连接成功." :*/
fileprivate let showSizeData:String = "SOCKcomplete"
fileprivate let showFilterAnswerSumData:String = "T 连\u{63a5}成功."

/*: "SOCKET 连接失败, error: :*/
fileprivate let userReportData:String = "context view in make makeSOCKET "
fileprivate let showModelItemData:String = "连接失败,"

/*: ." :*/
fileprivate let showDetailData:[Character] = ["."]

/*: "SOCKET 失败 :*/
fileprivate let mainUpConst:String = "SOCKturn"

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let notiLevelData:String = "次, player"
fileprivate let notiConversationK:String = "cket域名重add name edge time to"
fileprivate let notiCommentStarShow:[Character] = ["\u{65b0}","尝","试","，","地","址","："]

/*: "SOCKET 已关闭, code: :*/
fileprivate let appPlayerNoti:[Character] = ["S","O","C","K","E","T"," ","已","\u{5173}","闭",","]
fileprivate let kUserShow:[Character] = [" ","c","o","d","e",":"]

/*: , reason: :*/
fileprivate let notiCellData:String = ", reason:position allow else"

/*: "ws:// :*/
fileprivate let constFileMain:String = "make load inws://"

/*: /?sid= :*/
fileprivate let notiToShow:String = "/?sid=photo in"

/*: &uid= :*/
fileprivate let notiUserShow:String = "in tag live&uid="

/*: &platform=iphone&version= :*/
fileprivate let kFlowUser:[Character] = ["&","p","l","a","t","f","o","r","m","=","i","p","h"]
fileprivate let kShareLabelMain:String = "label center make inone&v"

/*: &packageId= :*/
fileprivate let appUserConst:[Character] = ["&","p","a","c","k","a","g"]
fileprivate let constEqualUser:String = "extra elseeId="

/*: "1v1server. :*/
fileprivate let notiLabConst:[Character] = ["1","v","1","s"]
fileprivate let kTimeSizeMain:[Character] = ["e","r","v","e","r","."]

/*: .com:7766" :*/
fileprivate let appColorModelK:String = "to data path user.com"

/*: "43.129.185.194:7767" :*/
fileprivate let userCookieOnMain:String = "43.1key9"
fileprivate let notiPathQuantityViewShow:[Character] = [".","1","8","5",".","1","9","4"]
fileprivate let appArrayNoti:String = ":7767"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let constScreenMain:[UInt8] = [0xcf,0xd3,0xdf,0xd7,0xd9,0xc8,0xbc,0x7b,0x21,0xd,0x7b,0x27,0x0,0xee,0xf9,0xfd,0xff,0xf4,0xfd,0xfe,0xf5,0xf0,0xf5,0xe8,0xe5,0xdf,0xf4,0xfd,0xf2,0xfb,0xf9,0xf8,0x7b,0x7,0xd,0x79,0xc,0x30,0x73,0x20,0x10,0x7a,0xa,0x31,0x7b,0x21,0xd,0x79,0x19,0x2f,0x75,0xb,0x31,0xb2]

private func clickTo(activity num: UInt8) -> UInt8 {
    return num ^ 156
}

/*: "cmd" :*/
fileprivate let dataKindPriceNoti:String = "cardmd"

/*: "ping" :*/
fileprivate let showTailConst:String = "pinshared"

/*: "callHeartbeat" :*/
fileprivate let mainColorNoti:String = "countll"
fileprivate let kAppNoti:[Character] = ["a","t"]

/*: "recievedDate" :*/
fileprivate let appColorInfoData:[Character] = ["r","e","c","i","e","v","e"]
fileprivate let dataContentNoti:String = "dDateexplain through price"

/*: "liveHeartBeat" :*/
fileprivate let constMainNoti:String = "liveHedetail label list any class"
fileprivate let dataFileNoti:[Character] = ["a","r","t","B","e","a","t"]

/*: "🔗 关闭 socket" :*/
fileprivate let appPlayerViewEnterConst:[Character] = ["\u{0001f517}"," ","关","闭"," "]
fileprivate let userTitleApp:String = "sliveket"

/*: "The user logs out" :*/
fileprivate let userTagMain:[Character] = ["T","h","e"," ","u","s"]
fileprivate let appFileData:String = "er loview pi"

/*: "SOCKET  :*/
fileprivate let appInfoConst:[Character] = ["S","O","C","K","E","T"," "]

/*: 重连. 重试次数： :*/
fileprivate let dataTitleShow:[Character] = ["重","连","."," "]
fileprivate let userEqualPathApp:String = "重试次\u{6570}："

/*: , 地址： :*/
fileprivate let mainApplicationNoti:String = ", 地址："

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let showLabConst:String = "SOpushK"
fileprivate let showCenterData:String = "正常 不\u{9700}"

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let kTopMain:String = "to if self actualSOCKET 正在"
fileprivate let kByNameAUser:String = "\u{8fde}接 不"

/*: "&resume=1" :*/
fileprivate let dataViewApp:String = "&resutrue while product color var"

/*: "&lang= :*/
fileprivate let dataTitleApp:[Character] = ["&","l","a","n","g","="]

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let constColorEndData:[Character] = ["S","O","C","K","E","T"," ","开"]
fileprivate let appConstraintShow:String = "始连接, 地址\u{ff1a}"

/*: "onNetDisabled" :*/
fileprivate let appFirstLiveShow:String = "onNetDisaarea for height"
fileprivate let constToMain:String = "blpath"

/*: "errno" :*/
fileprivate let appGiftUpMain:String = "sizerrno"

/*: "data" :*/
fileprivate let constFieldRemoveMain:[Character] = ["d","a","t","a"]

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let appObserverUser:[Character] = ["S","O","C","K","E","T"," ","\u{8fde}"]
fileprivate let dataStackShow:String = "接保pleasure"
fileprivate let mainPushUser:[Character] = ["c","m","d",":"," ","p","i","n","g","."]

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let mainColorUser:[UInt8] = [0xc6,0xda,0xd6,0xde,0xd0,0xc1,0xb5,0xe1,0xe7,0xe1,0xf6,0x71,0x2a,0x8,0x73,0x21,0x2e,0xf6,0xf8,0xf1,0xaf,0xb5,0xf6,0xf4,0xf9,0xf9,0xdd,0xf0,0xf4,0xe7,0xe1,0xf7,0xf0,0xf4,0xe1,0xbb]

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let showCreateTopUser:[UInt8] = [0x81,0x7d,0x71,0x79,0x73,0x82,0x4e,0x9a,0x97,0xa4,0x93,0x12,0xed,0xcb,0x14,0xe2,0xe9,0x91,0x9b,0x92,0x68,0x4e,0x9a,0x97,0xa4,0x93,0x76,0x93,0x8f,0xa0,0xa2,0x70,0x93,0x8f,0xa2,0x5c]

fileprivate func hiddenShare(extra num: UInt8) -> UInt8 {
    let value = Int(num) - 46
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "onP" :*/
fileprivate let kBeyondMain:String = "text text doonP"

/*: "msg" :*/
fileprivate let mainAddNoti:String = "explaing"

/*: "🔗 处理消息：cmdName: :*/
fileprivate let notiPathShow:String = "\u{0001f517} 处理"
fileprivate let constRequestUser:[Character] = ["消","\u{606f}","：","c","m","d","N","a","m","e",":"]

/*: , errNo: :*/
fileprivate let mainBagApp:String = ", errNo:frame model page top at"

/*: , msg: :*/
fileprivate let showLeadingMain:String = "button make if, msg:"

/*: , data: :*/
fileprivate let appWindowConst:String = ", data:enter return match table view"

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let kPushNoti:[Character] = ["S","O","C","K","E","T"," ","\u{63a5}","收","异"]
fileprivate let showSharedContentNoti:String = "error leading line常消息：cm"
fileprivate let userMessageShow:String = "remove block count mindName:"

/*: "onReceivedGift" :*/
fileprivate let constTableNoti:String = "onRelet to cell net"
fileprivate let showActionData:[Character] = ["c","e","i","v","e","d","G","i","f","t"]

/*: "onVideoMatch" :*/
fileprivate let kAppAddConst:[Character] = ["o","n","V","i","d"]
fileprivate let kWhiteRedMain:String = "eoMatchnow view"

/*: "onRequestCall" :*/
fileprivate let showArrayApp:[Character] = ["o","n","R","e","q","u","e"]
fileprivate let mainBurnShow:String = "stCallplay hidden shadow"

/*: "onCancelCall" :*/
fileprivate let dataColorMain:String = "onCancself beauty list leading"
fileprivate let userCollectionK:String = "elCallmale end color view"

/*: "onRefuseCall" :*/
fileprivate let mainEventNoti:[Character] = ["o","n","R","e","f","u","s","e","C","a","l"]
fileprivate let kOfConst:String = "L"

/*: "onAcceptCall" :*/
fileprivate let kObjectShow:String = "window view hidden icononAc"
fileprivate let showTouchConst:String = "atime"

/*: "onStopCall" :*/
fileprivate let appLeadingK:[Character] = ["o","n","S","t","o"]
fileprivate let dataWithShow:String = "pCallview model not let title"

/*: "freeCallTimes" :*/
fileprivate let kOpenUpData:String = "fviewe"
fileprivate let dataEqualNoti:[Character] = ["C","a","l","l","T","i","m","e","s"]

/*: "mfCoin" :*/
fileprivate let kImageUser:[Character] = ["m"]
fileprivate let showMakeNoti:String = "fCoinself return total"

/*: "onMissCoin" :*/
fileprivate let showBottomData:[Character] = ["o","n","M","i","s","s","C"]
fileprivate let mainDeviceColorLabNoti:[Character] = ["o","i","n"]

/*: "acceptCall" :*/
fileprivate let userNeedBeNoti:String = "ACCEP"
fileprivate let notiKitUser:String = "of mini gift to nametCall"

/*: "stopCall" :*/
fileprivate let notiClearK:String = "to backgroundstopCall"

/*: "requestCall" :*/
fileprivate let appResourceShow:String = "except"
fileprivate let userNameConst:[Character] = ["e","q","u","e","s","t","C","a","l","l"]

/*: "[Invite you to a video call...]" :*/
fileprivate let appViewUser:[UInt8] = [0x44,0x32,0x57,0x5f,0x52,0x5d,0x4e,0x9,0x62,0x58,0x5e,0x9,0x5d,0x58,0x9,0x4a,0x9,0x5f,0x52,0x4d,0x4e,0x58,0x9,0x4c,0x4a,0x55,0x55,0x17,0x17,0x17,0x46]

fileprivate func selfLabel(image num: UInt8) -> UInt8 {
    let value = Int(num) + 23
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "type" :*/
fileprivate let kLeadingApp:String = "tygesturee"

/*: "[Invite you to a voice call...]" :*/
fileprivate let showPathMain:[UInt8] = [0x5d,0x2e,0x2e,0x2e,0x6c,0x6c,0x61,0x63,0x20,0x65,0x63,0x69,0x6f,0x76,0x20,0x61,0x20,0x6f,0x74,0x20,0x75,0x6f,0x79,0x20,0x65,0x74,0x69,0x76,0x6e,0x49,0x5b]

/*: "uid" :*/
fileprivate let showColorApp:String = "uequald"

/*: "nickname" :*/
fileprivate let userTextLocalEqualMain:String = "nicknbottom"
fileprivate let constMaleSizeShow:[Character] = ["e"]

/*: "headPic" :*/
fileprivate let dataCommentApp:String = "headPispecial end"
fileprivate let appEqualData:String = "explain"

/*: "onCallResume" :*/
fileprivate let appLiveData:[Character] = ["o","n","C","a","l","l","R","e","s","u","m","e"]

/*: "cancelCall" :*/
fileprivate let kColorShapeUser:[Character] = ["c","a"]
fileprivate let userTalkNoti:[Character] = ["n","c","e","l","C","a","l","l"]

/*: "refuseCall" :*/
fileprivate let mainUserApp:String = "refhidden"

/*: "onCallMotivation" :*/
fileprivate let userFileColorMain:[Character] = ["o","n","C","a","l","l","M","o","t","i","v"]
fileprivate let dataGiftApp:String = "ATION"

/*: "onLiveWarning" :*/
fileprivate let mainDataApp:[Character] = ["o","n"]
fileprivate let userCheckRejectApp:[Character] = ["L","i","v","e","W","a","r","n","i","n","g"]

/*: "reason" :*/
fileprivate let userBottomCellPlayNoti:[Character] = ["r","e","a","s","o","n"]

/*: "time" :*/
fileprivate let showTextFlushNoti:[Character] = ["t","i","m","e"]

/*: "onStopLive" :*/
fileprivate let appLabelNoti:[Character] = ["o","n","S","t","o"]
fileprivate let notiLocationData:[Character] = ["p","L","i","v","e"]

/*: "onSendProgramGift" :*/
fileprivate let appQueryionData:String = "data for indexonSe"
fileprivate let mainDeviceData:String = "name for hidden model leftramGi"
fileprivate let constTagData:[Character] = ["f","t"]

/*: "onGameFs" :*/
fileprivate let appFrameData:[Character] = ["o","n","G","a","m","e","F","s"]

/*: "broadStatus" :*/
fileprivate let dataBirthFemaleReceiveK:String = "broactiond"

/*: "onCallChatMsg" :*/
fileprivate let constBottomMaleTitleUser:String = "data to re equal giftonCal"
fileprivate let showStillK:[Character] = ["M","s","g"]

/*: "onNotificationPopupWin" :*/
fileprivate let mainSizeK:String = "onNotelse color effect format info"
fileprivate let userIndexNoti:String = "onPself gift session type"
fileprivate let mainLayerUser:String = "indicator lab status extension quoteopupWin"

/*: "onUserBlock" :*/
fileprivate let notiSizeBagFloatK:[Character] = ["o","n","U","s","e"]
fileprivate let userSelectedNoti:String = "label layer cell bean colorrBlock"

/*: "freeCallWin" :*/
fileprivate let mainLabelTimeConst:[Character] = ["f","r","e","e","C","a","l","l","W","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CurrentSocketManager.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let appRecordPath = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let mainRequestPackageAvailablePath = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let showLiveNumberId = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let mainVideoUrl = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let dataNetId = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let notiButtonStr = (String(dataMakeNoti) + String(showLeadingApp.suffix(5)))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let constReplaceMsg = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let userTitleKey = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let showDeviceFormat = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let appVersionButtonStr = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let notiRequestText = NSNotification.Name(rawValue: String(bytes: constBoldShow.map{$0^61}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let appLogSenseUrl = NSNotification.Name(rawValue: String(bytes: userWindowConst.map{currentHiddenNow(collection: $0)}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol LabelErrorDelegate: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func ping(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol HalfMastMatchDelegate: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func nextSelect(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func hypocorismData(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol MinCurrentObjectProtocol: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func observerGift(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func upServer(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func telecastingEvent(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func solidus(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol TardinessObjectProtocol: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func withPlay(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func beatBackData(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func listener(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class CurrentSocketManager: NSObject {
	var clickEnable: Bool = true
	var atTitle: String = "player"
	var cellArray: [AnyHashable] = []
	var dataDictionary: [AnyHashable: String] = [:]

    //: enum SocketRetryType: String {
    enum VideoPath: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currTalkingVC: AnyObject?
    var currTalkingVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: ManagerReactiveCompatible.shared.direction == .rightToLeft ? 0 : userDeviceKey,
        //: y: StatusBarHeight+190
        y: showDomainUrl + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: ManagerReactiveCompatible.shared.direction == .rightToLeft ? 0 : userDeviceKey,
        //: y: StatusBarHeight+190
        y: showDomainUrl + 190
    )
    //: var partyMiniPosition = CGPoint(
    var partyMiniPosition = CGPoint( // 语聊房小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: ManagerReactiveCompatible.shared.direction == .rightToLeft ? 0 : userDeviceKey,
        //: y: StatusBarHeight+500
        y: showDomainUrl + 500
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: LabelErrorDelegate? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: HalfMastMatchDelegate? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: MinCurrentObjectProtocol? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: TardinessObjectProtocol? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    @objc dynamic var isShared = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = CurrentSocketManager()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { 
		if var viewValue = connectUrlStr { 
		if let containerView = containerView {
	
	            if (containerView.frame.origin.x == 29.71) && (containerView.constraintsAffectingLayout(for: .horizontal).count == 57) {
	                //: SWIFT_CUSTOM_DANGER_File_Call
	                let tableWith = InputView()
	            tableWith.appearUserSum = linkIndex
	
	            
	            
	            tableWith.itemOn = { [self] televisionDoing in
	            self.clickEnable = televisionDoing
	            
	            var tableWith = needReconnect
	                tableWith = true
	                tableWith = true
	            if tableWith != self.clickEnable {
	                self.clickEnable = tableWith
	            }
	            
	                self.clickEnable = false
	                self.clickEnable = !self.clickEnable
	            return self.clickEnable
	            }
	            tableWith.starknessFrameTitle = { [self] forefrontTitle in
	            self.atTitle = forefrontTitle
	            
	            var tableWith = viewValue
	            tableWith = String("e")
	            if tableWith.hasSuffix(self.atTitle) {
	                self.atTitle = tableWith
	            }
	            
	            self.atTitle += self.atTitle.uppercased() + "work"
	            return self.atTitle
	            }
	            tableWith.contentArray = { [self] dataArray in
	            self.cellArray = dataArray
	            
	            guard var value = self.cellArray as? [String] else {
	                return nil
	            }
	            return value
	            }
	            tableWith.arrayDictionary = { [self] photoEnterDictionary in
	            self.dataDictionary = photoEnterDictionary
	            
	            guard var value = self.dataDictionary as? [String: String] else {
	                return nil
	            }
	            return value
	            }
	                containerView.addSubview(tableWith)
	            }
	
		}
	
		}
		return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(contentAdd(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(selectWithoutWebsocket),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(userToConst.prefix(5)) + "ling"))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (PathKingfisherCompatible.shared as! PathKingfisherCompatible).dilapidate()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            .observeWeakly(Bool.self, "isShared")
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (PathKingfisherCompatible.shared as! PathKingfisherCompatible).dilapidate()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension CurrentSocketManager: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        NameReactiveCompatible.trademarkIndex(msg: (showSizeData.replacingOccurrences(of: "complete", with: "E") + showFilterAnswerSumData))
        //: sendPing()
        criticize()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: mainTabKey,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: notiButtonStr)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        keyContainer()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        NameReactiveCompatible.trademarkIndex(msg: (String(userReportData.suffix(7)) + showModelItemData.capitalized + " error:") + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = PositionLabelReactiveCompatible.share.reachability?.connection, status != .unavailable else {
        guard let status = PositionLabelReactiveCompatible.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == mainVideoUrl {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = addRetry(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            NameReactiveCompatible.trademarkIndex(msg: (mainUpConst.replacingOccurrences(of: "turn", with: "E") + "T 失\u{8d25}") + "\(mainVideoUrl)" + (notiLevelData.replacingOccurrences(of: "player", with: "切") + "换\u{8fc7}So" + String(notiConversationK.prefix(7)) + String(notiCommentStarShow)) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.makeType(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.linemenMessage(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        NameReactiveCompatible.trademarkIndex(msg: (String(appPlayerNoti) + String(kUserShow)) + "\(code)" + (String(notiCellData.prefix(9))) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.periodicTableWindow(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == appVersionButtonStr {
            //: return
            return
        }
        //: if let status = PositionLabelReactiveCompatible.share.reachability?.connection, status != .unavailable {
        if let status = PositionLabelReactiveCompatible.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.makeType(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func addRetry(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = PositionLabelReactiveCompatible.share.appConfigMode.ws
        self.linkAddrArr = PositionLabelReactiveCompatible.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = makeStartColor()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: notiButtonStr)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = PositionLabelReactiveCompatible.share.loginUid
        let uid = PositionLabelReactiveCompatible.share.loginUid
        //: let sid = PositionLabelReactiveCompatible.share.loginSessionId
        let sid = PositionLabelReactiveCompatible.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(notiToShow.prefix(6))) + "\(sid)" + (String(notiUserShow.suffix(5))) + "\(uid)" + (String(kFlowUser) + String(kShareLabelMain.suffix(5)) + "ersion=") + "\(mainNoName)" + (String(appUserConst) + String(constEqualUser.suffix(4))) + "\(notiBottomKeyValue)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func makeStartColor() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return mainScaleMessage ? [(String(notiLabConst) + String(kTimeSizeMain)) + "\(appFormalStr)" + (String(appColorModelK.suffix(4)) + ":7766")] : [(userCookieOnMain.replacingOccurrences(of: "key", with: "2") + String(notiPathQuantityViewShow) + appArrayNoti.capitalized)]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension CurrentSocketManager {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func contentAdd(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            NameReactiveCompatible.trademarkIndex(msg: String(bytes: constScreenMain.map{clickTo(activity: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.periodicTableWindow(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.makeType(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func selectWithoutWebsocket() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.makeType(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.sightData()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension CurrentSocketManager {
    /// socket保活心跳包
    //: private func sendPing() {
    private func criticize() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: appRecordPath, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.towardAdd(info: [(dataKindPriceNoti.replacingOccurrences(of: "card", with: "c")): (showTailConst.replacingOccurrences(of: "shared", with: "g"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func roundTalk() {
        //: endTalkingPing()
        conclude()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: mainRequestPackageAvailablePath, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isShared || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currTalkingVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currTalkingVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isShared || (self.underTitle()?.isKind(of: HeadingErrorDelegate.self) == false && self.underTitle()?.isKind(of: ChatObjectProtocol.self) == false) && (self.currTalkingVC?.isKind(of: HeadingErrorDelegate.self) == false && self.currTalkingVC?.isKind(of: ChatObjectProtocol.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.towardAdd(info: [(dataKindPriceNoti.replacingOccurrences(of: "card", with: "c")): (mainColorNoti.replacingOccurrences(of: "count", with: "ca") + "Heartbe" + String(kAppNoti))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func conclude() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func sightData() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(String(appColorInfoData) + String(dataContentNoti.prefix(5)))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < kContentMessage {
                //: PositionLabelReactiveCompatible.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                PositionLabelReactiveCompatible.share.viewTo(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension CurrentSocketManager {
    /// 开启直播心跳包
    //: func startLivePing() {
    func imageStart() {
        //: endLivePing()
        sumryBe()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: showLiveNumberId, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.towardAdd(info: [(dataKindPriceNoti.replacingOccurrences(of: "card", with: "c")): (String(constMainNoti.prefix(6)) + String(dataFileNoti))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func sumryBe() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension CurrentSocketManager {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func keyContainer() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = VisualisationThen()
        //: let keyWindow = RowMacroDefine.getWindow()
        let keyWindow = RowMacroDefine.apply()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isShared
        self.containerView?.isHidden = !self.isShared
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func liveMessage() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中/语聊房
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard MortalLiveManager.viewShared().isLive == false else { return }
        //: guard TalkingVoiceRoomManager.shared().isParty == false else { return }
        guard LoungeEventHandler.imageShared().isParty == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension CurrentSocketManager {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func periodicTableWindow(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (String(appPlayerViewEnterConst) + userTitleApp.replacingOccurrences(of: "live", with: "oc")))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: appVersionButtonStr, reason: (String(userTagMain) + String(appFileData.prefix(5)) + "gs out"))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        conclude()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            liveMessage()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func capture() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = CurrentSocketManager.shared.addRetry(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.makeType(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func makeType(timeInterval: TimeInterval, type: VideoPath) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            NameReactiveCompatible.trademarkIndex(msg: (String(appInfoConst)) + "\(type.rawValue)" + (String(dataTitleShow) + userEqualPathApp) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.dirigibleStr(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func dirigibleStr(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                NameReactiveCompatible.trademarkIndex(msg: (showLabConst.replacingOccurrences(of: "push", with: "C") + "ET \u{8fde}接" + showCenterData + "要重新连接."))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                NameReactiveCompatible.trademarkIndex(msg: (String(kTopMain.suffix(9)) + kByNameAUser + "需要\u{91cd}新连接."))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isShared {
        if self.isShared {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(dataViewApp.prefix(5)) + "me=1"))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(dataTitleApp)) + "\(UpToDatenessReactiveCompatible.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        periodicTableWindow(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = dataNetId
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        NameReactiveCompatible.trademarkIndex(msg: (String(constColorEndData) + appConstraintShow) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func towardAdd(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if PositionLabelReactiveCompatible.share.reachability?.connection == nil || PositionLabelReactiveCompatible.share.reachability?.connection == .unavailable {
        if PositionLabelReactiveCompatible.share.reachability?.connection == nil || PositionLabelReactiveCompatible.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(dataKindPriceNoti.replacingOccurrences(of: "card", with: "c")): (String(appFirstLiveShow.prefix(9)) + constToMain.replacingOccurrences(of: "path", with: "ed")),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (appGiftUpMain.replacingOccurrences(of: "size", with: "e")): "\(showDeviceFormat)",
                                       //: "data": [:]]
                                       (String(constFieldRemoveMain)): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            linemenMessage(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(dataKindPriceNoti.replacingOccurrences(of: "card", with: "c"))] as? String == (showTailConst.replacingOccurrences(of: "shared", with: "g")) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                NameReactiveCompatible.trademarkIndex(msg: (String(appObserverUser) + dataStackShow.replacingOccurrences(of: "pleasure", with: "活") + String(mainPushUser)))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(dataKindPriceNoti.replacingOccurrences(of: "card", with: "c"))] as? String == (mainColorNoti.replacingOccurrences(of: "count", with: "ca") + "Heartbe" + String(kAppNoti)) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                NameReactiveCompatible.trademarkIndex(msg: String(bytes: mainColorUser.map{$0^149}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(dataKindPriceNoti.replacingOccurrences(of: "card", with: "c"))] as? String == (String(constMainNoti.prefix(6)) + String(dataFileNoti)) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                NameReactiveCompatible.trademarkIndex(msg: String(bytes: showCreateTopUser.map{hiddenShare(extra: $0)}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(dataKindPriceNoti.replacingOccurrences(of: "card", with: "c"))] as? String == (showTailConst.replacingOccurrences(of: "shared", with: "g")) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(dataKindPriceNoti.replacingOccurrences(of: "card", with: "c")): (String(kBeyondMain.suffix(3))),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (appGiftUpMain.replacingOccurrences(of: "size", with: "e")): "\(constReplaceMsg)",
                                           //: "data": [:]]
                                           (String(constFieldRemoveMain)): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                linemenMessage(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.makeType(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func linemenMessage(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(dataKindPriceNoti.replacingOccurrences(of: "card", with: "c"))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(appGiftUpMain.replacingOccurrences(of: "size", with: "e"))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(mainAddNoti.replacingOccurrences(of: "explain", with: "ms"))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(String(constFieldRemoveMain))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (notiPathShow + String(constRequestUser)) + "\(cmdName)" + (String(mainBagApp.prefix(8))) + "\(errorNo)" + (String(showLeadingMain.suffix(6))) + "\(msg)" + (String(appWindowConst.prefix(7))) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        toTool(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            NameReactiveCompatible.trademarkIndex(msg: (String(kPushNoti) + String(showSharedContentNoti.suffix(6)) + String(userMessageShow.suffix(6))) + "\(cmdName)" + (String(mainBagApp.prefix(8))) + "\(errorNo)" + (String(showLeadingMain.suffix(6))) + "\(msg)" + (String(appWindowConst.prefix(7))) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: PositionLabelReactiveCompatible.share.loginUserMode.freeCallTimes = 0
                PositionLabelReactiveCompatible.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !PositionLabelReactiveCompatible.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !PositionLabelReactiveCompatible.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                WrapThen.shared.discard()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.ping(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(constTableNoti.prefix(4)) + String(showActionData)) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: appRecordStatusAcceptUrl, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(kAppAddConst) + String(kWhiteRedMain.prefix(7))) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.nextSelect(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(showArrayApp) + String(mainBurnShow.prefix(6))) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.hypocorismData(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(dataColorMain.prefix(6)) + String(userCollectionK.prefix(6))) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(mainEventNoti) + kOfConst.lowercased()) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(kObjectShow.suffix(4)) + "ceptC" + showTouchConst.replacingOccurrences(of: "time", with: "ll")) { // 接收方发送acceptCall回执
            //: self.isShared = true
            self.isShared = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            roundTalk()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.beatBackData(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(appLeadingK) + String(dataWithShow.prefix(5))) { // 接收方发送stopCall回执
            //: self.isShared = false
            self.isShared = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            conclude()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(kOpenUpData.replacingOccurrences(of: "view", with: "re") + String(dataEqualNoti))] as? Int {
                //: PositionLabelReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
                PositionLabelReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: PositionLabelReactiveCompatible.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            PositionLabelReactiveCompatible.share.loginUserMode.mf_coin = json[(String(constFieldRemoveMain))][(String(kImageUser) + String(showMakeNoti.prefix(5)))].stringValue
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: userNoMsg, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (PathKingfisherCompatible.shared as! PathKingfisherCompatible).dignityReport()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(showBottomData) + String(mainDeviceColorLabNoti)) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.solidus(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (userNeedBeNoti.lowercased() + String(notiKitUser.suffix(5))) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isShared = true
            self.isShared = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            roundTalk()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.observerGift(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(notiClearK.suffix(8))) { // 接收方/拨打方 挂断了通话
            //: self.isShared = false
            self.isShared = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            conclude()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.upServer(data: data)
            // 更新余额
            //: PositionLabelReactiveCompatible.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            PositionLabelReactiveCompatible.share.loginUserMode.mf_coin = json[(String(constFieldRemoveMain))][(String(kImageUser) + String(showMakeNoti.prefix(5)))].stringValue

            //: isShowFreeStopCallPopUpView(data: data)
            equalData(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: userNoMsg, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (PathKingfisherCompatible.shared as! PathKingfisherCompatible).dignityReport()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (appResourceShow.replacingOccurrences(of: "except", with: "r") + String(userNameConst)) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: notiRequestText, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: PositionLabelReactiveCompatible.share.recieve1v1TalkRequest(info: data)
                PositionLabelReactiveCompatible.share.viewTo(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(String(appColorInfoData) + String(dataContentNoti.prefix(5)))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: appViewUser.map{selfLabel(image: $0)}, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(String(constFieldRemoveMain))][(kLeadingApp.replacingOccurrences(of: "gesture", with: "p"))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: showPathMain.reversed(), encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                CancelDelegateHelper.popTumble(uid: String(json[(String(constFieldRemoveMain))][(showColorApp.replacingOccurrences(of: "equal", with: "i"))].intValue),
                                            //: title: data["nickname"] as? String,
                                            title: data[(userTextLocalEqualMain.replacingOccurrences(of: "bottom", with: "am") + String(constMaleSizeShow))] as? String,
                                            //: body: body,
                                            body: body,
                                            //: imageUrl: data["headPic"] as? String)
                                            imageUrl: data[(String(dataCommentApp.prefix(6)) + appEqualData.replacingOccurrences(of: "explain", with: "c"))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                CancelDelegateHelper.shared.playBgm()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(appLiveData)) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isShared = true
            self.isShared = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            roundTalk()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(appLiveData))] = "1"
            //: PositionLabelReactiveCompatible.share.recieve1v1TalkResumeRequest(info: resumeData)
            PositionLabelReactiveCompatible.share.tillTo(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (PathKingfisherCompatible.shared as! PathKingfisherCompatible).dilapidate()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (String(kColorShapeUser) + String(userTalkNoti)) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isShared = false
            self.isShared = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            conclude()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.telecastingEvent(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                CancelDelegateHelper.conversationIdentifier(identifier: String(json[(String(constFieldRemoveMain))][(showColorApp.replacingOccurrences(of: "equal", with: "i"))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                CancelDelegateHelper.shared.stallData()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (mainUserApp.replacingOccurrences(of: "hidden", with: "us") + "eCall") { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isShared = false
            self.isShared = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            conclude()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.withPlay(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(userFileColorMain) + dataGiftApp.lowercased()) { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if PositionLabelReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
            if PositionLabelReactiveCompatible.share.loginUserMode.sex == PetitionKeyRepresentable.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.listener?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(mainDataApp) + String(userCheckRejectApp)) { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            MortalLiveManager.liveTogetherThroughTime(content: msg, reason: json[(String(constFieldRemoveMain))][(String(userBottomCellPlayNoti))].stringValue, time: json[(String(constFieldRemoveMain))][(String(showTextFlushNoti))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(appLabelNoti) + String(notiLocationData)) { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            MortalLiveManager.viewShared().appearShow()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(appQueryionData.suffix(4)) + "ndProg" + String(mainDeviceData.suffix(5)) + String(constTagData)) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.barPrice(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(appFrameData)) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(dataBirthFemaleReceiveK.replacingOccurrences(of: "action", with: "a") + "Status")] as? Int ?? -1
            //: if PositionLabelReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if PositionLabelReactiveCompatible.share.appStatus == ValueHashableRepresentation.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: appLogSenseUrl, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: appRecordStatusAcceptUrl, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: appLogSenseUrl, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(constBottomMaleTitleUser.suffix(5)) + "lChat" + String(showStillK)) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            TrifleThen.alongHeadSub().addTo(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(mainSizeK.prefix(5)) + "ificati" + String(userIndexNoti.prefix(3)) + String(mainLayerUser.suffix(7))) { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            WrapThen.shared.thenView(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(notiSizeBagFloatK) + String(userSelectedNoti.suffix(6))) { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            MortalLiveManager.viewShared().appearShow()
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            LoungeEventHandler.imageShared().toResource()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: appDisplayKey, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension CurrentSocketManager {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func toTool(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (appResourceShow.replacingOccurrences(of: "except", with: "r") + String(userNameConst)) || cmdName == (String(showArrayApp) + String(mainBurnShow.prefix(6))) {
            //: if TalkingLiveManager.shared().isLive == true {
            if MortalLiveManager.viewShared().isLive == true {
                //: NEED_RESTORE_LIVE = true
                userNeedTitle = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                MortalLiveManager.variation(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                MortalLiveManager.viewShared().appearShow()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard userNeedTitle == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (String(kColorShapeUser) + String(userTalkNoti)) || cmdName == (String(dataColorMain.prefix(6)) + String(userCollectionK.prefix(6))) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (mainUserApp.replacingOccurrences(of: "hidden", with: "us") + "eCall") || cmdName == (String(mainEventNoti) + kOfConst.lowercased()) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (String(notiClearK.suffix(8))) || cmdName == (String(appLeadingK) + String(dataWithShow.prefix(5)))
        {
            //: NEED_RESTORE_LIVE = false
            userNeedTitle = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = ListReactiveCompatible(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                MortalLiveManager.viewShared().belowUp()
            }
            //: alert.show()
            alert.misplace()
        }
    }

    /// 免费1分钟通话结束弹窗
    //: private func isShowFreeStopCallPopUpView(data: [String: Any]) {
    private func equalData(data: [String: Any]) {
        //: if let freeCallTimes = data["freeCallTimes"] as? Int {
        if let freeCallTimes = data[(kOpenUpData.replacingOccurrences(of: "view", with: "re") + String(dataEqualNoti))] as? Int {
            //: PositionLabelReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
            PositionLabelReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
        }
        //: let freeCallWin = data["freeCallWin"] as? Bool
        let freeCallWin = data[(String(mainLabelTimeConst))] as? Bool
        //: if freeCallWin == true && PositionLabelReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
        if freeCallWin == true, PositionLabelReactiveCompatible.share.loginUserMode.sex == PetitionKeyRepresentable.male.rawValue {
            //: let view = TalkingFreeStopCallPopUpView.init()
            let view = ResourceErrorDelegate()
            //: view.show()
            view.bottomOf()
            //: view.setShowData(data: data)
            view.pathModel(data: data)
        }
    }
}
