
//: Declare String Begin

/*: "bg_message_top" :*/
fileprivate let dataAspectViewMain:String = "layer allbg_mess"
fileprivate let kFillData:String = "table"
fileprivate let dataRecommendShow:[Character] = ["e","_","t","o","p"]

/*: "777777" :*/
fileprivate let constColorAgeShow:[Character] = ["7","7","7","7","7","7"]

/*: "Messages" :*/
fileprivate let userMakeApp:String = "current beyond if new userMessages"

/*: "Who like me" :*/
fileprivate let notiGiftStorageUser:[Character] = ["W","h","o"," ","l","i","k","e"," "]
fileprivate let showSectionNoti:String = "targete"

/*: "Call" :*/
fileprivate let userLengthTabIndexApp:String = "Callstart remove"

/*: "#FF2348" :*/
fileprivate let kManagerData:String = "block first content if#FF2348"

/*: "Current network unavailable" :*/
fileprivate let userColorMain:String = "Currshow height disappear"
fileprivate let kObjectConst:String = "etwbottomrk"
fileprivate let appMomentMain:[Character] = [" ","u","n","a","v","a"]
fileprivate let kDataConst:String = "ikitabkite"

/*: "icon_yidu_pre" :*/
fileprivate let userCenterK:String = "iread"
fileprivate let kRowMain:String = "on_yleading self"

/*: "99+" :*/
fileprivate let mainMPackageData:[Character] = ["9","9","+"]

/*: "transform.rotation" :*/
fileprivate let userCollectionK:[Character] = ["t","r","a","n","s","f","o"]
fileprivate let showPriceNoti:String = "rm.roview image return"

/*: "transform.scale" :*/
fileprivate let showMakeNoti:String = "effectans"
fileprivate let kBlackConst:String = "scalocatione"

/*: "zoom&shake" :*/
fileprivate let kBeginData:[Character] = ["z","o","o","m","&","s","h","a","k","e"]

/*: "yyyy-MM-dd" :*/
fileprivate let kToolShow:[Character] = ["y","y","y","y","-","M","M","-","d","d"]

/*: "Allow %@ to send you notifications?" :*/
fileprivate let constTopApp:[UInt8] = [0x2a,0x7,0x7,0x4,0x1c,0x4b,0x4e,0x2b,0x4b,0x1f,0x4,0x4b,0x18,0xe,0x5,0xf,0x4b,0x12,0x4,0x1e,0x4b,0x5,0x4,0x1f,0x2,0xd,0x2,0x8,0xa,0x1f,0x2,0x4,0x5,0x18,0x54]

private func errorUser(sex num: UInt8) -> UInt8 {
    return num ^ 107
}

/*: "Cancel" :*/
fileprivate let notiContainerMain:String = "Canceltrue content list"

/*: "Settings" :*/
fileprivate let dataImageNoti:String = "Settingsgreen input stack"

/*: "badNumber" :*/
fileprivate let dataFrameApp:[Character] = ["b","a","d","N","u"]
fileprivate let kTabData:String = "mresultr"

/*: "isConnection" :*/
fileprivate let notiLiveConst:[Character] = ["i","s","C","o","n","n","e","c","t","i","o","n"]

/*: "networkStatus" :*/
fileprivate let kComponentUser:String = "neintimatewor"

/*: "unreadMessageNum" :*/
fileprivate let dataErrorK:[Character] = ["u","n","r","e","a","d","M","e","s","s","a"]
fileprivate let appViewData:String = "geNumtitle list text"

/*: "Do you want to mark all messages as read?" :*/
fileprivate let mainDisplayShow:[UInt8] = [0x2,0x29,0x66,0x3f,0x29,0x33,0x66,0x31,0x27,0x28,0x32,0x66,0x32,0x29,0x66,0x2b,0x27,0x34,0x2d,0x66,0x27,0x2a,0x2a,0x66,0x2b,0x23,0x35,0x35,0x27,0x21,0x23,0x35,0x66,0x27,0x35,0x66,0x34,0x23,0x27,0x22,0x79]

private func headGift(at num: UInt8) -> UInt8 {
    return num ^ 70
}

/*: "OK" :*/
fileprivate let kAppMain:String = "Olocation"

/*: "消息列表一键已读失败：code: :*/
fileprivate let showBetweenK:String = "消息列表to"
fileprivate let appModelK:[Character] = ["c","o","d","e",":"]

/*: , desc: :*/
fileprivate let showMakeData:[Character] = [","," ","d","e","s","c",":"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BlankThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/20.
//

//: import UIKit
import UIKit

//: class TalkingMessageViewController: TalkingBaseViewController {
class BlankThen: EmptyRecognizerDelegate {
	var imageCollectionDoing: Bool = true
	var atFromCount: Int = 54
	var valueText: String = "content"
	var italianRegionArray: [AnyHashable] = []
	var detailAnswerDictionary: [AnyHashable: String] = [:]

    //: var isNoChangeTop = false
    var isNoChangeTop = false
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var seleteIndex = 0
    private var seleteIndex = 0

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: setIsShowCleanAmimat()
        themeBlock()
    
		
		//: if_extract_code "effectValue" begin
		
		var effectValue = false
		if !effectValue {
		    var isValue = false
		    if let nextResponder = badgLB.next {
		        isValue = nextResponder.inputView != nil
		    }
		    effectValue = isValue
		}
		
		//: if_extract_code "effectValue" end
		
            if (badgLB.tintColor != nil && badgLB.tintColor.cgColor == UIColor(red: 0.29, green: 0.63, blue: 0.36, alpha: 0.92).cgColor) && (effectValue) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let nameTitle = WillView(frame: badgLB.bounds)
            nameTitle.chessPlayerLineSwitch = self.isNoChangeTop
            nameTitle.tvQuantity = seleteIndex
            nameTitle.imageSum = slideVC.makeScale

            
            nameTitle.stackOff = { [self] observeOn in
            self.imageCollectionDoing = observeOn
            
            var nameTitle = chatListVC.hideNavi
                nameTitle = true
                nameTitle = !nameTitle
            if nameTitle != self.imageCollectionDoing {
                self.imageCollectionDoing = nameTitle
            }
            
            return self.imageCollectionDoing
            }
            nameTitle.pathCount = { [self] soyaTotal in
            self.atFromCount = soyaTotal
            
            var nameTitle = seleteIndex
            nameTitle /= 4
            if nameTitle > self.atFromCount {
                self.atFromCount = nameTitle
            }
            
            return self.atFromCount
            }
            nameTitle.spellManagerText = { [self] hideTaskText in
            self.valueText = hideTaskText
            
            var popPrice: String.Encoding = .utf8
            if let popPriceString = try? String(contentsOf: URL(string: "system.begin")!, usedEncoding: &popPrice) {
                self.valueText = popPriceString
            }
            return self.valueText
            }
            nameTitle.playerMessageArray = { [self] itButtonArray in
            self.italianRegionArray = itButtonArray
            
            guard var value = self.italianRegionArray as? [String] else {
                return nil
            }
            return value
            }
            nameTitle.visualPropertyDictionary = { [self] indexDictionary in
            self.detailAnswerDictionary = indexDictionary
            
            guard var value = self.detailAnswerDictionary as? [String: String] else {
                return nil
            }
            return value
            }
                badgLB.addSubview(nameTitle)
            }

	}

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    
            if (chatListVC.modalPresentationCapturesStatusBarAppearance) && (chatListVC.viewIfLoaded != nil && chatListVC.viewIfLoaded!.frame.origin.y == 8.18) {
                //: SWIFT_CUSTOM_DANGER_File_Call
                let startMy = WillController()
            startMy.viewSizeCount = seleteIndex
            startMy.colorCount = slideVC.makeScale

            
                chatListVC.navigationController?.pushViewController(startMy.self, animated: true)
            }

	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: createUI()
        pushUi()
        //: bindInteraction()
        startBind()
        //: func__turnOnSystemNotification()
        insomniac()
    }

    // MARK: - Lazy load

    // 背景
    //: private lazy var colorImageView: UIImageView = {
    private lazy var colorImageView: UIImageView = {
        //: let colorV = UIImageView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: 100+StatusBarNavigationBarHeight))
        let colorV = UIImageView(frame: CGRect(x: 0, y: 0, width: userDeviceKey, height: 100 + constFailAppData))
        //: colorV.image = UIImage.BundleImageNamed(name: "bg_message_top")
        colorV.image = UIImage.imageName(name: (String(dataAspectViewMain.suffix(7)) + kFillData.replacingOccurrences(of: "table", with: "ag") + String(dataRecommendShow)))
        //: return colorV
        return colorV
        //: }()
    }()

    //: private lazy var noticeView: TalkingNoticeTipView = {
    private lazy var noticeView: CardThen = {
        //: let V = TalkingNoticeTipView()
        let V = CardThen()
        //: V.isHidden = true
        V.isHidden = true
        //: return V
        return V
        //: }()
    }()

    //: private lazy var slideVC: QYSlideNavigationViewController = {
    private lazy var slideVC: PlusViewDelegate = {
        //: let vc = QYSlideNavigationViewController()
        let vc = PlusViewDelegate()
        //: vc.topDistance = 0
        vc.topDistance = 0
        //: vc.controllerItems = controllerItems
        vc.controllerItems = controllerItems
        //: vc.titleItems = titleItems
        vc.titleItems = titleItems
        //: vc.rightSpace = 60
        vc.rightSpace = 60
        //: vc.titleViewBackgroundColor = .clear
        vc.titleViewBackgroundColor = .clear
        //: vc.titleFont = .pingfangFont(type: .Medium, fontSize: 16)
        vc.titleFont = .viewPath(type: .Medium, fontSize: 16)
        //: vc.selectTitleFont = .pingfangFont(type: .Medium, fontSize: 18)
        vc.selectTitleFont = .viewPath(type: .Medium, fontSize: 18)
        //: vc.normalColor = UIColor(hex: "777777")!
        vc.normalColor = UIColor(hex: (String(constColorAgeShow)))!
        //: vc.selectedColor = UIColor.appTitleColor()
        vc.selectedColor = UIColor.read()
        //: vc.hasBttomTabBar = true
        vc.hasBttomTabBar = true
        //: vc.delegate = self
        vc.delegate = self
        //: vc.scrollEnabled = false
        vc.scrollEnabled = false
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var controllerItems: [TalkingBaseViewController] = {
    private lazy var controllerItems: [EmptyRecognizerDelegate] = {
        //: var arr = [self.chatListVC, TalkingWhoLikeViewController()]
        var arr = [self.chatListVC, TempGiftViewController()]
        //: if PositionLabelReactiveCompatible.share.loginUserMode.callTabSwitch == 1 {
        if PositionLabelReactiveCompatible.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert(TalkingVideoCallVC(), at: arr.count-1)
            arr.insert(PlayerThen(), at: arr.count - 1)
        }
        //: return arr
        return arr

        //: }()
    }()

    //: private lazy var chatListVC: TalkingChatListViewController = {
    private lazy var chatListVC: MinViewDelegate = {
        //: let vc = TalkingChatListViewController()
        let vc = MinViewDelegate()
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var titleItems: [String] = {
    private lazy var titleItems: [String] = {
        //: var arr = ["Messages".localized, "Who like me".localized]
        var arr = [(String(userMakeApp.suffix(8))).localized, (String(notiGiftStorageUser) + showSectionNoti.replacingOccurrences(of: "target", with: "m")).localized]
        //: if PositionLabelReactiveCompatible.share.loginUserMode.callTabSwitch == 1 {
        if PositionLabelReactiveCompatible.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert("Call".localized, at: arr.count-1)
            arr.insert((String(userLengthTabIndexApp.prefix(4))).localized, at: arr.count - 1)
        }
        //: return arr
        return arr
        //: }()
    }()

    // 断网提醒
    //: private lazy var netTipLabel: UILabel = {
    private lazy var netTipLabel: UILabel = {
        //: let tipView = UILabel.init(frame: CGRect(x: 0, y: StatusBarNavigationBarHeight, width: ScreenWidth, height: 30))
        let tipView = UILabel(frame: CGRect(x: 0, y: constFailAppData, width: userDeviceKey, height: 30))
        //: tipView.font = UIFont.pingfangRugularFont(fontSize: 14)
        tipView.font = UIFont.startFontSize(fontSize: 14)
        //: tipView.textColor = .white
        tipView.textColor = .white
        //: tipView.backgroundColor = UIColor.init(hex: "#FF2348")?.withAlphaComponent(0.8)
        tipView.backgroundColor = UIColor(hex: (String(kManagerData.suffix(7))))?.withAlphaComponent(0.8)
        //: tipView.textAlignment = .center
        tipView.textAlignment = .center
        //: tipView.text = "Current network unavailable".localized
        tipView.text = (String(userColorMain.prefix(4)) + "ent n" + kObjectConst.replacingOccurrences(of: "bottom", with: "o") + String(appMomentMain) + kDataConst.replacingOccurrences(of: "kit", with: "l")).localized
        //: tipView.isHidden = true
        tipView.isHidden = true
        //: return tipView
        return tipView
        //: }()
    }()

    // message红点角标
    //: private lazy var msgNumLabel: BadgeLab = {
    private lazy var msgNumLabel: LightThen = {
        //: let numbLabel = BadgeLab()
        let numbLabel = LightThen()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // whoLikeMe红点角标
    //: private lazy var badgLB: BadgeLab = {
    private lazy var badgLB: LightThen = {
        //: let numbLabel = BadgeLab()
        let numbLabel = LightThen()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // 一键清除
    //: private lazy var cleanBtn: UIButton = {
    private lazy var cleanBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.backgroundColor = .clear
        btn.backgroundColor = .clear
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .normal)
        btn.setImage(UIImage.imageName(name: (userCenterK.replacingOccurrences(of: "read", with: "c") + String(kRowMain.prefix(4)) + "idu_pre")), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .highlighted)
        btn.setImage(UIImage.imageName(name: (userCenterK.replacingOccurrences(of: "read", with: "c") + String(kRowMain.prefix(4)) + "idu_pre")), for: .highlighted)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - PhotoVideoNavigationDelegate

//: extension TalkingMessageViewController: QYSlideNavigationDelegate {
extension BlankThen: PhotoVideoNavigationDelegate {
    //: func selectedViewController(_ viewController: UIViewController, index: Int) {
    func custom(_ viewController: UIViewController, index: Int) {
        //: self.seleteIndex = Int(index)
        self.seleteIndex = Int(index)
        //: if viewController.isKind(of: TalkingChatListViewController.self) {
        if viewController.isKind(of: MinViewDelegate.self) {
            //: self.cleanBtn.isHidden = false
            self.cleanBtn.isHidden = false
            //: setIsShowCleanAmimat()
            themeBlock()

            //: } else if viewController.isKind(of: TalkingWhoLikeViewController.self) {
        } else if viewController.isKind(of: TempGiftViewController.self) {
            //: self.cleanBtn.isHidden = true
            self.cleanBtn.isHidden = true
            //: uploadRecord.uploadRecordEvent(eventID: ClickWLikeMeTabNoP)
            constTimeStatusValue.fileStr(eventID: kLineContent)
        }
    }
}

// MARK: - Event

//: extension TalkingMessageViewController {
extension BlankThen {
    //: func setIsTopAll() {
    func columnAll() {
        /// 默认选中all
        //: if seleteIndex == 0 {
        if seleteIndex == 0 {
            //: let vc = slideVC.controllerItems.first as? TalkingChatListViewController
            let vc = slideVC.controllerItems.first as? MinViewDelegate
            //: vc?.resetToTopItemView()
            vc?.toChange()
            // 刷新在线状态
            //: vc?.refreshOnlineStatus(isFirst: true)
            vc?.enablee(isFirst: true)
        }
    }

    /// 无网络提示
    //: func func__checkNetTipState() {
    func barState() {
        //: if PositionLabelReactiveCompatible.share.networkStatus != .Unavailable && TabThen.shared.isConnection {
        if PositionLabelReactiveCompatible.share.networkStatus != .Unavailable, TabThen.shared.isConnection {
            //: netTipLabel.isHidden = true
            netTipLabel.isHidden = true
            //: } else {
        } else {
            //: netTipLabel.isHidden = false
            netTipLabel.isHidden = false
        }
    }

    /// message红点
    //: func func__messagebadgLBState(num: Int) {
    func administrativeDistrictNum(num: Int) {
        //: if num > 0 {
        if num > 0 {
            //: msgNumLabel.isHidden = false
            msgNumLabel.isHidden = false
            //: if num > 99 {
            if num > 99 {
                //: msgNumLabel.text = "99+"
                msgNumLabel.text = "99+"
                //: } else {
            } else {
                //: msgNumLabel.text = String(num)
                msgNumLabel.text = String(num)
            }
            //: } else {
        } else {
            //: msgNumLabel.isHidden = true
            msgNumLabel.isHidden = true
        }
    }

    /// 一键清除动画
    //: func setIsShowCleanAmimat() {
    func themeBlock() {
        //: let unreadMsgCount = PositionLabelReactiveCompatible.share.unreadMessageNum
        let unreadMsgCount = PositionLabelReactiveCompatible.share.unreadMessageNum
        //: let isClean: Bool = UserDefaults.standard.object(forKey: isShowCleanAnimatCacheKey) as? Bool ?? false
        let isClean: Bool = UserDefaults.standard.object(forKey: userLimitData) as? Bool ?? false
        //: if unreadMsgCount >= 10 && !isClean {
        if unreadMsgCount >= 10, !isClean {
            //: self.cleanBtnBtnAnimat()
            self.choiceAnimat()
        }
    }

    //: func cleanBtnBtnAnimat() {
    func choiceAnimat() {
        //: let degreesToRadians = 180.0 * .pi
        let degreesToRadians = 180.0 * .pi
        //: let angle1 = 0 / degreesToRadians
        let angle1 = 0 / degreesToRadians
        //: let angle2 = 100 / degreesToRadians
        let angle2 = 100 / degreesToRadians
        //: let angle3 = -100 / degreesToRadians
        let angle3 = -100 / degreesToRadians
        //: let angle4 = 100 / degreesToRadians
        let angle4 = 100 / degreesToRadians
        //: let angle5 = -100 / degreesToRadians
        let angle5 = -100 / degreesToRadians
        //: let angle6 = 0 / degreesToRadians
        let angle6 = 0 / degreesToRadians

        //: let du1: NSNumber = 0
        let du1: NSNumber = 0
        //: let du2: NSNumber = 0.13
        let du2: NSNumber = 0.13
        //: let du3: NSNumber = 0.3
        let du3: NSNumber = 0.3
        //: let du4: NSNumber = 0.47
        let du4: NSNumber = 0.47
        //: let du5: NSNumber = 0.8
        let du5: NSNumber = 0.8
        //: let du6: NSNumber = 1.0
        let du6: NSNumber = 1.0

        // 旋转
        //: let roate = CAKeyframeAnimation.init(keyPath: "transform.rotation")
        let roate = CAKeyframeAnimation(keyPath: (String(userCollectionK) + String(showPriceNoti.prefix(5)) + "tation"))
        //: roate.values = [(angle1), (angle2), (angle3), (angle4), (angle5), (angle6)]
        roate.values = [angle1, angle2, angle3, angle4, angle5, angle6]
        //: roate.keyTimes = [(du1), (du2), (du3), (du4), (du5), (du6)]
        roate.keyTimes = [du1, du2, du3, du4, du5, du6]

        // 缩放
        //: let zoom = CAKeyframeAnimation.init(keyPath: "transform.scale")
        let zoom = CAKeyframeAnimation(keyPath: (showMakeNoti.replacingOccurrences(of: "effect", with: "tr") + "form." + kBlackConst.replacingOccurrences(of: "location", with: "l")))
        //: zoom.values = [(1.0), (1.2), (1.0)]
        zoom.values = [1.0, 1.2, 1.0]
        //: zoom.keyTimes = [(0), (0.5), (1.0)]
        zoom.keyTimes = [0, 0.5, 1.0]

        //
        //: let groupAnnimation = CAAnimationGroup.init()
        let groupAnnimation = CAAnimationGroup()
        //: groupAnnimation.autoreverses = false
        groupAnnimation.autoreverses = false
        //: groupAnnimation.animations = [roate, zoom]
        groupAnnimation.animations = [roate, zoom]
        //: groupAnnimation.duration = 1.0
        groupAnnimation.duration = 1.0
        //: groupAnnimation.repeatCount = 2
        groupAnnimation.repeatCount = 2
        //: groupAnnimation.isRemovedOnCompletion = false
        groupAnnimation.isRemovedOnCompletion = false
        //: groupAnnimation.fillMode = .forwards
        groupAnnimation.fillMode = .forwards
        //: cleanBtn.layer.add(groupAnnimation, forKey: "zoom&shake")
        cleanBtn.layer.add(groupAnnimation, forKey: (String(kBeginData)))
    }

    /// 检查系统通知是否开启
    //: private func func__turnOnSystemNotification() {
    private func insomniac() {
        //: TalkingPermissionTool.checkPushNotification { isEnable in
        TimingThen.searchAt { isEnable in

            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: if isEnable {
                if isEnable {
                    //: self.noticeView.isHidden = true
                    self.noticeView.isHidden = true
                    //: self.func__hideNotificationTipView(hide: true)
                    self.commandHide(hide: true)
                    //: }else{
                } else {
                    /// 页面顶部提示
                    //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
                    let today = NSDate.dateMedia(date: Date(), dateFormat: (String(kToolShow)))
                    //: if let closeDateStr = Defaults.string(forKey: TalkingMessageTabCloseNotificationsKey), today == closeDateStr {
                    if let closeDateStr = constSizeFormat.string(forKey: notiInfoKey), today == closeDateStr {
                        //: self.func__hideNotificationTipView(hide: true)
                        self.commandHide(hide: true)
                        //: }else{
                    } else {
                        //: self.func__hideNotificationTipView(hide: false)
                        self.commandHide(hide: false)
                    }
                    /// 弹框逻辑
                    //: let isShow = Defaults.bool(forKey: TalkingMessageTabTurnOnNotificationsKey)
                    let isShow = constSizeFormat.bool(forKey: appClickValue)
                    //: if !isShow {
                    if !isShow {
                        //: Defaults.set(true, forKey: TalkingMessageTabTurnOnNotificationsKey)
                        constSizeFormat.set(true, forKey: appClickValue)
                        //: TalkingAlertShow.alert(title: nil,
                        StirReactiveCompatible.leftConfig(title: nil,
                                                    //: message: "Allow %@ to send you notifications?".localizedArguments(AppName),
                                                    message: String(bytes: constTopApp.map{errorUser(sex: $0)}, encoding: .utf8)!.targetPost(mainAppStr),
                                                    //: leftBtnTitle: "Cancel".localized,
                                                    leftBtnTitle: (String(notiContainerMain.prefix(6))).localized,
                                                    //: rightBtnTitle: "Settings".localized) {
                                                    rightBtnTitle: (String(dataImageNoti.prefix(8))).localized)
                        {
                            //: TalkingAlertShow.hideAlert()
                            StirReactiveCompatible.hidePush()
                            //: return
                            //: } rightBlock: {
                        } rightBlock: {
                            //: if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                            if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                                //: UIApplication.shared.open(settingsUrl)
                                UIApplication.shared.open(settingsUrl)
                            }
                        }
                    }
                }
            }
        }
    }

    //: private func func__hideNotificationTipView(hide: Bool) {
    private func commandHide(hide: Bool) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: if hide{
            if hide {
                //: self.noticeView.isHidden = true
                self.noticeView.isHidden = true
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight)
                    make.top.equalToSuperview().offset(showDomainUrl)
                }
                //: self.slideVC.viewHeight = ScreenHeight - StatusBarHeight - TabBarViewHeight
                self.slideVC.viewHeight = appNameMessage - showDomainUrl - constBeginScreenHeightKey
                //: }else{
            } else {
                //: self.noticeView.isHidden = false
                self.noticeView.isHidden = false
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight + self.noticeView.viewHeight)
                    make.top.equalToSuperview().offset(showDomainUrl + self.noticeView.viewHeight)
                }
                //: self.slideVC.viewHeight = ScreenHeight - self.noticeView.bottom - TabBarViewHeight
                self.slideVC.viewHeight = appNameMessage - self.noticeView.bottom - constBeginScreenHeightKey
            }
        }
    }
}

// MARK: - 通知

//: extension TalkingMessageViewController {
extension BlankThen {
    /// who like me 红点通知
    //: @objc func showbadgNumberNotif(notification: NSNotification) {
    @objc func showbadg(notification: NSNotification) {
        //: let userinfo = notification.userInfo as![String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let badNumber: Int  = userinfo["badNumber"] as! Int
        let badNumber: Int = userinfo[(String(dataFrameApp) + kTabData.replacingOccurrences(of: "result", with: "be"))] as! Int
        //: badgLB.isHidden = false
        badgLB.isHidden = false
        //: if badNumber > 99 {
        if badNumber > 99 {
            //: badgLB.text = "99+"
            badgLB.text = "99+"
            //: } else if badNumber<=0 {
        } else if badNumber <= 0 {
            //: badgLB.isHidden = true
            badgLB.isHidden = true
            //: badgLB.text = "0"
            badgLB.text = "0"
            //: } else {
        } else {
            //: badgLB.text = String(badNumber)
            badgLB.text = String(badNumber)
        }
    }
}

// MARK: - Layout

//: extension TalkingMessageViewController {
extension BlankThen {
    /// UI
    //: private func createUI() {
    private func pushUi() {
        //: view.backgroundColor = .white
        view.backgroundColor = .white

        //: view.addSubview(colorImageView)
        view.addSubview(colorImageView)
        //: view.addSubview(noticeView)
        view.addSubview(noticeView)
        //: self.addChild(slideVC)
        self.addChild(slideVC)

        //: self.view.addSubview(slideVC.view)
        self.view.addSubview(slideVC.view)
        //: slideVC.view.backgroundColor = UIColor.clear
        slideVC.view.backgroundColor = UIColor.clear
        //: slideVC.view.snp.makeConstraints { make in
        slideVC.view.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(StatusBarHeight)
            make.top.equalToSuperview().offset(showDomainUrl)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.bottom.equalToSuperview()
            make.bottom.equalToSuperview()
        }

        //: for btn in slideVC.allBtn {
        for btn in slideVC.allBtn {
            //: let curenbrn: UIButton = btn
            let curenbrn: UIButton = btn
            //: if curenbrn.titleLabel!.text == "Who like me".localized {
            if curenbrn.titleLabel!.text == (String(notiGiftStorageUser) + showSectionNoti.replacingOccurrences(of: "target", with: "m")).localized {
                //: (curenbrn as AnyObject).addSubview(badgLB)
                (curenbrn as AnyObject).addSubview(badgLB)
                //: badgLB.snp.makeConstraints { make in
                badgLB.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
                //: badgLB.isHidden = true
                badgLB.isHidden = true
                //: } else if (curenbrn.titleLabel!.text == "Messages".localized) {
            } else if curenbrn.titleLabel!.text == (String(userMakeApp.suffix(8))).localized {
                //: (curenbrn as AnyObject).addSubview(msgNumLabel)
                (curenbrn as AnyObject).addSubview(msgNumLabel)
                //: msgNumLabel.snp.makeConstraints { make in
                msgNumLabel.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
            }
        }

        //: view.addSubview(cleanBtn)
        view.addSubview(cleanBtn)
        //: cleanBtn.snp.makeConstraints { make in
        cleanBtn.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top)
            make.top.equalTo(slideVC.view.snp.top)
            //: make.trailing.equalTo(-5)
            make.trailing.equalTo(-5)
            //: make.size.equalTo(CGSize(width: 44, height: 44))
            make.size.equalTo(CGSize(width: 44, height: 44))
        }

        //: view.addSubview(netTipLabel)
        view.addSubview(netTipLabel)
        //: netTipLabel.snp.makeConstraints { make in
        netTipLabel.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top).offset(44)
            make.top.equalTo(slideVC.view.snp.top).offset(44)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.height.equalTo(30)
            make.height.equalTo(30)
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func startBind() {
        //: TabThen.shared.rx
        TabThen.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(notiLiveConst)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.barState()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: PositionLabelReactiveCompatible.share.rx.observeWeakly(Int.self, "networkStatus")
        PositionLabelReactiveCompatible.share.rx.observeWeakly(Int.self, (kComponentUser.replacingOccurrences(of: "intimate", with: "t") + "kStatus"))
            //: .subscribe(onNext: { [weak self]  (value) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.barState()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // 展示全部红点
        //: PositionLabelReactiveCompatible.share.rx.observeWeakly(Int.self, "unreadMessageNum")
        PositionLabelReactiveCompatible.share.rx.observeWeakly(Int.self, (String(dataErrorK) + String(appViewData.prefix(5))))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let valueInt = value ?? 0
                let valueInt = value ?? 0
                //: self.func__messagebadgLBState(num: valueInt)
                self.administrativeDistrictNum(num: valueInt)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: cleanBtn.rx.controlEvent(.touchUpInside)
        cleanBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: {
            .subscribe(onNext: {
                //: let config = ShowAlertConfig()
                let config = ShapeAlertConfig()
                //: config.alignment = .center
                config.alignment = .center

                //: TalkingAlertShow.customAlert(message: "Do you want to mark all messages as read?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, leftBlock: {
                StirReactiveCompatible.claim(message: String(bytes: mainDisplayShow.map{headGift(at: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(notiContainerMain.prefix(6))).localized, rightBtnTitle: "OK".localized, leftBlock: {
                    //: TalkingAlertShow.hideAlert()
                    StirReactiveCompatible.hidePush()

                    //: }, rightBlock: {
                }, rightBlock: {
                    //: V2TIMManager.sharedInstance().markAllMessage {
                    V2TIMManager.sharedInstance().markAllMessage {
                        //: UserDefaults.standard.set(true, forKey: isShowCleanAnimatCacheKey)
                        UserDefaults.standard.set(true, forKey: userLimitData)
                        //: } fail: { code, desc in
                    } fail: { code, desc in
                        //: UploadLogTool.writeLog(msg: "消息列表一键已读失败：code:\(code), desc:\(String(describing: desc))")
                        NameReactiveCompatible.trademarkIndex(msg: (showBetweenK.replacingOccurrences(of: "to", with: "一") + "键已读失败：" + String(appModelK)) + "\(code)" + (String(showMakeData)) + "\(String(describing: desc))")
                    }
                    //: }, config: config)
                }, config: config)

                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // who like me 红点通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(showbadgNumberNotif(notification:)),
                                               selector: #selector(showbadg(notification:)),
                                               //: name: WHOLIKEME_BADGENUMBER_NOTIF,
                                               name: constDomainValue,
                                               //: object: nil)
                                               object: nil)

        //: noticeView.closeBtnBlock = { [weak self] ()  -> Void in
        noticeView.closeBtnBlock = { [weak self] () in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
            let today = NSDate.dateMedia(date: Date(), dateFormat: (String(kToolShow)))
            //: Defaults.set(today, forKey: TalkingMessageTabCloseNotificationsKey)
            constSizeFormat.set(today, forKey: notiInfoKey)
            //: self.func__hideNotificationTipView(hide: true)
            self.commandHide(hide: true)
        }

        //: NotificationCenter.default.rx.notification( UIApplication.willEnterForegroundNotification)
        NotificationCenter.default.rx.notification(UIApplication.willEnterForegroundNotification)
            //: .subscribe(onNext: { [weak self] (notification) in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__turnOnSystemNotification()
                self.insomniac()
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
